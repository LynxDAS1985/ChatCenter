# Архив changelog: v1.2.495 – v1.2.498 (22–24 сентября 2026)

Вынесено из `features.md` в v1.2.501 — основной файл дошёл до 80 КБ при лимите 100 КБ
(предупреждение сторожа `memoryBankSizeLimits.test.cjs`, который срабатывает на 80%).

Темы: разбор служебных строк переехал в общую папку + сторож размеров памяти (v1.2.495);
семейство ошибок «молчит посредник (VPN/прокси)» в повторах подключения (v1.2.496);
предел ожидания попытки — экран «попытка идёт» мог висеть вечно (v1.2.497);
починки после ревью: пульс умирал от кнопки, догрузившаяся страница выбрасывалась (v1.2.498).

Актуальное устройство механизма связи — в `reconnect-plan.md` (раздел 4e и далее),
решения — ADR-076 … ADR-079 в `decisions.md`.

---

### v1.2.498 — Ревью тремя проверяющими: 🔴 пульс умирал от нажатия кнопки, 🔴 догрузившаяся страница выбрасывалась + 10 доводок

**ОТ ПОЛЬЗОВАТЕЛЯ (2026-09-24)**: «чини всё, и логи добавь везде, где надо, чтобы понимать ситуацию».
Разбор v1.2.497 тремя независимыми проверяющими (ломатель / взгляд снаружи / хранитель правил) + свои прогоны.

#### 🔴 Н1 — «Проверить связь» могла НАВСЕГДА выключить пульс интернета *(беда с v1.2.491)*
`checkNow` сначала ГАСИТ будильник, а `check` при защите от дребезга (`canCheckNow` — меньше 3 с с прошлой
проверки) выходил **без `schedule()`**. Итог: нажал кнопку раньше промежутка — пульс замолкал навсегда, и
«интернет вернулся» сказать было некому. Тот же путь давал сигнал `net:pulse-waiting` (0→>0), который шлётся
сам, без человека.
**Доказано прогоном** ([netPulseHandlers.js](../main/handlers/netPulseHandlers.js)): без нажатия за 20 с — **6**
опросов адресов, с нажатием на 1-й секунде — **3** (только стартовый). После фикса снова 6.
**ФИКС**: ветка пропуска ставит будильник заново и пишет в журнал причину и срок.

#### 🔴 Н2 — Страница догружалась после предела, а её выбрасывали и грузили заново *(новая, из v1.2.497)*
Запись после предела уходила в «ждём» с `failedAt` = момент предела; успешная загрузка в ближайшие 2 с считалась
«эхом страницы-ошибки» (`shouldAcceptLoaded`, [reconnectPlan.js](../shared/reconnectPlan.js)) и отбрасывалась.
Плюс брошенная загрузка продолжалась, и следующая попытка шла поверх неё.
**ФИКС**: запись помечается `timedOut`; для неё окно эха НЕ применяется (отказа загрузки не было — значит и
страницы-ошибки нет). Прошлая зависшая загрузка глушится `el.stop()` ПЕРЕД новой попыткой.

#### Остальные находки (все закрыты)
| # | Было | Стало | Где |
|---|---|---|---|
| 3 | предел затирал код беды своим `-1001` → экран переставал винить VPN | код беды сохраняется, факт ожидания — отдельное поле `timedOut` | [reconnectAttempt.js](../shared/reconnectAttempt.js), `planAfterRetryFail` |
| 4 | проба «жива ли страница» без предела → запись висла в фазе «идёт» навсегда | `PROBE_TIMEOUT_MS` = 10 с + запись в журнал | `reconnectAttempt.js` |
| 5 | два запуска разом → ДВЕ загрузки одной страницы, счётчик прыгал 1→3 | замок `inFlight`, второй запуск отклоняется (`busy`) с записью в журнал | `reconnectAttempt.js` |
| 10 | признак «молчит посредник» брался от ошибки ПОСЛЕДНЕГО адреса → мигал | собираются ошибки ВСЕХ адресов; сырой текст пишется в журнал | `netPulseHandlers.js` |
| 11 | постоянные 45 с → на медленном канале петля «не успел → бросили → заново» | предел растёт: `attemptTimeoutFor` 45 → 90 → 135 → 180 с | [reconnectErrorCodes.js](../shared/reconnectErrorCodes.js) |
| 14 | при неизвестном вердикте экран утверждал «интернет пропал» | третье состояние: «связь не проверялась — проверка выключена или ещё не ответила» | `reconnectTexts.js` `reasonTitle` |
| 15 | при долгой паузе на экране «300 с» | «5 мин» | [WebviewOfflineOverlay.jsx](../src/components/WebviewOfflineOverlay.jsx) |
| 6 | комментарии про долгую паузу противоречили коду («только при true») | приведены к коду | `reconnectPlan.js` |
| 7 | `code-todo.md` слал менять константу не в тот файл | указан `reconnectErrorCodes.js` | `code-todo.md` |
| 8 | `features.md` заявлял тест `vi.getTimerCount()===0`, которого в коде нет | запись приведена к реальному тесту | `features.md` |

#### Новые записи в журнале (просьба «логи везде, где надо»)
`попытка N — загружаю страницу (жду до 45с)` · `страница НЕ ОТВЕТИЛА за 90с — прекращаю ждать (причина осталась
прежней: код=-130 ERR_PROXY_CONNECTION_FAILED), следующая попытка через 20с` · `попытка уже идёт — второй запуск
пропущен` · `проверка «жива ли страница» не ответила за 10с` · `глушу прошлую зависшую загрузку перед новой
попыткой` · `страница догрузилась уже после предела — засчитываю` · `[net-pulse] проверка пропущена (прошлая была
1 с назад…) → следующая через 2 с` · `[net-pulse] ошибки адресов: host: net::ERR_… | host: …`.

**КАК ПРОВЕРЕНО**: полный vitest **229 файлов / 3096 тестов** зелёные; 48 статических тестов — 0 падений; линт 0;
лимиты 694/694; бюджет экранов 31 108/31 150; сборка OK; память здорова. **+10 тестов-ловушек** (по одной на
находку). **Прогон наоборот**: вернул затирание причины → тест покраснел; вернул выброс позднего успеха →
покраснел; убрал будильник после пропуска → покраснел. Фикс Н1 доказан тем же прогоном, что доказал беду (3 → 6).

**ЧТО ПРОВЕРИТЬ ВРУЧНУЮ** (TODO-59, только dev-запуск): выключить VPN → экран «Не отвечает посредник», и он НЕ
меняется на «Сайт недоступен» после 45 с; в журнале строка `ошибки адресов:` с настоящим текстом ошибки; нажать
«Проверить связь» дважды подряд → строки `[net-pulse]` продолжают идти.

### v1.2.497 — Экран «попытка идёт» мог висеть ВЕЧНО: поставлен предел ожидания + 6 доводок по ревью

**ОТ ПОЛЬЗОВАТЕЛЯ (2026-09-24)**: скриншот — «Подключаемся к ВКонтакте… попытка 1 · идёт», кнопка «Подождите…»,
остальные мессенджеры работают. Плюс просьба выполнить план исправления из придирчивого ревью v1.2.496 целиком.

#### 🔴 Находка #1 (главная, доказана чтением кода) — из фазы `trying` закрыты ВСЕ выходы
Пока запись мессенджера в фазе «идёт попытка»:
| Выход | Почему закрыт |
|---|---|
| будильник лестницы | `dueIds` такие записи не выдаёт |
| возврат интернета | `bringAllForward` двигает ТОЛЬКО фазу `wait` ([reconnectPlan.js](../shared/reconnectPlan.js)) |
| успешная загрузка страницы | `shouldAcceptLoaded` при `trying` возвращает `false` (ловушка v1.2.453 — защита от эха страницы-ошибки) |
| кнопка на экране | надпись «Подождите…», нажатие ничего не делает |
Единственным выходом был ответ `loadURL`, а на мёртвом посреднике (VPN) он может не прийти вовсе → **экран
залипал навсегда, лечилось только перезапуском**. Предела ожидания в коде не было (проверено поиском).

**ФИКС**: `ATTEMPT_TIMEOUT_MS = 45000` + `ATTEMPT_TIMEOUT_CODE = -1001` ([reconnectErrorCodes.js](../shared/reconnectErrorCodes.js));
в [reconnectAttempt.js](../shared/reconnectAttempt.js) загрузка идёт через `raceWithTimeout` (гонка с таймером,
таймер гасится в `finally` — П3). По истечении предела запись возвращается в `wait` → все три выхода снова работают.
45 с выбраны по журналу: обычная загрузка мессенджера 5–30 с.

#### Остальные находки ревью (все закрыты)
| # | Было | Стало | Файл |
|---|---|---|---|
| 2 | признак «молчит посредник» обновлялся ТОЛЬКО в момент перехода «была связь → пропала»: при смене беды без возврата связи метка «VPN?» врала | считается на КАЖДОЙ неудачной проверке | `netPulsePlan.js` `applyResult` |
| 3 | долгая пауза 5 мин только при `netOnline === true` → при ВЫКЛЮЧЕННОМ пульсе страница дёргалась раз в минуту вечно | пауза при `netOnline !== false` (при `false` до загрузки не доходит — ветка `net-down`) | `reconnectPlan.js` `nextPauseMs` |
| 4 | описание «Применить результат проверки» съехало к чужой функции; `lastError` не описан | описание на месте, параметр описан | `netPulsePlan.js` |
| 5 | «Не отвечает посредник» показывалось и когда пульс УЖЕ подтвердил интернет | ветка работает только при `netOnline !== true` (пульс ходит через посредника: «есть» = посредник жив) | `reconnectTexts.js` `reasonTitle` |
| 6 | кнопка «Проверить интернет» противоречила заголовку про посредника | «Проверить связь» | `reconnectTexts.js` `retryButtonLabel` |
| 7 | `looksLikeProxyDown` искал слово `PROXY` где угодно → адрес вида `https://proxy.example/ping` в тексте ошибки давал ложную причину | ищем ИМЕНА ошибок: `ERR_PROXY` / `ERR_TUNNEL` / `ERR_SOCKS` | `netPulsePlan.js` |
| журнал | между «обрыв связи» и ответом страницы журнал молчал — зависшая попытка выглядела как её отсутствие | `[reconnect] X: попытка N — загружаю страницу` и `X: страница НЕ ОТВЕТИЛА за 45с — прекращаю ждать` | `reconnectTexts.js` |

#### Заодно — разгрузка (правило П1 «за собой убирай»)
`shared/reconnectPlan.js` после правок встал 298/300. Блок кодов ошибок вынесен в новый
[shared/reconnectErrorCodes.js](../shared/reconnectErrorCodes.js) (100 строк): справочник кодов + `isNetworkError` /
`isProxyError` / `errorName` / `PROBE_FAIL_CODE` / `ATTEMPT_TIMEOUT_CODE`. В `reconnectPlan.js` оставлен **реэкспорт**
этих имён — восемь мест уже берут их оттуда, менять все ради переезда рискованнее, чем польза. Файл: 298 → **215/300**.

**КАК ПРОВЕРЕНО**: `reconnectAttempt.vitest.js` (+2: «страница не отвечает → выходим из trying за 45 с»,
«обычная загрузка внутри предела — успех, предел не мешает»), `reconnectPlan.vitest.js` (+1 на #3),
`netPulseUi.vitest.js` (+3 на #2/#6/#7), обновлены ТРИ старых теста, закреплявших прежние правила (#3 ×2, #6 ×1).
**Прогон наоборот**: предел 45 с → 24 ч → тест покраснел; признак посредника вернул «только на переходе» → покраснел.
Полный vitest, линт, лимиты, сборка — в отчёте сессии.

**ЧТО ПРОВЕРИТЬ ВРУЧНУЮ** (ПОЛНЫЙ перезапуск, TODO-58): выключить VPN при открытых веб-чатах → экран
«Не отвечает посредник (VPN или прокси)», через 45 с в журнале `страница НЕ ОТВЕТИЛА за 45с`, кнопка снова живая;
включить VPN → страницы поднимаются сами.

⚠️ **ВАЖНО ДЛЯ ПРОВЕРКИ**: 2026-09-24 пользователь запускал УСТАНОВЛЕННУЮ версию **1.2.490** (от 20.09,
`%LOCALAPPDATA%\Programs\chat-center`), где ничего из v1.2.491+ нет — отсюда и «ноль строк `[net-pulse]`» в журнале.
Проверять только dev-запуском из репозитория или после пересборки установщика.

### v1.2.496 — «Молчит посредник (VPN/прокси)»: целое семейство обрывов не запускало ни экран, ни повторы

**ЖАЛОБА (2026-09-23, 09:05)**: «запустил через терминал — не запустилась ни одна программа» (скриншот: у всех
пяти веб-значков красные точки, метка «нет сети» горит, родной Telegram при этом грузит чаты).

#### КОРЕНЬ (🟢 доказан журналом + реестром Windows + проверкой порта)
| Что смотрел | Что увидел |
|---|---|
| `chatcenter.log` 09:06:05 | у ВСЕХ пяти веб-мессенджеров одна ошибка: `ERR_PROXY_CONNECTION_FAILED` (код **−130**) |
| тот же журнал | `[reconnect] … код=-130 — повтор не нужен` ×5 → записи не создались |
| реестр Windows `HKCU\…\Internet Settings` | `ProxyEnable=1`, `ProxyServer=127.0.0.1:2080` — весь веб идёт через посредника |
| `Get-NetTCPConnection -LocalPort 2080` | **никто не слушает**; при этом процесс «Liberty VPN» запущен (туннель не поднят) |
| `chatcenter.prev.log` | таких ошибок **0** — накануне посредник работал |
| наш код | `NETWORK_ERROR_CODES` ([shared/reconnectPlan.js](../shared/reconnectPlan.js)) не содержал −130 → `isNetworkError`=false → ранний выход в [useWebviewReconnect.js:51](../src/hooks/useWebviewReconnect.js) ДО создания записи |

Следствие: **ни экрана «Нет связи», ни повторов, и после возврата VPN страницы не поднялись бы сами** — будить
было некому. Пульс интернета при этом отработал верно: `net.fetch` идёт тем же путём через посредника, поэтому
честно сказал «интернета нет» (а метка на полосе — первое боевое срабатывание v1.2.493).

#### ЧТО СДЕЛАНО
| # | Что | Файл |
|---|---|---|
| 1 | В список «стоит повторять» добавлено семейство «молчит ПОСРЕДНИК»: **−111** ERR_TUNNEL_CONNECTION_FAILED, **−120** ERR_SOCKS_CONNECTION_FAILED, **−121** ERR_SOCKS_CONNECTION_HOST_UNREACHABLE, **−130** ERR_PROXY_CONNECTION_FAILED (номера сверены с официальным перечнем Chromium `net_error_list.h` — факт уровня 1; моя память по этим номерам была НЕВЕРНОЙ) | [shared/reconnectPlan.js](../shared/reconnectPlan.js) `PROXY_ERROR_CODES`, `isProxyError` |
| 2 | Экран «Нет связи» говорит правду: **«Не отвечает посредник (VPN или прокси)»**. Ветка стоит ПЕРВОЙ — иначе мёртвый пульс дал бы «Нет интернета» | [shared/reconnectTexts.js](../shared/reconnectTexts.js) `reasonTitle` |
| 3 | Пульс уточняет причину в журнале (`· похоже, молчит ПОСРЕДНИК (VPN/прокси), а не сам интернет`) и отдаёт окну признак `proxyDown` | [shared/netPulsePlan.js](../shared/netPulsePlan.js) `looksLikeProxyDown`, [netPulseHandlers.js](../main/handlers/netPulseHandlers.js) (`lastError`) |
| 4 | Метка полосы: «нет сети · VPN?» + своя подсказка; строка состояния: «Интернет: нет · молчит посредник (VPN/прокси) · проверено N с назад» | [NativeSidebar.jsx](../src/native/components/NativeSidebar.jsx), `pulseStatusLine` |

**Намеренно НЕ добавлены** (повторами не лечатся, нужен человек): −115 `ERR_PROXY_AUTH_UNSUPPORTED`,
−127 `ERR_PROXY_AUTH_REQUESTED`, −131 `ERR_MANDATORY_PROXY_CONFIGURATION_FAILED`, −136 `ERR_PROXY_CERTIFICATE_INVALID`.
Закреплено тестом-ловушкой.

#### КАК РАБОТАЕТ / КАК ВЫКЛЮЧИТЬ
Ошибка загрузки → `isNetworkError` (теперь true для −111/−120/−121/−130) → запись + экран + лестница пауз
5→10→20→40→60 с. **Страницу при мёртвом пульсе по-прежнему НЕ перезагружаем** (ветка `net-down` в
`shared/reconnectAttempt.js`) — недописанное сообщение цело; закреплено тестом на коде −130.
Распознавание текста ошибки пульса — МЯГКОЕ (подстроки PROXY/TUNNEL/SOCKS): не узнали → просто не уточняем
причину, поведение как раньше. Надёжный путь — код ошибки страницы, он от текста не зависит.
Откат: `git revert` коммита версии; выключатель пульса прежний (`settings.netPulse === false`).

**КАК ПРОВЕРЕНО**: `reconnectPlan.vitest.js` (+2 проверки: новые коды = обрыв; 🔴 ловушка «беды с логином/сертификатом
НЕ повторяем»), `reconnectAttempt.vitest.js` (+ловушка «ветка посредника первее мёртвого пульса», +защита
«не дёргаем страницу» на коде −130) 13/13, `netPulseUi.vitest.js` (+5: распознавание текста, строка журнала,
снятие признака при возврате, строка состояния, проводка) 22/22. **Прогон наоборот**: ветку посредника отключил →
тест покраснел на «Нет интернета»; код −130 убрал из списка → тест покраснел. Полный vitest, линт, лимиты, сборка — в отчёте.

**ЧТО ПРОВЕРИТЬ ВРУЧНУЮ** (нужен ПОЛНЫЙ перезапуск): выключить VPN → у веб-мессенджеров должен появиться экран
**«Не отвечает посредник (VPN или прокси)»** и счётчик повтора; включить VPN → страницы поднимаются сами.
Строка в журнале `· похоже, молчит ПОСРЕДНИК` подтвердит, что распознавание текста ошибки пульса попало в цель
(если её нет, а экран верный — значит текст у `net.fetch` другой; это не ошибка, надёжный путь работает).

### v1.2.495 — Разгрузка бюджета интерфейса: разбор `__CC_*` переехал в `shared/` + память предупреждает при 80%

**ОТ ПОЛЬЗОВАТЕЛЯ**: «Найдено, НЕ трогал — делай» (три находки из отчёта v1.2.494).

#### Находка №1+№2 (одна правка): `consoleMessageParser` → `shared/`
| ❌ Было | ✅ Стало |
|---|---|
| `src/utils/consoleMessageParser.js` — 92 строки внутри бюджета экранов | `shared/consoleMessageParser.js` — вне бюджета (ADR-044) |
| бюджет 31 195 / 31 200 — **запас 3 строки** (следующая правка экранов упиралась в потолок) | 31 104 / **31 150** — запас 46 строк, планка опущена следом |

**ПОЧЕМУ это законный перенос, а не «спрятать в другую папку»** (правило ADR-044 — `shared/` = обособленный модуль, не код интерфейса):
- файл ЧИСТЫЙ: `grep -cE "window\.|document\.|from 'react'|navigator\."` → **0**;
- разбирает строки `__CC_*`, прилетающие из ЧУЖИХ страниц мессенджеров (как уже переехавшие
  [webviewDiagnostics.js](../shared/webviewDiagnostics.js), [browserBannerHider.js](../shared/browserBannerHider.js));
- его тянут ДВА разных потребителя — [webviewSetup.js](../src/utils/webviewSetup.js) (экраны) и
  [ozonBgWatcher.js](../src/utils/ozonBgWatcher.js) (фоновый наблюдатель).

**ЧТО ПРАВИЛОСЬ**: `git mv` + два импорта на `'../../shared/consoleMessageParser.js'` + путь чтения в
[consoleMessageParser.test.cjs](../src/__tests__/consoleMessageParser.test.cjs) + планка в
[fileSizeLimits.test.cjs](../src/__tests__/fileSizeLimits.test.cjs) (31200 → 31150) + 7 ссылок в памяти.
Логика файла НЕ менялась ни на строку (`git diff -M` показывает чистое переименование).

#### Находка №3 (features.md 83 КБ из 100) — сделано ИНАЧЕ, чем «архивировать сейчас»
Архивация при 83 КБ преждевременна: разрывать свежую историю ради 17 КБ запаса — вред (записи последних
версий читаются чаще всего). Вместо этого закрыта ПРИЧИНА, по которой находка вообще появилась — файл рос молча
до самого потолка: в [memoryBankSizeLimits.test.cjs](../src/__tests__/memoryBankSizeLimits.test.cjs) добавлено
**жёлтое предупреждение при 80%** (как в тесте лимитов кода) — тест от него не падает, но в каждом прогоне видно:
`⚠️ .memory-bank/decisions.md — 86 КБ, 86% от лимита`. Сразу подсветились ДВА файла (`decisions.md` 86 КБ,
`features.md` 81 КБ) — то есть сторож оказался нужен не «когда-нибудь», а прямо сейчас.

**КАК ПРОВЕРЕНО**: `consoleMessageParser.test.cjs` 30/30, `appStructure` 60/60, `integrationChains` 18/18,
`memoryBankSizeLimits` 47/47 (+2 предупреждения), лимиты 693/693, бюджет 31 104/31 150, линт, полный vitest,
сборка — в отчёте сессии. **Визуальная проверка НЕ требуется** (поведение не менялось).

