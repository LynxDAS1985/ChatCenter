# Архив changelog v1.2.244–255 — боковой рейл SourceRail (ЗАКРЫТ, 13 августа 2026)

**Подход SourceRail (отдельная панель за флагом) закрыт разворотом v1.2.256 (Модель 🅰️, см. decisions ADR-032).** Код удалён из рабочего дерева, но сохранён в истории git (коммиты c9b72b6…d461b3f). Эти записи ссылаются на удалённые файлы SourceRail.jsx/useSourceRail.js — вынесены из active features.md, чтобы тест dangling-ссылок не падал.

---

### v1.2.254 — Боковой рейл, Этап 3 (часть 1): «умный +» (Telegram-аккаунт ИЛИ веб)

Дата: 2026-08-13. Кнопка «+» в рейле [SourceRail.jsx](../src/components/SourceRail.jsx) теперь **показывает выбор**: ✈️ «Telegram-аккаунт (вход)» или 🌐 «Веб-мессенджер». **ЗАЧЕМ:** раньше «+» открывал только окно добавления веб-мессенджера — добавить Telegram-аккаунт из рейла было нельзя (мешало проверке аккаунтов 2C). **КАК:** (1) [NativeApp.jsx](../src/native/NativeApp.jsx) `openLogin` (открывает окно входа Telegram, им же пользуется нативный «+») обёрнута в `useCallback` и отдана наверх в наборе `onAccountActionsReady` рядом с `soloAccount`; (2) хук [useSourceRail.js](../src/hooks/useSourceRail.js) получил `onAddAccount` — переключить в общий чат + вызвать `openLogin`; (3) [App.jsx](../src/App.jsx) передаёт `onAddAccount={rail.onAddAccount}`; (4) «+» в рейле по клику раскрывает ДВА варианта ИНЛАЙН над собой (не выпадашка — иначе `overflow-x:hidden` рейла обрезал бы её): ✈️→`onAddAccount` (вход Telegram), 🌐→`onAdd` (существующее окно веб). Повторный клик по «+» (стал «×») закрывает выбор. **Крайние случаи:** `onAddAccount` нет → вариант Telegram ничего не делает; действия ещё не пришли (ref null) → «+»→Telegram просто переключит в общий чат без окна входа; веб-вариант всегда работает. Тесты [SourceRail.vitest.jsx](../src/components/SourceRail.vitest.jsx) 23 («+» открывает выбор, веб→onAdd, Telegram→onAddAccount) + [useSourceRail.vitest.js](../src/__tests__/useSourceRail.vitest.js) 5 (onAddAccount → переключение + openLogin) + смоук `appStructure`. Watermark 34050→34120, App.jsx 981/990. Проверки: vitest 2315/2315, линт 0, лимиты 578/578, guards ✅. **⚠️ Требует визуальной проверки:** «+»→«Telegram-аккаунт» открывает окно входа Telegram (скриншотом ранее подтверждено, что окно входа рабочее). **Остаток Этапа 3 (отдельно):** сворачивание рейла (низкий приоритет — рейл и так узкий), заглушить источник. Откат: `git checkout src/App.jsx src/native/NativeApp.jsx src/components/SourceRail.jsx src/hooks/useSourceRail.js` + версия на 1.2.253.

### v1.2.253 — Боковой рейл: точка связи на значке аккаунта (доводка 2C по ревью)

Дата: 2026-08-13. По плану исправления ревью 2C, находка #2. **ЗАЧЕМ:** в объект аккаунта, поднимаемый из `NativeApp` наверх, клалось поле `health` (состояние связи), но `AccountRailIcon` его НЕ рисовал → «мёртвое» поле + лишние перерисовки; плюс на значке аккаунта не было индикатора связи (в отличие от значка мессенджера). **КАК:** выбран Вариант Б (полнее): [SourceRail.jsx](../src/components/SourceRail.jsx) `AccountRailIcon` теперь рисует **точку связи** 🟢/🟡/🔴 в нижнем правом углу (переиспользован `ConnectionStatusDot`, `health` из поднятого поля), клик по точке → «Подключения» (`stopPropagation` — не выбирает аккаунт); неиспользуемое поле `messenger` убрано из поднимаемого объекта ([NativeApp.jsx](../src/native/NativeApp.jsx)). Тест [SourceRail.vitest.jsx](../src/components/SourceRail.vitest.jsx) 22 (+1: у значка аккаунта есть точка, клик по ней зовёт `onOpenConnections` и НЕ выбирает аккаунт). **Скриншот пользователя подтвердил:** значки аккаунтов с аватарами и счётчиками рисуются корректно (Avtoliberty/БНК). **Не проверено (отложено):** живое переключение по клику — пользователь не может добавить второй тестовый аккаунт из рейла (добавление Telegram-аккаунта = «умный +» Этапа 3, пока «+» рейла добавляет только веб). Проверки: vitest 2314/2314, линт 0, лимиты 578/578, guards ✅. Откат: `git checkout src/components/SourceRail.jsx src/native/NativeApp.jsx` + версия на 1.2.252.

### v1.2.252 — Боковой рейл: проводка аккаунтов вынесена в хук useSourceRail (+тест)

Дата: 2026-08-13. По совету после 2C. Проводка аккаунтов рейла (состояние `nativeAccounts`, ссылка на действия, обработчик клика по аккаунту) вынесена из [App.jsx](../src/App.jsx) в хук [useSourceRail.js](../src/hooks/useSourceRail.js). **ЗАЧЕМ:** логика «клик по аккаунту → общий чат + solo» раньше была inline в App.jsx и не покрывалась юнит-тестом (тест проверял только клик в SourceRail, но не App-обработчик). Теперь хук возвращает `{ nativeAccounts, onAccountsChange, onAccountActionsReady, onSelectAccount }`; App зовёт `const rail = useSourceRail({ onSelectSource: handleTabClick, nativeCcId })` и передаёт `rail.*` в NativeApp/SourceRail. Тест [useSourceRail.vitest.js](../src/__tests__/useSourceRail.vitest.js) (3: клик переключает источник + зовёт `soloAccount` после прихода действий; до прихода не падает; список обновляется). Смоук `appStructure.test.cjs` обновлён под хук. **ЧЕСТНО про размер:** вынос дал ТЕСТИРУЕМОСТЬ и инкапсуляцию, но App.jsx НЕ уменьшился (импорт+вызов хука ≈ убранным строкам; большой JSX-блок `<SourceRail>` с ~15 пропсами остаётся в App) — App.jsx как был 981/990. Т.е. обещанной экономии строк совет не дал; потолок 990 оставлен (при дальнейшем росте — переносить сам JSX-блок или ещё пропсы). Watermark 34000→34050. Проверки: vitest 2313/2313, линт 0, лимиты 578/578, guards ✅. Поведение не изменилось. Откат: `git checkout src/App.jsx` + удалить `useSourceRail.*` + версия на 1.2.251.

### v1.2.251 — Боковой рейл, Этап 2C: отдельные Telegram-аккаунты с аватарами

Дата: 2026-08-13. В боковом рейле теперь видны **отдельные Telegram-аккаунты значками-аватарами** (сверху, под «Общим чатом»). **ЗАЧЕМ:** раньше рейл показывал один значок «Общий чат» (слитый вид всех аккаунтов); аккаунты живут в `nativeStore` ВНУТРИ лениво-грузимого `NativeApp` и наверх не отдавались. **КАК:** (1) [NativeApp.jsx](../src/native/NativeApp.jsx) отдаёт наверх список аккаунтов новым колбэком `onAccountsChange` (id, имя, `avatar`, цвет-метка, непрочитанные) — только чтение из стора, существующий рейл аккаунтов внутри NativeApp НЕ меняется; (2) действие «показать чаты аккаунта» (`soloAccount`) отдаётся через `onAccountActionsReady` — тем же приёмом, что `onConnectionActionsReady`; (3) [App.jsx](../src/App.jsx) держит список (`nativeAccounts`) и ссылку на действия (`nativeAccountActionsRef`), передаёт в [SourceRail.jsx](../src/components/SourceRail.jsx); (4) рейл рисует круглый **аватар** каждого аккаунта (`background:url("cc-media://…")`, фолбэк — инициалы), бейдж непрочитанных, подсветку активного (`activeAccountId`). **Клик по аккаунту** → переключение в нативный режим (`handleTabClick(native_cc)`) + показ чатов этого аккаунта (`soloAccount(id)`). Аватар грузится тем же способом, что в [AccountAvatar.jsx](../src/native/components/AccountAvatar.jsx). **Крайние случаи:** нет аккаунтов → значки аккаунтов не рисуются (только «Общий чат»); пустой аватар → инициалы; действие ещё не пришло (ref null) → `?.` без эффекта. Тесты [SourceRail.vitest.jsx](../src/components/SourceRail.vitest.jsx) 21 (+4: рисование по имени, клик→onSelectAccount, активный+бейдж, без аккаунтов пусто) + смоук связки в [appStructure.test.cjs](../src/__tests__/appStructure.test.cjs) (+1: колбэки NativeApp↔App↔SourceRail). Потолок App.jsx 975→990 (временно), watermark 33920→34000. Проверки: vitest 2310/2310, линт 0, лимиты 576/576, guards ✅. **⚠️ ТРЕБУЕТ ВИЗУАЛЬНОЙ ПРОВЕРКИ (запуск агенту запрещён):** живое переключение аккаунтов (клик по аватару → показываются чаты именно этого аккаунта), загрузка аватаров, подсветка активного. Клик = solo (показать чаты аккаунта) — это интерпретация; если ожидалось иное (напр. просто «активный без фильтра») — скажи, поправлю. Откат: флаг `sideRail` выкл, или `git checkout src/App.jsx src/native/NativeApp.jsx src/components/SourceRail.jsx` + версия на 1.2.250.

### v1.2.250 — Боковой рейл: пункты меню вынесены в чистую функцию + тест (закрыта дыра ревью)

Дата: 2026-08-13. По плану исправления ревью 2B. **Находка #2 (правило меню без теста):** логика «нативный источник → без веб-only пунктов» (v1.2.249) жила прямо в JSX [TabBar.jsx](../src/components/TabBar.jsx) и юнит-тестом не проверялась → регрессию поймал бы только глаз. Вынес построение пунктов в чистую функцию `buildTabContextMenuItems({ isNative, pinned })` — новый файл [tabContextMenuItems.js](../src/utils/tabContextMenuItems.js); `TabBar` теперь зовёт её. Логика ушла из `TabBar` (349/700). Тест [tabContextMenuItems.vitest.js](../src/__tests__/tabContextMenuItems.vitest.js) (5 проверок: веб→все пункты; нативный→без reload/notifLog/copyUrl; закреплённая→без close + ярлык «Открепить»; дефолты не падают). **Находка #1 (меню упростилось и на нативной ВКЛАДКЕ, не только в рейле): принято намеренно** — пункты reload/notifLog/copyUrl и на вкладке «ЦентрЧатов» ничего не делали (нет веб-страницы), прятать их везде — правильное поведение; НЕ откатывал (вариант «различать рейл/вкладку» был бы сложнее и хуже). Пункт «Изменить вкладку» для нативного оставлен (может менять имя/цвет — осмысленно). Проверки: vitest 2306/2306, линт 0, лимиты 576/576, guards ✅. Поведение меню не изменилось — только вынесено под тест. Откат: `git checkout src/components/TabBar.jsx` + удалить `tabContextMenuItems.*` + версия на 1.2.249.

### v1.2.249 — Боковой рейл: 2 доводки 2B (перетаскивание в пределах секции + меню «Общего чата»)

Дата: 2026-08-13. Два улучшения по советам к Этапу 2B. **#2 Перетаскивание — только внутри своей секции** (API-значки среди API, веб среди веб): в [SourceRail.jsx](../src/components/SourceRail.jsx) значок получил `section` ('native'|'web'); при начале перетаскивания секция запоминается (`draggedSectionRef`), а события над/бросок у чужой секции игнорируются. Причина: рейл делит значки по типу, поэтому перетаскивание между секциями визуально ничего не давало («пустое» действие). Обёртки зовут те же обработчики вкладок (`handleDragStart/Over/Drop`), только с проверкой секции. **#3 Меню «Общего чата» без веб-only пунктов:** в [TabBar.jsx](../src/components/TabBar.jsx) для нативного источника (`isNative`) скрыты «Перезагрузить / Диагностика и логи / Копировать URL» — у него нет веб-страницы, эти действия ничего не делали (копировался `about:blank`). Касается и нативной вкладки, и значка рейла (меню общее). Тесты [SourceRail.vitest.jsx](../src/components/SourceRail.vitest.jsx) 17 (+1: бросок веб→API игнорируется, веб→веб срабатывает). Проверки: vitest 2301/2301, линт 0, лимиты 574/574, guards ✅. **Требует визуальной проверки.** Откат: `git checkout src/components/SourceRail.jsx src/components/TabBar.jsx src/App.jsx` + версия на 1.2.248.

### v1.2.248 — Боковой рейл, Этап 2B: правый клик + перетаскивание на значках

Дата: 2026-08-13. Перенос ещё двух функций вкладок на значки рейла ([SourceRail.jsx](../src/components/SourceRail.jsx)) — **переиспользование, новой логики нет**. **Правый клик** по значку открывает ТО ЖЕ меню, что у вкладок (Перезагрузить / Диагностика и логи / Копировать URL / Изменить / Закрепить / Закрыть): значок вызывает `onContextMenu(id, e.clientX, e.clientY)` → в [App.jsx](../src/App.jsx) `setContextMenuTab({id,x,y})`, а меню рендерит [TabBar.jsx:162-197](../src/components/TabBar.jsx) как `position:fixed` по координатам курсора (не привязано к вкладке — потому и работает из рейла). **Перетаскивание** значков — те же обработчики вкладок `handleDragStart/Over/Drop/End(id)` + подсветка цели (`dragOverId` → пунктирная рамка). Значок стал `draggable`; `dataTransfer` под гвардом (в тестовой среде может быть пустым). Тесты [SourceRail.vitest.jsx](../src/components/SourceRail.vitest.jsx) 16 (+2: правый клик с координатами, drag start/over/drop) + смоук `appStructure.test.cjs` (+1). **Важно (тех-долг):** меню сейчас живёт внутри `TabBar` — работает, пока вкладки на месте; при удалении вкладок в финале миграции меню надо будет вынести из `TabBar` (записано в план и [[decisions]] ADR-031). Watermark renderer 33860→33890, App.jsx 974/975. Проверки: vitest 2300/2300, линт 0, лимиты 574/574, guards ✅. **Требует визуальной проверки** (правый клик по значку → меню у курсора; перетащить значок → порядок меняется). Откат: `git checkout src/components/SourceRail.jsx src/App.jsx` + версия на 1.2.247.

**НЕ вошло (осознанно, отдельными шагами):** Этап 2C (отдельные Telegram-аккаунты сверху + аватар аккаунта) — самый большой архитектурный риск (поднять список аккаунтов из lazy `NativeApp`/`nativeStore` + научить `NativeApp` выбирать аккаунт по внешнему сигналу), плюс живое поведение переключения аккаунта нельзя проверить без запуска приложения. Остаток Этапа 3 (умный «+», сворачивание рейла, заглушить источник) — тоже отдельно.

### v1.2.247 — Боковой рейл: 2 фикса по ревью 2A (цифра бейджа + значок = div)

Дата: 2026-08-13. По плану исправления из ревью Этапа 2A. **Фикс #1 (цифра бейджа = как на вкладке):** рейл показывал ВСЕГО непрочитанных, а вкладка при режиме «Бейдж = только личные» (`overlayMode==='personal'`) — только личные → цифры расходились. Теперь [SourceRail.jsx](../src/components/SourceRail.jsx) получает проп `overlayMode` (из `settings.overlayMode`) и считает число по той же формуле, что [TabBar.jsx:79-81](../src/components/TabBar.jsx) (`personal ? split.personal : всего`). **Фикс #2 (значок — `<div role="button">`, не `<button>`):** внутри значка есть кликабельная точка связи (интерактивный элемент), а по стандарту HTML вкладывать интерактивное в `<button>` нельзя. Значок переведён на `<div role="button" tabIndex={0}>` + клавиатура (Enter/Пробел → переключение) — вложение стало корректным. Тесты [SourceRail.vitest.jsx](../src/components/SourceRail.vitest.jsx) 14 (+3: личные/всего по режиму, Enter на значке) + смоук `appStructure.test.cjs` (+`overlayMode`). Проверки: vitest 2298/2298, линт 0, лимиты 574/574, guards ✅. **Требует визуальной проверки.** Откат: `git checkout src/components/SourceRail.jsx src/App.jsx` + версия на 1.2.246.

### v1.2.246 — Боковой рейл: увеличенная область клика по точке связи (совет к 2A)

Дата: 2026-08-13. По совету из ревью 2A: точку связи 10px в углу значка было трудно поймать курсором. Теперь видимая точка осталась 10px, но вокруг неё — прозрачная кликабельная зона-обёртка 20px (растёт внутрь значка, в пустое место, не вылезает за него) в [SourceRail.jsx](../src/components/SourceRail.jsx). Обёртка — `role="button"` + `tabIndex` + `aria-label`/`title` + клавиатура (Enter/Пробел), клик/Enter → «Подключения»; `stopPropagation` — клик по точке НЕ переключает источник; сама точка `pointer-events:none` (клики ловит обёртка). Тест обновлён: клик по обёртке зовёт `onOpenConnections` И не зовёт `onSelect`. Watermark renderer 33840→33860. Проверки: vitest 2295/2295, линт 0, лимиты 574/574. **Требует визуальной проверки**. Откат: вернуть `onClick` на саму точку + версия на 1.2.245.

### v1.2.245 — Боковой рейл, Этап 2A: индикаторы вкладок на значках рейла

Дата: 2026-08-13. Перенос функций вкладок на значки рейла — **под-этап 2A (индикаторы)**. На каждом значке [SourceRail.jsx](../src/components/SourceRail.jsx): **бейдж непрочитанных** (верхний правый угол, «99+» при переполнении, разбивка 💬/📢 в подсказке), **точка связи** 🟢/🟡/🔴 (нижний правый угол, переиспользован [ConnectionStatusDot.jsx](../src/components/ConnectionStatusDot.jsx), клик → «Подключения»), **пульс** при новом сообщении (кольцо), **полоска загрузки** под значком, **✓ «всё прочитано»** (2с после падения счётчика до 0, как во вкладке), **подсказка** с именем аккаунта. Все данные — ТЕ ЖЕ объекты по id, что уже идут во вкладки ([TabBar.jsx:77-88](../src/components/TabBar.jsx)): `unreadCounts/unreadSplit/connectionHealth/webviewLoading/newMessageIds/accountInfo`; переданы в `<SourceRail>` в [App.jsx](../src/App.jsx) (+3 строки, App.jsx 971/975). Новой логики нет — переиспользование. Тесты [SourceRail.vitest.jsx](../src/components/SourceRail.vitest.jsx) +5 (11 всего: бейдж/«99+»/клик по точке→onOpenConnections/подсказка аккаунта/без объектов не падает) + смоук связки в [appStructure.test.cjs](../src/__tests__/appStructure.test.cjs) +1 (данные индикаторов). Разбито осознанно: **2B** (правый клик + перетаскивание) и **2C** (отдельные аккаунты + аватарки, источник аватарки веб-мессенджера не определён — ждёт решения пользователя) — отдельными шагами. Водяной знак renderer 33740→33840. Проверки: vitest 2295/2295, линт 0, лимиты 574/574, guards ✅. **Требует визуальной проверки** (включить рейл → на значках бейджи/точка/пульс/загрузка как на вкладках). Откат: флаг выкл или `git checkout src/components/SourceRail.jsx src/App.jsx` + версия на 1.2.244.

### v1.2.244 — Боковой рейл, Этап 1: костяк рейла источников (за флагом)

Дата: 2026-08-13. Реализован **костяк** бокового рейла (план [side-rail-migration-plan.md](side-rail-migration-plan.md), Этап 1). Новый компонент [SourceRail.jsx](../src/components/SourceRail.jsx): левая колонка значков источников — **нативные/API сверху** (`isNative` или id `native_cc`), разделитель, **веб-мессенджеры ниже**, кнопка «+» внизу. Значок = `emoji` на цветном фоне + пилюля активного слева (как Discord). Клик по значку зовёт `onSelect(id)` = тот же `handleTabClick` (переключение как у вкладок), «+» = `setShowAddModal`. Включается флагом `settings.sideRail` (по умолчанию **выкл** → приложение работает как сейчас, байт-в-байт). Вставлен первым ребёнком горизонтального ряда в [App.jsx](../src/App.jsx) — колонка-сосед, **НЕ прячет webview и НЕ трогает z-index-слои** (защита от чёрного экрана). Тумблер флага вернулся в [SettingsPanel.jsx](../src/components/SettingsPanel.jsx) (теперь у него есть консьюмер). **Верхние вкладки остаются** (по просьбе — пока не убираем). Полоска «← Общий чат» поверх веб-окна перенесена в Этап 2 (в Этапе 1 вкладки ещё на месте — возврат через вкладку «ЦентрЧатов»; и App.jsx у своего потолка). Тест [SourceRail.vitest.jsx](../src/components/SourceRail.vitest.jsx) — 6 проверок (порядок API↑/веб↓, клик→onSelect, aria-current активного, «+»→onAdd, пустой список→только «+»). Лимит `App.jsx` 960→975 (временно; вернётся вниз, когда в финале уйдёт TabBar), водяной знак renderer 33620→33740. Проверки: vitest 2290/2290, линт 0, лимиты 574/574. **Требует визуальной проверки** (включить тумблер → слева колонка значков; клик переключает источник; вкладки работают как раньше). Откат: флаг `sideRail=false` или удалить SourceRail.* + откатить App.jsx/SettingsPanel + версия на 1.2.243.

