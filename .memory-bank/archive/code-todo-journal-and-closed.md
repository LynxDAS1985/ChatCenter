# Архив: журнал отложенных задач + закрытые TODO

Вынесено из [`code-todo.md`](../code-todo.md) 2026-09-16 (v1.2.465): активный файл дошёл до
101891 байта при лимите 102400 — следующая запись не прошла бы.

Здесь лежит **история**: журнал отложенных задач за прошлые версии и TODO, которые уже закрыты.
Действующие задачи — в активном [`code-todo.md`](../code-todo.md), сюда за ними не ходить.

Правила архива — в [`README.md`](README.md).

---

## Журнал отложенных задач

| Дата записи | Версия в которой записано | Задача | Статус |
|---|---|---|---|
| 2026-05-15 | v0.89.16 | TODO-1 — убрать мёртвый `thumb` | 📋 в очереди |
| 2026-05-15 | v0.89.16 | TODO-2 — `tdlibPathToCcMediaUrl` fallback | 📋 в очереди |
| 2026-05-15 | v0.89.16 | TODO-3 — accountId в `tg-media/` именах | 📋 в очереди |
| 2026-05-15 | v0.89.16 | TODO-4 — progressive через `readFilePart` | 📋 на будущее |
| 2026-05-15 | v0.89.16 | TODO-5 — обновить api.md, architecture.md, decisions.md | 📋 в очереди |
| 2026-05-25 | v0.91.11 | TODO-6 — удалить диагностические `initial-restore-*` логи | 📋 в очереди |
| 2026-05-26 | v0.91.19 | TODO-7 — удалить `restore-start` / `scroll-save` / `autosave-save` | 📋 в очереди |
| 2026-05-26 | v0.91.20 | TODO-8 — удалить stack capture + multi-step postcheck (тики 50/100/300/500/1000мс) | 📋 в очереди |
| 2026-05-26 | v0.91.21 | TODO-9 — удалить `ipc-burst` счётчик в attachTelegramIpcListeners | 📋 в очереди |
| 2026-07-23 | v1.2.120 | TODO-10 — ВК mute-детект `_vkRowMuted`: убрать ветку `getClientRects().length` (лазейка ложного пропуска) | ✅ сделано v1.2.123 |
| 2026-07-23 | v1.2.120 | TODO-11 — ВК mute-детект: сузить селектор `[class*="muted" i]` → `[class*="mutedIcon" i],[class*="icon--muted" i]` | ✅ сделано v1.2.123 |
| 2026-07-24 | v1.2.123 | TODO-12 — Разгрузить `vk.hook.js` (300/300). Решено удалением мёртвого toast-наблюдателя (не выносом в модуль — впрыск одним `<script>`). Файл 300→247. | ✅ сделано v1.2.124 (требует визуальной проверки ВК-уведомлений) |
| 2026-07-24 | v1.2.128 | TODO-13 — `forceRemoveItem` не шлёт финальный отчёт по видимому, в отличие от `dismissItem`. Проверено (v1.2.128): оба вызова (`notification.js` — дубль-id и FIFO-выброс) находятся ВНУТРИ `addNotification` и ВСЕГДА сопровождаются добавлением карточки следом (новая / плитка в живой альбом / строка в живую стопку) → окно пустым-но-видимым не остаётся, стена по этому пути невозможна by construction. Правка НЕ требуется. См. [[notifications-ribbon]] «Невидимая стена» v1.2.128. | ✅ проверено — не требуется |
| 2026-07-24 | v1.2.131 | TODO-14 — разгрузить `nativeStoreIpc.js` (660/660) → импортировать `lastSenderLabel` и убрать inline-«зеркало» правила префикса имени. См. [[decisions]] ADR-022. | ✅ сделано v1.2.133 (блок превью вынесен в `nativeStoreLastMsgIpc.js`, зеркало убрано, заодно закрыт баг залипания имени) |
| 2026-07-27 | v1.2.133 | TODO-15 — свести резолв имени автора в один хелпер `tdlibClient` (сейчас 3 копии, файл 649/650) | 📋 в очереди |
| 2026-07-27 | v1.2.133 | TODO-16 — контракт-тест на форму события `chat:last-message` (поля `senderName`+`isOutgoing`) | 📋 в очереди |
| 2026-07-29 | v1.2.138 | TODO-17 — значок 📌 в компактном (узком) режиме списка чатов | ✅ сделано v1.2.139 |
| 2026-07-29 | v1.2.138 | TODO-18 — устойчивый порядок закреплённых (не пересортировывать по времени) | ✅ сделано v1.2.139 |
| 2026-07-29 | v1.2.138 | TODO-19 — тест: `InboxMode` реально поднимает закреплённые наверх (вынос `filterSortChats`) | ✅ сделано v1.2.139 |
| 2026-07-29 | v1.2.138 | TODO-20 — перетаскивание закреплённых для ручного порядка (после TODO-18) | ⛔ СНЯТО v1.2.161 — конфликтует с «закреплённые листаются в общем виртуальном списке»; делалось в v1.2.156-160, откачено. Порядок = порядок закрепления |
| 2026-07-29 | v1.2.138 | TODO-21 — чистка осиротевших закреплений (удалённые/покинутые чаты) | ⚠️ ОТКАЧЕНО v1.2.140 — пробная версия теряла данные (выводила «удалён» из отсутствия в неполном `store.chats`). Переоткрыто: нужен ЯВНЫЙ сигнал удаления чата от TDLib, а не вывод по отсутствию. См. features.md v1.2.140, [[electron-core]] грабля |
| 2026-07-29 | v1.2.145 | TODO-23 — остаток по удалению аккаунта: (а) проброс `account:renamed` на экран — ✅ СДЕЛАНО v1.2.146 (канал `tg:account-renamed`); (б) чистка кэш-файла `tg-cache-<id>.json` — ✅ СДЕЛАНО v1.2.146 (`removeAccountCacheFile`); (в) уже зависший призрак `tg_pending_1785329121471` — уйдёт, если выйти из него в приложении после пересборки (removeAccount удалит папку). (г) **НЕ сделано осознанно:** переименование папки сессии на диске `tg_pending_X → tg_<userId>` — опасно (TDLib держит файлы открытыми, переименование на Windows повредит сессию = потеря входа; функционально не нужно после v1.2.145). НЕ делать авто-очистку осиротевших папок вслепую (сотрёт живую сессию БНК при разовом сбое). См. features.md v1.2.145-146, [[electron-core]] грабля | 🟡 частично (а,б done; г — не делать без запуска) |
| 2026-07-29 | v1.2.142 | TODO-22 — удалить диагностику добавления/удаления аккаунта ПОСЛЕ разбора: логи `[acct-add]`/`[acct-remove]`/`[acct-auth]`/`[acct-update]`/`[acct-ipc]` (`tdlibBackend.js`, `tdlibIpcBridge.js`, `tdlibIpcHandlers.js`), `[acct-store] account-update` (`nativeStoreIpc.js`); снимок `[acct-store] sidebar …` (`InboxChatListSidebar.jsx`, v1.2.143). **Оставить НАВСЕГДА:** запись ошибки в лог из `ErrorBoundary.componentDidCatch` + обёртку `<ErrorBoundary name="NativeInbox">` вокруг `InboxMode` (`NativeApp.jsx`) — это польза, не диагностика. | 📋 в очереди (после диагностики) |
| 2026-07-30 | v1.2.149 | TODO-24 — рендер-тест `NativeApp`/`NativeMainContent`: (1) 4 ветки `NativeMainContent` (вход/пусто/чаты/заглушка режима) рисуются; (2) закрытие окна входа реально зовёт `store.resetLoginFlow` (spy). Чистые функции гейта (`shouldShowLoginScreen`/`shouldResetLoginFlowOnOpen`) уже покрыты, но JSX-проводку `onClose→resetLoginFlow` тест не проверяет — легко сломать. Нужен рендер-харнесс (тяжело), поэтому отложено. См. features.md v1.2.147-149. | 📋 в очереди |
| 2026-07-30 | v1.2.154 | TODO-25 — цвет-метка аккаунта: (1) спрашивать цвет при добавлении аккаунта (сейчас авто + 🎨); (2) разгрузить `InboxChatListSidebar.jsx` 584/600 и `NativeApp.jsx` 531/600. Оба необязательны. | 🟢 низкий |
| 2026-07-30 | v1.2.163 | TODO-26 — фильтр аккаунтов (ADR-026): (1) «активный аккаунт для входа» больше не задаётся кликом (клик=фильтр) — при желании добавить пункт правого клика «Использовать для входа»; (2) разгрузить `NativeApp.jsx` 595/600 (близко к лимиту). Оба необязательны. | 🟢 низкий |
| 2026-07-30 | v1.2.164 | TODO-27 — ресайз левого рейла аккаунтов перетаскиванием разделителя + пропорциональное уменьшение аватарок/значков; макс. ширина = текущая (76px), мин. — уточнить. Есть готовый паттерн ресайза панели чатов (ChatListResizeHandle + clampChatListWidth + isChatListCompact) для переиспользования. Крупная UI-правка, не проверить без запуска. | 🟡 средний |
| 2026-07-30 | v1.2.164 | TODO-27 — ресайз левого рейла аккаунтов + масштаб значков | ✅ сделано v1.2.165 (useAccountRailResize + масштаб railScale + подписи прячутся <64px; AccountAvatar вынесен в свой файл) |
| 2026-08-03 | v1.2.190 | TODO-28 — канал не запоминает прокрутку (перезагрузка окна вокруг read-line, [nativeStoreIpc.js:244]); план Вариант A готов, механизм aroundId уже есть | 📋 план готов, фикс не сделан (риск: общий механизм окна) |
| 2026-08-03 | v1.2.193 | TODO-29 — МАКС уведомляет о своих сообщениях (сторож списка, ветка stableUnreadBodyChanged); добавлен зонд `max-own-probe`, ждём лог → фильтр «моё» + УБРАТЬ зонд (файл 300/300) | 🟡 ждёт диагностику пользователя |
| 2026-08-05 | v1.2.199 | TODO-30 — альбом с файлом без пути (буфер/canvas) отправлять байтами, а не пропускать; сейчас предупреждает тостом+логом | ✅ сделано v1.2.203 (через новый IPC `tg:write-temp-file` — файл без пути пишется во временный и уходит в альбом) |
| 2026-08-06 | v1.2.202 | TODO-31 — разбить `windowManager.js` (272/300): вынести `attachDevRequestTiming` в `devRequestTiming.js` | ✅ сделано v1.2.205 (windowManager 195/300) |
| 2026-08-06 | v1.2.209 | TODO-32 — разбить `PhotoSendModal.jsx` (545/600, 91%): вынести ленту миниатюр (`.map` по urls + добавить/удалить/перетащить) в отдельный компонент `PhotoThumbStrip.jsx`. Рефактор renderer без запуска рискован — делать с визуальной проверкой | 🟡 средний (близко к лимиту) |
| 2026-09-01 | v1.2.323 | TODO-33 — Ozon-сторож. **Этап 1 СДЕЛАН (v1.2.329-331)**: `ozon.hook.js` = рабочий сторож чатов «Покупатели» (`__CC_NOTIF__ src=ozon-list`); v1.2.330 — замок раздела по `?group=` (fail-open) + сужен наблюдатель + переход к чату (`navigators/ozonNavigate.js`); v1.2.331 — журнал `__CC_DIAG__ozon-list` (sec-change / rows-unread-emitted по изменению / attach-error), чтобы тест не был «немым». **Остаётся**: (а) ПОДТВЕРДИТЬ замок раздела вживую — по журналу `sec-change to=other` при переключении на «Поддержку»; если строки нет (Ozon не меняет адрес) → добавить запасной DOM-признак раздела; (б) **Этап 2** — ответ из нашей системы В Ozon (заход с ОТКРЫТЫМ чатом → снять поле ввода `textarea.m9d-t2` + кнопку «Отправить» + «моё/чужое», затем функция отправки); (в) аватар покупателя в уведомлении; (г) 🟢 точное сравнение имени в `ozonNavigate` (сейчас по подстроке). | 🟡 Этап 1 готов, ждёт ручной проверки (журнал); Этап 2 не начат |
| 2026-09-01 | v1.2.332 | TODO-34 — удалить ВРЕМЕННУЮ ловушку `process.on('warning')` → `[max-listeners-diag]` в `main/main.js` (добавлена для поиска источника `MaxListenersExceededWarning: 11 did-stop-loading listeners`). После появления в `chatcenter.log` строки `[max-listeners-diag]` со стеком: (1) по стеку найти файл-виновник, кто вешает лишний `did-stop-loading`; (2) починить прицельно (снять старый слушатель / поставить гейт / либо `setMaxListeners` если это законные N подписок Electron); (3) удалить ловушку. Если стек укажет только внутренние файлы Electron → предупреждение безвредно, ловушку удалить, лимит не трогать. | ✅ ЗАКРЫТО v1.2.429: стек = ЧИСТО внутренний Electron (`executeJavaScript`→`once('did-stop-loading')`, самоснимающиеся, утечки нет). Ловушка удалена. Лимит ВСЁ ЖЕ поднят (`web-contents-created`→`setMaxListeners(30)`) — по явной просьбе убрать шум из журнала (безопасно). См. features.md v1.2.429. |
| 2026-09-01 | v1.2.333 | TODO-35 — Логотип в уведомлении. Запасной вариант `iconDataUrl: extra?.iconDataUrl \|\| getMessengerLogo(mInfo?.id)` ([webviewHandleNewMessage.js:110](../src/utils/webviewHandleNewMessage.js#L110)) показывает логотип мессенджера вместо эмодзи для ВСЕХ веб-мессенджеров без аватара отправителя (ВК/МАКС/WhatsApp/веб-Telegram), не только Ozon. **РЕШЕНО пользователем 2026-09-01: 🅰️ ОСТАВИТЬ у всех** (единообразно). Код НЕ менять — текущее поведение v1.2.333 и есть выбранный вариант. Настоящий аватар отправителя по-прежнему в приоритете. | ✅ решено (оставить у всех) |
| 2026-09-02 | v1.2.341 | TODO-36 — Ozon мульти-источник (Сообщения+Вопросы+будущий) через DOM-слежку, БЕЗ API (см. [[decisions]] ADR-037: API платный, не предлагать). **План 4 шага**: **Шаг 1** (v1.2.341, В РАБОТЕ) — разовый разведчик страницы «Вопросы» `/app/reviews/questions` в `ozon.hook.js` (`__CC_DIAG__ozon-q`): счётчик «Новое N» + строки вопросов + обновляется ли фон без клика. **Шаг 2** — сторож вопросов (`ozon-questions.hook.js`, `__CC_NOTIF__ src=ozon-questions`). **Шаг 3** — фоновая скрытая страница на партиции `persist:ozon` (одна учётка) в `App.jsx` + виджет быстрого перехода. **ВИДЖЕТ СДЕЛАН (v1.2.343-348)**: вертикальный док круглых иконок внутри вкладки Ozon (число новых, пульс, точка-статус, подсветка активного, тускнеет в покое, ручка перетаскивания). **СЧЁТЧИКИ ВИДЖЕТА ПОДКЛЮЧЕНЫ (v1.2.349)**: новый сигнал `__CC_OZON_COUNT__{s,n}` из `ozon.hook` → парсер → handler → `ozonCounts` в App → бейджи. `msg` = DOM непрочитанных «Покупателей» (сам сбрасывается). `qa` = ярлык «Новое N» (парсер УНВЕРИФИЦИРОВАН — нужна визуальная проверка на странице «Вопросы»; не совпадёт → 0 без вреда). Т.к. webview грузит ОДИН раздел за раз, второй счётчик показывает последнее известное. **ШАГ 2 СДЕЛАН (v1.2.353)**: сторож `_scanQ` в `ozon.hook` — новый вопрос БЕЗ ответа (`answers===0`, последняя числовая ячейка строки-таблицы) → `__CC_NOTIF__ src=ozon-questions` (товар+текст вопроса из BUTTON) + qa-счётчик = число без ответа. `_scan` (Покупатели) гейтнут путём `/app/messenger`, `_scanQ` — `/reviews/questions` (защита от кросс-загрязнения). **Правило «0=новый» — гипотеза** (одна строка разведки + слова пользователя; заголовки `ozon-q2 H*` не сняты) → проверить на реальном новом вопросе; если строка агрегирует несколько вопросов на товар — доработать. **ШАГ 3 ФАЗА 3a СДЕЛАНА (v1.2.358, ГИПОТЕЗА — ждёт проверки живьём)**: скрытая фоновая webview «Вопросы» (`src=/app/reviews/questions`, offscreen `left:-10000`, НЕ display:none, та же `persist:ozon`) в контейнере Ozon (App.jsx); `monitor.preload` сам впрыскивает `ozon.hook`; новый `src/utils/ozonBgWatcher.js` вешает слушатель `console-message` и переводит `__CC_NOTIF__ ozon-questions`/`__CC_OZON_COUNT__ qa`/`ozon-q scan` на основной id Ozon (лог `[ozon-bg]`). НЕ подтверждено: грузится ли скрытая страница в фоне + не блокирует ли антибот Ozon вторую страницу сессии → проверить по журналу (`[ozon-bg] ozon-q scan …` сидя на «Сообщениях»). Если нет — план Б (будить/отдельное окно). **Остаётся**: Фаза 3b (симметричная фоновая «Сообщения» — чтобы на «Вопросах» не терять сообщения покупателей); rail-сумма из фона; клик по уведомлению вопроса → открыть «Вопросы». **Шаг 4** — обобщение под 3-й источник. Риски: память (N фоновых страниц), переверстка Ozon. **v1.2.361**: журнал пользователя показал 0 строк `[ozon-bg]` → Фаза 3a МОЛЧИТ (страница не грузится / антибот / хук не впрыснут / консоль не долетает — неизвестно). Добавлена диаг-лесенка в `ozonBgWatcher.js` (страница создана→did-fail/finish-load→dom-ready→первое сообщение→ozon-q scan) — ждёт лог пользователя, чтобы найти обрыв. **Окно-фикс v1.2.360 ПОДТВЕРЖДЁН** журналом (`post-ready maximize isMaximized=true`). **Фаза 3b (фоновые «Сообщения») НЕ начата** — ждёт подтверждения, что механизм фоновой страницы вообще работает. **v1.2.365-366: КОРЕНЬ молчания найден и исправлен** — сторож не впрыскивался в фоновую страницу (Ozon CSP блокирует preload-`<script>`, а executeJavaScript-фолбэк webviewSetup покрывает только зарегистрированные webview; фоновая не регистрируется). Фикс: `ozonBgWatcher` на dom-ready сам впрыскивает хук через `app:read-hook`+`executeJavaScript`. **ПОДТВЕРЖДЕНО журналом 17:41**: `ozon-q scan rows=11 unans=1` — фон видит таблицу и считает вопросы без ответа. Обе гипотезы (антибот; SPA не рисует в скрытой вкладке) отвергнуты. **Осталось**: живое уведомление на НОВЫЙ вопрос (проверка юзером); **Фаза 3b** (симметричная фоновая «Сообщения»); клик по уведомлению вопроса → «Вопросы». **ФАЗА 3Б СДЕЛАНА (v1.2.368)**: `ozonBgWatcher` обобщён на оба раздела; в App.jsx `.map` монтирует ДВЕ скрытые фоновые страницы (`/reviews/questions` + `/app/messenger?group=customers_v2`), обе на `persist:ozon`, впрыск сторожа через executeJavaScript. Теперь и вопросы, и сообщения покупателей ловятся из фона, где бы ни был пользователь. Ждёт подтверждения по журналу (`[ozon-bg] ozon-list reason=… unread=…`). **Остаётся**: живая проверка уведомлений; клик по уведомлению вопроса → открыть «Вопросы» (мелочь); **Шаг 4** (3-й источник, позже); убрать диагностическую лесенку после стабилизации. **ШАГ 4 КОНКРЕТИЗИРОВАН (2026-09-03, запрос пользователя): 3-й источник = «Отзывы покупателей»** (`Товары → Отзывы`). Цель: отзыв со статусом «Новый» (есть вкладка «Новые N» + колонка «Статус» с бейджем «Новый») → уведомление, как вопросы/сообщения. **Подход = зеркало `_scanQ`**: (A) РАЗВЕДКА — 3-я скрытая фоновая webview на URL отзывов + диаг `_scanR` логирует колонки/бейджи (точный URL списка отзывов и селектор бейджа «Новый» НЕИЗВЕСТНЫ — снять живьём по журналу, как столбец «Ответы» для вопросов); (B) СТОРОЖ `_scanR` — строки со статусом «Новый» → `__CC_NOTIF__ src=ozon-reviews` (товар + текст отзыва/оценка) + `__CC_OZON_COUNT__{s:'rv'}`; база уже-показанных `_rPrev` в `localStorage['__ccOzonRSeen']` (переживает reload, как `__ccOzonQSeen`). **5 ТОЧЕК КАСАНИЯ**: (1) `ozon.hook.js` — `_scanR` + гейт по pathname отзывов (как `_scanQ` гейтит `/reviews/questions`); (2) `ozonBgWatcher.js` — добавить `'rv'` в маршрут счётчика (стр.83) и `'ozon-reviews'` в маршрут уведомления (стр.93); (3) `App.jsx` — в `.map` добавить 3-й URL (фоновая страница отзывов) + `periodicReloadMs` только если база персистится; (4) эффект суммы рейла `unreadCounts = msg+qa+rv` (App.jsx); (5) виджет `OzonQuickWidget` `SECTIONS` — 3-й раздел `{key:'rv',label:'Отзывы',icon:'⭐',url:…,match:…}`. **ГРАБЛЯ (найдена по коду):** виджет ищет активный раздел по `url.indexOf(match)` — `match` отзывов НЕ должен быть подстрокой URL вопросов: `/app/reviews` матчит и `/app/reviews/questions` → нужен непересекающийся/точный `match` (порядок в SECTIONS тоже важен). **ОПАСНАЯ ЗОНА:** ещё одна фоновая страница на `persist:ozon` = +память и +риск, что Ozon сочтёт частым (антибот); reload отзывов включать ТОЛЬКО при персисте базы (иначе повторные/пропущенные уведомления). **ШАГ A (РАЗВЕДКА) НАЧАТ (v1.2.380, 2026-09-03)**: `_scanR` в ozon.hook пишет `__CC_DIAG__ozon-r url/rows/heads/novy/tabNew/sample` (гейт `review && !question`); 3-я скрытая страница `/app/reviews` в App.jsx (best-guess URL); маршрут диагностики в ozonBgWatcher (`ozon-r`). Уведомлений/счётчиков ПОКА НЕТ. **Ждёт лог пользователя** `[ozon-bg] ozon-r …` (зайти на `Товары → Отзывы` с записью журнала) → по нему точный URL + селектор «Новый». Дальше Шаг B (сторож `_scanR` → `src=ozon-reviews` + `{s:'rv'}` + база `__ccOzonRSeen`), Шаг C (проводка: ozonBgWatcher `rv`/`ozon-reviews`, сумма рейла msg+qa+rv, виджет 3-й раздел ⭐), Шаг D (само-освежение при персисте базы). **ШАГИ B/C/D СДЕЛАНЫ (v1.2.383)**: по журналу разведки (`ozon-r` от 2026-09-03: URL `/app/reviews`, столбцы `Название|Статус|Отзыв|(бейдж)|Оценка`, бейдж «Новый» в колонке БЕЗ заголовка, novy=2=«Новые 2») сделан сторож `_scanR` (бейдж «Новый» по точному тексту ячейки → `__CC_NOTIF__ src=ozon-reviews` + счётчик `rv` + база `__ccOzonRSeen`); проводка в ozonBgWatcher (`rv`/`ozon-reviews`) + App (сумма msg+qa+rv, проброс rv) + виджет (кнопка ⭐ «Отзывы», `match:'/app/reviews'` ПОСЛЕ qa); авто-освежение отзывов (bgI 0 или 2). Автотеста на `_scanR` нет (DOM-логика) — ручная проверка по журналу `ozon-r scan` + живой отзыв. Остаток: клик по уведомлению отзыва → открыть «Отзывы» (мелочь). **ЦЕЛЬ TODO-36 «мульти-источник (Сообщения+Вопросы+Отзывы)» ЗАКРЫТА** (ждёт живой проверки). | 🟢 Шаг 4 = «Отзывы» B/C/D сделаны в v1.2.383 (ждёт живой проверки + перезапуск) |

---

## TODO-42 ✅ ЗАКРЫТО в v1.2.463 — МАКС: НЕТ проверки «это моё исходящее» — свои сообщения приходили уведомлениями (заявлено и закрыто 2026-09-15)

**Жалоба**: уведомление о собственном отправленном сообщении («смотрю», 18:20; «Понял, отлично»).

**Корень (🟢, доказан журналом + чтением кода)**: у МАКС проверки исходящего нет вообще.
`maxOutgoingReport` ([maxDiagnostics.js](../main/preloads/utils/maxDiagnostics.js)) ИЗМЕРЯЕТ признаки и
возвращает строку для журнала; во всём `main/` и `src/` она упоминается дважды — объявление и подстановка
в текст диагностики ([monitor.preload.cjs](../main/preloads/monitor.preload.cjs)). Решения по ней никто не принимает.
Единственное, что обычно гасит свои сообщения — правило «вкладка открыта» в
[webviewHandleNewMessage.js](../src/utils/webviewHandleNewMessage.js), а оно зависит от фокуса окна.
Сообщение идёт двумя путями (500 мс и 1200 мс): отправил и переключился в другое окно — второй путь проходит.
За 2026-09-15: поймано 4 своих, показано 2.

**Почему НЕ чиню сразу**: измеренных данных не хватает для безопасного правила. Из журнала видно:

| Что | Входящее | Исходящее |
|---|---|---|
| `bubbleL` (левый край пузыря), строка `rowL=471 rowW=524` | всегда **487** (= rowL + 16) | 634 / 724 / 791 / 848 |
| `ticks` | 1 у текста, **2 у файла** | 2 |

- по `ticks` различать НЕЛЬЗЯ (входящий файл тоже даёт 2);
- правило «отступ пузыря больше 16» на сегодняшних данных работает, но у ОЧЕНЬ ДЛИННОГО исходящего
  пузырь занимает почти всю ширину строки и тоже начнётся близко к левому краю → правило может промахнуться
  и **заглушить настоящее входящее**. Ошибка в эту сторону дороже, чем лишнее уведомление.

**Что сделать шаг 1 (безопасно, ничего не меняет в поведении)**: дописать в уже работающую строку
`[MAX-OUT-DIAG]` правый край пузыря (`bubbleR`) и классы внутренних элементов строки. Одна строка кода,
показ не затрагивается. Снять день данных.

**Что сделать шаг 2 (после данных)**: сделать `isMaxOutgoing(node)` отдельной проверкой «да/нет»
(правило: пузырь прижат к ПРАВОМУ краю строки) и вызывать её в `monitor.preload.cjs` перед отправкой —
не пускать исходящее дальше. Плюс тест на реальных числах из журнала (вход 487/…, исход 634…848).

**Ловушка, которую нельзя повторить**: не оставлять новое измерение без потребителя. Если проверка пока
не включается — написать это прямо в коде рядом (см. ловушку в `mistakes/webview-injection.md`).

**Где описан весь случай**: [notifications-debug.md](notifications-debug.md), раздел 3.2 и журнал наблюдений.

✅ **ЗАКРЫТО в v1.2.463 (2026-09-15).** Отдельный день сбора данных НЕ понадобился: правый край пузыря
измеряется прямо в коде, поэтому «шаг 1 / шаг 2» слились в один.
Что сделано: `maxOutgoingReport` (возвращала строку) заменена на `maxOutgoingVerdict` (возвращает решение);
само правило — чистая `maxOutgoingDecision(координаты)`: перевес отступа пузыря вправо больше 40 точек = своё.
Вердикт спрашивается ДО попадания текста в список на отправку → отсекает оба пути разом.
Сомнительный случай (пузырь во всю ширину) и сбой замера = ПРОПУСКАЕМ (односторонняя ошибка, ADR-059).
Проверено: [maxOutgoing.vitest.js](../src/__tests__/maxOutgoing.vitest.js) 12/12 на реальных числах журнала
+ прогон «наоборот» 5 из 5. ⏳ Ждёт подтверждения человеком (нужен полный перезапуск).

---
