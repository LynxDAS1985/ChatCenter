# Архив v1.1.14 – v1.1.15

Заархивировано 11 июня 2026 при выпуске v1.1.18. Промежуточные шаги — финальный UI-вариант в AISidebar (v1.1.15), `AiBridgeCheck.jsx`.

---

## v1.1.15 — Кнопка проверки AI перенесена из лог-вьюера в AISidebar

**Юзер**: «я не понял, ты тесты добавил в логи??? кнопки которые делают тесты?» Слово «Тест» сбило с толку. И не место кнопкам-фичам в окне логов.

**Что сделано**:
1. Файл переименован: `AiBridgeTester.jsx` → `AiBridgeCheck.jsx`.
2. Кнопка убрана из `LogModal.jsx` — логи снова чистые.
3. Кнопка добавлена в `AISidebar.jsx` — иконка 🤖 в шапке рядом с ⚙️ настройками.
4. Заголовок модалки: «🧪 Тест AI Bridge» → «🤖 Проверка AI».
5. Префикс в логах: `[ai-bridge-tester]` → `[ai-bridge-check]`.

**Логика та же**: 3 режима (Локальный Ollama / API / Веб-интерфейс) + 4 провайдера + поля URL+Модель + textarea + кнопка «📤 Спросить» → ответ AI. Все этапы через `app:log`.

**Регрессия**: lint 0, vitest 1708/1708, fileSizeLimits 437/437.

---

## v1.1.14 — UI-тестер AI Bridge без «инструментов разработчика»

**Корректировка**: в предыдущих changelog я ошибочно писал «можно проверить через инструменты разработчика». Юзер: «я запретил связывать что-то с этим инструментом, все должно быть в логах, или в нашу программную среду».

**Что сделано в коде**:
- `src/components/AiBridgeTester.jsx` (в v1.1.15 переименован в `AiBridgeCheck.jsx`) — модалка тестера.
- `LogModal.jsx` — была добавлена кнопка «🧪 Тест AI Bridge» (в v1.1.15 убрана из логов).

**Поправлены формулировки**: changelogData.js (записи v1.1.10/v1.1.11) + progress документы — убраны «через DevTools/консоль разработчика», заменены на «через 📒 Логи ChatCenter → 🧪 Тест AI Bridge» (потом → «через 🤖 Проверка AI в боковой панели»).

**Тесты (+11)**: рендер 4 полей, переключение mode, валидация пустого вопроса, happy path, error mapping, retryable, IPC throw, логи через app:log.

**Регрессия**: lint 0, vitest 1697 → 1708, fileSizeLimits 435 → 437.

**Финальное место кнопки** (после v1.1.15) — в шапке AISidebar.
