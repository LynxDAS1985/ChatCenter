# Архив features: v1.2.256 – v1.2.267 (окно «Добавить» + портал полосы)

Перенесено 2026-08-19 из features.md (разгрузка под лимит 100 КБ). Тема: единая полоса — веб-мессенджеры в нативном рейле, окно «протокол → мессенджер», портал полосы на уровне App.

### v1.2.267 — Окно «Добавить»: доводки по ревью (счётчик/стрелки/лог/статус) [ОЖИДАЕТ ВИЗУАЛЬНОЙ ПРОВЕРКИ]

Дата: 2026-08-14. По ревью v1.2.266 (3 из 10 улучшений были частичными). Исправлено: (#1) **счётчик «N доступно» теперь учитывает поиск** — считает ВИДИМЫЕ доступные плитки (`availShown = shown.filter(!locked)`), а не «всего» (при поиске «What» → «1 доступно»). (#2) **стрелки клавиатуры** ← → ↑ ↓ двигают фокус между плитками (`onGridKey` на сетке, cols=4; Tab работал и раньше). (#3) **лог добавления** — `app:log INFO «[add-source] добавлен веб-мессенджер: <имя>»` в [App.jsx](../src/App.jsx) onAddWeb (раньше путь был немой). (#4) **статус «уже добавлен»** сверяется по URL **И имени** (`connectedWeb` = `{url,name}` из NativeApp) — custom-вкладка с другим адресом, но тем же именем теперь тоже подсвечивается; `store.accounts` защищён `|| []`. Файлы: [AddSourceModal.jsx](../src/native/components/AddSourceModal.jsx) (191/600), [NativeApp.jsx](../src/native/NativeApp.jsx), [App.jsx](../src/App.jsx) (1012/1015). Тесты [AddSourceModal.vitest.jsx](../src/native/components/AddSourceModal.vitest.jsx) (12 — +счётчик-с-поиском, +стрелки, +статус-по-имени). Проверки: линт 0, лимиты 580/580, vitest зелёный. Осталось (не код): #5 стекинг окна — только визуальная проверка. **Требует ВИЗУАЛЬНОЙ проверки.** Откат: `git checkout src/native/components/AddSourceModal.jsx src/native/NativeApp.jsx src/App.jsx`.

### v1.2.266 — Окно «Добавить»: приподнятая карточка + 10 улучшений [ОЖИДАЕТ ВИЗУАЛЬНОЙ ПРОВЕРКИ]

Дата: 2026-08-14. По согласованному макету. Всё в [AddSourceModal.jsx](../src/native/components/AddSourceModal.jsx) (171/600) + 2 пропса из NativeApp. **10 улучшений:** (1) шаги-точки ①—② сверху вместо простого текста; (2) ⚡-иконка в заголовке; (3) поиск мессенджера (`useState search` фильтрует по имени); (4) статус-точка на плитке — зелёная «уже подключён/добавлен» (данные: `hasApiTelegram` из `store.accounts`, `connectedWebUrls` из `webSources`), серая «не добавлен»; (5) подсказка под именем («по номеру» для API, «в браузере» для веб); (6) ховер бренд-цветом (border+box-shadow цветом мессенджера); (7) фокус-кольцо (`onFocus`/`onBlur` outline, кнопки уже фокусируемы с клавиатуры); (8) счётчик «N доступно» (API=1, Веб=4); (9) «🔒 скоро» на недоступных по API; (10) плавное появление карточки — переиспользован существующий keyframe `native-menu-popin` (тот же, что у тоста). Фон карточки — «приподнятый» (`#171b23→#11141b` + мягкая тень + лёгкое свечение), не сливается с чёрным AMOLED. Логику добавления НЕ трогали. Пустой поиск → «Ничего не найдено». Тесты [AddSourceModal.vitest.jsx](../src/native/components/AddSourceModal.vitest.jsx) (9 — +поиск/счётчик/статус). Проверки: линт 0, лимиты 580/580, vitest зелёный. **Требует ВИЗУАЛЬНОЙ проверки.** Откат: `git checkout src/native/components/AddSourceModal.jsx src/native/NativeApp.jsx`.

### v1.2.265 — Окно «Добавить»: светлее фон + настоящий логотип Telegram (не эмодзи) [ОЖИДАЕТ ВИЗУАЛЬНОЙ ПРОВЕРКИ]

Дата: 2026-08-14. По жалобе (скриншот): окно `AddSourceModal` слишком тёмное + значок Telegram — «выдуманный» эмодзи ✈️, а в проекте есть НАСТОЯЩИЙ логотип. **Фикс:** (1) фон окна светлее — как карточка «инфо об аккаунте» ([AccountContextMenu.jsx:136](../src/native/components/AccountContextMenu.jsx#L136)): градиент `#1a1f2e→#141823`, плитки/карточки на `rgba(255,255,255,0.05-0.08)` с рамкой `rgba(255,255,255,0.14)` → не сливается с чёрным AMOLED. (2) Настоящий логотип Telegram через `<MessengerIcon messenger="telegram">` (data-URI PNG из [messengerLogos.js](../src/native/utils/messengerLogos.js), тот же, что на API-аккаунте в `AccountAvatar`); эмодзи ✈️ заменён на логотип в ДВУХ местах: плитки окна `AddSourceModal` (по `m.id` — Telegram картинка, остальные эмодзи-фолбэк через `getMessengerEmoji`) и значок Telegram в боковой полосе `RailWebIcon` (по `id==='telegram'` или имени). Остальные мессенджеры (WhatsApp/ВК/Макс) — логотипов-картинок нет, остаётся эмодзи (без регресса). Файлы: [AddSourceModal.jsx](../src/native/components/AddSourceModal.jsx), [RailWebIcon.jsx](../src/native/components/RailWebIcon.jsx). Проверки: линт 0, vitest зелёный. **Требует ВИЗУАЛЬНОЙ проверки.** Откат: `git checkout src/native/components/AddSourceModal.jsx src/native/components/RailWebIcon.jsx`.

### v1.2.264 — Одна кнопка «＋ Добавить» → окно «протокол → мессенджер» [ОЖИДАЕТ ВИЗУАЛЬНОЙ ПРОВЕРКИ]

Дата: 2026-08-14. По согласованному макету (вариант «окно по центру, 2 шага»). Раньше в полосе были ДВЕ кнопки — «+аккаунт» (вход Telegram) и «+веб» (окно добавления веб-мессенджера) — путали, и «+аккаунт» открывал вход ЗА веб-окном (не видно). **Что сделано:** (1) новый компонент [AddSourceModal.jsx](../src/native/components/AddSourceModal.jsx) — окно 2 шага: Шаг 1 протокол (⚡ API / 🌐 Веб), Шаг 2 доступные мессенджеры (по API — Telegram, остальные «🔒 скоро»; по Вебу — Telegram/WhatsApp/ВК/Макс из `DEFAULT_MESSENGERS` + «Другой»→ручной ввод URL). Рисуется ВНУТРИ NativeApp (`.native-mode`) → цвета AMOLED. (2) В [NativeSidebar.jsx](../src/native/components/NativeSidebar.jsx) две «+» заменены ОДНОЙ «＋ Добавить» (`data-testid="native-rail-add"`); API-аккаунты и веб-мессенджеры разделены линиями. Клик «Добавить» сперва зовёт `onActivateNative` (вернуть API-инбокс, чтобы окно/вход были видны над вебом — заодно чинит баг «+аккаунт»), затем `onOpenAddSource`. (3) В [App.jsx](../src/App.jsx) `onAddWeb` теперь принимает выбранный мессенджер: пресет `DEFAULT_MESSENGERS` → новая вкладка (свежая `persist:custom_<ts>` + `accountScript`, как в `AddMessengerModal`); `null` → старое окно ручного ввода URL. Механизм добавления/сессии НЕ трогали (переиспользование). Тесты: [AddSourceModal.vitest.jsx](../src/native/components/AddSourceModal.vitest.jsx) (5 — 2 шага, API→вход, Веб→добавить, «Другой»→ручной, Escape), NativeSidebar.vitest (одна кнопка + клик), appStructure (+тест v1.2.264). Проверки: линт 0, лимиты 579/579 (App.jsx 1011/1015 — временно), vitest зелёный. **Требует ВИЗУАЛЬНОЙ проверки** (окно не тестируется без запуска). Откат: удалить `AddSourceModal.jsx` + `git checkout src/App.jsx src/native/NativeApp.jsx src/native/components/NativeSidebar.jsx`.

### v1.2.263 — Ревью-фиксы полосы: цвет как у API-окна + возврат к API по клику аккаунта + ресайз при вебе [ОЖИДАЕТ ВИЗУАЛЬНОЙ ПРОВЕРКИ]

Дата: 2026-08-14. По придирчивому ревью v1.2.262 (нашёл 1 🔴 + 4 🟡) и жалобам пользователя («через API не работаю», «вёрстка сбилась», «полоса должна быть как окно API по цвету»). **Корни (по коду + скриншоты):** (1) **цвет** — переменные `--amoled-surface/border/text-dim` и фон заданы ТОЛЬКО в `.native-mode` ([styles-base.css:18-37](../src/native/styles-base.css#L18)); после портала полоса оказалась ВНЕ `.native-mode` → цвета «пропали», фон стал не как у API-окна. (2) **🔴 возврат к API** — клик по API-аккаунту/«Все» при открытом вебе не переключал вкладку (звался только стор), веб-слой оставался сверху → «API не работает». (3) ресайз-разделитель жил в native-слое → был недоступен при активном вебе; `minWidth` слота фиксировался и мешал сужать полосу. **Фиксы:** (a) полоса в портале обёрнута в `<div className="native-mode" style={flexDirection:'row'}>` → переменные+фон+тема как у API-окна; (b) новый колбэк `onActivateNative` (App→NativeApp→NativeSidebar): «Все» (`onClick`) и каждый аккаунт (`onClickCapture`) при вебе сперва зовут `handleTabClick(NATIVE_CC_ID)` → показываются API-чаты; (c) разделитель ресайза перенесён в портал → доступен всегда; `minWidth` убран, слот-поиск переведён на `useLayoutEffect` (вставка до кадра → нет «прыжка»); (d) активный веб-слой сдвинут на `top:34` → полоска «← Общий чат» не перекрывает шапку веба; (e) **логи** переходов веб↔API (INFO: «← Общий чат», «возврат к API аккаунт/Все») + WARN «слот не найден». Файлы: [App.jsx](../src/App.jsx) (1004/1005 — потолок 995→1005 временно), [NativeApp.jsx](../src/native/NativeApp.jsx), [NativeSidebar.jsx](../src/native/components/NativeSidebar.jsx), тест [appStructure.test.cjs](../src/__tests__/appStructure.test.cjs) (+проверки проводки/цвета/ресайза). Код-проверки: линт 0, лимиты 578/578, appStructure 56/56. **Требует ВИЗУАЛЬНОЙ проверки** (агенту запуск запрещён). Откат: `git checkout src/App.jsx src/native/NativeApp.jsx src/native/components/NativeSidebar.jsx`.

### v1.2.262 — Полоса всегда слева (портал) + «← Общий чат» над вебом

Дата: 2026-08-14. По уточнённому дизайну (вариант А): полоса-иконки видна ВСЕГДА слева; веб открывается СПРАВА от неё (внутри интерфейса), сверху веба — стрелка «← Общий чат» → возврат к API-чатам. **Что сделано:** (1) полоса рисуется порталом (`createPortal`) в слот `<div id="app-native-rail">` на уровне [App.jsx](../src/App.jsx) → видна всегда (в т.ч. когда справа веб); (2) над активным веб-мессенджером — полоска «← Общий чат · <имя>» (кнопка → `handleTabClick(NATIVE_CC_ID)`); (3) в NativeApp — WARN в журнал, если слот не найден. Доводки цвета/возврата/ресайза — в v1.2.263.

### v1.2.261 — ОТКАТ портала (v1.2.260): он сломал вёрстку. Полоса снова внутри вкладки

Дата: 2026-08-14. Портал (v1.2.260, рисовал полосу на уровне App через `createPortal` в слот `#app-native-rail`) **сломал вёрстку** — подтверждено скриншотом пользователя (столбцы native-инбокса разъехались). Плюс пользователь: «мы обсуждали по-другому как включать/выключать/переключаться» — подход был неверный. По правилам (TIME-BOX + «не сломать существующее» + «не угадывать») портал **откачен**: убраны слот `#app-native-rail` в [App.jsx](../src/App.jsx), `createPortal`+`railSlot` в [NativeApp.jsx](../src/native/NativeApp.jsx) — `NativeSidebar` снова рендерится ВНУТРИ вкладки «ЦентрЧатов» (как v1.2.259). Смоук портала в `appStructure` заменён на проверку «портала нет + один стор». Проверки: vitest 2298/2298, линт 0, лимиты 578/578, guards ✅. **Состояние после отката = v1.2.259:** веб-мессенджеры значками в нативной полосе с функциями (правый клик/drag/загрузка/добавить веб), два «+» подписаны (аккаунт/веб); клик по вебу открывает его как верхнюю вкладку (полоса видна только в хабе). **Открытый вопрос (ожидает пользователя):** как ИМЕННО должно работать включение/выключение/переключение источников — согласованный дизайн уточняется (был дрейф). Откат отката: вернуть v1.2.260 из git-истории (незакоммичено — только правки в рабочем дереве).

### v1.2.260 — [ОТКАЧЕНО v1.2.261] Полоса ВСЕГДА видна: портал на уровне App (сломал вёрстку)

Дата: 2026-08-14. Главный нерешённый пункт #3: клик по веб-значку уходил на верхнюю вкладку и полоса ПРОПАДАЛА (жила внутри вкладки «ЦентрЧатов»). **Решение — портал, без подъёма стора** (стор `nativeStore` — per-component с IPC, второй рендер сломал бы native): факт — все слои мессенджеров рендерятся всегда ([App.jsx:711-717](../src/App.jsx), z-index 2/0), значит **NativeApp всегда смонтирован** (стор/IPC живут). Теперь [NativeApp.jsx](../src/native/NativeApp.jsx) рисует боковую полосу [NativeSidebar](../src/native/components/NativeSidebar.jsx) через `createPortal` (react-dom) в новый слот `<div id="app-native-rail">` на уровне [App.jsx](../src/App.jsx) (первый в горизонтальном ряду, webview-область — правее). Полоса остаётся в React-дереве NativeApp (стор/обработчики/railWidth — те же, `useNativeStore()` вызывается РОВНО 1 раз), но её DOM рисуется на уровне App → **видна всегда**, в т.ч. поверх активного веб-мессенджера (портал не подчиняется z-index/pointer-events скрытого слоя NativeApp). Клик по вебу → его webview показывается ПРАВЕЕ полосы, а не на голой вкладке. `railSlot` находится через `getElementById` в `useEffect` (после монтирования; гвард `railSlot &&` — до этого кадра полоса не рисуется). Тесты: смоук в [appStructure.test.cjs](../src/__tests__/appStructure.test.cjs) (App-слот + createPortal + единственный `useNativeStore`). Проверки: vitest 2298/2298, линт 0, лимиты 578/578, guards ✅. **⚠️ ТРЕБУЕТ ВИЗУАЛЬНОЙ ПРОВЕРКИ (агенту запуск запрещён) — это каркас App:** (1) на вкладке «ЦентрЧатов» полоса + инбокс выглядят как раньше; (2) клик по веб-значку → веб открывается СПРАВА от полосы, полоса НЕ пропадает; (3) при старте возможен разовый сдвиг раскладки на ширину полосы (webview'ы ресайзятся один раз); (4) ресайз ширины полосы работает на нативной вкладке. Откат: `git checkout src/App.jsx src/native/NativeApp.jsx` + версия на 1.2.259.

## Текущая версия: v1.2.259 (14 августа 2026)

### v1.2.259 — Полоса: различены два «+» (аккаунт/веб); #3 «полоса на весь экран» — НЕ сделано (риск)

Дата: 2026-08-14. По жалобе (скриншот): пользователь жал «+» чтобы добавить веб, а открывался Telegram-вход. **Корень (по коду):** в полосе ДВА «+» — аккаунт-«+» ([NativeSidebar.jsx](../src/native/components/NativeSidebar.jsx), `onClick=openLogin`, синий пунктирный [styles-base.css](../src/native/styles-base.css) `.native-account__add`) стоит в СЕРЕДИНЕ (между аккаунтами и вебом), а веб-«+» (`onAddWeb`→окно добавления) — ниже; оба выглядели одинаково. **Фикс #1/#2:** обоим «+» добавлены видимые подписи — «аккаунт» (вход Telegram) и «веб» (добавить веб-мессенджер) + `data-testid` (`native-rail-add-account`/`native-rail-add-web`) + уточнён `title`. Теперь видно, какой для чего. Тест [NativeSidebar.vitest.jsx](../src/native/components/NativeSidebar.vitest.jsx) (+1: две кнопки различны по testid/title/подписи). Проверки: vitest 2298/2298, линт 0, лимиты 578/578, guards ✅. **#3 (клик по вебу уходит на верхнюю вкладку, полоса пропадает — веб не «в нашем интерфейсе») — НЕ сделано, риск.** Причина: полоса живёт ВНУТРИ вкладки «ЦентрЧатов» (`NativeApp`); чтобы она была видна на веб-экране, её надо рисовать на уровне App, но `nativeStore` — per-component (`nativeStore.js:66-84`: `useState` + IPC-слушатели в `useEffect`) → второй рендер = второй стор + ДУБЛЬ IPC → сломается native/аккаунты. Правильный фикс — поднять `useNativeStore()` в App один раз, полосу вынести на уровень App, webview-слои сдвинуть правее полосы (крупный рефактор каркаса App, **требует запуска для проверки** — агенту запрещён). Ожидает решения пользователя (см. side-rail-migration-plan.md). Откат #1/#2: вернуть глифы «+» без подписей + версия на 1.2.258.

## Текущая версия: v1.2.258 (14 августа 2026)

### v1.2.258 — Единая полоса: «+» добавить веб + вынос полосы в NativeSidebar (план исправления)

Дата: 2026-08-14. Оба пункта плана исправления ревью. **#1 «Добавить веб-мессенджер»** (единственная реально недостающая функция; зум работает хоткеями на открытой вкладке, закрепить/закрыть/перезагрузить — в правом-клик меню, split/✓ — не в стиле полосы): в веб-секции полосы добавлена кнопка «＋» (dashed, `data-testid="native-rail-add-web"`) → [App.jsx](../src/App.jsx) `onAddWeb={() => setShowAddModal(true)}` открывает существующее окно добавления. Секция теперь рисуется всегда (`webSources.length > 0 || onAddWeb`), чтобы можно было добавить веб даже при пустом списке. **#2 Вынос полосы:** блок `div.native-sidebar` (~117 строк) вынесен из [NativeApp.jsx](../src/native/NativeApp.jsx) в новый [NativeSidebar.jsx](../src/native/components/NativeSidebar.jsx) — чистый перенос JSX, ПОВЕДЕНИЕ не изменено; NativeApp получил 32 пропса-переброса (`store`, `orderedAccounts`, аккаунт-DnD, `railScale/railWidth/isRailResizing`, `openLogin`, `modes` + все веб-пропсы). Логика (стор, ресайз, вход, DnD-обработчики) осталась в NativeApp. **Страховка от забытого пропа:** ESLint `no-undef` — использованная в NativeSidebar, но не переданная переменная = ошибка линта (линт прошёл 0 → все пропсы на месте). Убраны ставшие мёртвыми импорты в NativeApp (`AccountAvatar`/`RailModeSwitcher`/`isAllVisible`/`visibleAccountCount` — переехали в NativeSidebar). **Результат:** NativeApp.jsx 494→**407**/600 (разгружен), NativeSidebar.jsx 132/600. Тесты: [NativeSidebar.vitest.jsx](../src/native/components/NativeSidebar.vitest.jsx) render-смоук (2) + смоук add-web в `appStructure`. Проверки: vitest 2297/2297, линт 0, лимиты 578/578, guards ✅, renderer 33792/33800. **⚠️ Требует визуальной проверки:** рейл аккаунтов работает как раньше (переключение/фильтр «Все»/цвета/перетаскивание/ресайз/вход); «+» добавить веб открывает окно. Откат: `git checkout src/App.jsx src/native/NativeApp.jsx` + удалить `NativeSidebar.jsx`/`.vitest` + версия на 1.2.257.

## Текущая версия: v1.2.257 (14 августа 2026)

### v1.2.257 — Единая полоса: перенос функций вкладок на веб-значки (аудит + правый клик/перетаскивание/загрузка)

Дата: 2026-08-14. По жалобе пользователя: в v1.2.256 веб-мессенджеры появились значками в нативной полосе, но БЕЗ функций вкладок (только вид). Проведён аудит чек-листа «не потерять функции» против [RailWebIcon.jsx](../src/native/components/RailWebIcon.jsx). Прикручены (переиспользование машинерии вкладок, новой логики нет): **правый клик** → то же меню вкладок (перезагрузить/логи/копировать URL/изменить/закрепить/**закрыть**), `position:fixed` по курсору из [TabBar.jsx](../src/components/TabBar.jsx); **перетаскивание** порядка (обработчики `handleDragStart/Over/Drop/End`) + подсветка цели; **полоска загрузки** (`webviewLoading[id]`). [App.jsx](../src/App.jsx) отдаёт в [NativeApp.jsx](../src/native/NativeApp.jsx): `onWebContextMenu`/`onWebDrag*`/`webDragOverId`/`webLoading`; NativeApp прокидывает в RailWebIcon. Тесты [RailWebIcon.vitest.jsx](../src/native/components/RailWebIcon.vitest.jsx) 6 (+2: правый клик с координатами, drag) + смоук `appStructure`. **ЧЕСТНО про остаток (аудит):** ещё НЕ перенесено на веб-значки — «добавить веб-мессенджер» (нативный «+» = только вход аккаунта), разбивка непрочитанных 💬/📢 + ✓ «прочитано», зум NN%, закреп 📌. Правый-клик «Закрыть» уже даёт удаление веб-мессенджера. Проверки: vitest (в фоне) / линт 0 / лимиты 576/576 / guards ✅. **⚠️ Требует визуальной проверки:** правый клик по веб-значку → меню у курсора; перетащить веб-значок → порядок меняется; при загрузке — полоска. NativeApp.jsx 494/600 (82% — кандидат на разбиение). Откат: `git checkout src/App.jsx src/native/NativeApp.jsx src/native/components/RailWebIcon.jsx` + версия на 1.2.256.

## Текущая версия: v1.2.256 (13 августа 2026)

### v1.2.256 — РАЗВОРОТ: одна полоса — веб-мессенджеры встроены в нативную полосу (SourceRail удалён)

Дата: 2026-08-13. **По решению пользователя (Модель 🅰️).** Отдельная боковая панель `SourceRail` за тумблером `settings.sideRail` ПРИ включении создавала ВТОРУЮ полосу рядом с нативной (аккаунты) → «дополнительная панель», которую пользователь отверг («не надо тумблер, всё в одном окне, а не в дополнительной панели»). **Что сделано:** веб-мессенджеры (ВК/WhatsApp/МАКС) встроены в СУЩЕСТВУЮЩУЮ нативную полосу [NativeApp.jsx](../src/native/NativeApp.jsx) `div.native-sidebar` — ПОД аккаунтами (порядок: «Все» → Telegram-аккаунты → «+» вход → разделитель → веб-значки). Новый значок [RailWebIcon.jsx](../src/native/components/RailWebIcon.jsx) (скруглённый квадрат: emoji+цвет, бейдж непрочитанных, точка связи, пульс нового, масштаб по railScale); клик → открыть вкладку этого мессенджера (`handleTabClick` из App через проп `onSelectSource`). App передаёт в NativeApp: `webSources` (messengers без native), `activeMessengerId`, `onSelectSource`, `webUnread/webHealth/webNew`. **Удалено:** компонент `SourceRail.jsx`+тест, хук `useSourceRail.js`+тест, тумблер `sideRail` в [SettingsPanel.jsx](../src/components/SettingsPanel.jsx), рендер `SourceRail` в [App.jsx](../src/App.jsx) + отдача аккаунтов наверх (2 эффекта в NativeApp). Существующий рейл аккаунтов (аватары, «Все», цвета-метки, правый клик, вход) — НЕ тронут. Тест [RailWebIcon.vitest.jsx](../src/native/components/RailWebIcon.vitest.jsx) (4) + смоук `appStructure` переписан (проверяет, что SourceRail/sideRail удалены и App отдаёт webSources в NativeApp). Потолок App.jsx 990→975 (раздутие ушло, App.jsx 963), watermark 34120→33800. Проверки: vitest 2293/2293, линт 0, лимиты 575/575, guards ✅. **⚠️ Требует визуальной проверки:** одна полоса — сверху аккаунты, ниже веб-значки; клик по веб-значку открывает его; НЕТ второй панели и тумблера. **Модель:** «хаб» = нативная полоса (видна на вкладке «ЦентрЧатов»); клик по веб-значку открывает его вкладку. Наработки SourceRail (Этапы 1–3, v1.2.244–255) остались в истории git (коммиты c9b72b6…d461b3f). Откат: `git checkout src/App.jsx src/native/NativeApp.jsx src/components/SettingsPanel.jsx` + `git checkout HEAD -- src/components/SourceRail.jsx src/hooks/useSourceRail.js` (восстановить) + версия на 1.2.254.

