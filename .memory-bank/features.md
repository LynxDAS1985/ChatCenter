# Реализованные функции — ChatCenter

## Текущая версия: v1.2.380 (3 сентября 2026)

### v1.2.380 — Ozon 3-й источник «Отзывы»: Шаг A — РАЗВЕДКА структуры (диагностика, уведомлений ещё нет)

**ЗАЧЕМ**: по просьбе пользователя — уведомлять о новом ОТЗЫВЕ (статус «Новый», раздел `Товары → Отзывы`), как о вопросах/сообщениях (TODO-36 Шаг 4). Точный URL списка отзывов и селектор бейджа «Новый» НЕИЗВЕСТНЫ — гадать нельзя (правило проекта), поэтому первый шаг — снять структуру страницы живьём (как делали для «Вопросов»).

**ЧТО** (только диагностика, БЕЗ уведомлений/счётчиков):
- [ozon.hook.js](main/preloads/hooks/ozon.hook.js) (258/300): функция `_scanR(reason)` — на странице отзывов (в адресе есть `review`, но НЕ `question`) раз в 60с (и на старте) пишет `__CC_DIAG__ozon-r url=… rows=… heads=[заголовки таблицы] novy=<число строк со словом «Новый»> tabNew=<счётчик вкладки «Новые N»> sample=<первая строка «Новый»>`. Гейт `review && !question` защищает от пересечения со страницей «Вопросы».
- [ozonBgWatcher.js](src/utils/ozonBgWatcher.js): в маршрут диагностики в журнал добавлен `ozon-r ` (строки разведки долетают в лог `[ozon-bg]`).
- [App.jsx](src/App.jsx): в `.map` фоновых страниц добавлен 3-й адрес `https://seller.ozon.ru/app/reviews` (**URL ПОДТВЕРЖДЁН пользователем 2026-09-03**). `periodicReloadMs` для неё = 0 (reload только у «Вопросов»).
- [ozonBgWatcher.js](src/utils/ozonBgWatcher.js) + [App.jsx](src/App.jsx): флаг `suppressFailNotice` — страница «Отзывы» (bgI=2) на этапе разведки НЕ показывает пользователю сообщение «не удалось открыть фоновую страницу» при сбое загрузки (у фичи ещё нет видимой части — не тревожим зря); диагностический лог `WARN [ozon-bg] загрузка НЕ удалась` при этом остаётся. «Вопросы»/«Сообщения» уведомляют как раньше.

**КАК РАБОТАЕТ / крайние случаи**: `_scanR` только читает DOM и пишет лог — НЕ шлёт `__CC_NOTIF__`, поэтому ложных уведомлений об отзывах быть не может. Если best-guess URL неверный → страница редиректит (ERR_ABORTED -3 игнорируется) или `did-fail-load` пишет реальный код + URL; если ушла не на отзывы — `did-finish-load`/`_scanR`-гейт покажут реальный адрес. На страницах «Вопросы»/«Сообщения» `_scanR` не срабатывает (гейт по адресу). **Откат**: `git checkout main/preloads/hooks/ozon.hook.js src/utils/ozonBgWatcher.js src/App.jsx`.

**КАК ПРОВЕРЕНО**: `node --check` OK; `npm run lint` 0; `fileSizeLimits` 598/598; `ozonBgWatcher.vitest` 11/11; полный `vitest`. **ДЕЙСТВИЕ ПОЛЬЗОВАТЕЛЯ (нужно для Шага B)**: зайти на `Товары → Отзывы` с включённой записью журнала → прислать строки `[ozon-bg] ozon-r …`. По ним — точный `_scanR` (уведомление `src=ozon-reviews` + счётчик `rv` + виджет). **Это Шаг A из TODO-36; Шаги B/C/D — после разведки.**

### v1.2.379 — Ozon: нет уведомлений на «Печатает…» + логотип в авто-ответе + единый resolveMessengerLogo с тестами (доводки по ревью v1.2.378)

**ЗАЧЕМ**: ревью v1.2.378 нашло 3 задачи: (#2) уведомление приходило на статус набора «Печатает…»; (#3) уведомление авто-ответа не показывало логотип источника (только эмодзи); (#1) фикс логотипа v1.2.378 не был покрыт тестом — мог «тихо откатиться» на поиск по id.

**ЧТО**:
- **#2 фильтр «Печатает…»** ([ozon.hook.js](main/preloads/hooks/ozon.hook.js) `_scan` 230→237 стр.): новая `_isTyping(s)` — якорная маска `/^печата(ет|ют)[.…\s]*$/i`. В `_scan` строка со статусом набора СЧИТАЕТСЯ непрочитанной (счётчик не мигает), но НЕ уведомляет и НЕ пишется в базу отпечатков. КОРЕНЬ: отпечаток строки = `имя|превью`; Ozon подменяет превью на «Печатает…» → менялся отпечаток → ложное «новое сообщение».
- **#3 логотип в авто-ответе** ([webviewHandleNewMessage.js](src/utils/webviewHandleNewMessage.js)): в payload уведомления авто-ответа добавлен `iconDataUrl: resolveMessengerLogo(mInfo)` (раньше только эмодзи 🤖).
- **#1 единый помощник + тесты** ([messengerLogos.js](src/native/utils/messengerLogos.js) `resolveMessengerLogo(mInfo)`): логика «логотип по типу из url, запасной ключ id» вынесена из webviewHandleNewMessage в ОДНУ экспортируемую функцию (её зовут и путь сообщения, и авто-ответ) → поиск по id не «вернётся» тихо. Тесты: `messengerLogos.vitest.js` (5 — Ozon с id=custom_… находит логотип по типу; unknown→undefined; запас по id; null-безопасность); `vkNotifyLogic.vitest.js` +2 (`_isTyping` гасит «Печатает…», пропускает «печатает отчёт»/«принтер не печатает»).

**КАК РАБОТАЕТ / крайние случаи**: `_isTyping` якорем `^…$` не задевает реальные фразы, начинающиеся со слова «печатает» (после слова — буквы, маска требует только точки/пробелы). `resolveMessengerLogo(null)` → undefined (без падения). **Остаточный минус (#2, не решено, гипотеза)**: если покупатель начал печатать и БРОСИЛ, не отправив — превью вернётся к прошлому сообщению, чей отпечаток за время набора вышел из базы → возможно повторное уведомление о старом сообщении (было и до фикса; частично гасит общий дедуп 5с). Отдельная задача (потребуется хранить отпечаток по имени чата). **Также не проверено**: точная строка статуса Ozon — маска покрывает «Печатает…/...», «Печатают…»; если Ozon использует иную фразу — по журналу `_diag` видно, что пришло. **Откат**: `git checkout main/preloads/hooks/ozon.hook.js src/native/utils/messengerLogos.js src/utils/webviewHandleNewMessage.js`.

**КАК ПРОВЕРЕНО**: `node --check` OK; `npm run lint` 0; `fileSizeLimits` 598/598 (бюджет renderer 35090→35105 — +resolveMessengerLogo); `messengerLogos.vitest` 5/5; `vkNotifyLogic.vitest` (с Ozon) — зелёно; полный `vitest`. **Требует перезапуска + ручной проверки**: покупатель печатает → уведомления НЕТ; отправил → есть, с логотипом Ozon; авто-ответ → логотип виден.

### v1.2.378 — Логотип мессенджера в уведомлении ищется по ТИПУ, а не по id (Ozon и все ручные источники)

**ЗАЧЕМ**: жалоба — у уведомления от Ozon (сообщение покупателя) вместо эмблемы Ozon показывалась заглушка-облачко 💬. КОРЕНЬ (по коду): логотипы в [messengerLogos.js](src/native/utils/messengerLogos.js) лежат по ключам-ТИПАМ (`ozon`/`telegram`/`whatsapp`/`vk`/`max`). Уведомление ([webviewHandleNewMessage.js:114](src/utils/webviewHandleNewMessage.js)) искало логотип по `getMessengerLogo(mInfo.id)` — по ID мессенджера. У добавленного вручную источника id = `custom_<время>` ([App.jsx:809](src/App.jsx)), НЕ `ozon` → `getMessengerLogo('custom_…')` = null → падало в эмодзи-заглушку `mInfo.emoji || '💬'`. Рейл ([RailWebIcon.jsx:99](src/native/components/RailWebIcon.jsx)) показывал логотип верно, т.к. передаёт в `getMessengerLogo` ТИП (`logoType`), а не id — отсюда рассинхрон «в панели логотип есть, в уведомлении нет».

**ЧТО** (`src/utils/webviewHandleNewMessage.js` 195 стр.): логотип ищется сперва по типу — `getMessengerLogo(detectMessengerType(mInfo.url))`, затем запасной ключ по id (для дефолтных, где id=тип), затем undefined. `detectMessengerType` импортирован из `messengerConfigs.js`. Правка чинит логотип уведомления не только для Ozon, но для ЛЮБОГО добавленного вручную мессенджера (у всех id `custom_…`).

**КАК РАБОТАЕТ / крайние случаи**: `mInfo.url` пуст → `detectMessengerType('')` = `unknown` → `getMessengerLogo('unknown')` = null → запасной по id → как раньше (заглушка), без регресса. Тип определяется по адресу (`seller.ozon.ru`→`ozon`). Аватар отправителя (`extra.iconDataUrl`), если есть, по-прежнему в приоритете. **Откат**: `git checkout src/utils/webviewHandleNewMessage.js`.

**КАК ПРОВЕРЕНО**: `node --check` OK; `npm run lint` 0; `fileSizeLimits` 597/597; `handleNewMessage.test.cjs` 25/25; полный `vitest`. **Требует перезапуска + ручной проверки**: новое сообщение Ozon → в уведомлении логотип Ozon, не облачко. **Побочно замечено (НЕ чинил)**: уведомление приходило на статус «Печатает…» — сторож Ozon `_scan` не отсекает статус набора (отдельная задача, как спам-фильтр «в сети/печатает» у ВК/Telegram).

### v1.2.377 — Ozon: авто-освежение «Вопросов» экономнее (пауза при свёрнутом окне) + лог сбоя reload (доводка по ревью)

**ЗАЧЕМ**: ревью v1.2.376 нашло два 🟡: (#3) `setInterval` перезагружал скрытую «Вопросы» каждые 3 мин КРУГЛОСУТОЧНО, пока приложение открыто (лишний трафик + риск, что Ozon сочтёт частым/антибот); (#5) синхронный сбой самого `el.reload()` глотался пустым `catch (_) {}` — при поломке по журналу ничего.

**ЧТО** ([ozonBgWatcher.js](src/utils/ozonBgWatcher.js), внутри reload-таймера): (#3) перед `el.reload()` добавлен пропуск, если окно приложения скрыто/свёрнуто — `if (document.hidden) return`. `document.hidden` — стандарт DOM (MDN): true, когда окно renderer свёрнуто/скрыто в трей. Тик тогда просто пропускается; при возврате к приложению следующий тик (в пределах 3 мин) освежит. (#5) `catch (_) {}` → `catch (e) { log('WARN', 'reload … не удался: '+msg) }`.

**КАК РАБОТАЕТ / крайние случаи**: приложение видимо (даже в фоне за другим окном) → reload идёт как раньше (`document.hidden` реагирует на СВЁРНУТО/скрыто, не на «за другим окном»); свёрнуто/трей → reload на паузе (счётчик всё равно не виден пользователю, освежится по возврату). `typeof document !== 'undefined'` — страховка для не-DOM контекста. Логика «только «Вопросы»» и персист базы `_qPrev` — без изменений (см. [[v1.2.376]]). **НЕ сделано осознанно (#2 из ревью)**: освежение «Сообщений» — их база `_prev` не персистится, reload проглотил бы новое сообщение (пропуск уведомления покупателя); отложено как отдельная задача (сначала персист базы сообщений). **Откат**: `git checkout src/utils/ozonBgWatcher.js`.

**КАК ПРОВЕРЕНО**: `node --check` OK; `npm run lint`; `fileSizeLimits` 597/597; полный `vitest`. **Требует перезапуска + ручной проверки**: свернуть приложение → в журнале НЕТ строк `освежена (reload)`; развернуть → снова появляются раз в ~3 мин.

### v1.2.376 — Ozon: само-освежение фоновой «Вопросы» (qa-счётчик не застывает) + единая заглушка счётчиков + чинит мигание значка

**ЗАЧЕМ**: (1) жалоба — ответила на вопрос НА ВЕБЕ, а виджет/значок держали старую цифру «2». КОРЕНЬ: qa-счётчик считает СКРЫТАЯ фоновая страница `/reviews/questions` ([App.jsx:846](src/App.jsx), `bgI===0`). Это отдельная копия страницы; Ozon (SPA) не всегда обновляет список вопросов, когда ответ дан в ДРУГОМ месте → скрытая страница держит СТАРЫЙ снимок DOM → `_scanQ` считает старый `unans` → qa застывает (пересчёт `qa` идёт по живому DOM, [ozon.hook.js:120-146](main/preloads/hooks/ozon.hook.js)). (2) наблюдение — при ПЕРЕХОДЕ в раздел Ozon цифра рейла на миг пропадала: передняя вкладка перезагружалась → монитор рестартовал → слал `unread-count`=0 → до v1.2.375 это затирало счётчик (мигание). В v1.2.375 уже закрыто; здесь — консолидация, чтобы третий счётчик не открыл дыру заново.

**ЧТО**:
- **Само-освежение (advice #1)** — [ozonBgWatcher.js](src/utils/ozonBgWatcher.js) (107→124 стр.): `bindOzonBgWatcher` получил параметр `periodicReloadMs`; при `>0` заводит `setInterval`, который делает `el.reload()` (Electron `<webview>.reload()`). В [App.jsx:850](src/App.jsx) флаг передаётся ТОЛЬКО «Вопросам» (`bgI===0 ? 180000 : 0` = раз в 3 мин). «Сообщения» (`bgI===1`) НЕ перезагружаем осознанно: их база `_prev` НЕ персистится, reload проглотил бы только что пришедшее сообщение в новый baseline (пропуск уведомления). «Вопросы» перезагружать безопасно: база `_qPrev` переживает reload через `localStorage['__ccOzonQSeen']` (v1.2.371) → повторных уведомлений нет. Таймер сам стопается при размонтировании (`el.isConnected===false`, стандарт DOM/MDN) — без утечки.
- **Единая заглушка (advice #2)** — [webviewSetup.js](src/utils/webviewSetup.js) (604/605): две дублирующие проверки «это Ozon?» (title-reset v1.2.374 строка ~440 + unread-count v1.2.375 строка ~477) сведены в один помощник `isOzonWebview(el, messengerId)` рядом с существующим `isVkWebview` (тот же паттерн). Поведение прежнее; побочно надёжнее: `isOzonWebview` использует `healthUrl` (фолбэк на URL из конфига), поэтому если `el.getURL()` временно пуст — Ozon всё равно опознаётся и обнуления не будет (раньше пустой URL → обнуление могло сработать).

**КАК РАБОТАЕТ / крайние случаи**: reload на грузящейся странице — Electron перезапускает загрузку (безопасно); `ERR_ABORTED (-3)` от редиректа Ozon уже игнорируется ([ozonBgWatcher.js](src/utils/ozonBgWatcher.js) did-fail-load); нет интернета → reload → did-fail-load → нейтральное сообщение один раз (флаг). **Остаточный риск**: 3-минутный интервал — компромисс свежесть/трафик; если Ozon сочтёт частым — увеличить `periodicReloadMs`. **Откат**: `git checkout src/utils/ozonBgWatcher.js src/utils/webviewSetup.js src/App.jsx`.

**КАК ПРОВЕРЕНО**: `node --check` OK (3 файла); `npm run lint`; `fileSizeLimits` (webviewSetup 604/605, ozonBgWatcher 124/300, App.jsx 1074/1075); полный `vitest`. **Требует перезапуска + ручной проверки**: ответить на вопрос на вебе → в течение ~3 мин цифра «Вопросы» уменьшается сама (в журнале `[ozon-bg] фоновая «Вопросы» освежена (reload)`); переход в раздел Ozon — значок рейла НЕ мигает.

### v1.2.375 — Ozon: ВТОРАЯ дыра обнуления значка рейла закрыта (общий счётчик `unread-count` не трогает Ozon)

**ЗАЧЕМ**: после v1.2.374 значок Ozon в рейле всё равно пропадал — виджет показывает «2» (qa), рейл пуст. КОРЕНЬ (по коду, второй обнулитель, НЕ тот, что чинил v1.2.374): у `unreadCounts[id]` несколько писателей. Общий счётчик непрочитанных считает Ozon как `type=null` — [unreadCounters.js:74-83](main/preloads/utils/unreadCounters.js) `getMessengerType()` не знает `seller.ozon.ru` (только telegram/whatsapp/vk/max) → `countUnread(null)` = 0 → [monitor.preload.cjs:258](main/preloads/monitor.preload.cjs) шлёт `unread-count` = 0. В [webviewSetup.js:484-489](src/utils/webviewSetup.js) при просмотре вкладки (`isViewing`) разрешено уменьшение → `unreadCounts[ozon]=0`, затирая цифру сторожа. Эффект-пересчёт ([App.jsx:117](src/App.jsx), deps `[ozonCounts]`) НЕ восстанавливает (qa=2 не менялось). Особенно проявляется при ПЕРЕХОДЕ в другой раздел Ozon: страница перезагружается → монитор рестартует → снова шлёт `unread-count`=0.

**ЧТО** (`src/utils/webviewSetup.js` 604/605, +4 строки): в начале ветки `unread-count` добавлен ранний выход для Ozon (`detectMessengerType(el.getURL()) === 'ozon' → return`). Счётчик Ozon ведёт ТОЛЬКО сторож (`__CC_OZON_COUNT__` → `ozonCounts` → эффект). Тот же паттерн-гвард, что title-reset v1.2.374 (строка 440). Прочие мессенджеры (их `unread-count` НЕ 0) не затронуты.

**КАК РАБОТАЕТ / крайние случаи**: `el` null / `getURL` упал → try/catch → `''` → не Ozon → как раньше (безопасно). Двойной/параллельный вызов — идемпотентно (просто пропуск). Прочтение сообщений/ответ на вопросы очищает счётчик через сторож (msg/qa по живому DOM), а не через `unread-count`. **Остаточный риск**: если фоновая страница «Вопросы» держит СТАРЫЙ снимок (не обновилась после ответа на вебе) — qa застынет; это ОТДЕЛЬНАЯ проблема свежести фоновой страницы (не этот фикс). **Откат**: `git checkout src/utils/webviewSetup.js`.

**КАК ПРОВЕРЕНО**: `npm run lint`; `fileSizeLimits` (webviewSetup 604/605); полный `vitest`. **Тест-ловушку** прямым юнитом не сделать (логика в inline-колбэке `addListener`) — проверка ручная после перезапуска: открыть Ozon → значок рейла = число из виджета, не гаснет при просмотре/переходе.

### v1.2.374 — Ozon: значок рейла не теряет цифру непрочитанного (обнуление по заголовку исключено для Ozon)

**ЗАЧЕМ**: жалоба — фон видит 2 вопроса без ответа (журнал `[ozon-bg] ozon-q scan unans=2`, виджет показывает «2»), но значок Ozon в БОКОВОЙ панели пуст. КОРЕНЬ (по коду + журналу + скриншоту): значок рейла = `unreadCounts[id]`. Есть эвристика ([webviewSetup.js:438](src/utils/webviewSetup.js) `page-title-updated`): «заголовок вкладки БЕЗ числа + пользователь смотрит + фокус → всё прочитано → `unreadCounts[id]=0`». Она для Telegram/MAX (у них заголовок «(N)»). У Ozon заголовок «OZON Seller» числа НЕ содержит НИКОГДА, а реальный счётчик ведёт наш фоновый сторож (`ozonCounts`→эффект→`unreadCounts`). Открыл вкладку Ozon → эвристика обнулила `unreadCounts[ozon]`; эффект-пересчёт ([App.jsx:117](src/App.jsx)) НЕ восстановил (срабатывает лишь при изменении `ozonCounts`, а qa=2 не менялось). Виджет читает `ozonCounts` напрямую → показывал 2; рейл читает обнулённый `unreadCounts` → 0.

**ЧТО** (`src/utils/webviewSetup.js` 601/605): обнуление по заголовку без числа теперь ПРОПУСКАЕТСЯ для Ozon (`detectMessengerType(url) !== 'ozon'`). Telegram/MAX/ВК/WhatsApp не затронуты. Второй обнулитель (`badge_blocked`, [consoleMessageHandler.js:85](src/utils/consoleMessageHandler.js)) Ozon НЕ трогает — подтверждено: `__CC_BADGE_BLOCKED__` шлют только max/telegram/vk/whatsapp.hook, ozon.hook НЕ шлёт (grep=0), поэтому его править не нужно (минимальное касание).

**КАК РАБОТАЕТ / крайние случаи**: пустой URL → `detectMessengerType('')≠'ozon'` → обнуление как раньше (безопасный дефолт для не-Ozon). Прочтение Ozon-сообщений/ответ на вопросы самоочищает счётчик через сторож (`ozonCounts.msg/qa`→0→эффект), а не через заголовок. **Откат**: `git checkout src/utils/webviewSetup.js`.

**КАК ПРОВЕРЕНО**: `npm run lint` 0; `fileSizeLimits` 597/597; `appStructure` 60/60; `rendererConsoleGuard` 0; полный `vitest` — запущен. **Тест-ловушку** прямым юнит-тестом не сделать (логика внутри inline-колбэка `addListener` в фабрике webviewSetup, без запуска Electron не изолируется) — проверка ручная. **Требует перезапуска**: открыть Ozon с непрочитанными вопросами → значок рейла показывает число (не пропадает при просмотре вкладки).

## Текущая версия: v1.2.373 (3 сентября 2026)

### v1.2.373 — Окно «на весь экран» сохраняется и из ТРЕЯ (доводка v1.2.372 по ревью) + единый помощник + тест

**ЗАЧЕМ**: ревью v1.2.372 нашло: `if (isMinimized()) restore()` покрывает случай «свёрнуто в панель задач», но у окна, СКРЫТОГО в трей (по ✕), `isMinimized()`=false → restore не зовётся, а сохранит ли `show()` «на весь экран» — не гарантировано. Пользователь написал «открылась программа» → скорее трей.

**ЧТО** (новый [main/utils/bringWindowToFront.js](main/utils/bringWindowToFront.js) 22 стр.): единый помощник подъёма окна, покрывает ОБА случая: (1) свёрнуто → `restore()` вернёт развёрнутое; (2) трей → `show()`, а если Windows дал обычный размер — ПОДСТРАХОВКА `maximize()` (запоминаем `isMaximized()` ДО show — у скрытого окна maximize сохраняется). Видимое-обычное окно не разворачиваем (не навязываем maximize). Пишет `[win-up] wasMin=… wantMax=… maximized=…`. Заменил разрозненные `show()/focus()` в 4 путях подъёма ([notifHandlers.js](main/handlers/notifHandlers.js) notif:click + notif:ai-process; [dockPinHandlers.js](main/handlers/dockPinHandlers.js) pin:go-to-chat + dock:go-to-chat) — теперь все логируются (закрыта «немота» 3 путей из ревью).

**КАК ПРОВЕРЕНО**: `npm run lint` 0; новый тест [bringWindowToFront.vitest.js](main/utils/bringWindowToFront.vitest.js) **7/7** (свёрнуто→restore; трей+show сохраняет; трей+show потерял→maximize; видимое-развёрнутое; видимое-обычное; destroyed; null); `fileSizeLimits` 597/597; `mainRuntime` 109/109; `pinTooltip` 69/69; полный vitest — запущен. **Откат**: `git checkout main/handlers/notifHandlers.js main/handlers/dockPinHandlers.js` + удалить `bringWindowToFront.js`. **Требует проверки**: развернуть окно → закрыть в трей (✕) → клик уведомления → окно на весь экран; в журнале `[win-up] … maximized=true`.

## Текущая версия: v1.2.372 (3 сентября 2026)

### v1.2.372 — Окно сохраняет «на весь экран» при клике по уведомлению/закреплённой задаче

**ЗАЧЕМ**: жалоба — окно было развёрнуто на весь экран, клик по уведомлению (переход в чат) поднял окно в ОБЫЧНОМ размере. КОРЕНЬ (по коду): обработчики, поднимающие главное окно, звали `show()`+`focus()` без `restore()`. У СВЁРНУТОГО окна на Windows голый `show()`/`focus()` разворачивает его в обычный размер, теряя «на весь экран». Проектный образец рядом (`notif:mark-read`, [notifHandlers.js:71-73](main/handlers/notifHandlers.js)) уже использовал `restore()` при свёрнутом — а пути клика нет.

**ЧТО** (4 места, старый баг, не из правок этой сессии): [notifHandlers.js](main/handlers/notifHandlers.js) `notif:click` и `notif:ai-process`; [dockPinHandlers.js](main/handlers/dockPinHandlers.js) клик по доку и по закреплённой задаче — во всех перед `show()` добавлено `if (isMinimized()) restore()` (`restore()` возвращает окно из свёрнутого в ПРЕДЫДУЩЕЕ состояние, в т.ч. «на весь экран»). У видимого/скрытого окна maximize не трогается. В `notif:click` добавлена запись `[notif-click] window up: wasMin=… maximized=…` для подтверждения по журналу.

**КАК РАБОТАЕТ / крайние случаи**: окно видимо-развёрнуто → restore не зовём (guard `isMinimized`), остаётся развёрнутым; окно свёрнуто-развёрнуто → restore → развёрнуто; окно свёрнуто-обычное → restore → обычное (как было). **Откат**: `git checkout main/handlers/notifHandlers.js main/handlers/dockPinHandlers.js`.

**КАК ПРОВЕРЕНО**: `npm run lint` 0; `fileSizeLimits` 595/595; `mainRuntime` 108/108; `pinTooltip` 69/69. **Требует проверки**: свернуть развёрнутое окно → кликнуть уведомление/задачу → окно должно вернуться на весь экран; в журнале `[notif-click] … maximized=true`.

## Текущая версия: v1.2.371 (3 сентября 2026)

### v1.2.371 — Ozon фон: память показанных вопросов (localStorage) + пульс сообщениям + разгрузка ozon.hook (4 улучшения по выбору)

**ЧТО** (`main/preloads/hooks/ozon.hook.js` 227/300):
- **#1 Память показанного (localStorage)**: базовая линия сторожа вопросов (`_qPrev` — отпечатки уже-виденных вопросов без ответа) теперь ПЕРЕЖИВАЕТ перезагрузку страницы через `localStorage['__ccOzonQSeen']` (`_loadSeen`/`_saveSeen`). До этого `_qPrev` жил только в контексте страницы → при перезагрузке/перезаходе фон делал НОВУЮ «фотографию», уже висящие вопросы считал старыми, а новые с прошлой сессии не слал. Теперь: загрузка при входе на `/reviews/questions`, сохранение после каждого скана. **ПОВЕДЕНИЕ (гипотеза, не подтверждено живьём)**: на старте/перезагрузке шлём вопросы, НОВЫЕ с момента последнего сохранения (в т.ч. пришедшие пока приложение было закрыто). Пусто в localStorage (первый запуск) → `_qPrev=null` → базовая линия без шторма.
- **#3 «Пульс» покупателям**: в `_scan` (Сообщения) добавлено условие времени (как в `_scanQ` v1.2.370) — диаг-запись при изменении ИЛИ раз в 60с (`_lastDiagTs`). Видно, жив ли фоновый сторож «Сообщения».
- **#4 Разгрузка**: удалена разовая разведка `_reconQ` (маркер `ozon-q2`) — она подтвердила столбцы таблицы (знания зашиты в `_qAnsCol`), больше не нужна. Файл 264→227 строк (снят жёлтый порог 88%). Разбить на файлы НЕЛЬЗЯ (хук впрыскивается как ОДИН файл через `app:read-hook`), поэтому разгрузка = удаление done-диагностики.

**КАК ПРОВЕРЕНО**: `npm run lint` 0; `node --check` OK (227/300); `fileSizeLimits` 595/595; `notifHooks` 87/87; полный `vitest` — запущен. **Откат**: `git checkout main/preloads/hooks/ozon.hook.js`. **Требует перезапуска + проверки по журналу**: «пульс» `[ozon-bg] ozon-q scan` раз в минуту; после перезахода на «Вопросы» новый вопрос → уведомление (память не даёт задублировать старые). **Риск (для ревью)**: #1 меняет поведение старта — новые-с-последнего-сохранения вопросы шлются на старте (для вопросов объём мал; для сообщений `_scan` НЕ персистится — во избежание «ночной стены»).

## Текущая версия: v1.2.370 (3 сентября 2026)

### v1.2.370 — Ozon фон: «пульс» сканера вопросов (диагностика — почему новый вопрос не дал уведомления)

**ЗАЧЕМ**: жалоба — пришёл 2-й вопрос без ответа, но фоновая слежка не дала уведомления. Журнал: 3 строки `[ozon-bg] ozon-q scan reason=initial unans=1` (разные сессии), НИ ОДНОЙ после старта 18:56, новый вопрос 19:43 не пойман, ни строки `фон: новый вопрос`. Гипотеза (не подтверждено): фоновая скрытая страница показывает «снимок» списка на момент загрузки и не обновляется в фоне → новые вопросы не видит. Но по журналу нельзя отличить «страница заморожена, сторож жив» от «сторож уснул» — обычные (backstop) сканы в лог не пишутся (гейт «только при изменении»).

**ЧТО** (`main/preloads/hooks/ozon.hook.js` 264/300, `_scanQ`): в существующий гейт диаг-лога добавлено условие ВРЕМЕНИ — писать `ozon-q scan` при изменении формы ИЛИ **раз в 60с** (`Date.now() - _lastQDiagTs > 60000`). Так виден «пульс»: жив ли фоновый сторож и сколько вопросов без ответа он видит каждую минуту. ДИАГНОСТИКА — убрать после диагноза.

**КАК РАБОТАЕТ**: `_scanQ` вызывается каждые 5с; теперь минимум раз в 60с пишет строку. Пульс идёт + `unans` застыл при реальном росте → страница заморожена (фикс: периодически обновлять фоновую страницу + помнить, о чём уже уведомляли). Пульса нет → сторож уснул (фикс: таймер). **Откат**: `git checkout main/preloads/hooks/ozon.hook.js`. Затрагивает и видимую вкладку на «Вопросах» (тоже +пульс в R:TRACE), но пользователь там редко.

**КАК ПРОВЕРЕНО**: `node --check` OK (264/300); `fileSizeLimits` 595/595 (⚠️ ozon.hook 88% — скоро разбивать); `notifHooks` 87/87. **Действие пользователя**: перезапуск, подождать ~2 мин, прислать строки `[ozon-bg] ozon-q scan` — по «пульсу» назову точную причину и сделаю фикс.

## Текущая версия: v1.2.369 (2 сентября 2026)

### v1.2.369 — Ozon фон: сообщение о сбое не путает раздел (фикс по ревью)

**ЗАЧЕМ**: ревью Шага 3Б нашло: текст уведомления «фоновая недоступна» был жёстко зашит «…страницу «Вопросы»…», а после обобщения `bindOzonBgWatcher` обслуживает ОБЕ фоновые страницы (Вопросы+Сообщения). Если бы упала «Сообщения» — пользователь увидел бы неверное «Вопросы».

**ЧТО** (`src/utils/ozonBgWatcher.js`): текст сделан НЕЙТРАЛЬНЫМ, без названия раздела — «Не удалось открыть фоновую страницу Ozon (проблема Ozon или интернета). Уведомления из этого раздела будут приходить, только когда он открыт». Тест `ozonBgWatcher.vitest.js` дополнен: текст notify НЕ содержит «Вопрос» (ловушка на возврат жёсткого названия).

**КАК ПРОВЕРЕНО**: `eslint` 0; `ozonBgWatcher.vitest.js` **11/11**; `fileSizeLimits` 595/595; `rendererConsoleGuard` 0. **Откат**: `git checkout src/utils/ozonBgWatcher.js`.

## Текущая версия: v1.2.368 (2 сентября 2026)

### v1.2.368 — Ozon Шаг 3Б: фоновая слежка за «Сообщениями» покупателей (оба раздела сразу) — цель TODO-36 закрыта

**ЗАЧЕМ**: Шаг 3А (v1.2.365-367) дал фоновую слежку за «Вопросами». Симметрично нужно ловить и СООБЩЕНИЯ покупателей, когда открыт другой раздел (напр. пользователь в «Вопросах» → пропускал бы сообщения). Механизм фоновой скрытой страницы подтверждён рабочим.

**ЧТО**: (1) [ozonBgWatcher.js](src/utils/ozonBgWatcher.js) ОБОБЩЁН на оба раздела: `ozon_count` любой секции (`msg`/`qa`) → `setOzonCounts[section]`; `notification` с `src` `ozon-questions` ИЛИ `ozon-list` (покупатели) → `handleNewMessage(ozonId, …, {background:true})`; диагностика `ozon-q scan` И `ozon-list reason=`. (2) [App.jsx](src/App.jsx) 1075/1075: вместо одной скрытой webview — `.map` по ДВУМ адресам (`/app/reviews/questions` и `/app/messenger?group=customers_v2`), обе с тем же `bindOzonBgWatcher`, offscreen, `persist:ozon`. Сторож впрыскивается в каждую (dom-ready → executeJavaScript); на странице мессенджера работает `_scan` (покупатели, гейт `/app/messenger`), на вопросах — `_scanQ` (гейт `/reviews/questions`).

**КАК РАБОТАЕТ / крайние случаи**: две фоновые + основная видимая = 3 страницы Ozon на одной сессии (пользователь принял «не важно сколько»). Двойные уведомления (основная вкладка на том же разделе + фон) гасит общий дедуп `recentNotifsRef` (10с) в `handleNewMessage`; фоновые не крадут фокус (`background:true`). **Откат**: вернуть один адрес в `.map` (или `git checkout src/App.jsx src/utils/ozonBgWatcher.js`).

**КАК ПРОВЕРЕНО**: `npm run lint` 0; `ozonBgWatcher.vitest.js` **11/11** (+2 теста: сообщение покупателя `ozon-list`→handleNewMessage, счётчик `msg`→setOzonCounts); `fileSizeLimits` 595/595; `appStructure` 60/60; `rendererConsoleGuard` 0; полный `vitest` — запущен. **Требует перезапуска + проверки по журналу**: `[ozon-bg] ozon-list reason=… unread=…` (фоновый сторож покупателей) + уведомление на новое сообщение покупателя, сидя в «Вопросах»/другом мессенджере.

## Текущая версия: v1.2.367 (2 сентября 2026)

### v1.2.367 — Ozon фон: сторож переставляется после перезагрузки страницы (фикс «липкого» флага по ревью) + тест впрыска

**ЗАЧЕМ**: ревью Шага 3А нашло: впрыск сторожа в фоновую страницу стоял за «липким» флагом `_hookInjected` (ставился в true сразу и навсегда). При ПЕРЕЗАГРУЗКЕ фоновой страницы (Ozon обновил сессию) или разовом сбое `read-hook` сторож бы НЕ переставился → слежка тихо умирает до перезапуска приложения. Плюс сам впрыск (главная логика) не был покрыт тестом.

**ЧТО** (`src/utils/ozonBgWatcher.js`): убран флаг `_hookInjected` — впрыск на КАЖДЫЙ `dom-ready`. Безопасно: в самом хуке гвард `window.__ccOzonWatch` не даёт запуститься дважды в одной странице; после реальной перезагрузки страница «чистая» (гвард сброшен) → впрыск нужен и срабатывает. Обновлён устаревший верхний комментарий файла (раньше писал «preload сам впрыскивает» — на деле preload-`<script>` блокирует CSP, впрыск делает executeJavaScript). Тест `ozonBgWatcher.vitest.js` +1 (9 всего): два `dom-ready` подряд → `app:read-hook` вызван ОБА раза (ловушка на возврат липкого флага).

**КАК РАБОТАЕТ / крайние случаи**: dom-ready в SPA при клиентской навигации НЕ повторяется (документ не перезагружается) → лишних впрысков нет; при полной перезагрузке — повтор нужен и происходит. **Откат**: `git checkout src/utils/ozonBgWatcher.js`.

**КАК ПРОВЕРЕНО**: `npm run lint` 0; `ozonBgWatcher.vitest.js` **9/9**; полный `npm run test:vitest` **2348, exit 0** (до этой правки; правка +1 тест); `fileSizeLimits` 595/595; `rendererConsoleGuard` 0.

## Текущая версия: v1.2.366 (2 сентября 2026)

### v1.2.366 — Ozon Шаг 3А ПОДТВЕРЖДЁН рабочим: фоновый сторож вопросов сканирует таблицу; шумная диагностика убрана

**ЗАЧЕМ / ПОДТВЕРЖДЕНИЕ**: после впрыска сторожа (v1.2.365) журнал пользователя (2026-09-02 17:41) дал: `[ozon-bg] ozon-q2` разведка (11 строк, столбцы H0-H5 Дата/Продавец/Товар/Вопрос/Ответы/Полезный) + **`[ozon-bg] ozon-list ozon-q scan reason=initial rows=11 unans=1`**. **ВЫВОД: фоновая скрытая страница «Вопросы» грузится, сторож впрыснут и РАБОТАЕТ — видит таблицу, считает вопросы без ответа (unans=1). Обе гипотезы-блокера (антибот; SPA не рисует в скрытой вкладке) ОТВЕРГНУТЫ фактом.** Шаг 3А функционален (осталось подтвердить живое уведомление на НОВЫЙ вопрос — придёт по той же логике, что у основной вкладки).

**ЧТО** (`src/utils/ozonBgWatcher.js`): форвард диагностики сужен с `/ozon-(list|q)/` обратно до `/ozon-q scan|no-answers-col/` — разовая разведка `ozon-q2` (~30 строк) больше не сыплется в журнал (разметку подтвердила). Оставлены строка сканирования (подтверждение работы) и `no-answers-col` (сигнал сбоя). Впрыск сторожа (v1.2.365), лесенка загрузки, downgrade ERR_ABORTED (v1.2.363) — без изменений.

**КАК ПРОВЕРЕНО**: журнал пользователя (`ozon-q scan rows=11 unans=1`); `eslint` 0; `ozonBgWatcher.vitest.js` 8/8; `fileSizeLimits` 595/595. **Осталось**: живое уведомление на реальный НОВЫЙ вопрос (проверка пользователем); Фаза 3b (симметрично для «Сообщений»); клик по уведомлению вопроса → открыть «Вопросы».

## Текущая версия: v1.2.365 (2 сентября 2026)

### v1.2.365 — Ozon Шаг 3А: КОРЕНЬ молчания найден — сторож не впрыскивался в фоновую страницу; впрыск через executeJavaScript

**ЗАЧЕМ / КОРЕНЬ (по коду + журналу)**: расширенная диагностика v1.2.364 (перезапуск 17:33) показала: фоновая страница проходит всю лесенку (создана→console→dom-ready→загрузилась URL=/app/reviews/questions), но НИ ОДНОГО сигнала хука (`ozon-list`/`ozon-q`). Значит `ozon.hook` НЕ впрыскивается. Причина (по коду): хук Ozon впрыскивается НЕ preload-ом (Ozon CSP блокирует preload-`<script>` — комментарий [monitor.preload.cjs:10](main/preloads/monitor.preload.cjs)), а через `executeJavaScript` из [webviewSetup.js:362-368](src/utils/webviewSetup.js) — но ТОЛЬКО для webview, зарегистрированных через `setWebviewRef`. Фоновая страница НЕ регистрируется (намеренно, чтобы не мешать health/refs) → executeJavaScript-фолбэк её не покрывал → сторож не запускался.

**ЧТО** (`src/utils/ozonBgWatcher.js`): в `dom-ready` фоновой страницы добавлен впрыск сторожа: `window.api.invoke('app:read-hook','ozon')` → `el.executeJavaScript(hookCode)` (в обход CSP, как для основной вкладки). Разово (флаг `_hookInjected`). Логи `[ozon-bg] сторож впрыснут` / `впрыск не удался`.

**КАК РАБОТАЕТ / крайние случаи**: `app:read-hook` пуст → WARN, не падаем; executeJavaScript упал → WARN; повторный dom-ready (SPA) → флаг + гвард `window.__ccOzonWatch` в самом хуке → двойного запуска нет. **Откат**: `git checkout src/utils/ozonBgWatcher.js`. **Требует перезапуска + проверки по журналу**: после впрыска должны пойти `[ozon-bg] ozon-q scan rows=N unans=M`. Если `rows=0`/`no-answers-col` → ОТДЕЛЬНАЯ гипотеза: SPA Ozon не рисует таблицу в СКРЫТОЙ вкладке (тогда — «будить» страницу).

**КАК ПРОВЕРЕНО**: `eslint` 0; `ozonBgWatcher.vitest.js` 8/8; `fileSizeLimits` 595/595; `rendererConsoleGuard` 0.

## Текущая версия: v1.2.364 (2 сентября 2026)

### v1.2.364 — Ozon Шаг 3А: фоновая страница ГРУЗИТСЯ (подтверждено) — диагностика хука расширена

**ЗАЧЕМ**: перезапуск с диагностикой v1.2.361 дал журнал (2026-09-02 17:16): `[ozon-bg] страница создана` → `первое console-сообщение получено` → `dom-ready` → `страница загрузилась URL=…/app/reviews/questions`. **ВЫВОД: скрытая фоновая страница РЕАЛЬНО загружается и консоль из неё идёт** (гипотеза «антибот блокирует» ОТВЕРГНУТА). НО строки `ozon-q scan` нет → сторож (`ozon.hook._scanQ`) либо не впрыснут, либо не находит таблицу. Причина неизвестна: маршрутизатор [ozonBgWatcher.js](src/utils/ozonBgWatcher.js) форвардил в журнал ТОЛЬКО `ozon-q scan`, а прочие сигналы хука (`ozon-list`, `no-answers-col`, `ozon-q2` разведка) глушил.

**ЧТО** (`src/utils/ozonBgWatcher.js`): форвард диагностики расширен с `/ozon-q scan/` до `/ozon-(list|q)/` — теперь в `[ozon-bg]` видно ВСЕ сигналы хука из фоновой страницы: впрыснулся ли он, сколько строк видит, найден ли столбец «Ответы». Сузить обратно после диагноза.

**КАК ПРОВЕРЕНО**: `eslint` 0; `ozonBgWatcher.vitest.js` 8/8; `fileSizeLimits` 595/595. **Действие пользователя**: перезапуск → прислать строки `[ozon-bg] ozon-…`. По ним — впрыснут ли хук / видит ли таблицу (гипотеза: SPA Ozon может не рисовать таблицу в СКРЫТОЙ вкладке). Это ДИАГНОСТИКА, не фикс.

## Текущая версия: v1.2.363 (2 сентября 2026)

### v1.2.363 — Красные `ERR_ABORTED (-3)` от навигации webview → WARN (логгер) + нейтральная формулировка сообщения

**ЗАЧЕМ**: жалоба — в журнале красные `[ERROR] GUEST_VIEW_MANAGER_CALL: ERR_ABORTED (-3) loading '.../app/messenger?group=customers_v2'` и `.../app/reviews/questions`. Разбор по журналу: обе в один момент (клик виджета «Сообщения» → переход главной вкладки; загрузка фоновой страницы «Вопросы»). `ERR_ABORTED (-3)` = навигация отменена редиректом Ozon (страница всё равно грузится) — безобидно. ВАЖНО: прошлый фикс виджета (v1.2.350, `.catch()` на `loadURL`) гасил только renderer-сторону (`renderer-unhandled-rejection`); эта красная запись идёт из ГЛАВНОГО процесса (Electron логирует отклонённый обработчик `GUEST_VIEW_MANAGER_CALL`) — `.catch()` в окне её не ловит.

**ЧТО**: (1) [main/utils/logger.js](main/utils/logger.js) (157/300) — в переопределении `console.error` добавлен downgrade: если текст содержит `GUEST_VIEW_MANAGER_CALL` И (`ERR_ABORTED` ИЛИ `(-3)`) → писать WARN `[webview nav aborted]` вместо ERROR (тот же приём, что для GramJS TIMEOUT). Реальные сбои загрузки (другие коды) остаются красными. (2) [ozonBgWatcher.js](src/utils/ozonBgWatcher.js) — сообщение при неудаче фоновой страницы стало НЕЙТРАЛЬНЫМ: «Не удалось открыть… (проблема Ozon или интернета)» (ревью-находка: `did-fail-load` бывает и от обрыва сети `-21/-106`, нельзя винить только Ozon) + запись в журнал `[ozon-bg] показал… сообщение`.

**КАК РАБОТАЕТ / крайние случаи**: downgrade требует ОБА признака (`GUEST_VIEW` + `-3/ERR_ABORTED`) — реальная ошибка типа `ERR_CONNECTION_REFUSED` останется красной (проверено прогоном node: messenger→WARN, questions→WARN, ERR_CONNECTION_REFUSED→ERROR). **Откат**: `git checkout main/utils/logger.js src/utils/ozonBgWatcher.js`.

**КАК ПРОВЕРЕНО**: `eslint` 0; `ozonBgWatcher.vitest.js` **8/8** (+нейтральный текст, +лог показа); node-прогон regex (3/3); `fileSizeLimits` 595/595; `rendererConsoleGuard` 0. **Требует перезапуска**: красные `ERR_ABORTED` при переходах Ozon больше не появятся (пойдут в «Предупр.»).

## Текущая версия: v1.2.362 (2 сентября 2026)

### v1.2.362 — Ozon: понятное сообщение при блокировке фоновой страницы + тест маршрутизатора

**ЗАЧЕМ**: улучшения по ревью Шага 3. (1) Если Ozon не пускает фоновую страницу «Вопросы», раньше была тишина (только `[ozon-bg]` в журнале). (2) У маршрутизатора фоновой страницы (`ozonBgWatcher.js`) не было теста — тихая поломка всплыла бы только у пользователя.

**ЧТО**: (1) [ozonBgWatcher.js](src/utils/ozonBgWatcher.js) — обработчик `did-fail-load`: при РЕАЛЬНОЙ неудаче загрузки ГЛАВНОГО документа (`isMainFrame`, `errorCode != null && !== -3`) один раз (флаг `el.__ccOzonBgBlockNotified`) зовёт `notify(title, body)` — понятную фразу пользователю. `ERR_ABORTED (-3)` (редирект Ozon) и под-ресурсы игнорируются (не ложная тревога). [App.jsx](src/App.jsx) — в `bindOzonBgWatcher` проброшен `notify` через `app:custom-notify` (лента, имя/логотип Ozon). (2) Новый тест [src/__tests__/ozonBgWatcher.vitest.js](src/__tests__/ozonBgWatcher.vitest.js) (8 проверок): уведомление→handleNewMessage с `background:true`; qa→setOzonCounts; не-`__CC_`→игнор; реальная ошибка→notify один раз; `-3`→не тревожит; под-ресурс→не тревожит; повторная привязка не дублирует слушатель; null-элемент не падает.

**КАК РАБОТАЕТ / крайние случаи**: notify — разово за жизнь элемента (флаг); `-3`/под-ресурс → молчим; нет `notify` в deps → `notify &&` пропускает. **Откат**: `git checkout src/App.jsx src/utils/ozonBgWatcher.js` + удалить тест. **Зависит** от подтверждения, что фоновая страница вообще грузится (диагностика v1.2.361, [[code-todo]] TODO-36) — сейчас Шаг 3А молчит.

**КАК ПРОВЕРЕНО**: `npm run lint` 0; `npx vitest run ozonBgWatcher.vitest.js` **8/8**; `fileSizeLimits` 595/595; `rendererConsoleGuard` 0; полный `npm run test:vitest` — запущен.

## Текущая версия: v1.2.361 (2 сентября 2026)

### v1.2.361 — Ozon Шаг 3А молчит: диагностическая лесенка в фоновую страницу «Вопросы»

**ЗАЧЕМ**: журнал пользователя (2026-09-02, сборка с окно-фиксом v1.2.360) показал **0 строк `[ozon-bg]`** — скрытая фоновая webview «Вопросы» (v1.2.358) не выдаёт признаков жизни. Причина обрыва неизвестна (страница не грузится / антибот Ozon / хук не впрыснулся / события консоли не приходят) — гадать нельзя.

**ЧТО** (`src/utils/ozonBgWatcher.js` ~63 стр.): в `bindOzonBgWatcher` добавлены разовые записи-ступени (текст вопросов НЕ логируется): `[ozon-bg] страница создана` (ref сработал/элемент смонтирован) → `did-fail-load` → `[ozon-bg] загрузка НЕ удалась code=…` (антибот/сеть) → `did-finish-load` → `страница загрузилась URL=…` → `dom-ready` → `первое console-сообщение получено` → (существующее) `ozon-q scan …`. По тому, ГДЕ обрывается лесенка, будет ясен корень.

**КАК ПРОВЕРЕНО**: `eslint` 0; `fileSizeLimits` 594/594; `rendererConsoleGuard` 0. **Действие пользователя**: перезапуск → прислать строки `[ozon-bg] …` из журнала. По ним — точечный фикс Шага 3А, потом Шаг 3Б. **Откат**: `git checkout src/utils/ozonBgWatcher.js`. Это ДИАГНОСТИКА, не фикс.

## Текущая версия: v1.2.360 (2 сентября 2026)

### v1.2.360-v1.2.349 — Счётчики виджета Ozon (msg/qa) + сторож «Вопросов» Шаг 2 (Ответы=0=новый) + фон Шаг 3a/3b + фиксы по ревью + окно ready-to-show (архив → archive/features-v1.2.349-360.md)

### v1.2.348-v1.2.329 — Виджет Ozon (ранний: док/перетаскивание/место) + окно «на весь экран» + ночная стена уведомлений + сторож «Покупатели» + логотип Ozon + заставка (архив → archive/features-v1.2.329-348.md)

### v1.2.328-v1.2.313 — Ozon (строгий режим сессии/антибот, каталог, разведчик структуры) + логотипы ВК/МАКС/WhatsApp + фиксы логов/дедупа уведомлений (архив → archive/features-v1.2.313-328.md)

### v1.2.312 — старт: не заваливать всплывашками offline-бэклог (архив → archive/features-v1.2.303-312.md)

### v1.2.311-v1.2.305 — Сага «чёткий аватар веб-Telegram»: диагностики → фикс → ПОДТВЕРЖДЕНО (архив → archive/features-v1.2.305-311.md)

### v1.2.303-304 — веб-значки рейла: чистые бейджи + свечение + поднятие при перетаскивании (архив → archive/features-v1.2.303-312.md)

### v1.2.302-v1.2.293 — верхняя панель (линейные значки, единая строка), 3-зонный рейл, фикс фото из уведомления, стекло-кнопка, чистка dist (архив → archive/features-v1.2.293-302.md)

### v1.2.288-v1.2.268 — Веб-аватарки (диагностика + чёткое фото из Настроек Telegram), скрытие верхних вкладок, карточки аккаунта/контакта, чистка журнала (архив → archive/features-v1.2.268-288.md)

### v1.2.267-v1.2.256 — Окно «Добавить» (протокол→мессенджер) + портал полосы «← Общий чат» (архив)

Перенесено в [archive/features-v1.2.256-267.md](archive/features-v1.2.256-267.md) (2026-08-19): единая полоса (веб встроен в нативный рейл, SourceRail удалён, ADR-032); одна кнопка «＋ Добавить» → окно «протокол→мессенджер» + доводки; портал полосы на уровне App (v1.2.260 откат→v1.2.262 рабочий) + кнопка «← Общий чат» над вебом.

### v1.2.254-v1.2.244 — боковой рейл SourceRail (Этапы 1–3, ЗАКРЫТ разворотом v1.2.256, архив)
Подход отдельной панели SourceRail закрыт (Модель 🅰️, ADR-032). История: [archive/features-v1.2.244-255.md](archive/features-v1.2.244-255.md).

### v1.2.243-v1.2.237 — Боковой рейл (флаг/тумблер) + карточка контакта (читаемость/контур/заметка) + МАКС мьют-фикс/диагностика (архив)

Перенесено в [archive/features-v1.2.237-243.md](archive/features-v1.2.237-243.md) (2026-09-01, разгрузка под лимит 100 КБ).

### v1.2.236-v1.2.222 — галочки списка + карточка контакта + МАКС-диагностика + строка отправки + пузырь «↓N» (архив)
Перенесено в [archive/features-v1.2.222-236.md](archive/features-v1.2.222-236.md).

### v1.2.221-v1.2.214 — Порядок фото/«текст отдельно» + ВК-навигация vk.ru + фикс счётчика ВК + окно отправки (архив)

Перенесено в [archive/features-v1.2.214-221.md](archive/features-v1.2.214-221.md) (2026-08-13, разгрузка под лимит 100 КБ): ждём серверное подтверждение фото/альбома перед текстом — порядок у получателя (v1.2.214) + таймаут-WARN (v1.2.215); «перейти в чат» по клику ВК-уведомления под vk.ru + имя мессенджера в статусе (v1.2.216) + тест-репродюсер (v1.2.217) + доводка сравнения имени (v1.2.218); фантомная «1 непрочитано» ВК — счётчик перестал считать бейджи «Друзья/Игры» (v1.2.219); окно отправки фото/файла — одна кнопка «Отправить» + инфо-строка (v1.2.220); лента уведомлений — не дёргать список при чтении + видимая прокрутка (v1.2.221).

### v1.2.213-v1.2.205 — Окно отправки фото: подпись/лимит 1024/«текст отдельно»/порядок/диагностика (архив)

Перенесено в [archive/features-v1.2.205-213.md](archive/features-v1.2.205-213.md) (2026-08-11, разгрузка под лимит 100 КБ): фикс двойной вставки фото + разбит windowManager (v1.2.205) + подтверждающий лог (v1.2.206); окно фото — 7 функций просмотра (v1.2.207); счётчик подписи + «фото, затем текст отдельно», лимит Telegram 1024 (v1.2.209); лимит подписи и «текст отдельно» для видео/документов (v1.2.210); длинный текст режется ≤4096 и шлётся ПОСЛЕ загрузки фото — порядок (v1.2.211); доводки «ждём именно это фото» + число сообщений (v1.2.212); диагностика отправки — лог провала/номера/результат (v1.2.213).

### v1.2.204-v1.2.197 — Окно отправки фото: инструменты просмотра (зум/поворот/сдвиг) + много фото + память окна (архив)

Перенесено в [archive/features-v1.2.197-204.md](archive/features-v1.2.197-204.md) (2026-08-11, разгрузка под лимит 100 КБ): окно отправки фото с зумом/поворотом/сдвигом (v1.2.197-198), логи потока + прозрачность PNG + пропуск в альбоме (v1.2.199), запоминание «на весь экран» (v1.2.200), приподнятая карточка + растущее поле подписи (v1.2.201), чистая логика памяти окна + тесты (v1.2.202), несколько фото в одном сообщении (v1.2.203), кнопки в шапке + вставка Ctrl+V + свободнее зум (v1.2.204).

### v1.2.196–v1.2.185 — диагностика ВК/МАКС, установщик/Win32, прокрутка чата (архив)

Перенесено в [archive/features-v1.2.185-196.md](archive/features-v1.2.185-196.md).

### v1.2.184-v1.2.170 — Логотип Telegram/живой статус собеседника/переключатель режимов/фикс поиска/читаемость шапки (архив)

Перенесено в [archive/features-v1.2.170-184.md](archive/features-v1.2.170-184.md).

### v1.2.169-v1.2.161 — Фильтр аккаунтов (мультивыбор+соло) + самолечение + закрепы/ресайз рейла (архив)

Перенесено в [archive/features-v1.2.161-169.md](./archive/features-v1.2.161-169.md) (2026-07-31, разгрузка под лимит 100 КБ): откат «приклеенного» списка закреплённых (161/162); фильтр аккаунтов на левой панели, клик=вкл/выкл, двойной=соло, кнопка «Все» + доводки (163/164); ресайз левого рейла перетаскиванием (165); полоса «Закреплённое» — читаемый фон + переход к сообщению (166/167); фиксы «после удаления 2-го аккаунта оставшийся не грузился» + самолечение фильтра при старте (168/169).

### v1.2.160-v1.2.156 — Сага «перетаскивание закреплённых» (ОТКАЧЕНА в v1.2.161, архив)

Перенесено в [archive/features-v1.2.156-160.md](./archive/features-v1.2.156-160.md) (2026-07-30): серия попыток сделать ручное перетаскивание порядка закреплённых чатов (HTML5 drag → свой образ за курсором → живая перестановка → вынос в отдельный не-виртуальный `PinnedChatList` с Pointer Events). **Итог — откачено в v1.2.161**: закреплённые должны листаться в ОБЩЕМ списке, а не быть отдельным блоком; перетаскивание же требует отдельного списка. Урок: drag-порядок и виртуальный список react-window (строки по индексу-слоту) несовместимы без замены движка. Из серии сохранились только НЕ-drag доводки (цвет-грань уведомления по `accountId`, цвет-полоса в узком режиме).

### v1.2.155-v1.2.147 — Цвет-метки аккаунтов (различимые цвета/полоса/обводка) + фиксы входа/удаления 2-го аккаунта (архив)

Перенесено в [archive/features-v1.2.147-155.md](./archive/features-v1.2.147-155.md) (2026-07-31, разгрузка под лимит 100 КБ): цвет аккаунта в закреплённых/уведомлении (v1.2.155); устойчивые цвет-метки — первый свободный + сохранение вместо хеша (v1.2.154); полоса/обводка/выбор цвета 🎨 (v1.2.153); спиннер ввода кода Telegram (v1.2.152); карточка выхода не заезжает под панель задач + спиннер «думания» (v1.2.150-151); сброс «залипшего success» гейта входа + разгрузка NativeApp (v1.2.148-149); фикс «чёрный экран» после добавления 2-го аккаунта (v1.2.147).

### v1.2.146-v1.2.140 — Добавление/удаление аккаунта (диагностика + фиксы следов) + закреп 📌 + маскировка кода в логах (архив)

Перенесено в [archive/features-v1.2.140-146.md](./archive/features-v1.2.140-146.md) (2026-07-31, разгрузка под лимит 100 КБ): фикс папки сессии при удалении аккаунта (v1.2.145) + чистка кэш-файла/проброс переименования (v1.2.146); диагностика чёрного экрана и операций с аккаунтом + ErrorBoundary вокруг InboxMode (v1.2.142-143); маскировка кода входа в логах (v1.2.144); значок 📌 в углу аватарки (v1.2.141); откат авто-чистки осиротевших закреплений — теряла данные (v1.2.140).

### v1.2.139-v1.2.138 — Локальное закрепление чатов (📌): базовая фича + доработки (архив)

Перенесено в [archive/features-v1.2.138-139.md](./archive/features-v1.2.138-139.md) (2026-07-30, разгрузка под лимит 100 КБ): локальное закрепление чатов полоской+значком 📌, правый клик, хранение в localStorage, НЕ синхронизируется с Telegram (v1.2.138); доработки — 📌 в узком режиме, устойчивый порядок по массиву pinnedIds, вынос `filterSortChats`, чистка осиротевших (позже откачена v1.2.140) (v1.2.139). Перетаскивание порядка сделано позже (v1.2.156→160).

### v1.2.137-v1.2.134 — Уведомление снимается при прочтении на другом устройстве + CI-фикс TZ снапшота + ВК vk.ru (архив)

Перенесено в [archive/features-v1.2.134-137.md](./archive/features-v1.2.134-137.md) (2026-07-30, разгрузка под лимит 100 КБ): прочитал чат в Telegram на телефоне → карточка-уведомление снимается, частичное чтение + чистая `shouldDismissForRead` (v1.2.137); snapshot ChatListItem сделан TZ-независимым — падал на CI под UTC (v1.2.135); ВК основной адрес → vk.ru + убрана лишняя ошибка `GUEST_VIEW_MANAGER_CALL` запасного впрыска (v1.2.134).

### v1.2.133-v1.2.130 — Строка списка чатов: время + имя отправителя (группы) + «Вы:» + значок форума + имя автора в уведомлении (архив)

Перенесено в [archive/features-v1.2.130-133.md](./archive/features-v1.2.130-133.md) (2026-07-30, разгрузка под лимит 100 КБ): время последнего сообщения + имя автора в превью групп/форумов + значок 🗂️ форума (v1.2.130); «Вы:» для своих + единая `lastSenderLabel` (v1.2.131); имя автора в заголовке уведомления + чистка мёртвого `pickNotifTitle` (v1.2.132); фикс залипания имени в превью + вынос `nativeStoreLastMsgIpc.js` (v1.2.133).

### v1.2.129-v1.2.126 — Уведомления: «невидимая стена»/heartbeat-сторож + ВК переносы строк (архив)

Перенесено в [archive/features-v1.2.126-129.md](./archive/features-v1.2.126-129.md) (2026-07-30, разгрузка под лимит 100 КБ): гасим «невидимую стену» сразу по видимой высоте (v1.2.128), heartbeat-сторож без спама (v1.2.127) и его первая версия (v1.2.126); ВК — переносы строк/абзацы в тексте уведомления через `_vkNodeText` (v1.2.129).

### v1.2.125-v1.2.122 — Уведомление «поверх всех» + ВК-хук (разгрузка/эмодзи/mute-детект/переносы строк) (архив)

Перенесено в [archive/features-v1.2.122-125.md](./archive/features-v1.2.122-125.md) (2026-07-29, разгрузка под лимит 100 КБ): окно уведомления надёжно поверх всех — `screen-saver`+reassert (v1.2.125); ВК-хук — удалён мёртвый toast-наблюдатель + эмодзи из `<img alt>` (v1.2.124), сужен mute-селектор + тесты чистых функций (v1.2.123), сохранены переносы строк + снят предел текста 1000 (v1.2.122).

### v1.2.121-v1.2.116 — Telegram: не показываем сторис; ВК/vk.ru: диагностики значка 🔕 + текст целиком + фикс «не уведомлять о заглушённых» (архив)

Перенесено в [archive/features-v1.2.116-121.md](./archive/features-v1.2.116-121.md) (2026-07-29, разгрузка под лимит 100 КБ): Telegram Web — гасим системную фразу «опубликовал(а) историю» (v1.2.121); ВК/vk.ru — итоговый фикс «не уведомлять о заглушённых чатах 🔕» по видимости значка (v1.2.120), текст уведомления целиком без своей мерки (v1.2.119), серия временных диагностик значка «беззвучно» (v1.2.116-118, диагностики давно удалены).

### v1.2.115-v1.2.108 — ВК переезд на vk.ru (детект/текст/диагностика) + время в источнике (архив)

Перенесено в [archive/features-v1.2.108-115.md](./archive/features-v1.2.108-115.md) (2026-07-27, разгрузка под лимит 100 КБ): переезд ВК на vk.ru + определение домена (v1.2.109); настоящий детект новых сообщений по списку чатов (v1.2.112) + подтверждение логом (v1.2.113); чистый текст уведомления по структуре строки (v1.2.114); 3 фикса по ревью — чистка времени/шторм на старте/дубль (v1.2.115); серия временных диагностик структуры vk.ru (v1.2.110/111); время сообщения в строке источника, единый формат (v1.2.108).

### v1.2.107-v1.2.101 — Уведомления: сторож стены + видео/фото в карточке (архив)

Перенесено в [archive/features-v1.2.101-107.md](./archive/features-v1.2.101-107.md) (2026-07-24, разгрузка под лимит 100 КБ): сторож «невидимой стены» + чистая decideNotifResize + быстрее закрытие (v1.2.107); фикс осиротевшего положительного отчёта — «невидимое зависшее окно», ловящее клики (v1.2.106); «Прочитано» для native-Telegram (v1.2.105); видео в уведомлении — постер/плеер как в чате, видео-как-документ, крутилка по факту (v1.2.101-104).

### v1.2.100-v1.2.90 — Уведомления/видео/фото + диагностика Telegram (архив)

Перенесено в [archive/features-v1.2.90-100.md](./archive/features-v1.2.90-100.md) (2026-07-23, разгрузка под лимит 100 КБ): страховка крутилки одиночного фото (v1.2.100), «размытый фон» одиночного фото (v1.2.99), крупный аватар + источник в шапке (v1.2.98), раскладка «Стопка» для всех карточек (v1.2.96-97), одиночное фото/видео в native-уведомлении (v1.2.95), диагностика фото/ссылок Telegram native+web (v1.2.90/92/93/94), расчёт вертикали дока в чистую функцию + тест (v1.2.91).

### v1.2.89-v1.2.74 — Док/подсказка/закреп + имя аккаунта + Telegram-аватар + альбом (архив)

Перенесено в [archive/features-v1.2.74-89.md](./archive/features-v1.2.74-89.md): фиксы дока/подсказки/закрепа (стабильный якорь верха, панель задач Windows, снятие snap, rAF/throttling; v1.2.76-89), доведение имени аккаунта до закрепа/подсказки (v1.2.83-88), Telegram аватар+источник в уведомлении (v1.2.75), листание альбома + чёткие плитки (v1.2.74).

### v1.2.73-v1.2.59 — Док/подсказка/закреп: серия фиксов (архив)

Перенесено в [.memory-bank/archive/features-v1.2.59-73.md](archive/features-v1.2.59-73.md) при разбиении (21 июля 2026). Архитектура — decisions.md ADR-017/ADR-018.

### v1.2.58-v1.2.46 — VK: тост/sidebar/active-chat уведомления, MAX dedup, ribbon (архив)

Перенесено в [.memory-bank/archive/features-v1.2.46-58.md](archive/features-v1.2.46-58.md) при разбиении (17 июля 2026).

### v1.2.45-v1.2.37 — VK DOM observer / диагностика уведомлений (архив)

Перенесено в [.memory-bank/archive/features-v1.2.37-45.md](archive/features-v1.2.37-45.md) при разбиении (17 июля 2026).

### v1.2.36-v1.2.30 — диагностика MAX/системы (архив)

Полные записи перенесены в [archive/features-v1.2.30-36.md](./archive/features-v1.2.30-36.md), чтобы active features.md оставался меньше лимита memory-bank. Суть: диагностика стала ручной, фоновой, с последним отчётом, полной MAX/VK-цепочкой и понятными кнопками; pipeline уведомлений этим блоком не менялся, кроме описанных в соответствующих версиях точечных MAX/VK исправлений.
### v1.2.29 — диагностика запускается только вручную и не мешает работе (архив)

Полная запись перенесена в [archive/features-v1.2.29.md](./archive/features-v1.2.29.md), чтобы активный features.md оставался меньше лимита memory-bank. Суть: большая модалка диагностики больше не стартует запись сама, управление записью стало явным, общий лог не очищается.
### v1.2.28 — MAX: sidebar watcher не создаёт фантомы во время поиска (архив)

Полная запись перенесена в [archive/features-v1.2.28.md](./archive/features-v1.2.28.md), чтобы активный features.md оставался меньше лимита memory-bank. Суть: `max-sidebar` при активном поиске MAX обновляет baseline, но не создаёт ribbon из старого preview; реальные `max-notification-api` и `max-sw-showNotification` не отключались.

**Структура файла**: этот features.md содержит только **последние активные версии**. Старое — в архиве:

| Архив | Содержимое | Размер |
|---|---|---|
| [`archive/features-v0.95.39.md`](./archive/features-v0.95.39.md) | v0.95.39 (убран twoPhase + RAF×2 + 350мс easeOutCubic; стабилизировано v0.95.40+) | ~2 КБ |
| [`archive/features-v0.95.40.md`](./archive/features-v0.95.40.md) | v0.95.40 (большие emoji + a11y reduced-motion + sticky bottom on media; стабилизировано v0.95.41+) | ~3 КБ |
| [`archive/features-v0.95.41.md`](./archive/features-v0.95.41.md) | v0.95.41 (Custom emoji premium WebM/WebP + reduced-motion интеграционные тесты; стабилизировано v0.95.42+) | ~3 КБ |
| [`archive/features-v0.95.42.md`](./archive/features-v0.95.42.md) | v0.95.42 (сохранение поиска + история + ✕ + подсветка совпадений; стабилизировано v0.95.43+) | ~2 КБ |
| [`archive/features-v0.95.38.md`](./archive/features-v0.95.38.md) | v0.95.38 (фикс дубля сообщений через updateMessageSendSucceeded, ⏳ индикатор, mistakes static guard) | ~2 КБ |
| [`archive/features-v0.95.35-37.md`](./archive/features-v0.95.35-37.md) | v0.95.35-37 (fade-in changelog, auto-scroll outgoing-other-device, sending_state polish, mistakes/outgoing-two-cases.md; стабилизировано v0.95.38+) | ~3 КБ |
| [`archive/features-v0.95.34.md`](./archive/features-v0.95.34.md) | v0.95.34 (темовые vars в :root, вспышка bubble, WhatsNewModal UX; стабилизировано v0.95.40+) | ~2 КБ |
| [`archive/features-v0.95.33.md`](./archive/features-v0.95.33.md) | v0.95.33 (фикс «цвет не применяется» через querySelectorAll, регресс-тест blur, деловой стиль; финал в v0.95.34) | ~2 КБ |
| [`archive/features-v0.95.32.md`](./archive/features-v0.95.32.md) | v0.95.32 (производительность WhatsNewModal: убран backdrop-filter + contain + деловой стиль changelog) | ~2 КБ |
| [`archive/features-v0.95.31.md`](./archive/features-v0.95.31.md) | v0.95.31 (аккаунты вниз + drag-n-drop + multi-user typing + throttle реакций; стабилизировано v0.95.34+) | ~3 КБ |
| [`archive/features-v0.95.30.md`](./archive/features-v0.95.30.md) | v0.95.30 (плавная auto-scroll + 5 цветовых тем + dropdown + opacity 0.95; стабилизировано v0.95.33-34) | ~3 КБ |
| [`archive/features-v0.95.29.md`](./archive/features-v0.95.29.md) | v0.95.29 (реакции 👍❤️🔥 + Telegram-style header + General 📢 + render-counter; стабилизировано v0.95.31-34) | ~5 КБ |
| [`archive/features-v0.95.28.md`](./archive/features-v0.95.28.md) | v0.95.28 (Telegram-style auto-scroll к новому + ↓N без «слепой зоны» Schmitt; стабилизировано v0.95.31) | ~4 КБ |
| [`archive/features-v0.95.27.md`](./archive/features-v0.95.27.md) | v0.95.27 (расширенная диагностика send pipeline; стабилизировано v0.95.29) | ~3 КБ |
| [`archive/features-v0.95.23-26.md`](./archive/features-v0.95.23-26.md) | v0.95.23 – v0.95.26 (курсор в input, initial backfill, voice/spellcheck/action-bar/WhatsNew, фикс 47-дневного бага unreadCount; стабилизировано) | ~15 КБ |
| [`archive/features-v0.95.19-22.md`](./archive/features-v0.95.19-22.md) | v0.95.19 – v0.95.22 (диагностика tg-new-message, финал jump-to-end, бейдж форум-группы, форум-overlay; стабилизировано) | ~6 КБ |
| [`archive/features-v0.95.15-18.md`](./archive/features-v0.95.15-18.md) | v0.95.15 – v0.95.18 (итеративный fetch + форум-топики + двухфазный scroll + ForumTopicEmptyState; стабилизировано v0.95.20-21) | ~8 КБ |
| [`archive/features-v0.95.12-14.md`](./archive/features-v0.95.12-14.md) | v0.95.12 – v0.95.14 (3 итерации jump-to-end до итеративного fetch v0.95.15, полная сага в jump-to-end-saga.md) | ~32 КБ |
| [`archive/features-v0.95.8-9.md`](./archive/features-v0.95.8-9.md) | v0.95.8 – v0.95.9 (счётчик ↓ обнуляется + анимация + live compact + порог 128) | ~14 КБ |
| [`archive/features-v0.95.5-7.md`](./archive/features-v0.95.5-7.md) | v0.95.5 – v0.95.7 (sticky pinned overlay, кнопка ↓ Telegram-style, drag-to-resize) | ~24 КБ |
| [`archive/features-v0.95.0-3.md`](./archive/features-v0.95.0-3.md) | v0.95.0 – v0.95.3 (контигуити-фикс, afterId load-newer, мигание ↓ Schmitt trigger, диагностика «дёрг») | ~13 КБ |
| [`archive/features-v0.94.1-7.md`](./archive/features-v0.94.1-7.md) | v0.94.1 – v0.94.7 (TDLib listener leak, scroll caskade, спокойная загрузка, пилюля прогресса — стабилизированы v0.95.0-2) | ~24 КБ |
| [`archive/features-v0.93.0.md`](./archive/features-v0.93.0.md) | v0.93.0 (pixel-perfect scroll restore через LocationOptions.offset, superseded v0.94.0) | ~7 КБ |
| [`archive/features-v0.92.0-6.md`](./archive/features-v0.92.0-6.md) | v0.92.0 – v0.92.6 (сага Virtuoso scroll restore, superseded v0.94.0) | ~37 КБ |
| [`archive/features-v0.91.11-24.md`](./archive/features-v0.91.11-24.md) | v0.91.11 – v0.91.24 (сага scroll restore: 13 версий → миграция на virtuoso) | ~47 КБ |
| [`archive/features-v0.91.1-10.md`](./archive/features-v0.91.1-10.md) | v0.91.1 – v0.91.10 (initial-load, scroll-jump, newBelow, forum topics, updateChatLastMessage) | ~12 КБ |
| [`archive/features-v0.87.106-114.md`](./archive/features-v0.87.106-114.md) | v0.87.106 – v0.87.114 (multi-account UI финал, кнопки режимов, мьют чата, аватарки отправителей в группах) | ~22 КБ |
| [`archive/features-v0.87.93-105.md`](./archive/features-v0.87.93-105.md) | v0.87.93 – v0.87.105 (multi-account native, login flow, разбиения, cc-media://) | ~30 КБ |
| [`archive/features-v0.87.80-92.md`](./archive/features-v0.87.80-92.md) | v0.87.80 – v0.87.92 (pre-push hook, разбиения 4-7, AccountContextMenu) | ~16 КБ |
| [`archive/features-v0.87.65-79.md`](./archive/features-v0.87.65-79.md) | v0.87.65 – v0.87.79 (план разбиения 1-3, pre-push hook, bubble UI) | ~54 КБ |
| [`archive/features-v0.87.51-64.md`](./archive/features-v0.87.51-64.md) | v0.87.51 – v0.87.64 (groupedUnread удалён, pre-commit hook) | ~54 КБ |
| [`archive/features-v0.87.40-50.md`](./archive/features-v0.87.40-50.md) | v0.87.40 – v0.87.50 (итерации native scroll + unread) | ~40 КБ |
| [`archive/features-v0.87-early.md`](./archive/features-v0.87-early.md) | v0.87.0 – v0.87.39 (запуск native + ранние фиксы) | ~140 КБ |
| [`archive/features-pre-v0.87.md`](./archive/features-pre-v0.87.md) | v0.1.0 – v0.86.10 (до native-режима, 3 марта – 14 апреля 2026) | ~210 КБ |

**Архив не читается по умолчанию.** Запрос к нему — только при явной просьбе («что было в v0.85», «покажи старый changelog»).

**До рефакторинга v0.87.57** файл был 445 КБ (3371 строк, 323 версии). После — ~100 КБ в корне.

---

### Архив v1.2.27 и старше

Подробная история v1.2.27 и более ранних версий перенесена в [archive/features-v1.2.27-and-older.md](./archive/features-v1.2.27-and-older.md). Активный файл держим компактным, чтобы memory-bank быстро читался и проходил лимиты.
