# Реализованные функции — ChatCenter

## Текущая версия: v1.2.496 (18 сентября 2026)

### v1.2.496 — «Молчит посредник (VPN/прокси)»: целое семейство обрывов не запускало ни экран, ни повторы

**ЖАЛОБА (2026-09-23, 09:05)**: «запустил через терминал — не запустилась ни одна программа» (скриншот: у всех
пяти веб-значков красные точки, метка «нет сети» горит, родной Telegram при этом грузит чаты).

#### КОРЕНЬ (🟢 доказан журналом + реестром Windows + проверкой порта)
| Что смотрел | Что увидел |
|---|---|
| `chatcenter.log` 09:06:05 | у ВСЕХ пяти веб-мессенджеров одна ошибка: `ERR_PROXY_CONNECTION_FAILED` (код **−130**) |
| тот же журнал | `[reconnect] … код=-130 — повтор не нужен` ×5 → записи не создались |
| реестр Windows `HKCU\…\Internet Settings` | `ProxyEnable=1`, `ProxyServer=127.0.0.1:2080` — весь веб идёт через посредника |
| `Get-NetTCPConnection -LocalPort 2080` | **никто не слушает**; при этом процесс «Liberty VPN» запущен (туннель не поднят) |
| `chatcenter.prev.log` | таких ошибок **0** — накануне посредник работал |
| наш код | `NETWORK_ERROR_CODES` ([shared/reconnectPlan.js](../shared/reconnectPlan.js)) не содержал −130 → `isNetworkError`=false → ранний выход в [useWebviewReconnect.js:51](../src/hooks/useWebviewReconnect.js) ДО создания записи |

Следствие: **ни экрана «Нет связи», ни повторов, и после возврата VPN страницы не поднялись бы сами** — будить
было некому. Пульс интернета при этом отработал верно: `net.fetch` идёт тем же путём через посредника, поэтому
честно сказал «интернета нет» (а метка на полосе — первое боевое срабатывание v1.2.493).

#### ЧТО СДЕЛАНО
| # | Что | Файл |
|---|---|---|
| 1 | В список «стоит повторять» добавлено семейство «молчит ПОСРЕДНИК»: **−111** ERR_TUNNEL_CONNECTION_FAILED, **−120** ERR_SOCKS_CONNECTION_FAILED, **−121** ERR_SOCKS_CONNECTION_HOST_UNREACHABLE, **−130** ERR_PROXY_CONNECTION_FAILED (номера сверены с официальным перечнем Chromium `net_error_list.h` — факт уровня 1; моя память по этим номерам была НЕВЕРНОЙ) | [shared/reconnectPlan.js](../shared/reconnectPlan.js) `PROXY_ERROR_CODES`, `isProxyError` |
| 2 | Экран «Нет связи» говорит правду: **«Не отвечает посредник (VPN или прокси)»**. Ветка стоит ПЕРВОЙ — иначе мёртвый пульс дал бы «Нет интернета» | [shared/reconnectTexts.js](../shared/reconnectTexts.js) `reasonTitle` |
| 3 | Пульс уточняет причину в журнале (`· похоже, молчит ПОСРЕДНИК (VPN/прокси), а не сам интернет`) и отдаёт окну признак `proxyDown` | [shared/netPulsePlan.js](../shared/netPulsePlan.js) `looksLikeProxyDown`, [netPulseHandlers.js](../main/handlers/netPulseHandlers.js) (`lastError`) |
| 4 | Метка полосы: «нет сети · VPN?» + своя подсказка; строка состояния: «Интернет: нет · молчит посредник (VPN/прокси) · проверено N с назад» | [NativeSidebar.jsx](../src/native/components/NativeSidebar.jsx), `pulseStatusLine` |

**Намеренно НЕ добавлены** (повторами не лечатся, нужен человек): −115 `ERR_PROXY_AUTH_UNSUPPORTED`,
−127 `ERR_PROXY_AUTH_REQUESTED`, −131 `ERR_MANDATORY_PROXY_CONFIGURATION_FAILED`, −136 `ERR_PROXY_CERTIFICATE_INVALID`.
Закреплено тестом-ловушкой.

#### КАК РАБОТАЕТ / КАК ВЫКЛЮЧИТЬ
Ошибка загрузки → `isNetworkError` (теперь true для −111/−120/−121/−130) → запись + экран + лестница пауз
5→10→20→40→60 с. **Страницу при мёртвом пульсе по-прежнему НЕ перезагружаем** (ветка `net-down` в
`shared/reconnectAttempt.js`) — недописанное сообщение цело; закреплено тестом на коде −130.
Распознавание текста ошибки пульса — МЯГКОЕ (подстроки PROXY/TUNNEL/SOCKS): не узнали → просто не уточняем
причину, поведение как раньше. Надёжный путь — код ошибки страницы, он от текста не зависит.
Откат: `git revert` коммита версии; выключатель пульса прежний (`settings.netPulse === false`).

**КАК ПРОВЕРЕНО**: `reconnectPlan.vitest.js` (+2 проверки: новые коды = обрыв; 🔴 ловушка «беды с логином/сертификатом
НЕ повторяем»), `reconnectAttempt.vitest.js` (+ловушка «ветка посредника первее мёртвого пульса», +защита
«не дёргаем страницу» на коде −130) 13/13, `netPulseUi.vitest.js` (+5: распознавание текста, строка журнала,
снятие признака при возврате, строка состояния, проводка) 22/22. **Прогон наоборот**: ветку посредника отключил →
тест покраснел на «Нет интернета»; код −130 убрал из списка → тест покраснел. Полный vitest, линт, лимиты, сборка — в отчёте.

**ЧТО ПРОВЕРИТЬ ВРУЧНУЮ** (нужен ПОЛНЫЙ перезапуск): выключить VPN → у веб-мессенджеров должен появиться экран
**«Не отвечает посредник (VPN или прокси)»** и счётчик повтора; включить VPN → страницы поднимаются сами.
Строка в журнале `· похоже, молчит ПОСРЕДНИК` подтвердит, что распознавание текста ошибки пульса попало в цель
(если её нет, а экран верный — значит текст у `net.fetch` другой; это не ошибка, надёжный путь работает).

### v1.2.495 — Разгрузка бюджета интерфейса: разбор `__CC_*` переехал в `shared/` + память предупреждает при 80%

**ОТ ПОЛЬЗОВАТЕЛЯ**: «Найдено, НЕ трогал — делай» (три находки из отчёта v1.2.494).

#### Находка №1+№2 (одна правка): `consoleMessageParser` → `shared/`
| ❌ Было | ✅ Стало |
|---|---|
| `src/utils/consoleMessageParser.js` — 92 строки внутри бюджета экранов | `shared/consoleMessageParser.js` — вне бюджета (ADR-044) |
| бюджет 31 195 / 31 200 — **запас 3 строки** (следующая правка экранов упиралась в потолок) | 31 104 / **31 150** — запас 46 строк, планка опущена следом |

**ПОЧЕМУ это законный перенос, а не «спрятать в другую папку»** (правило ADR-044 — `shared/` = обособленный модуль, не код интерфейса):
- файл ЧИСТЫЙ: `grep -cE "window\.|document\.|from 'react'|navigator\."` → **0**;
- разбирает строки `__CC_*`, прилетающие из ЧУЖИХ страниц мессенджеров (как уже переехавшие
  [webviewDiagnostics.js](../shared/webviewDiagnostics.js), [browserBannerHider.js](../shared/browserBannerHider.js));
- его тянут ДВА разных потребителя — [webviewSetup.js](../src/utils/webviewSetup.js) (экраны) и
  [ozonBgWatcher.js](../src/utils/ozonBgWatcher.js) (фоновый наблюдатель).

**ЧТО ПРАВИЛОСЬ**: `git mv` + два импорта на `'../../shared/consoleMessageParser.js'` + путь чтения в
[consoleMessageParser.test.cjs](../src/__tests__/consoleMessageParser.test.cjs) + планка в
[fileSizeLimits.test.cjs](../src/__tests__/fileSizeLimits.test.cjs) (31200 → 31150) + 7 ссылок в памяти.
Логика файла НЕ менялась ни на строку (`git diff -M` показывает чистое переименование).

#### Находка №3 (features.md 83 КБ из 100) — сделано ИНАЧЕ, чем «архивировать сейчас»
Архивация при 83 КБ преждевременна: разрывать свежую историю ради 17 КБ запаса — вред (записи последних
версий читаются чаще всего). Вместо этого закрыта ПРИЧИНА, по которой находка вообще появилась — файл рос молча
до самого потолка: в [memoryBankSizeLimits.test.cjs](../src/__tests__/memoryBankSizeLimits.test.cjs) добавлено
**жёлтое предупреждение при 80%** (как в тесте лимитов кода) — тест от него не падает, но в каждом прогоне видно:
`⚠️ .memory-bank/decisions.md — 86 КБ, 86% от лимита`. Сразу подсветились ДВА файла (`decisions.md` 86 КБ,
`features.md` 81 КБ) — то есть сторож оказался нужен не «когда-нибудь», а прямо сейчас.

**КАК ПРОВЕРЕНО**: `consoleMessageParser.test.cjs` 30/30, `appStructure` 60/60, `integrationChains` 18/18,
`memoryBankSizeLimits` 47/47 (+2 предупреждения), лимиты 693/693, бюджет 31 104/31 150, линт, полный vitest,
сборка — в отчёте сессии. **Визуальная проверка НЕ требуется** (поведение не менялось).

### v1.2.494 — Экран «Нет связи»: честная надпись кнопки, незамирающий счётчик, живой тест метки «нет сети»

**ОТ ПОЛЬЗОВАТЕЛЯ**: «ДЕЛАТЬ» по трём советам, прошедшим фильтр-критик (два ⚠️-совета отложены — см. ниже).

| # | Было | Стало | Где |
|---|---|---|---|
| 1 | Кнопка всегда «Повторить сейчас» — даже когда интернета нет и страницу НАМЕРЕННО не перезагружают (`reconnectAttempt.js`: при мёртвом пульсе `loadURL` не зовётся). Нажатие «ничего не делало» на вид | при мёртвом пульсе — **«Проверить интернет»** (нажатие просит главный процесс проверить сеть: `net:pulse-now`) | `retryButtonLabel` в [reconnectTexts.js](../shared/reconnectTexts.js); вызов в [WebviewOfflineOverlay.jsx](../src/components/WebviewOfflineOverlay.jsx); та же надпись идёт в журнал причиной проверки ([useWebviewReconnect.js](../src/hooks/useWebviewReconnect.js)) |
| 2 | Во время попытки таймер экрана выключался → строка «Интернет: … проверено N с назад» (v1.2.492) **замирала** на все секунды перезагрузки — вид зависшего экрана | тикает всегда, пока экран на виду | `WebviewOfflineOverlay.jsx` — убрано `if (trying) return undefined`, зависимость `[trying]` → `[]` |
| 3 | Метку «нет сети» на боковой полосе (v1.2.493) сторожила только проверка «такой текст есть в файле» | **поведенческий тест**: рисуем полосу, шлём вердикт «интернета нет» — метка появляется; при «есть»/«ещё не проверяли» её нет | [NativeSidebar.vitest.jsx](../src/native/components/NativeSidebar.vitest.jsx) (+2 проверки) |

**Почему надпись вынесена в общий файл, а не написана прямо в экране**: у бюджета строк интерфейса оставалось 5 строк
(31 195 / 31 200), а в `shared/` место есть; плюс правило «тексты этого экрана — в одном месте» (там же `reasonTitle`,
`pulseStatusLine`) — иначе журнал и кнопка разошлись бы в словах.

**Отложено осознанно** (вердикты фильтра-критика): вынос `consoleMessageParser.js` в `shared/` — ⚠️ его тянут три рабочих
файла (`webviewSetup.js:8`, `ozonBgWatcher.js:16`, `consoleMessageHandler.js:78`) и четыре теста-стража, ради 92 строк
трогать их нельзя «между делом»; скрипт-помощник архивации истории — ⚠️ отдельная задача на полчаса (история сейчас 72 КБ
из 100, не горит).

**КАК ПРОВЕРЕНО**
- [netPulseUi.vitest.js](../src/__tests__/netPulseUi.vitest.js) +4: «идёт попытка → Подождите…» (даже при мёртвом пульсе);
  **ловушка** «интернета нет → Проверить интернет»; «интернет есть / вердикта нет → прежняя надпись» (старые проверки экрана
  не ломаются); **ловушка** «счётчик больше не замирает» (в файле экрана нет `if (trying) return undefined`) + связь журнала
  с надписью.
- [NativeSidebar.vitest.jsx](../src/native/components/NativeSidebar.vitest.jsx) +2 (живой рендер; переход «есть → нет»
  берётся намеренно: возврат сети запускает 30-секундный подсчёт сводки и оставил бы висящий таймер в тесте).
- Старые проверки экрана (`useWebviewReconnect.vitest.jsx`: «Повторить сейчас», «Подождите…») — зелёные: вердикт пульса там
  не задан, надпись прежняя.
- Стражи `.cjs`, линт, лимиты, полный vitest, `npm run build` — в отчёте сессии.

**ЧТО ПРОВЕРИТЬ ВРУЧНУЮ** (ПОЛНЫЙ перезапуск): выдернуть кабель → на экране мессенджера кнопка называется «Проверить
интернет», секунды в строке «проверено N с назад» идут не останавливаясь (в том числе пока идёт попытка); вернуть кабель →
кнопка снова «Повторить сейчас».

**ОСТАТОЧНЫЕ РИСКИ**: (а) 🟢 постоянный тик — одна перерисовка ЭТОГО экрана в секунду, остальной интерфейс не трогается (у
экрана своё состояние времени, см. комментарий в файле); (б) 🟡 живьём не запускалось — правило проекта.

### v1.2.493 — Две находки ревью v1.2.492 починены + три совета из фильтра-критика (метка «нет сети» на полосе, «Проверить все» щупает интернет, «ждали N» в сводке)

**ОТ ПОЛЬЗОВАТЕЛЯ**: «чини всё и что подходит из советов — делай сразу».

#### Починено (обе находки доказаны прогоном в v1.2.492)
| # | Было | Стало | Где |
|---|---|---|---|
| 1 | строка `интернет ПРОПАЛ — ни один из **3** адресов` при одном адресе из настроек | число реально опрошенных адресов (`targetsCount`) | [netPulsePlan.js](../shared/netPulsePlan.js) `applyResult`, [netPulseHandlers.js](../main/handlers/netPulseHandlers.js) |
| 2 | сеть моргнула дважды за 30 с → **две** сводки, счёт первого окна терялся | окно подсчёта **продлевается**, таймер один; первые исходы сохраняются | [netRecoverySummary.js](../shared/netRecoverySummary.js) `startRecoveryWindow`, [useOpenPageWatch.js](../src/hooks/useOpenPageWatch.js) |

#### Советы, прошедшие фильтр-критик (факты — в отчёте сессии)
- **Метка «нет сети» на боковой полосе** — [NativeSidebar.jsx](../src/native/components/NativeSidebar.jsx): красная полоска над значками
  ТОЛЬКО при обрыве (пока интернет есть — ничего: здоровье каждого мессенджера уже показывают их кружки; правило памяти «встраивать в
  существующий UI, не плодить»). Данные — `useNetPulse()`.
- **«Проверить все» щупает интернет** — [App.jsx](../src/App.jsx) `refreshAllConnections` шлёт `net:pulse-now` (канал уже был).
- **Сводка знает, сколько ждали** — `[net-pulse] итог возврата сети за 30 с: … · ждали повтора N` (число берётся из
  `window.__ccReconnectWaiting`, которое хук ставит там же, где шлёт `net:pulse-waiting`).
- **features.md 94 КБ → записи v1.2.486-488 в [archive/features-v1.2.486-488.md](./archive/features-v1.2.486-488.md)**.
- На решение человека: «проверить `CustomEvent` в живом Electron» — 🧑 только запуск пользователем (правило проекта).

#### Заодно
Планка бюджета интерфейса в тесте **реально** опущена до 31200 (в v1.2.492 замена попала в комментарий, а не в число —
нашёл при ревью; факт 31 195 — запас 5 строк, следующая правка интерфейса начнётся с выноса).

**КАК ПРОВЕРЕНО**: [netPulseUi.vitest.js](../src/__tests__/netPulseUi.vitest.js) +4 (число адресов в строке; **ловушка дребезга**: два
возврата за 10 с → одна сводка с сохранённым счётом и «ждали повтора 2»; окно продлевается/обновляется; проводка полосы/кнопки/планки);
`reconnectPulseChain` 3/3; `NativeSidebar.vitest.jsx` — зелёный; стражи, линт, лимиты, полный vitest, сборка — в отчёте сессии.

**ЧТО ПРОВЕРИТЬ ВРУЧНУЮ** (ПОЛНЫЙ перезапуск): кабель вон → над значками на полосе красная метка «нет сети» (≤15 с), при возврате —
исчезает; «Проверить все» в панели → в журнале `причина проверки: окно: кнопка «Проверить все»`.

### v1.2.492 — Пять доводок пульса интернета: строка на экране, кружок в панели, адреса из настроек, сквозной тест, сводка возврата

**ОТ ПОЛЬЗОВАТЕЛЯ**: «это всё делай» — пять советов из отчёта v1.2.491.

| # | Что | Где |
|---|---|---|
| 1 | Экран «Нет связи»: строка **«Интернет: есть / нет · проверено N с назад»** (`pulseStatusLine`, [reconnectTexts.js](../shared/reconnectTexts.js)) | [WebviewOfflineOverlay.jsx](../src/components/WebviewOfflineOverlay.jsx) — под номером попытки; обновляется с тем же секундным тиком |
| 2 | Панель «Подключения»: **кружок «Интернет»** (цвета те же, что у здоровья подключений: зелёный/красный/серый) + строка | [ConnectionsPanel.jsx](../src/components/ConnectionsPanel.jsx); новый хук `useNetPulse()` в [useOpenPageWatch.js](../src/hooks/useOpenPageWatch.js) слушает событие окна `cc-net-pulse` |
| 3 | **Адреса пульса из настроек**: ключ `netPulseTargets` в `chatcenter.json` — список 1…6 строк с `https://`; ошибка формата → адреса по умолчанию + `WARN` в журнал с причиной (`resolveTargets`, [netPulsePlan.js](../shared/netPulsePlan.js)) | [netPulseHandlers.js](../main/handlers/netPulseHandlers.js) — строка старта пишет, откуда адреса |
| 4 | **Сквозной тест** «пульс → синтетическое online → лестница двигается сразу» через весь хук | [reconnectPulseChain.vitest.jsx](../src/__tests__/reconnectPulseChain.vitest.jsx) — 3 проверки на поддельной странице с фейковыми таймерами |
| 5 | **Сводка возврата сети**: через 30 с после «появился» — одна строка `[net-pulse] итог возврата сети за 30 с: ожили сами N · перезагружены M · не поднялись K` ([netRecoverySummary.js](../shared/netRecoverySummary.js); исход каждой попытки отдаёт `reconnectAttempt` через `onOutcome`) | строка в журнале — по ней через месяц видно, нужна ли перезагрузка или порог можно поднять |

**Что изменилось в уговоре пульса**: пакет `net:pulse` теперь уходит окну **на КАЖДУЮ проверку** (иначе «проверено N с назад» лгало
бы — окно знало только момент перехода). Безопасно: окно превращает пакет в событие `online/offline` **только на переходе**
(`applyPulse` сравнивает с прошлым вердиктом; сквозной тест «повторный пакет есть→есть лестницу не дёргает»). TDLib по-прежнему зовётся
только на переход. Раздел 4e `reconnect-plan.md` поправлен (прежний запрет «не слать на каждую проверку» снят с объяснением).

**Бюджет интерфейса**: правки добавили ~26 строк в `src/` → вынесен чистый `autoReplyStats.js` (101) в `shared/` (его тест — в
`src/__tests__/`); бюджет **31 179 / 31 200** (планка опущена 31270 → 31200, ADR-044).

**КАК ПРОВЕРЕНО**
- [netPulseUi.vitest.js](../src/__tests__/netPulseUi.vitest.js) — 9: строка про интернет (секунды/минуты/не проверяли); адреса из настроек
  (правильный список берётся; **ловушки формата**: не список / пусто / >6 / не https / мусор в списке → по умолчанию + причина); сводка
  (окно 30 с, опоздавший исход не считается, «попыток не было»); обвязка: адреса из настроек реально используются, плохие → WARN;
  **пакет окну на каждую проверку, TDLib — только на переход**; ловушки подключения (экран, панель, событие, `onOutcome`, переезд файла).
- [reconnectPulseChain.vitest.jsx](../src/__tests__/reconnectPulseChain.vitest.jsx) — 3: интернет появился → попытка **в ту же секунду**
  (а до того будильник лестницы при мёртвом пульсе страницу не трогал) + сводка «перезагружены 1» через 30 с; окно сообщает главному
  процессу число ожидающих и просит проверку по кнопке; повторный пакет «есть→есть» лестницу не дёргает.
- Старые: `netPulsePlan` 13/13, `reconnectAttempt`, `openPageWatch`, `useWebviewReconnect.vitest.jsx`, `autoReplyStats` (переехавший) — зелёные.
- Стражи `.cjs`, линт, лимиты 693/693, полный vitest, `npm run build` — в отчёте сессии.

**ЧТО ПРОВЕРИТЬ ВРУЧНУЮ** (ПОЛНЫЙ перезапуск): панель «Подключения» → сверху кружок «Интернет» и строка «есть · проверено N с назад»
(N растёт до ~60 и сбрасывается); выдернуть кабель → ≤15 с кружок красный, строка «нет»; экран «Нет связи» внизу показывает ту же
строку; вернуть кабель → через 30 с в журнале `итог возврата сети`. Опционально: в `chatcenter.json` добавить
`"netPulseTargets": ["https://ya.ru/"]` → строка старта `[net-pulse] запущен: адресов=1 (из настроек netPulseTargets)`.

**ОСТАТОЧНЫЕ РИСКИ**: (а) 🟡 живьём не запускалось; (б) 🟢 сводка считает только исходы попыток механизма — страницы, которые ожили сами
БЕЗ участия механизма (проба и не проваливалась), в неё не попадают (это и есть «попыток не было»); (в) 🟢 адреса из настроек — без
интерфейса, правка файла руками.

### v1.2.491 — «Пульс интернета» + присмотр за открытыми страницами: обрыв сети замечается в любой момент и чаты возвращаются сами

**ОТ ПОЛЬЗОВАТЕЛЯ**: «механизм, который проверяет появление интернета, должен работать как часы: понимать пропажу в любой момент,
а не только при загрузке, проверять сам и возвращать чаты, когда интернет появился».

#### Аудит (журнал двух настоящих обрывов, 21–22.09.2026) — что было не так

| № | Слабое место | Доказательство |
|---|---|---|
| 1 | Механизм видел только **сорвавшуюся загрузку** (`did-fail-load`); у ОТКРЫТОЙ страницы пропажа интернета не замечалась вовсе | документация Electron: события загрузки — про навигацию; 22.09 у открытых Telegram Web/WhatsApp — 0 строк `[reconnect]` |
| 2 | Сигнал «сеть вернулась» (`online/offline`) **не сработал ни разу** → после возврата интернета ждём до 60 с или кнопку | 0 строк `[net]` за весь журнал; Electron: «true is inconclusive» |
| 3 | Родному Telegram никто не сообщал о смене сети — «связь есть» через 2 мин после веб-страниц (10:13 → 10:15:10) | TDLib: `setNetworkType` «must be called whenever the network is changed» — вызовов 0 |
| 4 | «Интернета нет» и «этот сайт недоступен» лечились одинаково: WhatsApp и веб-Telegram 3,5 ч перезагружались раз в минуту при живом интернете (160+ и 110+ попыток, 431 строка) | журнал 21.09 15:38–19:12 |
| 5 | Пробы здоровья (fetch внутри страницы, раз в 30–300 с) ловили обрыв у открытой страницы, но только красили кружок и **ничего не писали** | `webviewHealthProbe.js`: 0 записей в журнал |

#### Что сделано (5 частей)

**1. «Пульс интернета»** — [netPulseHandlers.js](../main/handlers/netPulseHandlers.js) (обвязка Electron) +
[shared/netPulsePlan.js](../shared/netPulsePlan.js) (чистая логика). Главный процесс сам делает крошечный запрос
(`net.fetch` — по документации Electron идёт через сетевой стек Chrome) к трём служебным адресам проверки связи
(`msftconnecttest` — тем же адресом Windows проверяет интернет; `gstatic/generate_204`; `yandex.ru/favicon.ico`): любой
HTTP-ответ = интернет есть. Раз в 60 с в покое, раз в 15 с когда интернета нет ИЛИ окно сообщило, что мессенджеры ждут
повтора; немедленно при пробуждении компьютера / разблокировке экрана (`powerMonitor`) и по просьбе окна (кнопка
«Повторить сейчас»). В журнал — только **переходы**: `[net-pulse] интернет ПРОПАЛ …` / `ПОЯВИЛСЯ через N с (ответил host)`;
пока нет — напоминание раз в 5 мин. Каналы: main→окно `net:pulse`; окно→main `net:pulse-state` (invoke),
`net:pulse-now`, `net:pulse-waiting`. Выключатель: `settings.netPulse === false` (без интерфейса — аварийный рычаг).

**2. Пульс командует.** В окне ([useOpenPageWatch.js](../src/hooks/useOpenPageWatch.js) → `applyPulse`) вердикт кладётся в
`window.__ccNetOnline` и превращается в СТАНДАРТНОЕ событие окна `online`/`offline` — существующие слушатели
(`useWebviewReconnect`: все ожидающие пробуют СРАЗУ; `NativeConnectionStrip`: полоса «нет связи») оживают без переделки.
Главный процесс на «появился» зовёт TDLib `setNetworkType(networkTypeOther)` для всех аккаунтов
([tdlibBackendHelpers.js](../main/native/backends/tdlibBackendHelpers.js) `networkChangedRaw`; `networkTypeNone` не шлём никогда —
иначе сами отключили бы TDLib по ложному пульсу).

**3. Открытая страница под присмотром** — [shared/openPageWatch.js](../shared/openPageWatch.js) (правило) + хук выше. Исход
КАЖДОЙ пробы здоровья идёт в `onProbeOutcome`: 1-й провал → наблюдаем (мессенджеры переподключаются сами); 2-й подряд при живом
интернете → «страница зависла» → механизму повторов (перезагрузка по лестнице 5 с…); 2-й при мёртвом пульсе → экран «Нет
интернета», **страницу не трогаем**. Сама попытка вынесена в [shared/reconnectAttempt.js](../shared/reconnectAttempt.js): при
мёртвом пульсе `loadURL` НЕ зовётся (перезагрузка стирает недописанное); запись от пробы перед перезагрузкой **спрашивает
страницу** (`quickProbe`, тот же скрипт, что у панели связи) — ожила сама → экран снимается **без перезагрузки**.

**4. Различаем беды.** Экран «Нет связи» ([reconnectTexts.js](../shared/reconnectTexts.js) `reasonTitle`): «Нет интернета» /
«Сайт X недоступен — интернет есть, не отвечает сам сайт» / «X: страница не отвечает». После **10** неудач при ПОДТВЕРЖДЁННОМ
пульсом интернете пауза — 5 минут (`LONG_FAIL_ATTEMPTS`/`LONG_FAIL_PAUSE_MS`); эхо страницы-ошибки в журнал с 3-й попытки —
каждую 10-ю (`shouldLogEcho`). Сторож Ozon при мёртвом пульсе не перезагружает фоновые страницы.

**5. Проверяемость и разгрузка.** `useWebviewReconnect.js` 149 → **132/170** (attempt() вынесен); `reconnectPlan.js` упёрся в 300 →
тексты в `reconnectTexts.js` (247/300 + 75); `tdlibBackend.js` 929/930 (networkChanged одной строкой, тело в helpers);
`connectionHealthScheduler.js` (чистый планировщик) → `shared/` (бюджет интерфейса 31 289 → **31 254**, планка 31300 → **31270**).

**Два способа**: (А) опрос интернета из окна приложения — проще, но без пробуждения компьютера и без команды TDLib;
(Б) пульс в главном процессе + событие окну — одна точка правды для трёх потребителей. Выбрано **Б**.

**КАК ПРОВЕРЕНО**
- [netPulsePlan.vitest.js](../src/__tests__/netPulsePlan.vitest.js) — 13: частота, антидребезг 3 с, переходы и строки, напоминание
  раз в 5 мин, обвязка на подделках Electron (первый адрес упал → ответил второй; пропал → offline окну, TDLib не трогаем; вернулся →
  online + TDLib; «ждут 2» → проверка сразу и частота 15 с; пробуждение → проверка; выключатель), ловушки подключения в `main.js`/бэкенд.
- [reconnectAttempt.vitest.js](../src/__tests__/reconnectAttempt.vitest.js) — 13: интернета нет → `loadURL` не зовём; фаза «идёт» в зеркале
  синхронно (ловушка v1.2.453 сохранена); ожила сама → без перезагрузки; долгая неудача только при `netOnline === true`; причины экрана;
  страж «useRef в хуке = 2».
- [openPageWatch.vitest.js](../src/__tests__/openPageWatch.vitest.js) — 7: правило по исходу пробы, «уже под присмотром — не дублируем»,
  пульс → события окна ровно по одному на переход, проводка в App/Ozon, планировщик переехал.
- Старые: `reconnectPlan` 44/44 (правило «без пульса — лестница 60 с» сохранено), `useWebviewReconnect.vitest.jsx` (заголовок экрана
  обновлён), `bootNet`, `connectionHealthScheduler` 8/8. **Прогон наоборот** (3 поломки) → падения.
- Стражи `.cjs` (ipcChannels, mainProcess, memoryLeaks, hookOrder, …), линт, лимиты 690/690, полный vitest, `npm run build` — итоги в отчёте сессии.

**ЧТО ПРОВЕРИТЬ ВРУЧНУЮ** (ПОЛНЫЙ перезапуск): (а) при старте в журнале `[net-pulse] запущен` и `интернет есть (ответил …)`;
(б) выдернуть кабель на 2 минуты при открытых чатах → через ≤15 с `[net-pulse] интернет ПРОПАЛ`, у «Общего чата» полоса «Нет связи с
интернетом», у веб-страниц после двух проб — экран «Нет интернета» БЕЗ перезагрузок; (в) вернуть кабель → `ПОЯВИЛСЯ через N с`, в ту же
секунду `[reconnect] … ожила сама` или `связь восстановлена`, `[tg-conn] … связь есть` в пределах ~10 с, кнопку нажимать не надо;
(г) закрыть один сайт через файл hosts → экран «Сайт X недоступен — интернет есть», после 10 попыток пауза 5 мин.

**ОСТАТОЧНЫЕ РИСКИ**: (а) 🟡 живьём не запускалось — правило проекта; (б) 🟡 поведение самих сайтов МАКС/ВК/Ozon при обрыве у открытой
страницы не проверено — поэтому перезагрузка только после двух провалов и «спроси страницу» перед ней; (в) 🟡 адреса пульса могут быть
недоступны в отдельной сети (блокировки) — их три, любой ответивший достаточен; если все три закрыты, а интернет есть — пульс скажет
«нет», лестница повторов веб-страниц при этом работает как раньше (только без ускорения), TDLib не трогается; (г) 🟢 старый механизм не
удалён — пульс дополняет его.

### v1.2.490 — Кнопка «✕ Закрыть все (N)» над стопкой уведомлений + разгрузка features.md + правило для notification.js

**ОТ ПОЛЬЗОВАТЕЛЯ**: «Найдено, НЕ трогал — делай» (три пункта из отчёта v1.2.489).

#### 1. Кнопка «Закрыть все» — новый независимый модуль [notificationCloseAll.js](../main/notificationCloseAll.js)

Зачем: при «не закрывать сами» карточки копятся, закрывать можно было только по одной (смежный пункт дела
[notif-window-input-loss-case.md](./notif-window-input-loss-case.md) — теперь закрыт).

Как устроено (по образцу `notificationNewPill.js`, `notification.js` НЕ тронут — он 719/730):
- кнопка живёт в `body`, **не в `#container`** — дети контейнера считаются карточками (линейка, снимок DOM, сверка
  `items↔containerChildren`), чужой элемент там сломал бы инварианты;
- при **≥2** карточках контейнер получает класс `has-toolbar` (`padding-top: 32px` = 4 обычных + 28 под полосу), кнопка —
  `position:fixed` в освободившейся полосе, чтобы **не накрывать крестик первой карточки**;
- линейка высоты `calcHeight` ([notification-helpers.js](../main/notification-helpers.js), `CLOSE_ALL_BAR_PX = 28`) добавляет те же
  28 — иначе окно было бы ниже содержимого на полосу; **без карточек полосу не считает** — «пусто» остаётся нулём (иначе
  пустое окно не пряталось бы: `computeRendererPure` смотрит на `visibleHeight===0`);
- нажатие = **нажать крестик каждой карточки** (`.close-btn.click()`): тот же путь, что у человека, со всеми записями в
  журнал (`close-click`, `dismiss start …`) и той же анимацией — ничего нового про закрытие не изобретаем;
- пересчёт в самом `MutationObserver` (микрозадача), **без `requestAnimationFrame`**: отчёт высоты уходит через 60 мс после
  добавления карточки, класс должен стоять раньше. Журнал: `close-all: ready / shown count=N / hidden / click: закрываю N…`.

**Два способа**: (А) кнопка внутри `notification.js` с прямым вызовом `dismissItem` — файл на пределе (719/730) и ломаем
инварианты счёта детей; (Б) независимый модуль, закрывающий через крестики — ноль правок в хрупком файле, тот же путь
закрытия. Выбрано **Б**.

#### 2. `features.md` 89 КБ → **записи v1.2.483-485 в [archive/features-v1.2.483-485.md](./archive/features-v1.2.483-485.md)** (+ `archive/README.md`).

#### 3. Правило для `notification.js` (719/730) — записано в [code-limits-status.md](./code-limits-status.md): следующие правки окна
уведомлений класть в модули-помощники (`notification-limits.js`, `notificationCloseAll.js`, `notification-helpers.js`),
а не в основной файл.

**КАК ПРОВЕРЕНО**
- Новый [notifCloseAll.vitest.js](../src/__tests__/notifCloseAll.vitest.js) — **9 проверок на живом DOM**: одна карточка — кнопки и
  полосы нет; две — кнопка с числом + полоса + лог `shown`; карточки ушли — исчезает, лог только на переходе; чистое правило
  «с двух»; **нажатие зовёт крестик КАЖДОЙ карточки** (3 шпиона × 1 вызов); `calcHeight` +28 при полосе и **0 без карточек**;
  **ловушка согласованности** 28 в линейке ↔ `padding-top: 32px` в стиле; подключение после `notification.js` + строка копирования
  в сборку; `notification.js` про кнопку не знает.
- **Прогон наоборот**: убрал `+28` из `calcHeight` и класс из `render` → падения.
- Стражи окна (`notificationWindowBounds`, `transparentWindowGuard`, `memoryLeaks`, `mainProcess`, `buildContract`); линт; лимиты;
  **`npm run build`** — файл в `out/main/`; полный vitest — ниже.

**ЧТО ПРОВЕРИТЬ ВРУЧНУЮ** (ПОЛНЫЙ перезапуск): два уведомления → над стопкой кнопка «✕ Закрыть все (2)», крестик первой карточки
не перекрыт; нажатие — все карточки уезжают, окно прячется; одно уведомление — кнопки нет. Пузырь «↓ N новых» (центр
сверху) и кнопка (справа сверху) не мешают друг другу.

**ОСТАТОЧНЫЕ РИСКИ**: (а) 🟡 живьём не запускал — вид кнопки и полосы оценить только глазами; (б) 🟡 пока карточки уезжают
(0,4 с), они ещё в DOM → кнопка держится до конца анимации, затем исчезает вместе с окном — ожидаемо; (в) 🟢 `notification.js`
не тронут — регрессия там исключена.

### v1.2.489 — План исправления из ревью v1.2.488 выполнен: свой файл предела + закрывающиеся карточки не считаем + постоянное место под новую

**ОТ ПОЛЬЗОВАТЕЛЯ**: «план исправления — делай полностью». Порядок из плана соблюдён: сначала #2 (вынос), потом #1 и #3
(правки уже в новом файле); #4 по плану не трогали (падение при незагруженном помощнике должно быть ГРОМКИМ —
с v1.2.486 оно пишется в журнал строкой `ОШИБКА В ОКНЕ УВЕДОМЛЕНИЙ`).

| # | Было | Стало | Где |
|---|---|---|---|
| 2 | `notification-helpers.js` 291/300 — следующая правка окна не влезала | блок предела (≈55 строк) — свой файл [notification-limits.js](../main/notification-limits.js) (86 строк); helpers **236/300** | `notification.html` (подключение ДО `notification.js`), `electron.vite.config.js` (копирование в сборку — без этого собранная программа окно бы не собрала) |
| 1 | закрывающаяся карточка (0,4 с анимации, `pointerEvents='none'`) считалась в высоту → снимали одну лишнюю | `cardHeights` пропускает такие, как `calcHeight` | `notification-limits.js` `cardHeights` |
| 3 | место под новую = высота самой свежей карточки → после высокого альбома короткая карточка снимала лишнюю | **постоянные 160 точек** (`NEW_CARD_RESERVE_PX`) | `notification-limits.js` `decidePersistentTrim` |

**Почему #3 — постоянная, а не медиана (план предлагал медиану)**: посчитал числами — медиана НЕ чинит пример из ревью:
`[200, 200, 400]` при бюджете 796 → медиана 200 → сумма 812 + резерв 204 = 1016 → снять 200 → 812 > 796 → снять ещё →
итого 2 (как и было). Постоянные 160: 812 + 164 = 976 → снять одну → 608 + 164 = 772 ≤ 796 → **1**. Цена постоянной: под
высокую новую карточку места «не хватит» → стопка разово вылезет на разницу, main зажмёт окно до экрана, следующее
уведомление снимет лишнее. Недооценка самоисправляется; переоценка теряет карточку навсегда — поэтому недооценка.

**КАК ПРОВЕРЕНО**
- [notifPersistentTrim.vitest.js](../src/__tests__/notifPersistentTrim.vitest.js) — **15 проверок** (было 11): + **ловушка #1**
  «закрывающаяся карточка не считается» (контейнер с 3 живыми по 250 и одной закрывающейся → снять 1, не 2 — ровно
  сценарий из ревью); + **ловушка #3** `[200,200,400]` → 1; смешанные высоты 19.09 теперь → снять 3 (остаются 133+170+261
  + новая = 750 ≤ 796); + **ловушки подключения**: порядок `<script>` в `notification.html` (limits ДО notification.js),
  строка копирования в `electron.vite.config.js`, функции предела больше НЕ в helpers (нет дубля), комментарий в
  `notification.js` указывает на новый файл.
- **Прогон наоборот**: вернул счёт закрывающихся + медианный резерв → 3 падения.
- Стражи `notificationWindowBounds` OK, `transparentWindowGuard`, `memoryLeaks`, `mainProcess`, `buildContract`; линт 0;
  лимиты; полный vitest — ниже. **`npm run build`** — файл попал в `out/main/` (проверка копирования фактом).

**ЧТО ПРОВЕРИТЬ ВРУЧНУЮ** (ПОЛНЫЙ перезапуск): уведомления показываются как раньше (три файла-помощника + основной
загрузились); при «не закрывать сами» стопка не выше экрана; в журнале `trim-by-height`.

**ОСТАТОЧНЫЕ РИСКИ**: (а) 🟡 живьём не запускал; если файл не загрузится — строка `ОШИБКА В ОКНЕ УВЕДОМЛЕНИЙ … shouldTrimOldest`
в журнале (это и есть #4 из плана: падать громко); (б) 🟢 под высокую новую карточку стопка может разово вылезти на
≤ 260 точек до следующего уведомления — осознанно (см. выше).

### v1.2.488-v1.2.486 — Предел «вечных» карточек по реальной высоте · дело о мёртвых кнопках окна уведомлений + наблюдатель мыши · ширина списка чатов слушается окна (архив → [archive/features-v1.2.486-488.md](archive/features-v1.2.486-488.md); дело — [notif-window-input-loss-case.md](notif-window-input-loss-case.md))

### v1.2.485-v1.2.483 — Разгрузка бюджета интерфейса (−3082 строки, 11 модулей в `shared/`) · большой снимок страницы ВК ожил и стал безопасным для журнала · разовый осмотр строки списка ВК (архив → [archive/features-v1.2.483-485.md](archive/features-v1.2.483-485.md))

### v1.2.482-v1.2.480 — ВК: одно сообщение давало несколько карточек (память «уже видели» копится) · соседние перехватчики проверены + разгрузка монитора · отчёт о впрыске и сборка проверены фактом (архив → [archive/features-v1.2.480-482.md](archive/features-v1.2.480-482.md))

### v1.2.479-v1.2.477 — ВК: убрана пометка вложения из карточки · фото вложения веб-мессенджера · разгрузка бюджета интерфейса и разбиение помощника карточки (архив → [archive/features-v1.2.477-479.md](archive/features-v1.2.477-479.md))

### v1.2.476-v1.2.474 — Двойные карточки МАКСа (склейка пары + единый срок ожидания) и временная диагностика строки списка ВК (архив → [archive/features-v1.2.474-476.md](archive/features-v1.2.474-476.md))

### v1.2.473-v1.2.470 — Молчание журнала при красном экране + окно повтора переживает заставку + честные доказательства «страница жива» + Ozon не тревожит при коротком обрыве (архив → [archive/features-v1.2.470-473.md](archive/features-v1.2.470-473.md))

### v1.2.469-v1.2.466 — Прошлый журнал в окне логов + уборка «найдено, не трогал» + карточка Ozon перестала повторяться + доводка по проверке (архив → [archive/features-v1.2.466-469.md](archive/features-v1.2.466-469.md))

### v1.2.465-v1.2.463 — Честная подпись вместо «[медиа]» + прогрев файлов; окно «Повторить» вместо красного экрана; МАКС перестал уведомлять о своих сообщениях (архив → [archive/features-v1.2.463-465.md](archive/features-v1.2.463-465.md))

### v1.2.462-v1.2.460 — МАКС: задвоенное имя ломало защиту от повторов + пустое сообщение Telegram попало в журнал + «Компания» в свойствах файла (архив → [archive/features-v1.2.460-462.md](archive/features-v1.2.460-462.md))

### v1.2.459-v1.2.457 — Дёргание при переключении мессенджеров + ширина списка чатов от размера окна + значок приложения наконец вшивается в `.exe` (архив → [archive/features-v1.2.457-459.md](archive/features-v1.2.457-459.md))

### v1.2.454-v1.2.456 — РАСКЛАДКА ГЛАВНОГО ОКНА: панели перестали уезжать за край (разрешение сжиматься), панель ИИ научилась считаться с размером окна, потолок вынут из анимируемого свойства. Действующие правила — [mistakes/app-layout.md](mistakes/app-layout.md) и ADR-054/055 (архив → [archive/features-v1.2.454-456.md](archive/features-v1.2.454-456.md))

### v1.2.452-v1.2.453 — ОБРЫВ СВЯЗИ ПРИ ЗАПУСКЕ: окно переподключения самого приложения (живёт в `index.html`, ничего не подключает) + доводка по ревью (экран «Нет связи» держался только до ПЕРВОЙ попытки; мигание сети перезагружало работающее приложение; старт без интернета крутил перезагрузки). **Рабочее описание темы — в [reconnect-plan.md](reconnect-plan.md), раздел 4b** (архив → [archive/features-v1.2.452-453.md](archive/features-v1.2.452-453.md))

### v1.2.447-v1.2.451 — ЗНАК ПРИЛОЖЕНИЯ (прозрачный фон, размер до предела, логотип в шапке) + УБОРКА ПО СПИСКУ «нашёл, не трогал» (общий код вынесен из бюджета интерфейса, страж «модуль подключён?», разгрузка `webviewSetup.js`, полоса «нет связи» для «Общего чата») + ФИКС поломки счётчиков вкладок + ЕДИНЫЙ список проверок вместо двух копий (архив → [archive/features-v1.2.447-451.md](archive/features-v1.2.447-451.md))

### v1.2.445-v1.2.446 — ПЕРЕПОДКЛЮЧЕНИЕ мессенджеров после обрыва связи (первая реализация: растущая пауза, экран «Нет связи», непрозрачный фон закрыл «вижу Telegram вместо WhatsApp») + ВОССТАНОВЛЕНА потерянная защита от чёрного экрана веб-МАКС и добавлен страж проводки. Продолжение — v1.2.452-453 выше (архив → [archive/features-v1.2.445-446.md](archive/features-v1.2.445-446.md))

### v1.2.443-v1.2.444 — ЗНАК ПРИЛОЖЕНИЯ: своя иконка (трей + окно + установщик) + знак на экранах загрузки; доводка по ревью — свой файл `.ico` (иначе в панели задач серое пятно), сломанный тест-страж, защита запуска и записи в журнал (архив → [archive/features-v1.2.443-444.md](archive/features-v1.2.443-444.md))

### v1.2.441-v1.2.442 — Аватарки: доводка по ревью (кнопка «Обновить фото» видна на значке, неудача не теряется, ключ записи журнала без цифры) + РАЗГРУЗКА бюджета интерфейса (данные «Что нового» и код проверок вынесены, App.jsx 1074→946, планка опущена 36090→35300) (архив → [archive/features-v1.2.441-442.md](archive/features-v1.2.441-442.md))

### v1.2.439-v1.2.440 — АВАТАРКИ веб-аккаунтов: вечные метки в памяти страницы (чужое фото у Telegram, нет фото у МАКС) + доводка (снимок для любого мессенджера, причина отказа в журнал, пункт меню «Обновить фото») (архив → [archive/features-v1.2.439-440.md](archive/features-v1.2.439-440.md))

### v1.2.438 — ОЗОН, третий заход: память «уже показывали» перенесена ИЗ чужой страницы В наше приложение (запись в localStorage Ozon не доходила) (архив → [archive/features-v1.2.438.md](archive/features-v1.2.438.md))

### v1.2.437 — ФИКС «Ozon: один и тот же вопрос уведомляет каждую минуту»: отпечаток считался от позиционно взятого товара + память ЗАМЕНЯЛАСЬ вместо дополнения; плюс запись о смене вкладки (архив → [archive/features-v1.2.437.md](archive/features-v1.2.437.md))

### v1.2.436 — ФИКС «выкидывает в ЦентрЧатов»: загрузка списка мессенджеров отбирала активную вкладку на КАЖДОМ монтировании (в разработке — на каждую горячую перезагрузку от правки файла) (архив → [archive/features-v1.2.436.md](archive/features-v1.2.436.md))

### v1.2.435 — Доводка фикса чёрного экрана веб-МАКС по самопроверке: журнал отказов по КАТЕГОРИЯМ (без цифр в ключе), сбой впрыска больше не молчит, сторож реже, страж ссылок перестал быть слепым (архив → [archive/features-v1.2.435.md](archive/features-v1.2.435.md))

### v1.2.434 — Чёрный экран веб-МАКС: НАСТОЯЩАЯ причина — НАШ ЖЕ впрыск прятал приложение (`display:none` на корне) → предохранители по тексту/высоте/площади/детям + отчёт в журнал (архив → [archive/features-v1.2.434.md](archive/features-v1.2.434.md))

### v1.2.433-v1.2.424 — МАКС: фантомы/дубль уведомлений, тормоз наблюдателя, анти-завал (лавина из одного чата), фикс MaxListeners + сага сторожа «пустого экрана» (3 итерации, теория перерисовки ОПРОВЕРГНУТА в v1.2.434) (архив → [archive/features-v1.2.424-433.md](archive/features-v1.2.424-433.md))
### v1.2.423-v1.2.416 — Сага «чёрный экран веб-МАКС»: 5 попыток (1px-пинок → SW/429-шторм → реальный UA → диагностика сокета → sandbox=no) + доводки (архив → [archive/features-v1.2.416-423.md](archive/features-v1.2.416-423.md)). НАСТОЯЩАЯ причина найдена только в v1.2.434 — см. запись выше.
### v1.2.415-v1.2.406 — Единый экран загрузки чатов + ВК-значок рейла (console __CC_UNREAD__) + Ozon-виджет/«Отзывы» + ФИКС/диагностики чёрного экрана МАКС (архив → archive/features-v1.2.406-415.md)
### v1.2.405-v1.2.401 — Заставка загрузки чатов: один экран, видимая анимация (счётчик/аватарки), на всё окно, бесшовный старт (архив → archive/features-v1.2.401-405.md)
### v1.2.400-v1.2.396 — Старт/заставка загрузки: живые кружки, старт на «ЦентрЧатов», мин-время видимости, холодный старт; Ozon «Вопросы» reload 3мин→1мин (архив → archive/features-v1.2.396-400.md)
### v1.2.395-v1.2.393 — Заставка первой загрузки чатов: старт саги (7 улучшений) + мин-время видимости + холодный старт + разбор ВК (архив → archive/features-v1.2.393-395.md)

### v1.2.392-v1.2.389 — Ozon «Отзывы»: rv-счётчик (Вариант C), откат ведения счётчика, убрана авто-перезагрузка, фильтр статуса «Получено…» (архив → archive/features-v1.2.389-392.md)


### v1.2.388-v1.2.381 — Ozon счётчики разделов (msg/qa/rv корни+откаты, парсер rv→msg, фильтр статуса «Отзывов», Шаг B/C/D) + ВК аватарки/анти-дубль/диагностика «Игры» (архив → archive/features-v1.2.381-388.md)

### v1.2.380-v1.2.373 — Ozon (значок рейла/авто-освежение «Вопросов»/логотип по типу/«Печатает…»/разведка «Отзывов» Шаг A) + ВК аватарки+анти-дубль + фикс авто-ответа (архив → archive/features-v1.2.373-380.md)

### v1.2.372-v1.2.367 — Ozon фон (пульс/память показанного/впрыск после перезагрузки/оба раздела) + окно «на весь экран» при клике по уведомлению/задаче (архив → archive/features-v1.2.367-372.md)

### v1.2.366-v1.2.361 — Ozon Шаг 3А: диагностика фоновой «Вопросы» + подтверждение работы (впрыск сторожа через executeJavaScript) + ERR_ABORTED→WARN (архив → archive/features-v1.2.361-366.md)

### v1.2.360-v1.2.349 — Счётчики виджета Ozon (msg/qa) + сторож «Вопросов» Шаг 2 (Ответы=0=новый) + фон Шаг 3a/3b + фиксы по ревью + окно ready-to-show (архив → archive/features-v1.2.349-360.md)

### v1.2.348-v1.2.329 — Виджет Ozon (ранний: док/перетаскивание/место) + окно «на весь экран» + ночная стена уведомлений + сторож «Покупатели» + логотип Ozon + заставка (архив → archive/features-v1.2.329-348.md)

### v1.2.328-v1.2.313 — Ozon (строгий режим сессии/антибот, каталог, разведчик структуры) + логотипы ВК/МАКС/WhatsApp + фиксы логов/дедупа уведомлений (архив → archive/features-v1.2.313-328.md)

### v1.2.312 — старт: не заваливать всплывашками offline-бэклог (архив → archive/features-v1.2.303-312.md)

### v1.2.311-v1.2.305 — Сага «чёткий аватар веб-Telegram»: диагностики → фикс → ПОДТВЕРЖДЕНО (архив → archive/features-v1.2.305-311.md)

### v1.2.303-304 — веб-значки рейла: чистые бейджи + свечение + поднятие при перетаскивании (архив → archive/features-v1.2.303-312.md)

### v1.2.302-v1.2.293 — верхняя панель (линейные значки, единая строка), 3-зонный рейл, фикс фото из уведомления, стекло-кнопка, чистка dist (архив → archive/features-v1.2.293-302.md)

### v1.2.288-v1.2.268 — Веб-аватарки (диагностика + чёткое фото из Настроек Telegram), скрытие верхних вкладок, карточки аккаунта/контакта, чистка журнала (архив → archive/features-v1.2.268-288.md)

### v1.2.267-v1.2.256 — Окно «Добавить» (протокол→мессенджер) + портал полосы «← Общий чат» (архив)

Перенесено в [archive/features-v1.2.256-267.md](archive/features-v1.2.256-267.md) (2026-08-19): единая полоса (веб встроен в нативный рейл, SourceRail удалён, ADR-032); одна кнопка «＋ Добавить» → окно «протокол→мессенджер» + доводки; портал полосы на уровне App (v1.2.260 откат→v1.2.262 рабочий) + кнопка «← Общий чат» над вебом.

### v1.2.254-v1.2.244 — боковой рейл SourceRail (Этапы 1–3, ЗАКРЫТ разворотом v1.2.256, архив)
Подход отдельной панели SourceRail закрыт (Модель 🅰️, ADR-032). История: [archive/features-v1.2.244-255.md](archive/features-v1.2.244-255.md).

### v1.2.243-v1.2.237 — Боковой рейл (флаг/тумблер) + карточка контакта (читаемость/контур/заметка) + МАКС мьют-фикс/диагностика (архив)

Перенесено в [archive/features-v1.2.237-243.md](archive/features-v1.2.237-243.md) (2026-09-01, разгрузка под лимит 100 КБ).

### v1.2.236-v1.2.222 — галочки списка + карточка контакта + МАКС-диагностика + строка отправки + пузырь «↓N» (архив)
Перенесено в [archive/features-v1.2.222-236.md](archive/features-v1.2.222-236.md).

### v1.2.221-v1.2.214 — Порядок фото/«текст отдельно» + ВК-навигация vk.ru + фикс счётчика ВК + окно отправки (архив)

Перенесено в [archive/features-v1.2.214-221.md](archive/features-v1.2.214-221.md) (2026-08-13, разгрузка под лимит 100 КБ): ждём серверное подтверждение фото/альбома перед текстом — порядок у получателя (v1.2.214) + таймаут-WARN (v1.2.215); «перейти в чат» по клику ВК-уведомления под vk.ru + имя мессенджера в статусе (v1.2.216) + тест-репродюсер (v1.2.217) + доводка сравнения имени (v1.2.218); фантомная «1 непрочитано» ВК — счётчик перестал считать бейджи «Друзья/Игры» (v1.2.219); окно отправки фото/файла — одна кнопка «Отправить» + инфо-строка (v1.2.220); лента уведомлений — не дёргать список при чтении + видимая прокрутка (v1.2.221).

### v1.2.213-v1.2.205 — Окно отправки фото: подпись/лимит 1024/«текст отдельно»/порядок/диагностика (архив)

Перенесено в [archive/features-v1.2.205-213.md](archive/features-v1.2.205-213.md) (2026-08-11, разгрузка под лимит 100 КБ): фикс двойной вставки фото + разбит windowManager (v1.2.205) + подтверждающий лог (v1.2.206); окно фото — 7 функций просмотра (v1.2.207); счётчик подписи + «фото, затем текст отдельно», лимит Telegram 1024 (v1.2.209); лимит подписи и «текст отдельно» для видео/документов (v1.2.210); длинный текст режется ≤4096 и шлётся ПОСЛЕ загрузки фото — порядок (v1.2.211); доводки «ждём именно это фото» + число сообщений (v1.2.212); диагностика отправки — лог провала/номера/результат (v1.2.213).

### v1.2.204-v1.2.197 — Окно отправки фото: инструменты просмотра (зум/поворот/сдвиг) + много фото + память окна (архив)

Перенесено в [archive/features-v1.2.197-204.md](archive/features-v1.2.197-204.md) (2026-08-11, разгрузка под лимит 100 КБ): окно отправки фото с зумом/поворотом/сдвигом (v1.2.197-198), логи потока + прозрачность PNG + пропуск в альбоме (v1.2.199), запоминание «на весь экран» (v1.2.200), приподнятая карточка + растущее поле подписи (v1.2.201), чистая логика памяти окна + тесты (v1.2.202), несколько фото в одном сообщении (v1.2.203), кнопки в шапке + вставка Ctrl+V + свободнее зум (v1.2.204).

### v1.2.196–v1.2.185 — диагностика ВК/МАКС, установщик/Win32, прокрутка чата (архив)

Перенесено в [archive/features-v1.2.185-196.md](archive/features-v1.2.185-196.md).

### v1.2.184-v1.2.170 — Логотип Telegram/живой статус собеседника/переключатель режимов/фикс поиска/читаемость шапки (архив)

Перенесено в [archive/features-v1.2.170-184.md](archive/features-v1.2.170-184.md).

### v1.2.169-v1.2.161 — Фильтр аккаунтов (мультивыбор+соло) + самолечение + закрепы/ресайз рейла (архив)

Перенесено в [archive/features-v1.2.161-169.md](./archive/features-v1.2.161-169.md) (2026-07-31, разгрузка под лимит 100 КБ): откат «приклеенного» списка закреплённых (161/162); фильтр аккаунтов на левой панели, клик=вкл/выкл, двойной=соло, кнопка «Все» + доводки (163/164); ресайз левого рейла перетаскиванием (165); полоса «Закреплённое» — читаемый фон + переход к сообщению (166/167); фиксы «после удаления 2-го аккаунта оставшийся не грузился» + самолечение фильтра при старте (168/169).

### v1.2.160-v1.2.156 — Сага «перетаскивание закреплённых» (ОТКАЧЕНА в v1.2.161, архив)

Перенесено в [archive/features-v1.2.156-160.md](./archive/features-v1.2.156-160.md) (2026-07-30): серия попыток сделать ручное перетаскивание порядка закреплённых чатов (HTML5 drag → свой образ за курсором → живая перестановка → вынос в отдельный не-виртуальный `PinnedChatList` с Pointer Events). **Итог — откачено в v1.2.161**: закреплённые должны листаться в ОБЩЕМ списке, а не быть отдельным блоком; перетаскивание же требует отдельного списка. Урок: drag-порядок и виртуальный список react-window (строки по индексу-слоту) несовместимы без замены движка. Из серии сохранились только НЕ-drag доводки (цвет-грань уведомления по `accountId`, цвет-полоса в узком режиме).

### v1.2.155-v1.2.147 — Цвет-метки аккаунтов (различимые цвета/полоса/обводка) + фиксы входа/удаления 2-го аккаунта (архив)

Перенесено в [archive/features-v1.2.147-155.md](./archive/features-v1.2.147-155.md) (2026-07-31, разгрузка под лимит 100 КБ): цвет аккаунта в закреплённых/уведомлении (v1.2.155); устойчивые цвет-метки — первый свободный + сохранение вместо хеша (v1.2.154); полоса/обводка/выбор цвета 🎨 (v1.2.153); спиннер ввода кода Telegram (v1.2.152); карточка выхода не заезжает под панель задач + спиннер «думания» (v1.2.150-151); сброс «залипшего success» гейта входа + разгрузка NativeApp (v1.2.148-149); фикс «чёрный экран» после добавления 2-го аккаунта (v1.2.147).

### v1.2.146-v1.2.140 — Добавление/удаление аккаунта (диагностика + фиксы следов) + закреп 📌 + маскировка кода в логах (архив)

Перенесено в [archive/features-v1.2.140-146.md](./archive/features-v1.2.140-146.md) (2026-07-31, разгрузка под лимит 100 КБ): фикс папки сессии при удалении аккаунта (v1.2.145) + чистка кэш-файла/проброс переименования (v1.2.146); диагностика чёрного экрана и операций с аккаунтом + ErrorBoundary вокруг InboxMode (v1.2.142-143); маскировка кода входа в логах (v1.2.144); значок 📌 в углу аватарки (v1.2.141); откат авто-чистки осиротевших закреплений — теряла данные (v1.2.140).

### v1.2.139-v1.2.138 — Локальное закрепление чатов (📌): базовая фича + доработки (архив)

Перенесено в [archive/features-v1.2.138-139.md](./archive/features-v1.2.138-139.md) (2026-07-30, разгрузка под лимит 100 КБ): локальное закрепление чатов полоской+значком 📌, правый клик, хранение в localStorage, НЕ синхронизируется с Telegram (v1.2.138); доработки — 📌 в узком режиме, устойчивый порядок по массиву pinnedIds, вынос `filterSortChats`, чистка осиротевших (позже откачена v1.2.140) (v1.2.139). Перетаскивание порядка сделано позже (v1.2.156→160).

### v1.2.137-v1.2.134 — Уведомление снимается при прочтении на другом устройстве + CI-фикс TZ снапшота + ВК vk.ru (архив)

Перенесено в [archive/features-v1.2.134-137.md](./archive/features-v1.2.134-137.md) (2026-07-30, разгрузка под лимит 100 КБ): прочитал чат в Telegram на телефоне → карточка-уведомление снимается, частичное чтение + чистая `shouldDismissForRead` (v1.2.137); snapshot ChatListItem сделан TZ-независимым — падал на CI под UTC (v1.2.135); ВК основной адрес → vk.ru + убрана лишняя ошибка `GUEST_VIEW_MANAGER_CALL` запасного впрыска (v1.2.134).

### v1.2.133-v1.2.130 — Строка списка чатов: время + имя отправителя (группы) + «Вы:» + значок форума + имя автора в уведомлении (архив)

Перенесено в [archive/features-v1.2.130-133.md](./archive/features-v1.2.130-133.md) (2026-07-30, разгрузка под лимит 100 КБ): время последнего сообщения + имя автора в превью групп/форумов + значок 🗂️ форума (v1.2.130); «Вы:» для своих + единая `lastSenderLabel` (v1.2.131); имя автора в заголовке уведомления + чистка мёртвого `pickNotifTitle` (v1.2.132); фикс залипания имени в превью + вынос `nativeStoreLastMsgIpc.js` (v1.2.133).

### v1.2.129-v1.2.126 — Уведомления: «невидимая стена»/heartbeat-сторож + ВК переносы строк (архив)

Перенесено в [archive/features-v1.2.126-129.md](./archive/features-v1.2.126-129.md) (2026-07-30, разгрузка под лимит 100 КБ): гасим «невидимую стену» сразу по видимой высоте (v1.2.128), heartbeat-сторож без спама (v1.2.127) и его первая версия (v1.2.126); ВК — переносы строк/абзацы в тексте уведомления через `_vkNodeText` (v1.2.129).

### v1.2.125-v1.2.122 — Уведомление «поверх всех» + ВК-хук (разгрузка/эмодзи/mute-детект/переносы строк) (архив)

Перенесено в [archive/features-v1.2.122-125.md](./archive/features-v1.2.122-125.md) (2026-07-29, разгрузка под лимит 100 КБ): окно уведомления надёжно поверх всех — `screen-saver`+reassert (v1.2.125); ВК-хук — удалён мёртвый toast-наблюдатель + эмодзи из `<img alt>` (v1.2.124), сужен mute-селектор + тесты чистых функций (v1.2.123), сохранены переносы строк + снят предел текста 1000 (v1.2.122).

### v1.2.121-v1.2.116 — Telegram: не показываем сторис; ВК/vk.ru: диагностики значка 🔕 + текст целиком + фикс «не уведомлять о заглушённых» (архив)

Перенесено в [archive/features-v1.2.116-121.md](./archive/features-v1.2.116-121.md) (2026-07-29, разгрузка под лимит 100 КБ): Telegram Web — гасим системную фразу «опубликовал(а) историю» (v1.2.121); ВК/vk.ru — итоговый фикс «не уведомлять о заглушённых чатах 🔕» по видимости значка (v1.2.120), текст уведомления целиком без своей мерки (v1.2.119), серия временных диагностик значка «беззвучно» (v1.2.116-118, диагностики давно удалены).

### v1.2.115-v1.2.108 — ВК переезд на vk.ru (детект/текст/диагностика) + время в источнике (архив)

Перенесено в [archive/features-v1.2.108-115.md](./archive/features-v1.2.108-115.md) (2026-07-27, разгрузка под лимит 100 КБ): переезд ВК на vk.ru + определение домена (v1.2.109); настоящий детект новых сообщений по списку чатов (v1.2.112) + подтверждение логом (v1.2.113); чистый текст уведомления по структуре строки (v1.2.114); 3 фикса по ревью — чистка времени/шторм на старте/дубль (v1.2.115); серия временных диагностик структуры vk.ru (v1.2.110/111); время сообщения в строке источника, единый формат (v1.2.108).

### v1.2.107-v1.2.101 — Уведомления: сторож стены + видео/фото в карточке (архив)

Перенесено в [archive/features-v1.2.101-107.md](./archive/features-v1.2.101-107.md) (2026-07-24, разгрузка под лимит 100 КБ): сторож «невидимой стены» + чистая decideNotifResize + быстрее закрытие (v1.2.107); фикс осиротевшего положительного отчёта — «невидимое зависшее окно», ловящее клики (v1.2.106); «Прочитано» для native-Telegram (v1.2.105); видео в уведомлении — постер/плеер как в чате, видео-как-документ, крутилка по факту (v1.2.101-104).

### v1.2.100-v1.2.90 — Уведомления/видео/фото + диагностика Telegram (архив)

Перенесено в [archive/features-v1.2.90-100.md](./archive/features-v1.2.90-100.md) (2026-07-23, разгрузка под лимит 100 КБ): страховка крутилки одиночного фото (v1.2.100), «размытый фон» одиночного фото (v1.2.99), крупный аватар + источник в шапке (v1.2.98), раскладка «Стопка» для всех карточек (v1.2.96-97), одиночное фото/видео в native-уведомлении (v1.2.95), диагностика фото/ссылок Telegram native+web (v1.2.90/92/93/94), расчёт вертикали дока в чистую функцию + тест (v1.2.91).

### v1.2.89-v1.2.74 — Док/подсказка/закреп + имя аккаунта + Telegram-аватар + альбом (архив)

Перенесено в [archive/features-v1.2.74-89.md](./archive/features-v1.2.74-89.md): фиксы дока/подсказки/закрепа (стабильный якорь верха, панель задач Windows, снятие snap, rAF/throttling; v1.2.76-89), доведение имени аккаунта до закрепа/подсказки (v1.2.83-88), Telegram аватар+источник в уведомлении (v1.2.75), листание альбома + чёткие плитки (v1.2.74).

### v1.2.73-v1.2.59 — Док/подсказка/закреп: серия фиксов (архив)

Перенесено в [.memory-bank/archive/features-v1.2.59-73.md](archive/features-v1.2.59-73.md) при разбиении (21 июля 2026). Архитектура — decisions.md ADR-017/ADR-018.

### v1.2.58-v1.2.46 — VK: тост/sidebar/active-chat уведомления, MAX dedup, ribbon (архив)

Перенесено в [.memory-bank/archive/features-v1.2.46-58.md](archive/features-v1.2.46-58.md) при разбиении (17 июля 2026).

### v1.2.45-v1.2.37 — VK DOM observer / диагностика уведомлений (архив)

Перенесено в [.memory-bank/archive/features-v1.2.37-45.md](archive/features-v1.2.37-45.md) при разбиении (17 июля 2026).

### v1.2.36-v1.2.30 — диагностика MAX/системы (архив)

Полные записи перенесены в [archive/features-v1.2.30-36.md](./archive/features-v1.2.30-36.md), чтобы active features.md оставался меньше лимита memory-bank. Суть: диагностика стала ручной, фоновой, с последним отчётом, полной MAX/VK-цепочкой и понятными кнопками; pipeline уведомлений этим блоком не менялся, кроме описанных в соответствующих версиях точечных MAX/VK исправлений.
### v1.2.29 — диагностика запускается только вручную и не мешает работе (архив)

Полная запись перенесена в [archive/features-v1.2.29.md](./archive/features-v1.2.29.md), чтобы активный features.md оставался меньше лимита memory-bank. Суть: большая модалка диагностики больше не стартует запись сама, управление записью стало явным, общий лог не очищается.
### v1.2.28 — MAX: sidebar watcher не создаёт фантомы во время поиска (архив)

Полная запись перенесена в [archive/features-v1.2.28.md](./archive/features-v1.2.28.md), чтобы активный features.md оставался меньше лимита memory-bank. Суть: `max-sidebar` при активном поиске MAX обновляет baseline, но не создаёт ribbon из старого preview; реальные `max-notification-api` и `max-sw-showNotification` не отключались.

**Структура файла**: этот features.md содержит только **последние активные версии**. Старое — в архиве:

| Архив | Содержимое | Размер |
|---|---|---|
| [`archive/features-v0.95.39.md`](./archive/features-v0.95.39.md) | v0.95.39 (убран twoPhase + RAF×2 + 350мс easeOutCubic; стабилизировано v0.95.40+) | ~2 КБ |
| [`archive/features-v0.95.40.md`](./archive/features-v0.95.40.md) | v0.95.40 (большие emoji + a11y reduced-motion + sticky bottom on media; стабилизировано v0.95.41+) | ~3 КБ |
| [`archive/features-v0.95.41.md`](./archive/features-v0.95.41.md) | v0.95.41 (Custom emoji premium WebM/WebP + reduced-motion интеграционные тесты; стабилизировано v0.95.42+) | ~3 КБ |
| [`archive/features-v0.95.42.md`](./archive/features-v0.95.42.md) | v0.95.42 (сохранение поиска + история + ✕ + подсветка совпадений; стабилизировано v0.95.43+) | ~2 КБ |
| [`archive/features-v0.95.38.md`](./archive/features-v0.95.38.md) | v0.95.38 (фикс дубля сообщений через updateMessageSendSucceeded, ⏳ индикатор, mistakes static guard) | ~2 КБ |
| [`archive/features-v0.95.35-37.md`](./archive/features-v0.95.35-37.md) | v0.95.35-37 (fade-in changelog, auto-scroll outgoing-other-device, sending_state polish, mistakes/outgoing-two-cases.md; стабилизировано v0.95.38+) | ~3 КБ |
| [`archive/features-v0.95.34.md`](./archive/features-v0.95.34.md) | v0.95.34 (темовые vars в :root, вспышка bubble, WhatsNewModal UX; стабилизировано v0.95.40+) | ~2 КБ |
| [`archive/features-v0.95.33.md`](./archive/features-v0.95.33.md) | v0.95.33 (фикс «цвет не применяется» через querySelectorAll, регресс-тест blur, деловой стиль; финал в v0.95.34) | ~2 КБ |
| [`archive/features-v0.95.32.md`](./archive/features-v0.95.32.md) | v0.95.32 (производительность WhatsNewModal: убран backdrop-filter + contain + деловой стиль changelog) | ~2 КБ |
| [`archive/features-v0.95.31.md`](./archive/features-v0.95.31.md) | v0.95.31 (аккаунты вниз + drag-n-drop + multi-user typing + throttle реакций; стабилизировано v0.95.34+) | ~3 КБ |
| [`archive/features-v0.95.30.md`](./archive/features-v0.95.30.md) | v0.95.30 (плавная auto-scroll + 5 цветовых тем + dropdown + opacity 0.95; стабилизировано v0.95.33-34) | ~3 КБ |
| [`archive/features-v0.95.29.md`](./archive/features-v0.95.29.md) | v0.95.29 (реакции 👍❤️🔥 + Telegram-style header + General 📢 + render-counter; стабилизировано v0.95.31-34) | ~5 КБ |
| [`archive/features-v0.95.28.md`](./archive/features-v0.95.28.md) | v0.95.28 (Telegram-style auto-scroll к новому + ↓N без «слепой зоны» Schmitt; стабилизировано v0.95.31) | ~4 КБ |
| [`archive/features-v0.95.27.md`](./archive/features-v0.95.27.md) | v0.95.27 (расширенная диагностика send pipeline; стабилизировано v0.95.29) | ~3 КБ |
| [`archive/features-v0.95.23-26.md`](./archive/features-v0.95.23-26.md) | v0.95.23 – v0.95.26 (курсор в input, initial backfill, voice/spellcheck/action-bar/WhatsNew, фикс 47-дневного бага unreadCount; стабилизировано) | ~15 КБ |
| [`archive/features-v0.95.19-22.md`](./archive/features-v0.95.19-22.md) | v0.95.19 – v0.95.22 (диагностика tg-new-message, финал jump-to-end, бейдж форум-группы, форум-overlay; стабилизировано) | ~6 КБ |
| [`archive/features-v0.95.15-18.md`](./archive/features-v0.95.15-18.md) | v0.95.15 – v0.95.18 (итеративный fetch + форум-топики + двухфазный scroll + ForumTopicEmptyState; стабилизировано v0.95.20-21) | ~8 КБ |
| [`archive/features-v0.95.12-14.md`](./archive/features-v0.95.12-14.md) | v0.95.12 – v0.95.14 (3 итерации jump-to-end до итеративного fetch v0.95.15, полная сага в jump-to-end-saga.md) | ~32 КБ |
| [`archive/features-v0.95.8-9.md`](./archive/features-v0.95.8-9.md) | v0.95.8 – v0.95.9 (счётчик ↓ обнуляется + анимация + live compact + порог 128) | ~14 КБ |
| [`archive/features-v0.95.5-7.md`](./archive/features-v0.95.5-7.md) | v0.95.5 – v0.95.7 (sticky pinned overlay, кнопка ↓ Telegram-style, drag-to-resize) | ~24 КБ |
| [`archive/features-v0.95.0-3.md`](./archive/features-v0.95.0-3.md) | v0.95.0 – v0.95.3 (контигуити-фикс, afterId load-newer, мигание ↓ Schmitt trigger, диагностика «дёрг») | ~13 КБ |
| [`archive/features-v0.94.1-7.md`](./archive/features-v0.94.1-7.md) | v0.94.1 – v0.94.7 (TDLib listener leak, scroll caskade, спокойная загрузка, пилюля прогресса — стабилизированы v0.95.0-2) | ~24 КБ |
| [`archive/features-v0.93.0.md`](./archive/features-v0.93.0.md) | v0.93.0 (pixel-perfect scroll restore через LocationOptions.offset, superseded v0.94.0) | ~7 КБ |
| [`archive/features-v0.92.0-6.md`](./archive/features-v0.92.0-6.md) | v0.92.0 – v0.92.6 (сага Virtuoso scroll restore, superseded v0.94.0) | ~37 КБ |
| [`archive/features-v0.91.11-24.md`](./archive/features-v0.91.11-24.md) | v0.91.11 – v0.91.24 (сага scroll restore: 13 версий → миграция на virtuoso) | ~47 КБ |
| [`archive/features-v0.91.1-10.md`](./archive/features-v0.91.1-10.md) | v0.91.1 – v0.91.10 (initial-load, scroll-jump, newBelow, forum topics, updateChatLastMessage) | ~12 КБ |
| [`archive/features-v0.87.106-114.md`](./archive/features-v0.87.106-114.md) | v0.87.106 – v0.87.114 (multi-account UI финал, кнопки режимов, мьют чата, аватарки отправителей в группах) | ~22 КБ |
| [`archive/features-v0.87.93-105.md`](./archive/features-v0.87.93-105.md) | v0.87.93 – v0.87.105 (multi-account native, login flow, разбиения, cc-media://) | ~30 КБ |
| [`archive/features-v0.87.80-92.md`](./archive/features-v0.87.80-92.md) | v0.87.80 – v0.87.92 (pre-push hook, разбиения 4-7, AccountContextMenu) | ~16 КБ |
| [`archive/features-v0.87.65-79.md`](./archive/features-v0.87.65-79.md) | v0.87.65 – v0.87.79 (план разбиения 1-3, pre-push hook, bubble UI) | ~54 КБ |
| [`archive/features-v0.87.51-64.md`](./archive/features-v0.87.51-64.md) | v0.87.51 – v0.87.64 (groupedUnread удалён, pre-commit hook) | ~54 КБ |
| [`archive/features-v0.87.40-50.md`](./archive/features-v0.87.40-50.md) | v0.87.40 – v0.87.50 (итерации native scroll + unread) | ~40 КБ |
| [`archive/features-v0.87-early.md`](./archive/features-v0.87-early.md) | v0.87.0 – v0.87.39 (запуск native + ранние фиксы) | ~140 КБ |
| [`archive/features-pre-v0.87.md`](./archive/features-pre-v0.87.md) | v0.1.0 – v0.86.10 (до native-режима, 3 марта – 14 апреля 2026) | ~210 КБ |

**Архив не читается по умолчанию.** Запрос к нему — только при явной просьбе («что было в v0.85», «покажи старый changelog»).

**До рефакторинга v0.87.57** файл был 445 КБ (3371 строк, 323 версии). После — ~100 КБ в корне.

---

### Архив v1.2.27 и старше

Подробная история v1.2.27 и более ранних версий перенесена в [archive/features-v1.2.27-and-older.md](./archive/features-v1.2.27-and-older.md). Активный файл держим компактным, чтобы memory-bank быстро читался и проходил лимиты.
