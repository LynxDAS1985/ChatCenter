# Реализованные функции — ChatCenter

## Текущая версия: v1.2.252 (13 августа 2026)

### v1.2.252 — Боковой рейл: проводка аккаунтов вынесена в хук useSourceRail (+тест)

Дата: 2026-08-13. По совету после 2C. Проводка аккаунтов рейла (состояние `nativeAccounts`, ссылка на действия, обработчик клика по аккаунту) вынесена из [App.jsx](../src/App.jsx) в хук [useSourceRail.js](../src/hooks/useSourceRail.js). **ЗАЧЕМ:** логика «клик по аккаунту → общий чат + solo» раньше была inline в App.jsx и не покрывалась юнит-тестом (тест проверял только клик в SourceRail, но не App-обработчик). Теперь хук возвращает `{ nativeAccounts, onAccountsChange, onAccountActionsReady, onSelectAccount }`; App зовёт `const rail = useSourceRail({ onSelectSource: handleTabClick, nativeCcId })` и передаёт `rail.*` в NativeApp/SourceRail. Тест [useSourceRail.vitest.js](../src/__tests__/useSourceRail.vitest.js) (3: клик переключает источник + зовёт `soloAccount` после прихода действий; до прихода не падает; список обновляется). Смоук `appStructure.test.cjs` обновлён под хук. **ЧЕСТНО про размер:** вынос дал ТЕСТИРУЕМОСТЬ и инкапсуляцию, но App.jsx НЕ уменьшился (импорт+вызов хука ≈ убранным строкам; большой JSX-блок `<SourceRail>` с ~15 пропсами остаётся в App) — App.jsx как был 981/990. Т.е. обещанной экономии строк совет не дал; потолок 990 оставлен (при дальнейшем росте — переносить сам JSX-блок или ещё пропсы). Watermark 34000→34050. Проверки: vitest 2313/2313, линт 0, лимиты 578/578, guards ✅. Поведение не изменилось. Откат: `git checkout src/App.jsx` + удалить `useSourceRail.*` + версия на 1.2.251.

### v1.2.251 — Боковой рейл, Этап 2C: отдельные Telegram-аккаунты с аватарами

Дата: 2026-08-13. В боковом рейле теперь видны **отдельные Telegram-аккаунты значками-аватарами** (сверху, под «Общим чатом»). **ЗАЧЕМ:** раньше рейл показывал один значок «Общий чат» (слитый вид всех аккаунтов); аккаунты живут в `nativeStore` ВНУТРИ лениво-грузимого `NativeApp` и наверх не отдавались. **КАК:** (1) [NativeApp.jsx](../src/native/NativeApp.jsx) отдаёт наверх список аккаунтов новым колбэком `onAccountsChange` (id, имя, `avatar`, цвет-метка, непрочитанные) — только чтение из стора, существующий рейл аккаунтов внутри NativeApp НЕ меняется; (2) действие «показать чаты аккаунта» (`soloAccount`) отдаётся через `onAccountActionsReady` — тем же приёмом, что `onConnectionActionsReady`; (3) [App.jsx](../src/App.jsx) держит список (`nativeAccounts`) и ссылку на действия (`nativeAccountActionsRef`), передаёт в [SourceRail.jsx](../src/components/SourceRail.jsx); (4) рейл рисует круглый **аватар** каждого аккаунта (`background:url("cc-media://…")`, фолбэк — инициалы), бейдж непрочитанных, подсветку активного (`activeAccountId`). **Клик по аккаунту** → переключение в нативный режим (`handleTabClick(native_cc)`) + показ чатов этого аккаунта (`soloAccount(id)`). Аватар грузится тем же способом, что в [AccountAvatar.jsx](../src/native/components/AccountAvatar.jsx). **Крайние случаи:** нет аккаунтов → значки аккаунтов не рисуются (только «Общий чат»); пустой аватар → инициалы; действие ещё не пришло (ref null) → `?.` без эффекта. Тесты [SourceRail.vitest.jsx](../src/components/SourceRail.vitest.jsx) 21 (+4: рисование по имени, клик→onSelectAccount, активный+бейдж, без аккаунтов пусто) + смоук связки в [appStructure.test.cjs](../src/__tests__/appStructure.test.cjs) (+1: колбэки NativeApp↔App↔SourceRail). Потолок App.jsx 975→990 (временно), watermark 33920→34000. Проверки: vitest 2310/2310, линт 0, лимиты 576/576, guards ✅. **⚠️ ТРЕБУЕТ ВИЗУАЛЬНОЙ ПРОВЕРКИ (запуск агенту запрещён):** живое переключение аккаунтов (клик по аватару → показываются чаты именно этого аккаунта), загрузка аватаров, подсветка активного. Клик = solo (показать чаты аккаунта) — это интерпретация; если ожидалось иное (напр. просто «активный без фильтра») — скажи, поправлю. Откат: флаг `sideRail` выкл, или `git checkout src/App.jsx src/native/NativeApp.jsx src/components/SourceRail.jsx` + версия на 1.2.250.

### v1.2.250 — Боковой рейл: пункты меню вынесены в чистую функцию + тест (закрыта дыра ревью)

Дата: 2026-08-13. По плану исправления ревью 2B. **Находка #2 (правило меню без теста):** логика «нативный источник → без веб-only пунктов» (v1.2.249) жила прямо в JSX [TabBar.jsx](../src/components/TabBar.jsx) и юнит-тестом не проверялась → регрессию поймал бы только глаз. Вынес построение пунктов в чистую функцию `buildTabContextMenuItems({ isNative, pinned })` — новый файл [tabContextMenuItems.js](../src/utils/tabContextMenuItems.js); `TabBar` теперь зовёт её. Логика ушла из `TabBar` (349/700). Тест [tabContextMenuItems.vitest.js](../src/__tests__/tabContextMenuItems.vitest.js) (5 проверок: веб→все пункты; нативный→без reload/notifLog/copyUrl; закреплённая→без close + ярлык «Открепить»; дефолты не падают). **Находка #1 (меню упростилось и на нативной ВКЛАДКЕ, не только в рейле): принято намеренно** — пункты reload/notifLog/copyUrl и на вкладке «ЦентрЧатов» ничего не делали (нет веб-страницы), прятать их везде — правильное поведение; НЕ откатывал (вариант «различать рейл/вкладку» был бы сложнее и хуже). Пункт «Изменить вкладку» для нативного оставлен (может менять имя/цвет — осмысленно). Проверки: vitest 2306/2306, линт 0, лимиты 576/576, guards ✅. Поведение меню не изменилось — только вынесено под тест. Откат: `git checkout src/components/TabBar.jsx` + удалить `tabContextMenuItems.*` + версия на 1.2.249.

### v1.2.249 — Боковой рейл: 2 доводки 2B (перетаскивание в пределах секции + меню «Общего чата»)

Дата: 2026-08-13. Два улучшения по советам к Этапу 2B. **#2 Перетаскивание — только внутри своей секции** (API-значки среди API, веб среди веб): в [SourceRail.jsx](../src/components/SourceRail.jsx) значок получил `section` ('native'|'web'); при начале перетаскивания секция запоминается (`draggedSectionRef`), а события над/бросок у чужой секции игнорируются. Причина: рейл делит значки по типу, поэтому перетаскивание между секциями визуально ничего не давало («пустое» действие). Обёртки зовут те же обработчики вкладок (`handleDragStart/Over/Drop`), только с проверкой секции. **#3 Меню «Общего чата» без веб-only пунктов:** в [TabBar.jsx](../src/components/TabBar.jsx) для нативного источника (`isNative`) скрыты «Перезагрузить / Диагностика и логи / Копировать URL» — у него нет веб-страницы, эти действия ничего не делали (копировался `about:blank`). Касается и нативной вкладки, и значка рейла (меню общее). Тесты [SourceRail.vitest.jsx](../src/components/SourceRail.vitest.jsx) 17 (+1: бросок веб→API игнорируется, веб→веб срабатывает). Проверки: vitest 2301/2301, линт 0, лимиты 574/574, guards ✅. **Требует визуальной проверки.** Откат: `git checkout src/components/SourceRail.jsx src/components/TabBar.jsx src/App.jsx` + версия на 1.2.248.

### v1.2.248 — Боковой рейл, Этап 2B: правый клик + перетаскивание на значках

Дата: 2026-08-13. Перенос ещё двух функций вкладок на значки рейла ([SourceRail.jsx](../src/components/SourceRail.jsx)) — **переиспользование, новой логики нет**. **Правый клик** по значку открывает ТО ЖЕ меню, что у вкладок (Перезагрузить / Диагностика и логи / Копировать URL / Изменить / Закрепить / Закрыть): значок вызывает `onContextMenu(id, e.clientX, e.clientY)` → в [App.jsx](../src/App.jsx) `setContextMenuTab({id,x,y})`, а меню рендерит [TabBar.jsx:162-197](../src/components/TabBar.jsx) как `position:fixed` по координатам курсора (не привязано к вкладке — потому и работает из рейла). **Перетаскивание** значков — те же обработчики вкладок `handleDragStart/Over/Drop/End(id)` + подсветка цели (`dragOverId` → пунктирная рамка). Значок стал `draggable`; `dataTransfer` под гвардом (в тестовой среде может быть пустым). Тесты [SourceRail.vitest.jsx](../src/components/SourceRail.vitest.jsx) 16 (+2: правый клик с координатами, drag start/over/drop) + смоук `appStructure.test.cjs` (+1). **Важно (тех-долг):** меню сейчас живёт внутри `TabBar` — работает, пока вкладки на месте; при удалении вкладок в финале миграции меню надо будет вынести из `TabBar` (записано в план и [[decisions]] ADR-031). Watermark renderer 33860→33890, App.jsx 974/975. Проверки: vitest 2300/2300, линт 0, лимиты 574/574, guards ✅. **Требует визуальной проверки** (правый клик по значку → меню у курсора; перетащить значок → порядок меняется). Откат: `git checkout src/components/SourceRail.jsx src/App.jsx` + версия на 1.2.247.

**НЕ вошло (осознанно, отдельными шагами):** Этап 2C (отдельные Telegram-аккаунты сверху + аватар аккаунта) — самый большой архитектурный риск (поднять список аккаунтов из lazy `NativeApp`/`nativeStore` + научить `NativeApp` выбирать аккаунт по внешнему сигналу), плюс живое поведение переключения аккаунта нельзя проверить без запуска приложения. Остаток Этапа 3 (умный «+», сворачивание рейла, заглушить источник) — тоже отдельно.

### v1.2.247 — Боковой рейл: 2 фикса по ревью 2A (цифра бейджа + значок = div)

Дата: 2026-08-13. По плану исправления из ревью Этапа 2A. **Фикс #1 (цифра бейджа = как на вкладке):** рейл показывал ВСЕГО непрочитанных, а вкладка при режиме «Бейдж = только личные» (`overlayMode==='personal'`) — только личные → цифры расходились. Теперь [SourceRail.jsx](../src/components/SourceRail.jsx) получает проп `overlayMode` (из `settings.overlayMode`) и считает число по той же формуле, что [TabBar.jsx:79-81](../src/components/TabBar.jsx) (`personal ? split.personal : всего`). **Фикс #2 (значок — `<div role="button">`, не `<button>`):** внутри значка есть кликабельная точка связи (интерактивный элемент), а по стандарту HTML вкладывать интерактивное в `<button>` нельзя. Значок переведён на `<div role="button" tabIndex={0}>` + клавиатура (Enter/Пробел → переключение) — вложение стало корректным. Тесты [SourceRail.vitest.jsx](../src/components/SourceRail.vitest.jsx) 14 (+3: личные/всего по режиму, Enter на значке) + смоук `appStructure.test.cjs` (+`overlayMode`). Проверки: vitest 2298/2298, линт 0, лимиты 574/574, guards ✅. **Требует визуальной проверки.** Откат: `git checkout src/components/SourceRail.jsx src/App.jsx` + версия на 1.2.246.

### v1.2.246 — Боковой рейл: увеличенная область клика по точке связи (совет к 2A)

Дата: 2026-08-13. По совету из ревью 2A: точку связи 10px в углу значка было трудно поймать курсором. Теперь видимая точка осталась 10px, но вокруг неё — прозрачная кликабельная зона-обёртка 20px (растёт внутрь значка, в пустое место, не вылезает за него) в [SourceRail.jsx](../src/components/SourceRail.jsx). Обёртка — `role="button"` + `tabIndex` + `aria-label`/`title` + клавиатура (Enter/Пробел), клик/Enter → «Подключения»; `stopPropagation` — клик по точке НЕ переключает источник; сама точка `pointer-events:none` (клики ловит обёртка). Тест обновлён: клик по обёртке зовёт `onOpenConnections` И не зовёт `onSelect`. Watermark renderer 33840→33860. Проверки: vitest 2295/2295, линт 0, лимиты 574/574. **Требует визуальной проверки**. Откат: вернуть `onClick` на саму точку + версия на 1.2.245.

### v1.2.245 — Боковой рейл, Этап 2A: индикаторы вкладок на значках рейла

Дата: 2026-08-13. Перенос функций вкладок на значки рейла — **под-этап 2A (индикаторы)**. На каждом значке [SourceRail.jsx](../src/components/SourceRail.jsx): **бейдж непрочитанных** (верхний правый угол, «99+» при переполнении, разбивка 💬/📢 в подсказке), **точка связи** 🟢/🟡/🔴 (нижний правый угол, переиспользован [ConnectionStatusDot.jsx](../src/components/ConnectionStatusDot.jsx), клик → «Подключения»), **пульс** при новом сообщении (кольцо), **полоска загрузки** под значком, **✓ «всё прочитано»** (2с после падения счётчика до 0, как во вкладке), **подсказка** с именем аккаунта. Все данные — ТЕ ЖЕ объекты по id, что уже идут во вкладки ([TabBar.jsx:77-88](../src/components/TabBar.jsx)): `unreadCounts/unreadSplit/connectionHealth/webviewLoading/newMessageIds/accountInfo`; переданы в `<SourceRail>` в [App.jsx](../src/App.jsx) (+3 строки, App.jsx 971/975). Новой логики нет — переиспользование. Тесты [SourceRail.vitest.jsx](../src/components/SourceRail.vitest.jsx) +5 (11 всего: бейдж/«99+»/клик по точке→onOpenConnections/подсказка аккаунта/без объектов не падает) + смоук связки в [appStructure.test.cjs](../src/__tests__/appStructure.test.cjs) +1 (данные индикаторов). Разбито осознанно: **2B** (правый клик + перетаскивание) и **2C** (отдельные аккаунты + аватарки, источник аватарки веб-мессенджера не определён — ждёт решения пользователя) — отдельными шагами. Водяной знак renderer 33740→33840. Проверки: vitest 2295/2295, линт 0, лимиты 574/574, guards ✅. **Требует визуальной проверки** (включить рейл → на значках бейджи/точка/пульс/загрузка как на вкладках). Откат: флаг выкл или `git checkout src/components/SourceRail.jsx src/App.jsx` + версия на 1.2.244.

### v1.2.244 — Боковой рейл, Этап 1: костяк рейла источников (за флагом)

Дата: 2026-08-13. Реализован **костяк** бокового рейла (план [side-rail-migration-plan.md](side-rail-migration-plan.md), Этап 1). Новый компонент [SourceRail.jsx](../src/components/SourceRail.jsx): левая колонка значков источников — **нативные/API сверху** (`isNative` или id `native_cc`), разделитель, **веб-мессенджеры ниже**, кнопка «+» внизу. Значок = `emoji` на цветном фоне + пилюля активного слева (как Discord). Клик по значку зовёт `onSelect(id)` = тот же `handleTabClick` (переключение как у вкладок), «+» = `setShowAddModal`. Включается флагом `settings.sideRail` (по умолчанию **выкл** → приложение работает как сейчас, байт-в-байт). Вставлен первым ребёнком горизонтального ряда в [App.jsx](../src/App.jsx) — колонка-сосед, **НЕ прячет webview и НЕ трогает z-index-слои** (защита от чёрного экрана). Тумблер флага вернулся в [SettingsPanel.jsx](../src/components/SettingsPanel.jsx) (теперь у него есть консьюмер). **Верхние вкладки остаются** (по просьбе — пока не убираем). Полоска «← Общий чат» поверх веб-окна перенесена в Этап 2 (в Этапе 1 вкладки ещё на месте — возврат через вкладку «ЦентрЧатов»; и App.jsx у своего потолка). Тест [SourceRail.vitest.jsx](../src/components/SourceRail.vitest.jsx) — 6 проверок (порядок API↑/веб↓, клик→onSelect, aria-current активного, «+»→onAdd, пустой список→только «+»). Лимит `App.jsx` 960→975 (временно; вернётся вниз, когда в финале уйдёт TabBar), водяной знак renderer 33620→33740. Проверки: vitest 2290/2290, линт 0, лимиты 574/574. **Требует визуальной проверки** (включить тумблер → слева колонка значков; клик переключает источник; вкладки работают как раньше). Откат: флаг `sideRail=false` или удалить SourceRail.* + откатить App.jsx/SettingsPanel + версия на 1.2.243.

### v1.2.243 — Боковой рейл: убран преждевременный тумблер флага (по ревью)

Дата: 2026-08-13. По придирчивому ревью v1.2.242: тумблер «🧪 Боковой рейл источников» переключал флаг `settings.sideRail`, у которого ещё НЕТ консьюмера (самого рейла) → включение ничего не делало (пустышка). Показывать неработающий переключатель не нужно — убран из [SettingsPanel.jsx](../src/components/SettingsPanel.jsx) (ровно 6 строк из v1.2.242). Флаг и тумблер вернутся ВМЕСТЕ с компонентом `SourceRail` на Этапе 1 (план [side-rail-migration-plan.md](side-rail-migration-plan.md)), чтобы переключатель всегда имел эффект. Верхние вкладки не тронуты, данные/сессии не тронуты. Проверки: линт 0, лимиты 572/572, `settings.sideRail`/тумблера в `src/` не осталось. Откат: вернуть блок тумблера + версия на 1.2.242.

### v1.2.242 — Боковой рейл источников: план зафиксирован + Этап 0 (флаг `sideRail`, выключен)

Дата: 2026-08-13. Начата крупная переделка: перенести мессенджеры из ВЕРХНИХ ВКЛАДОК в БОКОВОЙ РЕЙЛ (API-аккаунты сверху, веб ниже; клик по вебу → веб-интерфейс на весь экран с полоской «← Общий чат»). Полный план (карта кода «как есть», этапы 0-4, чек-лист «ни одна функция вкладок не потеряна», опасные зоны, откат) зафиксирован в [side-rail-migration-plan.md](side-rail-migration-plan.md) (проиндексирован в CLAUDE.md).

**Этап 0 (этот релиз):** введён флаг-переключатель `settings.sideRail` (по образцу `useWebContentsView`) — тумблер «🧪 Боковой рейл источников (в разработке)» в [SettingsPanel.jsx](../src/components/SettingsPanel.jsx). По умолчанию **ВЫКЛЮЧЕН** → интерфейс не меняется, верхние вкладки работают как раньше (по просьбе пользователя вкладки пока НЕ убираем). Флаг — фундамент, чтобы строить рейл поэтапно, не ломая рабочую версию; читатель-консьюмер (сам рейл) появится на Этапе 1. Только настройка (boolean), данные/сессии/вкладки не тронуты. Проверки: линт 0, лимиты 572/572, `SettingsPanel.jsx` 526/700. Откат: убрать тумблер + версия на 1.2.241. **Дальше — Этап 1:** компонент `SourceRail.jsx` за флагом (костяк рейла).

### v1.2.241 — Карточка контакта: читаемый вторичный текст + строка «Имя пользователя» всегда

Дата: 2026-08-13. Жалоба: в карточке контакта (1) вторичный текст плохо читается — тусклый на тёмном фоне; (2) строка «Имя пользователя» пропадает, если у контакта нет публичного @username. Выбран вариант «Ярче (минимум правок)».

**(1) Читаемость.** Токены были тёмными: `--amoled-text-dim: #a0a0a0`, `--amoled-text-muted: #606060` (styles-base.css:24-25). В [ContactCardModal.jsx](../src/native/components/ContactCardModal.jsx) введена локальная константа `SUB = '#bcc6d6'` (светлее) и применена к вторичному тексту: статус («был(а) недавно»), источник-чип («Telegram · …», был самый тёмный #606060), подписи быстрых кнопок («Написать»/«В Telegram», `qlabel`), метки полей (`CopyField` — «Телефон»/«Имя пользователя»), «О себе», подпись звука. Значения (телефон, имя, bio) остаются белыми (`--amoled-text` #fff). Декоративное (иконки, Esc/✕) не трогали.

**(2) Имя пользователя всегда.** Для ЛИЧНОГО чата (`chat.type === 'user'`) строка «Имя пользователя» теперь показывается всегда: есть логин → копируемая строка `@username`; нет → «— не указан» (курсивом, приглушённо) — понятно, что публичного логина в Telegram нет, а не «строка пропала». Для групп/каналов строки нет (username там не применим). Телефон по-прежнему только при наличии.

Только стиль/условие показа — данные/хранилище/поведение не менялись. Проверки: линт 0, тесты `ContactCardModal.vitest.jsx` 11/11, лимиты 572/572 (watermark 33600→33620). **Требует визуальной проверки** (окно без запуска не тестируется): текст читается лучше, строка «Имя пользователя: — не указан» присутствует у контактов без логина. Откат: вернуть цвета на `--amoled-text-dim`/`--amoled-text-muted`, убрать `SUB` и ветку пустого username + версия на 1.2.240.

### v1.2.240 — Заметка о клиенте — в строке статуса шапки чата (полный текст в подсказке)

Дата: 2026-08-13. По просьбе (выбран вариант «вместе со статусом»): заметку о клиенте, которую оператор пишет в карточке контакта, теперь видно в ШАПКЕ открытого чата — в строке статуса рядом с «был(а) недавно». Раньше заметка жила только внутри карточки → во время переписки её не видно.

**Как работает:** [InboxChatPanel.jsx](../src/native/components/InboxChatPanel.jsx) читает заметку активного чата `getContactNote(activeChat.id)` ([contactNotes.js](../src/native/utils/contactNotes.js), localStorage, ключ `cc:contact-note:{accountId}:{rawId}`) и в строке статуса выводит «`был(а) недавно` · 📝 `<заметка>`» золотистым (#ffd479). Не влезает → обрезка `text-overflow:ellipsis`, а ПОЛНЫЙ текст показывается во всплывающей подсказке при наведении (атрибут `title`, факт уровня 1 MDN). Клик по заметке открывает карточку для правки. **Обновление:** заметка перечитывается при перерисовке шапки; после правки в карточке она закрывается (`setShowContact(false)`) → шапка перерисовывается → заметка обновляется (живьём во время набора в карточке — не обновляется, это ожидаемо). **Дублирования в саму карточку НЕ делаем** — там заметка только редактируется. Только показ (чтение localStorage + отрисовка), хранилище не менялось. Тему форума не трогали (заметка в обычной строке статуса личного/группового чата). Нет заметки → строки нет (как раньше). Проверки: линт 0, полный vitest 2284/2284, лимиты 572/572. **Требует визуальной проверки** (окно без запуска не тестируется): заметка видна в шапке; длинная — «…» + полный текст при наведении. Откат: убрать блок заметки в строке статуса + импорт `getContactNote` + `contactNote` + версия на 1.2.239.

### v1.2.239 — Карточка контакта: «Контур + свечение» (не сливается с тёмным фоном)

Дата: 2026-08-13. Жалоба: карточка контакта почти не видна — она `--amoled-surface` (#0a0a0a) с рамкой `--amoled-border` (#1a1a1a) и чёрной тенью на почти чёрном фоне чата (#0a0b0f) → границы не видно. По выбранному макету (вариант «Контур + свечение») контейнер модалки ([ContactCardModal.jsx](../src/native/components/ContactCardModal.jsx)) получил: приподнятый фон `--amoled-surface-hover` (#131313), акцентную рамку `rgba(42,171,238,0.55)` (= `--amoled-accent` #2AABEE, styles-base.css:12) и многослойную тень `box-shadow` — кольцо `0 0 0 1px rgba(42,171,238,.22)` + мягкое свечение `0 0 30px rgba(42,171,238,.20)` + падающая тень `0 22px 55px rgba(0,0,0,.65)`. Правка только стиля контейнера (3 значения), поведение/разметка не тронуты. Фон-подложка (backdrop `rgba(0,0,0,0.75)`) не менялся. Тема нативного UI фиксированная тёмная (переменные `--amoled-*` не переключаются) → светлой темы касаться не нужно. Проверки: линт 0, тесты `ContactCardModal.vitest.jsx` 11/11 (проверяют текст/поведение, стиль не завязан), лимиты 572/572. **Требует визуальной проверки** (окно без запуска не тестируется): карточка теперь с голубой обводкой и свечением, чётко отделена от фона. Откат: вернуть `background/border/boxShadow` контейнера к `--amoled-surface`/`--amoled-border`/`0 20px 50px rgba(0,0,0,0.6)` + версия на 1.2.238.

### v1.2.238 — ФИКС: МАКС больше НЕ уведомляет о заглушённых (🔕) чатах

Дата: 2026-08-13. Жалоба (v1.2.236): чат МАКС помечен 🔕, но уведомления идут. КОРЕНЬ (🟢 по коду+логу): единственный рабочий путь уведомлений МАКС — наблюдатель за списком чатов (`_maxScanList` в [max.hook.js](../main/preloads/hooks/max.hook.js), лог `src=max-sidebar`), а он фильтровал только спам (`_isSpam`), проверки мьюта не было (пути Notification API / ServiceWorker у МАКС не работают — `serviceWorker.ready` не резолвится). **Фикс:** в `_maxScanList` перед отправкой уведомления добавлена проверка «заглушён ли чат» по СТРОКЕ списка: заглушённым считается строка, если (а) в её тексте есть эмодзи 🔕, ИЛИ (б) в ней есть ВИДИМЫЙ элемент с признаком мьюта — класс `mute|silen`, либо `aria-label`/`title` содержит «выключ/беззвуч/mute/silen», либо svg-иконка (`href`) вида `mute|silen|bell-off|notif-off`. Видимость проверяется (`getBoundingClientRect`>0 + `display≠none`+`visibility≠hidden`+`opacity≥0.1`) — как у ВК (v1.2.120), чтобы скрытый шаблонный значок НЕ заглушил нужный чат. Заглушённая строка пропускается (`continue`) с логом `[Макс] mute-skip заглушённый чат: <имя>`. Временная диагностика v1.2.236/237 (`max-mute-probe`) и старые диаг-логи `max-sidebar-decision`/`max-own-probe` удалены (меньше спама, освобождено место под фикс); мёртвая переменная `action` убрана. Файл: `main/preloads/hooks/max.hook.js` (300/300). Проверки: `node --check` OK, лимиты 572/572. **Требует ПОЛНОГО перезапуска + визуальной проверки:** заглушённый «А.Х. как вкусно» БОЛЬШЕ не даёт уведомлений (в логе `mute-skip`), обычные чаты — дают. **Ограничение (гипотеза):** если МАКС метит мьют совсем неочевидно (без 🔕/класса/aria с этими словами) — не поймаем; тогда в логе НЕ будет `mute-skip` для заглушённого чата → доработаем маску по факту.

### v1.2.237 — Диагностика мьюта МАКС: КОРОТКИЕ строки (журнал webview-консоли режет на ~60 символах)

Дата: 2026-08-13. Прогон диагностики v1.2.236 сработал (`max-mute-probe: sender="А.Х. как вкусно" rowCls="wrapper wra…`), НО строка обрезалась журналом на ~60 символах — важное (`bell=`, значки) не влезло. Та же беда, что с ВК v1.2.111. Переписал вывод `max-mute-probe` на серию КОРОТКИХ строк (каждая ≤ ~55 символов): `mmp bell=<есть ли 🔕 в тексте>`, `mmp rc0=/rc1=…` (класс строки чанками по 46), `mmp c=<tag>|<class30>|a:<aria>|h:<href>` для дочерних элементов, чьи класс/aria/href подходят под маску мьюта (`mute|notif|silent|bell|disab|off|volume|sound|alert`), либо `mmp c=none-matched` если ничего не совпало (значит метка названа неочевидно — тогда следующий шаг дампит сырьё). Фикс НЕ делался. Файл: `main/preloads/hooks/max.hook.js` (300/300). **Действие пользователя:** перезапуск + МАКС с записью → прислать строки `mmp` (bell/rc/c).

### v1.2.236-v1.2.222 — галочки списка + карточка контакта + МАКС-диагностика + строка отправки + пузырь «↓N» (архив)
Перенесено в [archive/features-v1.2.222-236.md](archive/features-v1.2.222-236.md).

### v1.2.221-v1.2.214 — Порядок фото/«текст отдельно» + ВК-навигация vk.ru + фикс счётчика ВК + окно отправки (архив)

Перенесено в [archive/features-v1.2.214-221.md](archive/features-v1.2.214-221.md) (2026-08-13, разгрузка под лимит 100 КБ): ждём серверное подтверждение фото/альбома перед текстом — порядок у получателя (v1.2.214) + таймаут-WARN (v1.2.215); «перейти в чат» по клику ВК-уведомления под vk.ru + имя мессенджера в статусе (v1.2.216) + тест-репродюсер (v1.2.217) + доводка сравнения имени (v1.2.218); фантомная «1 непрочитано» ВК — счётчик перестал считать бейджи «Друзья/Игры» (v1.2.219); окно отправки фото/файла — одна кнопка «Отправить» + инфо-строка (v1.2.220); лента уведомлений — не дёргать список при чтении + видимая прокрутка (v1.2.221).

### v1.2.213-v1.2.205 — Окно отправки фото: подпись/лимит 1024/«текст отдельно»/порядок/диагностика (архив)

Перенесено в [archive/features-v1.2.205-213.md](archive/features-v1.2.205-213.md) (2026-08-11, разгрузка под лимит 100 КБ): фикс двойной вставки фото + разбит windowManager (v1.2.205) + подтверждающий лог (v1.2.206); окно фото — 7 функций просмотра (v1.2.207); счётчик подписи + «фото, затем текст отдельно», лимит Telegram 1024 (v1.2.209); лимит подписи и «текст отдельно» для видео/документов (v1.2.210); длинный текст режется ≤4096 и шлётся ПОСЛЕ загрузки фото — порядок (v1.2.211); доводки «ждём именно это фото» + число сообщений (v1.2.212); диагностика отправки — лог провала/номера/результат (v1.2.213).

### v1.2.204-v1.2.197 — Окно отправки фото: инструменты просмотра (зум/поворот/сдвиг) + много фото + память окна (архив)

Перенесено в [archive/features-v1.2.197-204.md](archive/features-v1.2.197-204.md) (2026-08-11, разгрузка под лимит 100 КБ): окно отправки фото с зумом/поворотом/сдвигом (v1.2.197-198), логи потока + прозрачность PNG + пропуск в альбоме (v1.2.199), запоминание «на весь экран» (v1.2.200), приподнятая карточка + растущее поле подписи (v1.2.201), чистая логика памяти окна + тесты (v1.2.202), несколько фото в одном сообщении (v1.2.203), кнопки в шапке + вставка Ctrl+V + свободнее зум (v1.2.204).

### v1.2.196–v1.2.185 — диагностика ВК/МАКС, установщик/Win32, прокрутка чата (архив)

Перенесено в [archive/features-v1.2.185-196.md](archive/features-v1.2.185-196.md).

### v1.2.184-v1.2.170 — Логотип Telegram/живой статус собеседника/переключатель режимов/фикс поиска/читаемость шапки (архив)

Перенесено в [archive/features-v1.2.170-184.md](archive/features-v1.2.170-184.md).

### v1.2.169-v1.2.161 — Фильтр аккаунтов (мультивыбор+соло) + самолечение + закрепы/ресайз рейла (архив)

Перенесено в [archive/features-v1.2.161-169.md](./archive/features-v1.2.161-169.md) (2026-07-31, разгрузка под лимит 100 КБ): откат «приклеенного» списка закреплённых (161/162); фильтр аккаунтов на левой панели, клик=вкл/выкл, двойной=соло, кнопка «Все» + доводки (163/164); ресайз левого рейла перетаскиванием (165); полоса «Закреплённое» — читаемый фон + переход к сообщению (166/167); фиксы «после удаления 2-го аккаунта оставшийся не грузился» + самолечение фильтра при старте (168/169).

### v1.2.160-v1.2.156 — Сага «перетаскивание закреплённых» (ОТКАЧЕНА в v1.2.161, архив)

Перенесено в [archive/features-v1.2.156-160.md](./archive/features-v1.2.156-160.md) (2026-07-30): серия попыток сделать ручное перетаскивание порядка закреплённых чатов (HTML5 drag → свой образ за курсором → живая перестановка → вынос в отдельный не-виртуальный `PinnedChatList` с Pointer Events). **Итог — откачено в v1.2.161**: закреплённые должны листаться в ОБЩЕМ списке, а не быть отдельным блоком; перетаскивание же требует отдельного списка. Урок: drag-порядок и виртуальный список react-window (строки по индексу-слоту) несовместимы без замены движка. Из серии сохранились только НЕ-drag доводки (цвет-грань уведомления по `accountId`, цвет-полоса в узком режиме).

### v1.2.155-v1.2.147 — Цвет-метки аккаунтов (различимые цвета/полоса/обводка) + фиксы входа/удаления 2-го аккаунта (архив)

Перенесено в [archive/features-v1.2.147-155.md](./archive/features-v1.2.147-155.md) (2026-07-31, разгрузка под лимит 100 КБ): цвет аккаунта в закреплённых/уведомлении (v1.2.155); устойчивые цвет-метки — первый свободный + сохранение вместо хеша (v1.2.154); полоса/обводка/выбор цвета 🎨 (v1.2.153); спиннер ввода кода Telegram (v1.2.152); карточка выхода не заезжает под панель задач + спиннер «думания» (v1.2.150-151); сброс «залипшего success» гейта входа + разгрузка NativeApp (v1.2.148-149); фикс «чёрный экран» после добавления 2-го аккаунта (v1.2.147).

### v1.2.146-v1.2.140 — Добавление/удаление аккаунта (диагностика + фиксы следов) + закреп 📌 + маскировка кода в логах (архив)

Перенесено в [archive/features-v1.2.140-146.md](./archive/features-v1.2.140-146.md) (2026-07-31, разгрузка под лимит 100 КБ): фикс папки сессии при удалении аккаунта (v1.2.145) + чистка кэш-файла/проброс переименования (v1.2.146); диагностика чёрного экрана и операций с аккаунтом + ErrorBoundary вокруг InboxMode (v1.2.142-143); маскировка кода входа в логах (v1.2.144); значок 📌 в углу аватарки (v1.2.141); откат авто-чистки осиротевших закреплений — теряла данные (v1.2.140).

### v1.2.139-v1.2.138 — Локальное закрепление чатов (📌): базовая фича + доработки (архив)

Перенесено в [archive/features-v1.2.138-139.md](./archive/features-v1.2.138-139.md) (2026-07-30, разгрузка под лимит 100 КБ): локальное закрепление чатов полоской+значком 📌, правый клик, хранение в localStorage, НЕ синхронизируется с Telegram (v1.2.138); доработки — 📌 в узком режиме, устойчивый порядок по массиву pinnedIds, вынос `filterSortChats`, чистка осиротевших (позже откачена v1.2.140) (v1.2.139). Перетаскивание порядка сделано позже (v1.2.156→160).

### v1.2.137-v1.2.134 — Уведомление снимается при прочтении на другом устройстве + CI-фикс TZ снапшота + ВК vk.ru (архив)

Перенесено в [archive/features-v1.2.134-137.md](./archive/features-v1.2.134-137.md) (2026-07-30, разгрузка под лимит 100 КБ): прочитал чат в Telegram на телефоне → карточка-уведомление снимается, частичное чтение + чистая `shouldDismissForRead` (v1.2.137); snapshot ChatListItem сделан TZ-независимым — падал на CI под UTC (v1.2.135); ВК основной адрес → vk.ru + убрана лишняя ошибка `GUEST_VIEW_MANAGER_CALL` запасного впрыска (v1.2.134).

### v1.2.133-v1.2.130 — Строка списка чатов: время + имя отправителя (группы) + «Вы:» + значок форума + имя автора в уведомлении (архив)

Перенесено в [archive/features-v1.2.130-133.md](./archive/features-v1.2.130-133.md) (2026-07-30, разгрузка под лимит 100 КБ): время последнего сообщения + имя автора в превью групп/форумов + значок 🗂️ форума (v1.2.130); «Вы:» для своих + единая `lastSenderLabel` (v1.2.131); имя автора в заголовке уведомления + чистка мёртвого `pickNotifTitle` (v1.2.132); фикс залипания имени в превью + вынос `nativeStoreLastMsgIpc.js` (v1.2.133).

### v1.2.129-v1.2.126 — Уведомления: «невидимая стена»/heartbeat-сторож + ВК переносы строк (архив)

Перенесено в [archive/features-v1.2.126-129.md](./archive/features-v1.2.126-129.md) (2026-07-30, разгрузка под лимит 100 КБ): гасим «невидимую стену» сразу по видимой высоте (v1.2.128), heartbeat-сторож без спама (v1.2.127) и его первая версия (v1.2.126); ВК — переносы строк/абзацы в тексте уведомления через `_vkNodeText` (v1.2.129).

### v1.2.125-v1.2.122 — Уведомление «поверх всех» + ВК-хук (разгрузка/эмодзи/mute-детект/переносы строк) (архив)

Перенесено в [archive/features-v1.2.122-125.md](./archive/features-v1.2.122-125.md) (2026-07-29, разгрузка под лимит 100 КБ): окно уведомления надёжно поверх всех — `screen-saver`+reassert (v1.2.125); ВК-хук — удалён мёртвый toast-наблюдатель + эмодзи из `<img alt>` (v1.2.124), сужен mute-селектор + тесты чистых функций (v1.2.123), сохранены переносы строк + снят предел текста 1000 (v1.2.122).

### v1.2.121-v1.2.116 — Telegram: не показываем сторис; ВК/vk.ru: диагностики значка 🔕 + текст целиком + фикс «не уведомлять о заглушённых» (архив)

Перенесено в [archive/features-v1.2.116-121.md](./archive/features-v1.2.116-121.md) (2026-07-29, разгрузка под лимит 100 КБ): Telegram Web — гасим системную фразу «опубликовал(а) историю» (v1.2.121); ВК/vk.ru — итоговый фикс «не уведомлять о заглушённых чатах 🔕» по видимости значка (v1.2.120), текст уведомления целиком без своей мерки (v1.2.119), серия временных диагностик значка «беззвучно» (v1.2.116-118, диагностики давно удалены).

### v1.2.115-v1.2.108 — ВК переезд на vk.ru (детект/текст/диагностика) + время в источнике (архив)

Перенесено в [archive/features-v1.2.108-115.md](./archive/features-v1.2.108-115.md) (2026-07-27, разгрузка под лимит 100 КБ): переезд ВК на vk.ru + определение домена (v1.2.109); настоящий детект новых сообщений по списку чатов (v1.2.112) + подтверждение логом (v1.2.113); чистый текст уведомления по структуре строки (v1.2.114); 3 фикса по ревью — чистка времени/шторм на старте/дубль (v1.2.115); серия временных диагностик структуры vk.ru (v1.2.110/111); время сообщения в строке источника, единый формат (v1.2.108).

### v1.2.107-v1.2.101 — Уведомления: сторож стены + видео/фото в карточке (архив)

Перенесено в [archive/features-v1.2.101-107.md](./archive/features-v1.2.101-107.md) (2026-07-24, разгрузка под лимит 100 КБ): сторож «невидимой стены» + чистая decideNotifResize + быстрее закрытие (v1.2.107); фикс осиротевшего положительного отчёта — «невидимое зависшее окно», ловящее клики (v1.2.106); «Прочитано» для native-Telegram (v1.2.105); видео в уведомлении — постер/плеер как в чате, видео-как-документ, крутилка по факту (v1.2.101-104).

### v1.2.100-v1.2.90 — Уведомления/видео/фото + диагностика Telegram (архив)

Перенесено в [archive/features-v1.2.90-100.md](./archive/features-v1.2.90-100.md) (2026-07-23, разгрузка под лимит 100 КБ): страховка крутилки одиночного фото (v1.2.100), «размытый фон» одиночного фото (v1.2.99), крупный аватар + источник в шапке (v1.2.98), раскладка «Стопка» для всех карточек (v1.2.96-97), одиночное фото/видео в native-уведомлении (v1.2.95), диагностика фото/ссылок Telegram native+web (v1.2.90/92/93/94), расчёт вертикали дока в чистую функцию + тест (v1.2.91).

### v1.2.89-v1.2.74 — Док/подсказка/закреп + имя аккаунта + Telegram-аватар + альбом (архив)

Перенесено в [archive/features-v1.2.74-89.md](./archive/features-v1.2.74-89.md): фиксы дока/подсказки/закрепа (стабильный якорь верха, панель задач Windows, снятие snap, rAF/throttling; v1.2.76-89), доведение имени аккаунта до закрепа/подсказки (v1.2.83-88), Telegram аватар+источник в уведомлении (v1.2.75), листание альбома + чёткие плитки (v1.2.74).

### v1.2.73-v1.2.59 — Док/подсказка/закреп: серия фиксов (архив)

Перенесено в [.memory-bank/archive/features-v1.2.59-73.md](archive/features-v1.2.59-73.md) при разбиении (21 июля 2026). Архитектура — decisions.md ADR-017/ADR-018.

### v1.2.58-v1.2.46 — VK: тост/sidebar/active-chat уведомления, MAX dedup, ribbon (архив)

Перенесено в [.memory-bank/archive/features-v1.2.46-58.md](archive/features-v1.2.46-58.md) при разбиении (17 июля 2026).

### v1.2.45-v1.2.37 — VK DOM observer / диагностика уведомлений (архив)

Перенесено в [.memory-bank/archive/features-v1.2.37-45.md](archive/features-v1.2.37-45.md) при разбиении (17 июля 2026).

### v1.2.36-v1.2.30 — диагностика MAX/системы (архив)

Полные записи перенесены в [archive/features-v1.2.30-36.md](./archive/features-v1.2.30-36.md), чтобы active features.md оставался меньше лимита memory-bank. Суть: диагностика стала ручной, фоновой, с последним отчётом, полной MAX/VK-цепочкой и понятными кнопками; pipeline уведомлений этим блоком не менялся, кроме описанных в соответствующих версиях точечных MAX/VK исправлений.
### v1.2.29 — диагностика запускается только вручную и не мешает работе (архив)

Полная запись перенесена в [archive/features-v1.2.29.md](./archive/features-v1.2.29.md), чтобы активный features.md оставался меньше лимита memory-bank. Суть: большая модалка диагностики больше не стартует запись сама, управление записью стало явным, общий лог не очищается.
### v1.2.28 — MAX: sidebar watcher не создаёт фантомы во время поиска (архив)

Полная запись перенесена в [archive/features-v1.2.28.md](./archive/features-v1.2.28.md), чтобы активный features.md оставался меньше лимита memory-bank. Суть: `max-sidebar` при активном поиске MAX обновляет baseline, но не создаёт ribbon из старого preview; реальные `max-notification-api` и `max-sw-showNotification` не отключались.

**Структура файла**: этот features.md содержит только **последние активные версии**. Старое — в архиве:

| Архив | Содержимое | Размер |
|---|---|---|
| [`archive/features-v0.95.39.md`](./archive/features-v0.95.39.md) | v0.95.39 (убран twoPhase + RAF×2 + 350мс easeOutCubic; стабилизировано v0.95.40+) | ~2 КБ |
| [`archive/features-v0.95.40.md`](./archive/features-v0.95.40.md) | v0.95.40 (большие emoji + a11y reduced-motion + sticky bottom on media; стабилизировано v0.95.41+) | ~3 КБ |
| [`archive/features-v0.95.41.md`](./archive/features-v0.95.41.md) | v0.95.41 (Custom emoji premium WebM/WebP + reduced-motion интеграционные тесты; стабилизировано v0.95.42+) | ~3 КБ |
| [`archive/features-v0.95.42.md`](./archive/features-v0.95.42.md) | v0.95.42 (сохранение поиска + история + ✕ + подсветка совпадений; стабилизировано v0.95.43+) | ~2 КБ |
| [`archive/features-v0.95.38.md`](./archive/features-v0.95.38.md) | v0.95.38 (фикс дубля сообщений через updateMessageSendSucceeded, ⏳ индикатор, mistakes static guard) | ~2 КБ |
| [`archive/features-v0.95.35-37.md`](./archive/features-v0.95.35-37.md) | v0.95.35-37 (fade-in changelog, auto-scroll outgoing-other-device, sending_state polish, mistakes/outgoing-two-cases.md; стабилизировано v0.95.38+) | ~3 КБ |
| [`archive/features-v0.95.34.md`](./archive/features-v0.95.34.md) | v0.95.34 (темовые vars в :root, вспышка bubble, WhatsNewModal UX; стабилизировано v0.95.40+) | ~2 КБ |
| [`archive/features-v0.95.33.md`](./archive/features-v0.95.33.md) | v0.95.33 (фикс «цвет не применяется» через querySelectorAll, регресс-тест blur, деловой стиль; финал в v0.95.34) | ~2 КБ |
| [`archive/features-v0.95.32.md`](./archive/features-v0.95.32.md) | v0.95.32 (производительность WhatsNewModal: убран backdrop-filter + contain + деловой стиль changelog) | ~2 КБ |
| [`archive/features-v0.95.31.md`](./archive/features-v0.95.31.md) | v0.95.31 (аккаунты вниз + drag-n-drop + multi-user typing + throttle реакций; стабилизировано v0.95.34+) | ~3 КБ |
| [`archive/features-v0.95.30.md`](./archive/features-v0.95.30.md) | v0.95.30 (плавная auto-scroll + 5 цветовых тем + dropdown + opacity 0.95; стабилизировано v0.95.33-34) | ~3 КБ |
| [`archive/features-v0.95.29.md`](./archive/features-v0.95.29.md) | v0.95.29 (реакции 👍❤️🔥 + Telegram-style header + General 📢 + render-counter; стабилизировано v0.95.31-34) | ~5 КБ |
| [`archive/features-v0.95.28.md`](./archive/features-v0.95.28.md) | v0.95.28 (Telegram-style auto-scroll к новому + ↓N без «слепой зоны» Schmitt; стабилизировано v0.95.31) | ~4 КБ |
| [`archive/features-v0.95.27.md`](./archive/features-v0.95.27.md) | v0.95.27 (расширенная диагностика send pipeline; стабилизировано v0.95.29) | ~3 КБ |
| [`archive/features-v0.95.23-26.md`](./archive/features-v0.95.23-26.md) | v0.95.23 – v0.95.26 (курсор в input, initial backfill, voice/spellcheck/action-bar/WhatsNew, фикс 47-дневного бага unreadCount; стабилизировано) | ~15 КБ |
| [`archive/features-v0.95.19-22.md`](./archive/features-v0.95.19-22.md) | v0.95.19 – v0.95.22 (диагностика tg-new-message, финал jump-to-end, бейдж форум-группы, форум-overlay; стабилизировано) | ~6 КБ |
| [`archive/features-v0.95.15-18.md`](./archive/features-v0.95.15-18.md) | v0.95.15 – v0.95.18 (итеративный fetch + форум-топики + двухфазный scroll + ForumTopicEmptyState; стабилизировано v0.95.20-21) | ~8 КБ |
| [`archive/features-v0.95.12-14.md`](./archive/features-v0.95.12-14.md) | v0.95.12 – v0.95.14 (3 итерации jump-to-end до итеративного fetch v0.95.15, полная сага в jump-to-end-saga.md) | ~32 КБ |
| [`archive/features-v0.95.8-9.md`](./archive/features-v0.95.8-9.md) | v0.95.8 – v0.95.9 (счётчик ↓ обнуляется + анимация + live compact + порог 128) | ~14 КБ |
| [`archive/features-v0.95.5-7.md`](./archive/features-v0.95.5-7.md) | v0.95.5 – v0.95.7 (sticky pinned overlay, кнопка ↓ Telegram-style, drag-to-resize) | ~24 КБ |
| [`archive/features-v0.95.0-3.md`](./archive/features-v0.95.0-3.md) | v0.95.0 – v0.95.3 (контигуити-фикс, afterId load-newer, мигание ↓ Schmitt trigger, диагностика «дёрг») | ~13 КБ |
| [`archive/features-v0.94.1-7.md`](./archive/features-v0.94.1-7.md) | v0.94.1 – v0.94.7 (TDLib listener leak, scroll caskade, спокойная загрузка, пилюля прогресса — стабилизированы v0.95.0-2) | ~24 КБ |
| [`archive/features-v0.93.0.md`](./archive/features-v0.93.0.md) | v0.93.0 (pixel-perfect scroll restore через LocationOptions.offset, superseded v0.94.0) | ~7 КБ |
| [`archive/features-v0.92.0-6.md`](./archive/features-v0.92.0-6.md) | v0.92.0 – v0.92.6 (сага Virtuoso scroll restore, superseded v0.94.0) | ~37 КБ |
| [`archive/features-v0.91.11-24.md`](./archive/features-v0.91.11-24.md) | v0.91.11 – v0.91.24 (сага scroll restore: 13 версий → миграция на virtuoso) | ~47 КБ |
| [`archive/features-v0.91.1-10.md`](./archive/features-v0.91.1-10.md) | v0.91.1 – v0.91.10 (initial-load, scroll-jump, newBelow, forum topics, updateChatLastMessage) | ~12 КБ |
| [`archive/features-v0.87.106-114.md`](./archive/features-v0.87.106-114.md) | v0.87.106 – v0.87.114 (multi-account UI финал, кнопки режимов, мьют чата, аватарки отправителей в группах) | ~22 КБ |
| [`archive/features-v0.87.93-105.md`](./archive/features-v0.87.93-105.md) | v0.87.93 – v0.87.105 (multi-account native, login flow, разбиения, cc-media://) | ~30 КБ |
| [`archive/features-v0.87.80-92.md`](./archive/features-v0.87.80-92.md) | v0.87.80 – v0.87.92 (pre-push hook, разбиения 4-7, AccountContextMenu) | ~16 КБ |
| [`archive/features-v0.87.65-79.md`](./archive/features-v0.87.65-79.md) | v0.87.65 – v0.87.79 (план разбиения 1-3, pre-push hook, bubble UI) | ~54 КБ |
| [`archive/features-v0.87.51-64.md`](./archive/features-v0.87.51-64.md) | v0.87.51 – v0.87.64 (groupedUnread удалён, pre-commit hook) | ~54 КБ |
| [`archive/features-v0.87.40-50.md`](./archive/features-v0.87.40-50.md) | v0.87.40 – v0.87.50 (итерации native scroll + unread) | ~40 КБ |
| [`archive/features-v0.87-early.md`](./archive/features-v0.87-early.md) | v0.87.0 – v0.87.39 (запуск native + ранние фиксы) | ~140 КБ |
| [`archive/features-pre-v0.87.md`](./archive/features-pre-v0.87.md) | v0.1.0 – v0.86.10 (до native-режима, 3 марта – 14 апреля 2026) | ~210 КБ |

**Архив не читается по умолчанию.** Запрос к нему — только при явной просьбе («что было в v0.85», «покажи старый changelog»).

**До рефакторинга v0.87.57** файл был 445 КБ (3371 строк, 323 версии). После — ~100 КБ в корне.

---

### Архив v1.2.27 и старше

Подробная история v1.2.27 и более ранних версий перенесена в [archive/features-v1.2.27-and-older.md](./archive/features-v1.2.27-and-older.md). Активный файл держим компактным, чтобы memory-bank быстро читался и проходил лимиты.
