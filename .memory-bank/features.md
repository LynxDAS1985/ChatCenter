# Реализованные функции — ChatCenter

## Текущая версия: v1.2.407 (4 сентября 2026)

### v1.2.407 — Единый экран загрузки: доводка по ревью (упрощён NativeAppFallback + тест-страж фазы «скелет»)

**ЗАЧЕМ**: по итогам самопроверки v1.2.406 закрыты два долга. (1) Кружки анимации загрузки были задублированы в ТРЁХ местах (стартовая `index.html`, наш экран CSS, заглушка `NativeAppFallback`) — при правке вида легко забыть одно. (2) На новую промежуточную фазу «скелет» не было автотеста — её мог кто-то случайно убрать, а поймали бы только по жалобе.

**ЧТО**:
1. [appFallbacks.jsx](src/appFallbacks.jsx) `NativeAppFallback` (заглушка главной области, пока `NativeApp` грузится через `lazy()`): убраны кружки/градиенты/счётчик/полоса → простой тёмный экран с подписью «Загрузка чатов…». Причина: эту фазу (0 → монтирование NativeApp) теперь ПЕРЕКРЫВАЕТ стартовая заставка `#cc-splash` (boot-hold, v1.2.404 — заставка держится до показа нашего экрана «загрузка чатов»), поэтому `NativeAppFallback` фактически не виден. Простой экран оставлен запасом, если заставку по какой-то причине сняли раньше. Кружки теперь только в ДВУХ местах (`index.html` + `ChatListLoadingSplash`).
2. [ChatListLoadingSplash.vitest.jsx](src/native/components/ChatListLoadingSplash.vitest.jsx): +3 проверки source-guard (читают исходник) — есть переход `setPhase('skeleton')` + ветка рендера скелета + шапка «ЦентрЧатов»; в CSS есть шиммер `@keyframes native-chatload-shim` и стиль `.native-chatload-brand`. Ловит удаление промежуточного скелета/шапки.

**КАК РАБОТАЕТ / ОТКАТ**: поведение видимого экрана НЕ изменилось (упрощена только невидимая заглушка). Откат — `git checkout src/appFallbacks.jsx src/native/components/ChatListLoadingSplash.vitest.jsx`.

**КАК ПРОВЕРЕНО**: линт 0; `ChatListLoadingSplash.vitest` 11/11; полный vitest (см. запись сессии). **Требует визуальной проверки** пользователем (перезапуск: заставка → кружки → скелет → список; заглушка не должна мелькать).

**ГРАНИЦА**: `index.html` (стартовая заставка) грузится ДО общих стилей React → её кружки нельзя вынести в общий CSS, полное объединение в одно место невозможно (только сближение вида вручную).

### v1.2.406 — Единый экран загрузки (Вариант 1, согласован по макету): «ЦентрЧатов» + кружки/аватарки/счётчик → СКЕЛЕТ списка → список

**ЗАЧЕМ**: согласованный с пользователем макет (артефакт, Вариант 1). Один экран от старта: «ЦентрЧатов» сверху → кружки с аватарками + счётчик → плавный переход через СКЕЛЕТ (серые строки списка с бегущим бликом) → реальный список. Убрать лишние экраны.

**ЧТО**:
1. [ChatListLoadingSplash.jsx](src/native/components/ChatListLoadingSplash.jsx): (а) шапка «ЦентрЧатов» сверху (`.native-chatload-brand`, градиент как на стартовой); (б) новая фаза `phase='skeleton'` — после анимации кружков (+завершения загрузки) показывается СКЕЛЕТ списка ~1.1с, затем плавный уход (fade) → реальный список. Фазы через setTimeout в useEffect+cleanup (React docs «Synchronizing with Effects»).
2. [styles-chatlist-loading.css](src/native/styles-chatlist-loading.css): стили шапки + скелета (шиммер `native-chatload-shim`).
3. [index.html](index.html) стартовая заставка (`#cc-splash`, до React): добавлены ТЕ ЖЕ кружки + «ЦентрЧатов» + «Загрузка чатов…» (убрана полоса) → стартовая и наш экран выглядят как ОДИН непрерывный (кружки от старта до скелета). boot-hold (v1.2.404) оставлен → generic-экрана между ними нет.

**ИТОГ последовательности**: «ЦентрЧатов» + кружки (старт, долгая загрузка кода) → кружки с аватарками + растущий счётчик (когда пришли данные) → СКЕЛЕТ списка ~1с → реальный список. Один вид, без лишних экранов.

**КРАЙНИЕ СЛУЧАИ**: 0 аккаунтов → boot-таймер 0.8с; `prefers-reduced-motion` → шиммер/анимации выключены; скелет — фиксированные ~1.1с (не завязан на данные). **ОПАСНАЯ ЗОНА**: `index.html` — стартовая заставка ДО React; правка только разметки/стилей (кружки), структура сохранена; если сломать — заставка криво, но приложение грузится (снимет appReady/страховка 180с). **КАК ПРОВЕРЕНО**: `lint` 0; `ChatListLoadingSplash.vitest` 8/8; `appStructure` 60/60; `startupDiagnostics` 4/4; `fileSizeLimits` 602/602; полный `vitest`. **Требует визуальной проверки** (boot-заставку и скелет autotest'ом не покрыть): перезапуск → «ЦентрЧатов»+кружки → аватарки+счётчик → скелет → список. **ОТКАТ**: `git checkout index.html src/native/components/ChatListLoadingSplash.jsx src/native/styles-chatlist-loading.css` + версия 1.2.405.

### v1.2.405 — Заставка загрузки чатов на ВСЁ окно (была в области чата, сбоку от полосы аккаунтов)

**ЗАЧЕМ**: жалоба — заставка с аватарками+счётчиком показывалась «в окне чата» (в области контента), а полоса аккаунтов торчала слева; надо в БОЛЬШОМ окне (как «второй экран» NativeAppFallback). **КОРЕНЬ (по коду, 🟢)**: `ChatListLoadingSplash` рендерится в `InboxMode` (область список+чат), а полоса аккаунтов рисуется ОТДЕЛЬНЫМ порталом на уровень App ([NativeApp.jsx:361](src/native/NativeApp.jsx) `createPortal` в `#app-native-rail`). CSS `.native-chatload` был `position:absolute` (относительно InboxMode) → заставка накрывала только список+чат, полоса оставалась видна сбоку. «Второй экран» (NativeAppFallback) был на всё окно, т.к. рисуется ДО монтирования NativeApp (полосы ещё нет).

**ЧТО** ([styles-chatlist-loading.css](src/native/styles-chatlist-loading.css)): `.native-chatload` — `position:absolute` → **`position:fixed; inset:0; z-index:9000`** → заставка перекрывает ВСЁ окно (полосу аккаунтов + список + чат + верхнюю панель), как второй экран. z-index ниже стартовой заставки (99999). Компонент НЕ двигал — только позиционирование. Проверено: на нативных контейнерах нет статического `transform` (только keyframes у WhatsNewModal) → `fixed` корректно относительно окна.

**КРАЙНИЕ СЛУЧАИ**: `--amoled-*` переменные доступны (элемент всё ещё в дереве `.native-mode`, `fixed` меняет только позицию) + фолбэки в CSS; `--leaving` fade и pointer-events работают. **КАК ПРОВЕРЕНО**: `lint` 0; `ChatListLoadingSplash.vitest` 8/8; `fileSizeLimits` 602/602; полный `vitest`. **Требует визуальной проверки**: перезапуск → анимация загрузки на ВСЁ окно (полосы аккаунтов сбоку не видно). **Остаточный риск**: если у окна есть редкий CSS-эффект на предке (filter/contain/transform во время анимации монтирования) — `fixed` может привязаться к предку; тогда портал в body. **ОТКАТ**: `git checkout src/native/styles-chatlist-loading.css` + версия 1.2.404.

### v1.2.404 — Загрузка чатов: ВИДИМАЯ анимация (счётчик реально растёт 0→N, аватарки по одной за ~3.5с) + убран «третий» generic-экран

**ЗАЧЕМ**: жалоба — счётчик стоял «0 из 890», потом МГНОВЕННО список; загрузка «не идёт»; плюс «третий экран». КОРЕНЬ (по коду + журналу, 🟢): чаты приходят из кэша ОДНИМ пакетом в самом конце (loadCachedChats/loadChats резолвится разом), а `chatsFirstLoadDone` наступает почти одновременно → счётчик привязан к `chatsLen`, который прыгает 0→N ровно когда заставка ГАСНЕТ → «загрузки» не видно. Реального «по одному» у чатов нет — долго грузится КОД (dev ~2 мин), а чаты — миг. «Третий экран» — промежуточный generic `NativeAppFallback` между стартовой и нашим экраном (v1.2.403 снимал стартовую на appReady).

**ЧТО (видимая анимация + один экран)**:
1. [ChatListLoadingSplash.jsx](src/native/components/ChatListLoadingSplash.jsx): счётчик/аватарки/полоса теперь гонит `progress` 0→1 за ~3с (rAF, React docs «Synchronizing with Effects»), НЕ привязан к моменту прихода данных. Счётчик растёт 0→total, аватарки проявляются ПО ОДНОЙ (`revealed = round(progress·5)`), полоса заполняется. `MIN_VISIBLE` 2200→**3500мс** — держим заставку ≥3.5с, чтобы анимация (≈3с) прошла НА ГЛАЗАХ, а не мелькнула.
2. Убран «третий экран»: стартовую заставку (`index.html`) снова НЕ снимаем на appReady ([useAppBootstrap.js](src/hooks/useAppBootstrap.js)); её убирает наш экран при показе ([ChatListLoadingSplash.jsx] `__ccHideSplash`) + запасной таймер 0.8с в [NativeApp.jsx](src/native/NativeApp.jsx). Итог: «ЦентрЧатов» → СРАЗУ анимация загрузки (без generic-экрана) → список.

**ЧЕСТНО**: анимация — «красивая индикация», а не реальный счёт по одному чату (чаты приходят пакетом; реального пошагового прогресса нет). Так делают многие приложения (минимальная анимация загрузки). Задержка старта +~3.5с (осознанно — ради видимой загрузки). В dev стартовая «ЦентрЧатов» всё равно долгая (грузится код), анимация — после неё.

**КРАЙНИЕ СЛУЧАИ**: 0 аккаунтов → запасной таймер 0.8с снимает стартовую; total=0 (первый запуск) → «Собираем чаты…»; данные грузятся дольше 3с → progress=1 (счётчик на total, все аватарки), ждём завершения; `prefers-reduced-motion` → без анимаций. **КАК ПРОВЕРЕНО**: `lint` 0; `ChatListLoadingSplash.vitest` 8/8; `fileSizeLimits` 602/602; полный `vitest`. **Требует визуальной проверки**: перезапуск → «ЦентрЧатов» → видимая анимация (счётчик растёт, аватарки по одной, полоса) ≥3с → список; промежуточного generic-экрана нет. **ОТКАТ**: `git checkout src/native/components/ChatListLoadingSplash.jsx src/hooks/useAppBootstrap.js src/native/NativeApp.jsx` + версия 1.2.403.

### v1.2.403 — Загрузка чатов по согласованному макету: кружки на ВСЮ загрузку + аватарки проявляются + счётчик РАСТЁТ (откат v1.2.402)

**ЗАЧЕМ**: по макету (артефакт, согласован с пользователем). Жалоба на v1.2.402 — я зря спрятал «средний экран» кружков за бренд-заставку, из-за чего кружки с аватарами мелькали в КОНЦЕ (когда чаты уже загружены), а не бежали ВСЮ загрузку. Надо: старт «ЦентрЧатов» → кружки бегают всю сборку → на них ПРОЯВЛЯЮТСЯ аватарки + РАСТЁТ счётчик «Загружено N из M» → список.

**ЧТО**:
1. **Откат v1.2.402** — стартовая заставка снова снимается на `appReady` ([useAppBootstrap.js](src/hooks/useAppBootstrap.js)); убран запасной таймер снятия в [NativeApp.jsx](src/native/NativeApp.jsx) и вызов `__ccHideSplash` из [ChatListLoadingSplash.jsx](src/native/components/ChatListLoadingSplash.jsx). Теперь после appReady видна ЖИВАЯ заставка кружков (NativeAppFallback → ChatListLoadingSplash) на всю загрузку, а не бренд-заставка.
2. **Счётчик РАСТЁТ** ([ChatListLoadingSplash.jsx](src/native/components/ChatListLoadingSplash.jsx)) — новое `displayCount` плавно анимируется от 0 к реальному числу чатов за ~1.2с (rAF в useEffect+cleanup, React docs «Synchronizing with Effects»), даже если чаты пришли из кэша разом. Показывается «Загружено N из M».
3. **Аватарки ПРОЯВЛЯЮТСЯ** — аватар теперь отдельный слой `.native-chatload-face` поверх цветного кружка с fade-in (0.5с), а не мгновенная подмена фона ([styles-chatlist-loading.css](src/native/styles-chatlist-loading.css)).
4. **Прогресс-полоса** под счётчиком (как на макете) — в ChatListLoadingSplash и на раннем экране.
5. **Ранний экран выровнен** ([appFallbacks.jsx](src/appFallbacks.jsx)) — «Сборка чатов…» + «Загружено 0 из <из прошлого запуска>» + полоса, кружки 38px (как в основной заставке) → переход бесшовный.

**ЧЕСТНЫЙ ПРЕДЕЛ (в макете отмечен)**: реальные аватарки и точный счётчик появляются, когда загрузился код чат-части; в dev это ближе к концу (~2 мин грузится код). Но кружки бегут ВСЮ загрузку (ранний экран), а аватарки+рост счётчика — когда данные пришли. В установленной сборке всё быстро и плавно.

**КРАЙНИЕ СЛУЧАИ**: 0 аккаунтов → кружки generic + «Готовим…»; hint=0 (первый запуск) → «Собираем чаты…» без «из N»; happy-dom тест (rAF не бежит) → счётчик остаётся 0 (тест ждёт `\d+ из 1`). **КАК ПРОВЕРЕНО**: `lint` 0; `ChatListLoadingSplash.vitest` 8/8; `startupDiagnostics` 4/4; `rendererConsoleGuard` 0; `fileSizeLimits` 602/602 (бюджет 35580); полный `vitest`. **Требует визуальной проверки**: перезапуск → «ЦентрЧатов» → кружки бегут → аватарки проявляются + счётчик растёт + полоса → список. **ОТКАТ**: `git checkout src/hooks/useAppBootstrap.js src/native/NativeApp.jsx src/native/components/ChatListLoadingSplash.jsx src/appFallbacks.jsx src/native/styles-chatlist-loading.css` + версия на 1.2.402.

### v1.2.402 — Загрузка чатов: ОДИН экран вместо двух — стартовая заставка держится до появления нашего экрана чатов (бесшовно)

**ЗАЧЕМ**: жалоба — всё ещё ДВА экрана загрузки чатов подряд: (1) кружки БЕЗ аватарок «Загрузка чатов…» (`NativeAppFallback`), (2) кружки С аватарками+счётчиком «Загружаем X… Загружено N из M» (`ChatListLoadingSplash`). Хотели ОДИН: кружки бегают, по мере загрузки на них появляются аватарки, счётчик — там же.

**КОРЕНЬ (по коду, 🟢)**: это ДВА разных компонента, сменяющих друг друга на границе загрузки кода нативного интерфейса. `NativeAppFallback` показывается, ПОКА грузится код NativeApp (у него НЕТ данных чатов → generic-кружки). `ChatListLoadingSplash` (реальные аватары+счётчик) — ВНУТРИ NativeApp, показывается ПОСЛЕ его монтирования. Стартовую заставку (`index.html #cc-splash`) снимали на `appReady` ([useAppBootstrap.js:92](src/hooks/useAppBootstrap.js)), а NativeApp монтируется ПОЗЖЕ appReady → между ними виден generic-экран `NativeAppFallback`. Отсюда «два экрана».

**ЧТО (Вариант «стартовая держится до нашего экрана»)**:
1. [useAppBootstrap.js:92](src/hooks/useAppBootstrap.js): убран вызов `window.__ccHideSplash?.()` на appReady — стартовая заставка НЕ снимается раньше времени.
2. [ChatListLoadingSplash.jsx](src/native/components/ChatListLoadingSplash.jsx): когда наш экран чатов ПОКАЗАЛСЯ — сам зовёт `window.__ccHideSplash?.()` → БЕСШОВНАЯ передача (стартовая гаснет ровно когда появляются кружки+аватары+счётчик; промежуточного generic-экрана НЕ видно).
3. [NativeApp.jsx](src/native/NativeApp.jsx): запасное снятие стартовой через 0.8с после монтирования — на случай 0 аккаунтов (заставка чатов не покажется) → не застреваем на стартовой.

**ИТОГ**: старт «ЦентрЧатов/Загрузка…» (бренд) → ОДИН экран чатов (кружки с аватарами + «Загружено N из M») → список. Generic-кружки `NativeAppFallback` больше не видны (перекрыты стартовой до появления нашего экрана). **ЧЕСТНО про «аватарки появляются по мере загрузки»**: чаты грузятся из КЭША разом, поэтому аватары появляются на кружках сразу при показе экрана (не по одному) — это предел (кэш отдаёт всё сразу), но экран ОДИН.

**КРАЙНИЕ СЛУЧАИ**: 0 аккаунтов → запасной таймер 0.8с снимает стартовую; заставка чатов не покажется, но и не нужна. Если `__ccHideSplash` не вызовется вовсе — ловит дальний предохранитель boot-probe 180с. **КАК ПРОВЕРЕНО**: `lint` 0; `ChatListLoadingSplash.vitest` 8/8; `startupDiagnostics` 4/4; `fileSizeLimits` 602/602; полный `vitest`. **Требует визуальной проверки**: перезапуск → «ЦентрЧатов/Загрузка» → сразу наш экран (кружки+аватары+счётчик), БЕЗ промежуточного generic-экрана. **ОТКАТ**: `git checkout src/hooks/useAppBootstrap.js src/native/NativeApp.jsx src/native/components/ChatListLoadingSplash.jsx` + версия на 1.2.401.

### v1.2.401 — Заставка загрузки чатов — ОДНА на всю область (была «в три экрана»), убрана из панели списка

**ЗАЧЕМ**: жалоба — при старте ТРИ экрана подряд: (1) стартовая «ЦентрЧатов/Загрузка…», (2) кружки на всю область «Загрузка чатов…» (NativeAppFallback), (3) ещё заставка с аватарами+счётчиком «Загружаем БНК… Загружено 0 из 875» в УЗКОЙ панели списка. Хотели: после стартовой — кружки на всю область, на них появляются аватары мессенджеров + счётчик, и НИЧЕГО отдельного в панели списка.

**ЧТО**: заставка `ChatListLoadingSplash` (с реальными аватарами чатов + «Загружено N из M») ПЕРЕНЕСЕНА из [InboxChatListSidebar.jsx](src/native/components/InboxChatListSidebar.jsx) (узкая панель списка) на уровень [InboxMode.jsx](src/native/modes/InboxMode.jsx) — overlay `position:absolute inset:0` (корневой div InboxMode сделан `position:relative`), закрывает ВСЮ область (список + окно чата). Из панели списка заставка убрана. Итог: старт «ЦентрЧатов» → кружки на всю область (NativeAppFallback, generic) → при монтировании NativeApp эстафету принимает та же заставка на всю область с РЕАЛЬНЫМИ аватарами + счётчиком → список. Один непрерывный экран вместо трёх.

**КРАЙНИЕ СЛУЧАИ**: при поиске (`!search`) заставка не показывается. Аккаунт-рейл (слева, вне InboxMode) остаётся виден — заставка на области контента (список+чат). Тесты-стражи обновлены: `ChatListLoadingSplash.vitest.jsx` — заставка теперь в InboxMode (не в сайдбаре), плюс регресс-страж «сайдбар БОЛЬШЕ не рендерит заставку» (если вернут — снова «три экрана»). **КАК ПРОВЕРЕНО**: `lint` 0; `ChatListLoadingSplash.vitest`+`InboxChatListSidebar.vitest` 23/23; `fileSizeLimits` 602/602 (InboxMode 1119/1122); полный `vitest`. **Требует визуальной проверки**: перезапуск → кружки/аватары+счётчик на всю область, в панели списка отдельной заставки нет. **ОТКАТ**: `git checkout src/native/modes/InboxMode.jsx src/native/components/InboxChatListSidebar.jsx src/native/components/ChatListLoadingSplash.vitest.jsx` + версия на 1.2.400.

### v1.2.400 — Ранний экран загрузки «ЦентрЧатов» = живая заставка с кружками (а не сухое «Загрузка…»)

**ЗАЧЕМ**: жалоба — после старта на «ЦентрЧатов» (v1.2.399) в главной области сухая надпись «Загрузка…», а не наша заставка с кружками («где наша разработка?»). Причина: `NativeAppFallback` ([appFallbacks.jsx](src/appFallbacks.jsx)) показывается, ПОКА грузится КОД нативного интерфейса (в dev ~115с), а наша `ChatListLoadingSplash` живёт ВНУТРИ NativeApp → может показаться только после его монтирования. Т.е. ранний экран был отдельным «Загрузка…».

**ЧТО** ([appFallbacks.jsx](src/appFallbacks.jsx)): `NativeAppFallback` теперь рисует ЖИВУЮ заставку — 5 градиентных кружков, вспыхивающих по очереди (как `ChatListLoadingSplash`), + «Загрузка чатов…». **Самодостаточно**: этот компонент грузится РАНЬШЕ нативного CSS (`styles-chatlist-loading.css` импортится из NativeApp) и без `store` → используются ТОЛЬКО инлайн-стили + инлайн-`@keyframes ccNfPop` (className-классы заставки тут ещё не подгружены; кружки без реальных имён — их нет до загрузки store). Так «наша разработка» видна С НАЧАЛА загрузки, а не только в конце. После монтирования NativeApp эстафету принимает настоящая `ChatListLoadingSplash` (с реальными именами чатов).

**КРАЙНИЕ СЛУЧАИ**: инлайн-keyframes глобальны, но имя `ccNfPop` уникально (без конфликтов). **DEV-оговорка**: в dev эта заставка висит ~115с (медленная сборка), в установленной сборке — секунды. **КАК ПРОВЕРЕНО**: `lint` 0; `startupDiagnostics` 4/4, `appStructure` 60/60, `rendererConsoleGuard` 0; `fileSizeLimits` 602/602 (бюджет 35510); полный `vitest`. **Требует визуальной проверки**: перезапуск → на раннем экране «ЦентрЧатов» кружки, а не сухое «Загрузка…». **ОТКАТ**: `git checkout src/appFallbacks.jsx` + версия на 1.2.399.

### v1.2.399 — Старт открывается на «ЦентрЧатов» (чаты+заставка), а НЕ на первом веб-мессенджере (Макс) + фикс лога заставки

**ЗАЧЕМ**: жалоба — при запуске сначала грузится окно Макс, потом резко все чаты; заставки не видно; чаты должны грузиться С НАЧАЛА, а не после Макса.

**КОРЕНЬ (по коду + журналу, 🟢)**: [useAppBootstrap.js:54](src/hooks/useAppBootstrap.js) на старте ставил активной `withNative[0]?.id` — ПЕРВЫЙ веб-мессенджер. А нативная вкладка «ЦентрЧатов» (`NATIVE_CC_TAB`) добавляется В КОНЕЦ списка ([useAppBootstrap.js:50](src/hooks/useAppBootstrap.js)) → `withNative[0]` = первый webview (у пользователя `custom_1772704264107` = Макс, подтверждено журналом `messengers loaded ids=custom_1772704264107,...`). Итог: приложение всегда открывалось на Максе, он грузился первым, чаты — потом. Веб-вкладки видны стопкой (активная сверху, z-index 2 — [App.jsx:774](src/App.jsx)), поэтому был виден Макс.

**ЧТО**:
1. [useAppBootstrap.js:54](src/hooks/useAppBootstrap.js): активной на старте ставим **`NATIVE_CC_ID`** («ЦентрЧатов»), если он в списке (иначе запасной прежний — первый веб). Веб-мессенджеры всё равно грузятся в фоне (стопка, НЕ `display:none` — иначе Chromium усыпит) → уведомления из них работают; меняется только КАКАЯ вкладка видна первой. Теперь старт: «Загрузка» → список чатов + заставка сборки, без Макса впереди.
2. [ChatListLoadingSplash.jsx](src/native/components/ChatListLoadingSplash.jsx) — фикс лога v1.2.398: метку времени показа ставили по `!rendered`, но при первом монтировании `rendered` уже мог быть `true` (`useState(show)`) → метка не ставилась → в лог шла мусорная «длительность» = сам таймстамп (`~1788506507794мс`). Теперь метка по `shownAtRef` (первый раз, когда show=true) + сброс после скрытия. Лог `заставка ПОКАЗАНА` / `скрыта, была видна ~Nмс` теперь корректен.

**КРАЙНИЕ СЛУЧАИ**: native_cc нет в списке (сбой) → запасной вариант прежний (первый веб). Веб-уведомления работают (вкладки в фоне). **DEV-оговорка**: native грузится ~+115с в dev (медленная раздача модулей) — на это время видно «Загрузка», затем чаты+заставка; в установленной сборке — секунды. **КАК ПРОВЕРЕНО**: `lint` 0; `ChatListLoadingSplash.vitest` 8/8; `fileSizeLimits` 602/602; полный `vitest`. **Требует визуальной проверки**: перезапуск → приложение открывается на ЦентрЧатов (не Макс); в журнале `[chatload] заставка ПОКАЗАНА` + корректная `видна ~Nмс`. **ОТКАТ**: `git checkout src/hooks/useAppBootstrap.js src/native/components/ChatListLoadingSplash.jsx` + версия на 1.2.398.

### v1.2.398 — Заставка сборки чатов: гарантия минимального времени видимости (мелькала) + точная запись показа/скрытия в журнал

**ЗАЧЕМ**: жалоба — «где заставка загрузки чатов? сразу Макс опять» (после перезапуска заставки НЕ видно). Разбор: логика заставки отрабатывала (журнал `[chatload] первая загрузка завершена`), НО по журналу было видно только ЗАВЕРШЕНИЕ, а не показалась ли заставка и на сколько. Загрузка чатов из КЭША быстрая (`loadCachedChats` мгновенно) → `loadChats` резолвится за ~2с, а часть перезапусков — ещё быстрее → заставка мелькала доли секунды и терялась среди появления окна/Макса. **Причина не подтверждена замером** (гипотеза: слишком короткое время видимости) → добавлены И запись, И гарантия.

**ЧТО** ([ChatListLoadingSplash.jsx](src/native/components/ChatListLoadingSplash.jsx)):
1. **Гарантия видимости**: если заставка стала видимой — держим её МИНИМУМ `MIN_VISIBLE=2200мс` (даже если загрузка завершилась быстрее), потом плавный финал. Быстрый кэш больше не «съедает» показ.
2. **Точная запись в журнал**: `[chatload] заставка ПОКАЗАНА` при появлении + `[chatload] заставка скрыта, была видна ~Nмс` при скрытии. Теперь по логам ТОЧНО видно, показалась ли заставка и на сколько (раньше — только «загрузка завершена»).

**КРАЙНИЕ СЛУЧАИ**: загрузка дольше 2200мс → держим по факту загрузки (min не мешает); повторный show во время ожидания min → таймеры чистятся (`clear()` в начале эффекта). Существующие тесты 8/8. **DEV-оговорка**: заставка живёт в панели списка чатов, показывается после монтирования NativeApp (в dev ~+105с из-за медленной раздачи модулей); ускорение старта — отдельная тема. **КАК ПРОВЕРЕНО**: `lint` 0; `ChatListLoadingSplash.vitest` 8/8; `fileSizeLimits` 602/602 (бюджет 35480); полный `vitest`. **Требует визуальной проверки + журнала**: перезапуск → заставка видна ≥2с; в журнале строки `заставка ПОКАЗАНА`/`скрыта … Nмс`. **ОТКАТ**: `git checkout src/native/components/ChatListLoadingSplash.jsx` + версия на 1.2.397.

### v1.2.397 — Старт: ХОЛОДНЫЙ старт — убран пустой экран по-настоящему (страховка заставки без ложной проверки «жив ли main.jsx»)

**ЗАЧЕМ**: жалоба (со скриншотами) — «Загрузка…» → ДОЛГИЙ пустой тёмный экран → значок Макс → чат Макс → чаты; заставки сборки чатов не видно. v1.2.395 это чинил, но НЕ на холодном старте — я нашёл баг ещё в ревью v1.2.395, но не успел закрыть (переключились на счётчик Ozon). Журнал v1.2.396 подтвердил на живой машине.

**КОРЕНЬ (по журналу v1.2.396, 2026-09-04)**: страховка боот-заставки ([boot-probe.js](src/boot-probe.js)) на 30с проверяла `window.__ccHideSplash` (жив ли main.jsx) и, если его нет, убирала заставку. Но на ХОЛОДНОМ старте (пустой кэш dev-сервера / первый запуск) тело `main.jsx` (которое и ставит `__ccHideSplash`) выполняется ПОЗЖЕ 30с — журнал: `splash +30006ms safety hid splash: main.jsx не запустился за 30с`, а `main +60738ms react root created` (main.jsx реально запустился на 60с). Итог: на 30с заставку убрали ложно → ~30-105с пустого экрана (`NativeApp mounted +105249ms`). Проверка `__ccHideSplash` на фиксированной ранней секунде НЕНАДЁЖНА (метку ставят поздно).

**ЧТО** ([boot-probe.js](src/boot-probe.js)): убрана ложная проверка. Страховка теперь — ПРОСТО дальний таймер **180с**. В норме заставку убирает `appReady` ([useAppBootstrap.js:87](src/hooks/useAppBootstrap.js) → `__ccHideSplash`) и на холодную (~60с), и на тёплую (быстро). Таймер 180с — лишь предохранитель на реально зависший запуск (битый бандл / appReady не наступил). 180 > худшего холодного (~105с) с запасом. Заглушка `NativeAppFallback` (v1.2.395) закрывает промежуток appReady→монтирование «Загрузкой…».

**ПРО ЗАСТАВКУ СБОРКИ ЧАТОВ**: она РАБОТАЕТ (журнал `[chatload] первая загрузка завершена: чатов=241`), но показывается ~2с ПОСЛЕ монтирования NativeApp (+105с на холодную dev) — на фоне 105с боота теряется. Это DEV-артефакт: в реальной сборке боот — секунды, и последовательность «Загрузка → заставка сборки чатов → список» видна нормально. Сам dev-старт (~105с) НЕ ускоряли (это раздача сотен модулей dev-сервером).

**ОПАСНАЯ ЗОНА (boot)**: правка чисто ВИЗУАЛЬНАЯ (тайминг снятия заставки), на загрузку кода/данных не влияет. **КАК ПРОВЕРЕНО**: `node --check` OK; `lint` 0; `startupDiagnostics` 4/4, `appStructure` 60/60, `rendererConsoleGuard` 0; полный `vitest`. **Требует визуальной проверки на ХОЛОДНОМ старте** (`rm -rf node_modules/.vite` → запуск: «Загрузка…» держится без пустоты до списка). **ОТКАТ**: `git checkout src/boot-probe.js` + версия на 1.2.396.

### v1.2.396 — Ozon «Вопросы»: цифра после ответа гаснет быстрее (reload 3мин→1мин) + запись значения счётчика в журнал

**ЗАЧЕМ**: жалоба — ответил на 2 вопроса Ozon, а цифра «❓» ещё висела «1». Разбор по журналу `chatcenter.log`: счётчик НЕ застрял — он сам стал 0 (`qa=1` в 11:15:10 → `qa=0` в 11:17:41), но с задержкой ~2.5 мин. Корень: скрытая фоновая страница «Вопросы» держит СТАРЫЙ снимок и перечитывается только раз в 3 мин (`periodicReloadMs=180000`, [App.jsx:850](src/App.jsx)); передняя вкладка пересчитывает лишь при пере-скане. Виджет ([OzonQuickWidget.jsx:134,168](src/native/components/OzonQuickWidget.jsx)) исправен — рисует цифру только при значении >0 и берёт `unread[qa]` напрямую (не баг показа, проверено).

**ЧТО**:
1. **Ускорение** ([App.jsx:850](src/App.jsx)): reload фоновой «Вопросы» 180000→**60000мс** (3мин→1мин). Худшая задержка цифры после ответа: ~3мин → ~1мин. **Безопасно от спама**: «память показанного» вопросов `__ccOzonQSeen` переживает reload ([ozon.hook.js:129,150](main/preloads/hooks/ozon.hook.js)) → повторных уведомлений о старых вопросах НЕТ (подтверждено журналом: reload 11:17:55 → `emitted=0`). **ВАЖНО (правка по ревью):** первая версия правки условия `(bgI===0 \|\| bgI===2) ? 180000` → `bgI===0 ? 60000 : 0` СЛУЧАЙНО выключила reload «Отзывов»(2); исправлено на `bgI===0 ? 60000 : (bgI===2 ? 180000 : 0)` — «Отзывы» reload СОХРАНЁН 180000 (был в коде с v1.2.383, база `_rPrev` персистится). Трогаем ТОЛЬКО «Вопросы». (В коде HEAD reviews reload был ВКЛючён, хотя features v1.2.390 писал «убран» — рассинхрон дока/код, см. [[webview-navigation-ui]].) «Сообщения»(1) reload не имеют.
2. **Лог значения** ([ozonBgWatcher.js:29,84-87](src/utils/ozonBgWatcher.js)): фоновый путь теперь пишет `[ozon-bg] счётчик из фона: qa=N (значение для виджета/значка)` — ТОЛЬКО при изменении цифры (`_lastCountLog` гасит повтор на каждый reload). Передняя вкладка значение уже логировала (`qa=N | ozon_count`). Теперь по журналу видно ОБА пути — как цифра обновляется (напр. qa 1→0 после ответа).

**КАК РАБОТАЕТ / крайние случаи**: reload идёт по СКРЫТОЙ фоновой странице — не мешает пользователю на его вкладке. `document.hidden` (окно свёрнуто) → тик reload пропускается. Если задержку даёт САМ Ozon (помечает ответ в списке с лагом) — быстрее не станем (цифра верна, обновится, когда Ozon пометит). **Откат**: `git checkout src/App.jsx src/utils/ozonBgWatcher.js` + версия на 1.2.395.

**КАК ПРОВЕРЕНО**: `npm run lint` 0; `ozonBgWatcher.vitest` 14/14 (+1: qa-лог пишется при изменении, не повторно на ту же цифру); `fileSizeLimits` 602/602 (ozonBgWatcher 144/300); полный `vitest`. **Требует визуальной проверки** (ответить на вопрос → «❓» гаснет за ~минуту).

### v1.2.395 — Старт: убран «долгий пустой экран» — стартовая заставка «Загрузка…» держится до готовности (страховка 30с не гасит её раньше времени)

**ЗАЧЕМ**: жалоба — при запуске: заставка «Загрузка…» → потом ДОЛГО пустой тёмно-синий экран → потом грузится Макс и список. Хотелось: «Загрузка…» без долгой пустоты, затем заставка сборки чатов.

**КОРЕНЬ (по журналу + коду)**: стартовую заставку (`#cc-splash` в [index.html](index.html), встроена до React) в норме снимает `useAppBootstrap` при `appReady` ([useAppBootstrap.js:87](src/hooks/useAppBootstrap.js) → `window.__ccHideSplash`). НО страховка в [boot-probe.js](src/boot-probe.js) БЕЗУСЛОВНО убирала заставку через **30с**. В dev-режиме старт честно длится дольше: журнал 2026-09-04 — заставку сняла страховка на `+30004ms`, а `appReady` наступил на `+70809ms`, `NativeApp mounted` на `+127682ms` (сотни модулей грузятся по одному через dev-сервер, отдельные по 6-53с). Итог: заставку убрали на 30с → **~40-90с пустого экрана** до контента. Моя заставка сборки чатов (v1.2.393) при этом ОТРАБОТАЛА (журнал `[chatload] первая загрузка завершена: чатов=880`), просто показывалась ~2с ПОСЛЕ пустого экрана.

**ЧТО**:
1. [boot-probe.js](src/boot-probe.js) — страховка 30с теперь на 30с проверяет, ЖИВ ли `main.jsx` (он выставил `window.__ccHideSplash`): жив (медленный dev-старт) → заставку НЕ трогаем, её снимет `appReady`; ставим ДАЛЬНИЙ потолок 120с на случай реально зависшего `appReady`. Не запустился (битый бандл) → убираем, как раньше.
2. [appFallbacks.jsx](src/appFallbacks.jsx) `NativeAppFallback` — раньше чёрный div (между `appReady` и монтированием NativeApp ~57с в dev — пусто). Теперь та же подпись «Загрузка…» на тёмном фоне стартовой заставки → нет «пустоты» без индикатора.

**ИТОГ последовательности**: «Загрузка…» (до appReady) → «Загрузка…» (заглушка NativeApp до его монтирования) → заставка сборки чатов (v1.2.393) → список. Пустого экрана нет.

**ГРАНИЧНЫЕ / ОПАСНАЯ ЗОНА (boot)**: если `appReady` реально зависнет — заставку снимет дальний потолок 120с (не навсегда). Если `main.jsx` не запустится (битый бандл) — прежнее поведение (снять на 30с). Правки чисто ВИЗУАЛЬНЫЕ, на работу приложения (загрузку кода/данных) не влияют. **Длительность dev-старта НЕ уменьшилась** (127с — это медленная раздача модулей dev-сервером; в реальной сборке — секунды): фикс убирает ПУСТОТУ, а не ускоряет старт.

**УРОК**: страховочный таймер, гасящий индикатор загрузки, должен отличать «код не запустился» (гасить) от «код жив, но медленно грузится» (ждать) — иначе в медленной среде он срабатывает при НОРМАЛЬНОМ старте.

**КАК ПРОВЕРЕНО**: `node --check` boot-probe OK; `npm run lint` 0; `appStructure` 60/60, `rendererConsoleGuard` 0 нарушений, `startupDiagnostics` 4/4; `fileSizeLimits` 602/602; полный `vitest`. **Требует визуальной проверки** (перезапуск: «Загрузка…» держится без пустого экрана до появления списка) — boot-код автотестом не покрыть.

**ОТКАТ**: `git checkout src/boot-probe.js src/appFallbacks.jsx` + версия на 1.2.394.

### v1.2.394 — Заставка загрузки: 2 фикса по ревью (сохранение «итога» по завершению, а не по скрытию; клики при затухании) + логи

**ЗАЧЕМ**: придирчивое ревью заставки v1.2.393 нашло 2 своих (внесённых той же правкой) крайних бага.

**ФИКС #1 — «итог» (общее число чатов) сохранялся при ЛЮБОМ скрытии заставки, в т.ч. при поиске во время загрузки** → в `localStorage['cc_chat_total_hint']` попадало НЕПОЛНОЕ число (напр. 12 вместо 692), и следующий запуск показывал «из 12». КОРЕНЬ: сохранение висело в ветке скрытия эффекта показа ([ChatListLoadingSplash.jsx](src/native/components/ChatListLoadingSplash.jsx)), а заставка прячется по `show = chatsLoading && !search` — т.е. и при вводе в поиск. РЕШЕНИЕ: сохранение перенесено в ОТДЕЛЬНЫЙ эффект по зависимости `[loadDone]` — срабатывает ровно при ЗАВЕРШЕНИИ загрузки, независимо от скрытия. Новый проп `loadDone` (= `chatsFirstLoadDone`) проброшен InboxMode → сайдбар → заставка. `saveTotalHint` гардит `n>0` (0 чатов не перетрёт прошлый «итог»). Урок: overlay-состояние сохранять по СОБЫТИЮ завершения, а не по факту скрытия (у скрытия много причин).

**ФИКС #2 — заставка 0.42с при затухании перехватывала клики** по списку (нет `pointer-events`). РЕШЕНИЕ: `pointer-events: none` добавлен в `.native-chatload--leaving` ([styles-chatlist-loading.css](src/native/styles-chatlist-loading.css)) — во время финального затухания клики проходят к уже готовому списку. Во время самой загрузки (`show=true`) заставка клики по-прежнему блокирует (список не готов) — это правильно.

**ЛОГИ** (через `app:log`, не `console.*` — правило проекта): (1) INFO `[chatload] первая загрузка завершена: чатов=N, total сохранён/не сохранён` (в эффекте `loadDone` заставки); (2) WARN `[chatload] страховка 15с — loadChats не завершился, открываю список принудительно` (в `InboxMode.jsx`, когда сеть висит). Логи вне цикла (не в тик-таймере подписи) — без спама.

**КАК ПРОВЕРЕНО**: `npm run lint` 0; `ChatListLoadingSplash.vitest.jsx` 8/8 (в т.ч. +2 ловушки: «итог» пишется только при loadDone, а не при скрытии-из-за-поиска; `pointer-events:none` присутствует в CSS); `fileSizeLimits` 602/602 (InboxMode 1112/1116 — потолок поднят с обоснованием; бюджет renderer 35450); полный `vitest`. **Требует визуальной проверки** (клик в момент затухания срабатывает; поиск во время загрузки не портит «из N»).

**ОТКАТ**: `git checkout src/native/components/ChatListLoadingSplash.jsx src/native/components/InboxChatListSidebar.jsx src/native/modes/InboxMode.jsx src/native/styles-chatlist-loading.css src/native/components/ChatListLoadingSplash.vitest.jsx` + версия на 1.2.393.

### v1.2.393 — Нативный режим: живая заставка первой загрузки списка чатов (7 улучшений) + разбор значка ВК (без правки кода)

**ЗАЧЕМ**: жалоба — при первом запуске чаты появляются ПО ОДНОМУ (сначала Макс, потом остальные) — «мигает». Просьба: показать живую заставку, пока грузится, а список открыть РАЗОМ, готовым. Выбрана основа «аватарки по одному» + 7 улучшений (согласованы по макету-артефакту).

**ЧТО** (overlay-подход — список ПОД заставкой не размонтируется, как форум-панель в [InboxChatListSidebar.jsx:406](src/native/components/InboxChatListSidebar.jsx)):
- Новый компонент [ChatListLoadingSplash.jsx](src/native/components/ChatListLoadingSplash.jsx) + стили [styles-chatlist-loading.css](src/native/styles-chatlist-loading.css) (@import в `styles.css`). 7 улучшений: (1) подпись «Загружаем <имя>…» крутится по чатам; (2) настоящие буквы+цвета кружков (или фото-аватар из кэша); (3) галочка «готов» ✓; (4) живой счётчик «Загружено N» (реальный `store.chats.length`); (5) кольцо вокруг текущего; (6) плавный перелив цвета (hue); (7) плавный финал (fade → список).
- Флаг `chatsFirstLoadDone` (локальный `useState` в [InboxMode.jsx:118](src/native/modes/InboxMode.jsx)): ставится `true` по РЕЗОЛВУ `store.loadChats()` (полный батч), либо по страховке 15с (нет сети → не держим заставку вечно). Пробрасывается как `chatsLoading` в сайдбар; там заставка показывается поверх списка, пока флаг не выставлен, и не показывается при поиске.
- **Совет-улучшение «N из общего»**: заставка знает общее число чатов заранее — из прошлого запуска. Хранится в `localStorage['cc_chat_total_hint']` (записывается при завершении загрузки, читается на старте). Счётчик: «Загружено 5 из 692»; первый запуск (значения нет) → «Собираем чаты…». `try/catch` вокруг хранилища (приватный режим/запрет). Реализовано внутри `ChatListLoadingSplash.jsx` (InboxMode не рос).
- **Совет-страж (тест)**: новый [ChatListLoadingSplash.vitest.jsx](src/native/components/ChatListLoadingSplash.vitest.jsx) — поведение (show=true показывает имя+счётчик; show=false = нет в DOM; «из N» из localStorage; пустой store не падает) + source-guard проводки (сайдбар импортит+рендерит заставку; InboxMode держит `chatsFirstLoadDone` и передаёт `chatsLoading`). Падает, если заставку уберут → «мигание» не вернётся молча.

**ГРАНИЧНЫЕ СЛУЧАИ**: нет аккаунтов → заставки нет («Нет аккаунтов»); сеть висит → 15с-страховка открывает то, что успело подгрузиться; поиск во время загрузки → показываем результаты, не заставку; повторная загрузка при добавлении 2-го аккаунта → флаг остаётся true, заставка не всплывает снова (главная жалоба — первая загрузка — закрыта). `prefers-reduced-motion` → анимации выключаются.

**ФАКТЫ (основание)**: 🥈 Ур.2 [nativeStore.js:266-279](src/native/store/nativeStore.js) — `loadChats()` резолвится после полного батча (сигнал «готово»); 🥈 Ур.2 [InboxChatListSidebar.jsx:377-380](src/native/components/InboxChatListSidebar.jsx) — раньше «Загрузка чатов...» исчезала при ПЕРВОМ чате (постепенный налив не был прикрыт); 🥇 Ур.1 React docs «Synchronizing with Effects» (setTimeout в useEffect + cleanup) — паттерн отложенного финала, уже применён в `MessageListOverlay`.

**ПРОВЕРЕНО**: `npm run lint` 0; `test:vitest`; `fileSizeLimits` (InboxMode 1102/1110, новые файлы под лимитом). **Требует визуальной проверки** (перезапуск → при старте видна заставка, потом список разом).

**ОТКАТ**: `git checkout src/native/modes/InboxMode.jsx src/native/components/InboxChatListSidebar.jsx src/native/styles.css` + удалить `src/native/components/ChatListLoadingSplash.jsx` и `src/native/styles-chatlist-loading.css`.

**ВК (значок) — Вариант А выбран, КОД НЕ ПОНАДОБИЛСЯ (уже реализовано)**: пользователь выбрал «значок = число непрочитанных ЧАТОВ». Разбор по коду показал, что это УЖЕ действующее поведение — см. [[decisions]] ADR-039. Цепочка: `countUnreadVK` ([unreadCounters.js](main/preloads/utils/unreadCounters.js)) читает бейдж «Мессенджер N» (метод 2) → `sendToHost('unread-count', allTotal)` ([monitor.preload.cjs:258](main/preloads/monitor.preload.cjs)) → обработчик [webviewSetup.js:475-513](src/utils/webviewSetup.js) кладёт число в `unreadCounts[vk]` → значок рейла. Журнал подтвердил значение: `vk-src msgBadge=2 msgTx="Мессенджер2"` = 2 непрочитанных чата. Честный предел: VK НЕ отдаёт странице «сумму сообщений» (в заголовке окна числа нет — `titleN=нет`), только «число чатов». Моя временная запись `__CC_DIAG__unread_vk` в `countUnreadVK` в журнал НЕ попала (console.log из preload-утилиты не перехватывается webview-логгером — так же не видно давнего `unread_max`) → **откачена** как бесполезная. **Ожидает визуальной проверки**: пользователь смотрит, стоит ли «2» на значке ВК; если пусто — метод 2 не совпал с вёрсткой vk.ru (готовый мелкий фикс — расширить селектор до `a,[role=link]`, как в рабочем `vk-src`).

### v1.2.392-v1.2.389 — Ozon «Отзывы»: rv-счётчик (Вариант C), откат ведения счётчика, убрана авто-перезагрузка, фильтр статуса «Получено…» (архив → archive/features-v1.2.389-392.md)


### v1.2.388-v1.2.381 — Ozon счётчики разделов (msg/qa/rv корни+откаты, парсер rv→msg, фильтр статуса «Отзывов», Шаг B/C/D) + ВК аватарки/анти-дубль/диагностика «Игры» (архив → archive/features-v1.2.381-388.md)

### v1.2.380-v1.2.373 — Ozon (значок рейла/авто-освежение «Вопросов»/логотип по типу/«Печатает…»/разведка «Отзывов» Шаг A) + ВК аватарки+анти-дубль + фикс авто-ответа (архив → archive/features-v1.2.373-380.md)

### v1.2.372-v1.2.367 — Ozon фон (пульс/память показанного/впрыск после перезагрузки/оба раздела) + окно «на весь экран» при клике по уведомлению/задаче (архив → archive/features-v1.2.367-372.md)

### v1.2.366-v1.2.361 — Ozon Шаг 3А: диагностика фоновой «Вопросы» + подтверждение работы (впрыск сторожа через executeJavaScript) + ERR_ABORTED→WARN (архив → archive/features-v1.2.361-366.md)

### v1.2.360-v1.2.349 — Счётчики виджета Ozon (msg/qa) + сторож «Вопросов» Шаг 2 (Ответы=0=новый) + фон Шаг 3a/3b + фиксы по ревью + окно ready-to-show (архив → archive/features-v1.2.349-360.md)

### v1.2.348-v1.2.329 — Виджет Ozon (ранний: док/перетаскивание/место) + окно «на весь экран» + ночная стена уведомлений + сторож «Покупатели» + логотип Ozon + заставка (архив → archive/features-v1.2.329-348.md)

### v1.2.328-v1.2.313 — Ozon (строгий режим сессии/антибот, каталог, разведчик структуры) + логотипы ВК/МАКС/WhatsApp + фиксы логов/дедупа уведомлений (архив → archive/features-v1.2.313-328.md)

### v1.2.312 — старт: не заваливать всплывашками offline-бэклог (архив → archive/features-v1.2.303-312.md)

### v1.2.311-v1.2.305 — Сага «чёткий аватар веб-Telegram»: диагностики → фикс → ПОДТВЕРЖДЕНО (архив → archive/features-v1.2.305-311.md)

### v1.2.303-304 — веб-значки рейла: чистые бейджи + свечение + поднятие при перетаскивании (архив → archive/features-v1.2.303-312.md)

### v1.2.302-v1.2.293 — верхняя панель (линейные значки, единая строка), 3-зонный рейл, фикс фото из уведомления, стекло-кнопка, чистка dist (архив → archive/features-v1.2.293-302.md)

### v1.2.288-v1.2.268 — Веб-аватарки (диагностика + чёткое фото из Настроек Telegram), скрытие верхних вкладок, карточки аккаунта/контакта, чистка журнала (архив → archive/features-v1.2.268-288.md)

### v1.2.267-v1.2.256 — Окно «Добавить» (протокол→мессенджер) + портал полосы «← Общий чат» (архив)

Перенесено в [archive/features-v1.2.256-267.md](archive/features-v1.2.256-267.md) (2026-08-19): единая полоса (веб встроен в нативный рейл, SourceRail удалён, ADR-032); одна кнопка «＋ Добавить» → окно «протокол→мессенджер» + доводки; портал полосы на уровне App (v1.2.260 откат→v1.2.262 рабочий) + кнопка «← Общий чат» над вебом.

### v1.2.254-v1.2.244 — боковой рейл SourceRail (Этапы 1–3, ЗАКРЫТ разворотом v1.2.256, архив)
Подход отдельной панели SourceRail закрыт (Модель 🅰️, ADR-032). История: [archive/features-v1.2.244-255.md](archive/features-v1.2.244-255.md).

### v1.2.243-v1.2.237 — Боковой рейл (флаг/тумблер) + карточка контакта (читаемость/контур/заметка) + МАКС мьют-фикс/диагностика (архив)

Перенесено в [archive/features-v1.2.237-243.md](archive/features-v1.2.237-243.md) (2026-09-01, разгрузка под лимит 100 КБ).

### v1.2.236-v1.2.222 — галочки списка + карточка контакта + МАКС-диагностика + строка отправки + пузырь «↓N» (архив)
Перенесено в [archive/features-v1.2.222-236.md](archive/features-v1.2.222-236.md).

### v1.2.221-v1.2.214 — Порядок фото/«текст отдельно» + ВК-навигация vk.ru + фикс счётчика ВК + окно отправки (архив)

Перенесено в [archive/features-v1.2.214-221.md](archive/features-v1.2.214-221.md) (2026-08-13, разгрузка под лимит 100 КБ): ждём серверное подтверждение фото/альбома перед текстом — порядок у получателя (v1.2.214) + таймаут-WARN (v1.2.215); «перейти в чат» по клику ВК-уведомления под vk.ru + имя мессенджера в статусе (v1.2.216) + тест-репродюсер (v1.2.217) + доводка сравнения имени (v1.2.218); фантомная «1 непрочитано» ВК — счётчик перестал считать бейджи «Друзья/Игры» (v1.2.219); окно отправки фото/файла — одна кнопка «Отправить» + инфо-строка (v1.2.220); лента уведомлений — не дёргать список при чтении + видимая прокрутка (v1.2.221).

### v1.2.213-v1.2.205 — Окно отправки фото: подпись/лимит 1024/«текст отдельно»/порядок/диагностика (архив)

Перенесено в [archive/features-v1.2.205-213.md](archive/features-v1.2.205-213.md) (2026-08-11, разгрузка под лимит 100 КБ): фикс двойной вставки фото + разбит windowManager (v1.2.205) + подтверждающий лог (v1.2.206); окно фото — 7 функций просмотра (v1.2.207); счётчик подписи + «фото, затем текст отдельно», лимит Telegram 1024 (v1.2.209); лимит подписи и «текст отдельно» для видео/документов (v1.2.210); длинный текст режется ≤4096 и шлётся ПОСЛЕ загрузки фото — порядок (v1.2.211); доводки «ждём именно это фото» + число сообщений (v1.2.212); диагностика отправки — лог провала/номера/результат (v1.2.213).

### v1.2.204-v1.2.197 — Окно отправки фото: инструменты просмотра (зум/поворот/сдвиг) + много фото + память окна (архив)

Перенесено в [archive/features-v1.2.197-204.md](archive/features-v1.2.197-204.md) (2026-08-11, разгрузка под лимит 100 КБ): окно отправки фото с зумом/поворотом/сдвигом (v1.2.197-198), логи потока + прозрачность PNG + пропуск в альбоме (v1.2.199), запоминание «на весь экран» (v1.2.200), приподнятая карточка + растущее поле подписи (v1.2.201), чистая логика памяти окна + тесты (v1.2.202), несколько фото в одном сообщении (v1.2.203), кнопки в шапке + вставка Ctrl+V + свободнее зум (v1.2.204).

### v1.2.196–v1.2.185 — диагностика ВК/МАКС, установщик/Win32, прокрутка чата (архив)

Перенесено в [archive/features-v1.2.185-196.md](archive/features-v1.2.185-196.md).

### v1.2.184-v1.2.170 — Логотип Telegram/живой статус собеседника/переключатель режимов/фикс поиска/читаемость шапки (архив)

Перенесено в [archive/features-v1.2.170-184.md](archive/features-v1.2.170-184.md).

### v1.2.169-v1.2.161 — Фильтр аккаунтов (мультивыбор+соло) + самолечение + закрепы/ресайз рейла (архив)

Перенесено в [archive/features-v1.2.161-169.md](./archive/features-v1.2.161-169.md) (2026-07-31, разгрузка под лимит 100 КБ): откат «приклеенного» списка закреплённых (161/162); фильтр аккаунтов на левой панели, клик=вкл/выкл, двойной=соло, кнопка «Все» + доводки (163/164); ресайз левого рейла перетаскиванием (165); полоса «Закреплённое» — читаемый фон + переход к сообщению (166/167); фиксы «после удаления 2-го аккаунта оставшийся не грузился» + самолечение фильтра при старте (168/169).

### v1.2.160-v1.2.156 — Сага «перетаскивание закреплённых» (ОТКАЧЕНА в v1.2.161, архив)

Перенесено в [archive/features-v1.2.156-160.md](./archive/features-v1.2.156-160.md) (2026-07-30): серия попыток сделать ручное перетаскивание порядка закреплённых чатов (HTML5 drag → свой образ за курсором → живая перестановка → вынос в отдельный не-виртуальный `PinnedChatList` с Pointer Events). **Итог — откачено в v1.2.161**: закреплённые должны листаться в ОБЩЕМ списке, а не быть отдельным блоком; перетаскивание же требует отдельного списка. Урок: drag-порядок и виртуальный список react-window (строки по индексу-слоту) несовместимы без замены движка. Из серии сохранились только НЕ-drag доводки (цвет-грань уведомления по `accountId`, цвет-полоса в узком режиме).

### v1.2.155-v1.2.147 — Цвет-метки аккаунтов (различимые цвета/полоса/обводка) + фиксы входа/удаления 2-го аккаунта (архив)

Перенесено в [archive/features-v1.2.147-155.md](./archive/features-v1.2.147-155.md) (2026-07-31, разгрузка под лимит 100 КБ): цвет аккаунта в закреплённых/уведомлении (v1.2.155); устойчивые цвет-метки — первый свободный + сохранение вместо хеша (v1.2.154); полоса/обводка/выбор цвета 🎨 (v1.2.153); спиннер ввода кода Telegram (v1.2.152); карточка выхода не заезжает под панель задач + спиннер «думания» (v1.2.150-151); сброс «залипшего success» гейта входа + разгрузка NativeApp (v1.2.148-149); фикс «чёрный экран» после добавления 2-го аккаунта (v1.2.147).

### v1.2.146-v1.2.140 — Добавление/удаление аккаунта (диагностика + фиксы следов) + закреп 📌 + маскировка кода в логах (архив)

Перенесено в [archive/features-v1.2.140-146.md](./archive/features-v1.2.140-146.md) (2026-07-31, разгрузка под лимит 100 КБ): фикс папки сессии при удалении аккаунта (v1.2.145) + чистка кэш-файла/проброс переименования (v1.2.146); диагностика чёрного экрана и операций с аккаунтом + ErrorBoundary вокруг InboxMode (v1.2.142-143); маскировка кода входа в логах (v1.2.144); значок 📌 в углу аватарки (v1.2.141); откат авто-чистки осиротевших закреплений — теряла данные (v1.2.140).

### v1.2.139-v1.2.138 — Локальное закрепление чатов (📌): базовая фича + доработки (архив)

Перенесено в [archive/features-v1.2.138-139.md](./archive/features-v1.2.138-139.md) (2026-07-30, разгрузка под лимит 100 КБ): локальное закрепление чатов полоской+значком 📌, правый клик, хранение в localStorage, НЕ синхронизируется с Telegram (v1.2.138); доработки — 📌 в узком режиме, устойчивый порядок по массиву pinnedIds, вынос `filterSortChats`, чистка осиротевших (позже откачена v1.2.140) (v1.2.139). Перетаскивание порядка сделано позже (v1.2.156→160).

### v1.2.137-v1.2.134 — Уведомление снимается при прочтении на другом устройстве + CI-фикс TZ снапшота + ВК vk.ru (архив)

Перенесено в [archive/features-v1.2.134-137.md](./archive/features-v1.2.134-137.md) (2026-07-30, разгрузка под лимит 100 КБ): прочитал чат в Telegram на телефоне → карточка-уведомление снимается, частичное чтение + чистая `shouldDismissForRead` (v1.2.137); snapshot ChatListItem сделан TZ-независимым — падал на CI под UTC (v1.2.135); ВК основной адрес → vk.ru + убрана лишняя ошибка `GUEST_VIEW_MANAGER_CALL` запасного впрыска (v1.2.134).

### v1.2.133-v1.2.130 — Строка списка чатов: время + имя отправителя (группы) + «Вы:» + значок форума + имя автора в уведомлении (архив)

Перенесено в [archive/features-v1.2.130-133.md](./archive/features-v1.2.130-133.md) (2026-07-30, разгрузка под лимит 100 КБ): время последнего сообщения + имя автора в превью групп/форумов + значок 🗂️ форума (v1.2.130); «Вы:» для своих + единая `lastSenderLabel` (v1.2.131); имя автора в заголовке уведомления + чистка мёртвого `pickNotifTitle` (v1.2.132); фикс залипания имени в превью + вынос `nativeStoreLastMsgIpc.js` (v1.2.133).

### v1.2.129-v1.2.126 — Уведомления: «невидимая стена»/heartbeat-сторож + ВК переносы строк (архив)

Перенесено в [archive/features-v1.2.126-129.md](./archive/features-v1.2.126-129.md) (2026-07-30, разгрузка под лимит 100 КБ): гасим «невидимую стену» сразу по видимой высоте (v1.2.128), heartbeat-сторож без спама (v1.2.127) и его первая версия (v1.2.126); ВК — переносы строк/абзацы в тексте уведомления через `_vkNodeText` (v1.2.129).

### v1.2.125-v1.2.122 — Уведомление «поверх всех» + ВК-хук (разгрузка/эмодзи/mute-детект/переносы строк) (архив)

Перенесено в [archive/features-v1.2.122-125.md](./archive/features-v1.2.122-125.md) (2026-07-29, разгрузка под лимит 100 КБ): окно уведомления надёжно поверх всех — `screen-saver`+reassert (v1.2.125); ВК-хук — удалён мёртвый toast-наблюдатель + эмодзи из `<img alt>` (v1.2.124), сужен mute-селектор + тесты чистых функций (v1.2.123), сохранены переносы строк + снят предел текста 1000 (v1.2.122).

### v1.2.121-v1.2.116 — Telegram: не показываем сторис; ВК/vk.ru: диагностики значка 🔕 + текст целиком + фикс «не уведомлять о заглушённых» (архив)

Перенесено в [archive/features-v1.2.116-121.md](./archive/features-v1.2.116-121.md) (2026-07-29, разгрузка под лимит 100 КБ): Telegram Web — гасим системную фразу «опубликовал(а) историю» (v1.2.121); ВК/vk.ru — итоговый фикс «не уведомлять о заглушённых чатах 🔕» по видимости значка (v1.2.120), текст уведомления целиком без своей мерки (v1.2.119), серия временных диагностик значка «беззвучно» (v1.2.116-118, диагностики давно удалены).

### v1.2.115-v1.2.108 — ВК переезд на vk.ru (детект/текст/диагностика) + время в источнике (архив)

Перенесено в [archive/features-v1.2.108-115.md](./archive/features-v1.2.108-115.md) (2026-07-27, разгрузка под лимит 100 КБ): переезд ВК на vk.ru + определение домена (v1.2.109); настоящий детект новых сообщений по списку чатов (v1.2.112) + подтверждение логом (v1.2.113); чистый текст уведомления по структуре строки (v1.2.114); 3 фикса по ревью — чистка времени/шторм на старте/дубль (v1.2.115); серия временных диагностик структуры vk.ru (v1.2.110/111); время сообщения в строке источника, единый формат (v1.2.108).

### v1.2.107-v1.2.101 — Уведомления: сторож стены + видео/фото в карточке (архив)

Перенесено в [archive/features-v1.2.101-107.md](./archive/features-v1.2.101-107.md) (2026-07-24, разгрузка под лимит 100 КБ): сторож «невидимой стены» + чистая decideNotifResize + быстрее закрытие (v1.2.107); фикс осиротевшего положительного отчёта — «невидимое зависшее окно», ловящее клики (v1.2.106); «Прочитано» для native-Telegram (v1.2.105); видео в уведомлении — постер/плеер как в чате, видео-как-документ, крутилка по факту (v1.2.101-104).

### v1.2.100-v1.2.90 — Уведомления/видео/фото + диагностика Telegram (архив)

Перенесено в [archive/features-v1.2.90-100.md](./archive/features-v1.2.90-100.md) (2026-07-23, разгрузка под лимит 100 КБ): страховка крутилки одиночного фото (v1.2.100), «размытый фон» одиночного фото (v1.2.99), крупный аватар + источник в шапке (v1.2.98), раскладка «Стопка» для всех карточек (v1.2.96-97), одиночное фото/видео в native-уведомлении (v1.2.95), диагностика фото/ссылок Telegram native+web (v1.2.90/92/93/94), расчёт вертикали дока в чистую функцию + тест (v1.2.91).

### v1.2.89-v1.2.74 — Док/подсказка/закреп + имя аккаунта + Telegram-аватар + альбом (архив)

Перенесено в [archive/features-v1.2.74-89.md](./archive/features-v1.2.74-89.md): фиксы дока/подсказки/закрепа (стабильный якорь верха, панель задач Windows, снятие snap, rAF/throttling; v1.2.76-89), доведение имени аккаунта до закрепа/подсказки (v1.2.83-88), Telegram аватар+источник в уведомлении (v1.2.75), листание альбома + чёткие плитки (v1.2.74).

### v1.2.73-v1.2.59 — Док/подсказка/закреп: серия фиксов (архив)

Перенесено в [.memory-bank/archive/features-v1.2.59-73.md](archive/features-v1.2.59-73.md) при разбиении (21 июля 2026). Архитектура — decisions.md ADR-017/ADR-018.

### v1.2.58-v1.2.46 — VK: тост/sidebar/active-chat уведомления, MAX dedup, ribbon (архив)

Перенесено в [.memory-bank/archive/features-v1.2.46-58.md](archive/features-v1.2.46-58.md) при разбиении (17 июля 2026).

### v1.2.45-v1.2.37 — VK DOM observer / диагностика уведомлений (архив)

Перенесено в [.memory-bank/archive/features-v1.2.37-45.md](archive/features-v1.2.37-45.md) при разбиении (17 июля 2026).

### v1.2.36-v1.2.30 — диагностика MAX/системы (архив)

Полные записи перенесены в [archive/features-v1.2.30-36.md](./archive/features-v1.2.30-36.md), чтобы active features.md оставался меньше лимита memory-bank. Суть: диагностика стала ручной, фоновой, с последним отчётом, полной MAX/VK-цепочкой и понятными кнопками; pipeline уведомлений этим блоком не менялся, кроме описанных в соответствующих версиях точечных MAX/VK исправлений.
### v1.2.29 — диагностика запускается только вручную и не мешает работе (архив)

Полная запись перенесена в [archive/features-v1.2.29.md](./archive/features-v1.2.29.md), чтобы активный features.md оставался меньше лимита memory-bank. Суть: большая модалка диагностики больше не стартует запись сама, управление записью стало явным, общий лог не очищается.
### v1.2.28 — MAX: sidebar watcher не создаёт фантомы во время поиска (архив)

Полная запись перенесена в [archive/features-v1.2.28.md](./archive/features-v1.2.28.md), чтобы активный features.md оставался меньше лимита memory-bank. Суть: `max-sidebar` при активном поиске MAX обновляет baseline, но не создаёт ribbon из старого preview; реальные `max-notification-api` и `max-sw-showNotification` не отключались.

**Структура файла**: этот features.md содержит только **последние активные версии**. Старое — в архиве:

| Архив | Содержимое | Размер |
|---|---|---|
| [`archive/features-v0.95.39.md`](./archive/features-v0.95.39.md) | v0.95.39 (убран twoPhase + RAF×2 + 350мс easeOutCubic; стабилизировано v0.95.40+) | ~2 КБ |
| [`archive/features-v0.95.40.md`](./archive/features-v0.95.40.md) | v0.95.40 (большие emoji + a11y reduced-motion + sticky bottom on media; стабилизировано v0.95.41+) | ~3 КБ |
| [`archive/features-v0.95.41.md`](./archive/features-v0.95.41.md) | v0.95.41 (Custom emoji premium WebM/WebP + reduced-motion интеграционные тесты; стабилизировано v0.95.42+) | ~3 КБ |
| [`archive/features-v0.95.42.md`](./archive/features-v0.95.42.md) | v0.95.42 (сохранение поиска + история + ✕ + подсветка совпадений; стабилизировано v0.95.43+) | ~2 КБ |
| [`archive/features-v0.95.38.md`](./archive/features-v0.95.38.md) | v0.95.38 (фикс дубля сообщений через updateMessageSendSucceeded, ⏳ индикатор, mistakes static guard) | ~2 КБ |
| [`archive/features-v0.95.35-37.md`](./archive/features-v0.95.35-37.md) | v0.95.35-37 (fade-in changelog, auto-scroll outgoing-other-device, sending_state polish, mistakes/outgoing-two-cases.md; стабилизировано v0.95.38+) | ~3 КБ |
| [`archive/features-v0.95.34.md`](./archive/features-v0.95.34.md) | v0.95.34 (темовые vars в :root, вспышка bubble, WhatsNewModal UX; стабилизировано v0.95.40+) | ~2 КБ |
| [`archive/features-v0.95.33.md`](./archive/features-v0.95.33.md) | v0.95.33 (фикс «цвет не применяется» через querySelectorAll, регресс-тест blur, деловой стиль; финал в v0.95.34) | ~2 КБ |
| [`archive/features-v0.95.32.md`](./archive/features-v0.95.32.md) | v0.95.32 (производительность WhatsNewModal: убран backdrop-filter + contain + деловой стиль changelog) | ~2 КБ |
| [`archive/features-v0.95.31.md`](./archive/features-v0.95.31.md) | v0.95.31 (аккаунты вниз + drag-n-drop + multi-user typing + throttle реакций; стабилизировано v0.95.34+) | ~3 КБ |
| [`archive/features-v0.95.30.md`](./archive/features-v0.95.30.md) | v0.95.30 (плавная auto-scroll + 5 цветовых тем + dropdown + opacity 0.95; стабилизировано v0.95.33-34) | ~3 КБ |
| [`archive/features-v0.95.29.md`](./archive/features-v0.95.29.md) | v0.95.29 (реакции 👍❤️🔥 + Telegram-style header + General 📢 + render-counter; стабилизировано v0.95.31-34) | ~5 КБ |
| [`archive/features-v0.95.28.md`](./archive/features-v0.95.28.md) | v0.95.28 (Telegram-style auto-scroll к новому + ↓N без «слепой зоны» Schmitt; стабилизировано v0.95.31) | ~4 КБ |
| [`archive/features-v0.95.27.md`](./archive/features-v0.95.27.md) | v0.95.27 (расширенная диагностика send pipeline; стабилизировано v0.95.29) | ~3 КБ |
| [`archive/features-v0.95.23-26.md`](./archive/features-v0.95.23-26.md) | v0.95.23 – v0.95.26 (курсор в input, initial backfill, voice/spellcheck/action-bar/WhatsNew, фикс 47-дневного бага unreadCount; стабилизировано) | ~15 КБ |
| [`archive/features-v0.95.19-22.md`](./archive/features-v0.95.19-22.md) | v0.95.19 – v0.95.22 (диагностика tg-new-message, финал jump-to-end, бейдж форум-группы, форум-overlay; стабилизировано) | ~6 КБ |
| [`archive/features-v0.95.15-18.md`](./archive/features-v0.95.15-18.md) | v0.95.15 – v0.95.18 (итеративный fetch + форум-топики + двухфазный scroll + ForumTopicEmptyState; стабилизировано v0.95.20-21) | ~8 КБ |
| [`archive/features-v0.95.12-14.md`](./archive/features-v0.95.12-14.md) | v0.95.12 – v0.95.14 (3 итерации jump-to-end до итеративного fetch v0.95.15, полная сага в jump-to-end-saga.md) | ~32 КБ |
| [`archive/features-v0.95.8-9.md`](./archive/features-v0.95.8-9.md) | v0.95.8 – v0.95.9 (счётчик ↓ обнуляется + анимация + live compact + порог 128) | ~14 КБ |
| [`archive/features-v0.95.5-7.md`](./archive/features-v0.95.5-7.md) | v0.95.5 – v0.95.7 (sticky pinned overlay, кнопка ↓ Telegram-style, drag-to-resize) | ~24 КБ |
| [`archive/features-v0.95.0-3.md`](./archive/features-v0.95.0-3.md) | v0.95.0 – v0.95.3 (контигуити-фикс, afterId load-newer, мигание ↓ Schmitt trigger, диагностика «дёрг») | ~13 КБ |
| [`archive/features-v0.94.1-7.md`](./archive/features-v0.94.1-7.md) | v0.94.1 – v0.94.7 (TDLib listener leak, scroll caskade, спокойная загрузка, пилюля прогресса — стабилизированы v0.95.0-2) | ~24 КБ |
| [`archive/features-v0.93.0.md`](./archive/features-v0.93.0.md) | v0.93.0 (pixel-perfect scroll restore через LocationOptions.offset, superseded v0.94.0) | ~7 КБ |
| [`archive/features-v0.92.0-6.md`](./archive/features-v0.92.0-6.md) | v0.92.0 – v0.92.6 (сага Virtuoso scroll restore, superseded v0.94.0) | ~37 КБ |
| [`archive/features-v0.91.11-24.md`](./archive/features-v0.91.11-24.md) | v0.91.11 – v0.91.24 (сага scroll restore: 13 версий → миграция на virtuoso) | ~47 КБ |
| [`archive/features-v0.91.1-10.md`](./archive/features-v0.91.1-10.md) | v0.91.1 – v0.91.10 (initial-load, scroll-jump, newBelow, forum topics, updateChatLastMessage) | ~12 КБ |
| [`archive/features-v0.87.106-114.md`](./archive/features-v0.87.106-114.md) | v0.87.106 – v0.87.114 (multi-account UI финал, кнопки режимов, мьют чата, аватарки отправителей в группах) | ~22 КБ |
| [`archive/features-v0.87.93-105.md`](./archive/features-v0.87.93-105.md) | v0.87.93 – v0.87.105 (multi-account native, login flow, разбиения, cc-media://) | ~30 КБ |
| [`archive/features-v0.87.80-92.md`](./archive/features-v0.87.80-92.md) | v0.87.80 – v0.87.92 (pre-push hook, разбиения 4-7, AccountContextMenu) | ~16 КБ |
| [`archive/features-v0.87.65-79.md`](./archive/features-v0.87.65-79.md) | v0.87.65 – v0.87.79 (план разбиения 1-3, pre-push hook, bubble UI) | ~54 КБ |
| [`archive/features-v0.87.51-64.md`](./archive/features-v0.87.51-64.md) | v0.87.51 – v0.87.64 (groupedUnread удалён, pre-commit hook) | ~54 КБ |
| [`archive/features-v0.87.40-50.md`](./archive/features-v0.87.40-50.md) | v0.87.40 – v0.87.50 (итерации native scroll + unread) | ~40 КБ |
| [`archive/features-v0.87-early.md`](./archive/features-v0.87-early.md) | v0.87.0 – v0.87.39 (запуск native + ранние фиксы) | ~140 КБ |
| [`archive/features-pre-v0.87.md`](./archive/features-pre-v0.87.md) | v0.1.0 – v0.86.10 (до native-режима, 3 марта – 14 апреля 2026) | ~210 КБ |

**Архив не читается по умолчанию.** Запрос к нему — только при явной просьбе («что было в v0.85», «покажи старый changelog»).

**До рефакторинга v0.87.57** файл был 445 КБ (3371 строк, 323 версии). После — ~100 КБ в корне.

---

### Архив v1.2.27 и старше

Подробная история v1.2.27 и более ранних версий перенесена в [archive/features-v1.2.27-and-older.md](./archive/features-v1.2.27-and-older.md). Активный файл держим компактным, чтобы memory-bank быстро читался и проходил лимиты.
