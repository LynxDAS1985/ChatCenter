# Реализованные функции — ChatCenter

## Текущая версия: v1.2.448 (10 сентября 2026)

### v1.2.448 — УБОРКА ПО СПИСКУ «нашёл, не трогал»: общий код вынесен из бюджета интерфейса, опасный путь наконец покрыт тестами, две «мёртвые» находки оказались НЕ мусором

**КОНТЕКСТ**: пользователь сказал делать всё из списка «нашёл, НЕ трогал» отчёта v1.2.447. Сделано 3 пункта из 5; по 2 пунктам вывод после проверки ИЗМЕНИЛСЯ — и это главное в этой записи.

**1. 🔴 ДВЕ «МЁРТВЫЕ» НАХОДКИ УДАЛЯТЬ НЕЛЬЗЯ — я ошибся в прошлом отчёте.**
Я предложил удалить `src/hooks/useNotifyDispatcher.js` (98 строк) и `src/components/AIPermissionsSettings.jsx` (176 строк) как «мёртвый код с апреля». Вывод был сделан по ОДНОМУ признаку — «никто не вызывает» — и оказался неверным. Поиск по `.memory-bank/` показал: это **заготовки плана ИИ-агента**, у которых просто не доделана проводка:
- `useNotifyDispatcher` — «общая шина» щелчков по уведомлению, фаза 0 шаг M0.2, покрыт 9 тестами; в плане (`ai-agent-plan/checkpoints.md:158`, шаг M0.3) прямо расписано, как его подключить к `App.jsx`;
- `AIPermissionsSettings` — экран «Настройки → ИИ → Разрешения», фаза 2 шаг M2.7 (`phases/phase-2-permissions-write-impl.md:297`), а в CLAUDE.md прямо сказано «Phase 2 (write actions + permissions UI) — **ожидает**».
Удаление уничтожило бы запланированную и протестированную работу. Оба файла ОСТАВЛЕНЫ, вместо удаления заведена запись [code-todo.md](code-todo.md) **TODO-39** с готовыми шагами подключения и объяснением, почему это отдельная задача (меняется путь «щелчок по уведомлению» — самый частый источник прошлых поломок, без запуска приложения непроверяем).
**Урок**: «никто не вызывает» ≠ «мусор». Перед удалением — поиск по памяти проекта: не заготовка ли это по плану.

**2. Общий код уехал из бюджета интерфейса: папки `src/shared/` больше нет**
| ❌ Было | ✅ Стало |
|---|---|
| `src/shared/` — 16 файлов, **918 строк** внутри бюджета интерфейса | `shared/notificationSource.js` + `shared/tools/**` — вне бюджета |
| Главный процесс лез внутрь интерфейса: `main/ai/aiAgentSetup.js` импортировал `../../src/shared/tools/...` | `../../shared/tools/...` — ядро больше не тянется в папку окна |
| Бюджет 35494 / 35540 (запас 46) | **34565 / 34640** (планка ОПУЩЕНА на 900) |

Перенос механический (`git mv` + правка путей в 10 файлах), внутренних ссылок обратно в `src/` у переносимых файлов не было — проверил до переноса. Планка опущена сразу, чтобы освободившееся место не заросло молча (правило [[decisions]] ADR-044).

**3. `webviewSetup.js` разгружен ещё раз: 569 → 517/605** (запас 36 → **88 строк**)
Вынесен второй по величине блок — «непрочитанное по заголовку вкладки» (событие `page-title-updated`, 70 строк) → новый [shared/webviewTitleUnread.js](../shared/webviewTitleUnread.js) (113 строк).
**Главная выгода не в строках, а в проверяемости**: этот путь НЕ БЫЛ покрыт ни одним тестом, хотя исторически давал больше всего повторных поломок (звук-фантомы у МАКС v1.2.7, обнуление счётчика Ozon v1.2.374). Теперь есть [webviewTitleUnread.vitest.js](../src/__tests__/webviewTitleUnread.vitest.js) — **15/15**, в том числе три ловушки:
- 🔴 у МАКСа звук по заголовку НЕ играет (только после подтверждённой карточки);
- 🔴 у Ozon заголовок без числа НЕ обнуляет счётчик;
- 🔴 **список связей, который собирает `webviewSetup`, сверяется со списком, который разбирает модуль** — разойдутся, и счётчик тихо перестал бы работать; тест ловит и «не передали», и «передали зря».
Связи передаются ОДНИМ объектом (их 28) — приём как у `AppModals` (v1.2.442): перечислять 28 параметров = однажды опечататься.

**ПОЧЕМУ НЕ РАЗДЕЛИЛ ФАЙЛ ДО КОНЦА**: остался блок `ipc-message` (~144 строки). Он держится на общем закрытии функции-фабрики и, в отличие от вынесенных, участвует в обмене сообщениями с чужой страницей — его перенос без запуска приложения непроверяем. Файл теперь на 85% лимита, запас 88 строк — задача перестала быть срочной.

**4. Закрыты ДВЕ дыры в самих проверках** (нашёл по ходу работы, не искал специально)
- `npm run lint` смотрел только `src/` и `main/` — папка `shared/` (а там уже 20 файлов, включая весь код ИИ-инструментов) **не проверялась вообще**. Теперь `eslint src/ main/ shared/`.
- Страж проводки (v1.2.447) читал только верхний уровень `shared/` — вложенная `tools/` осталась бы без присмотра. Теперь обходит подпапки; поиск идёт по ИМЕНИ файла, потому что соседи внутри подпапки импортируют друг друга коротко (`'../toolSchemas.js'`). Страж: 20 → **34 проверки**.
- Консольный страж (`console.*` в интерфейсе запрещён) тоже смотрел только `src/`. Теперь обходит и `shared/`; в baseline честно записаны 2 файла, где `console.log` стоит ВНУТРИ строк, впрыскиваемых в чужую страницу — там нашего журнала нет, и мост через консоль единственный способ доставки (`browserBannerHider`, `vkExecFallback`).
- Заодно: установленный хук `.git/hooks/pre-push` оказался УСТАРЕВШИМ (гонял 33 теста из 36 — новых стражей в нём не было). Обновлён из `scripts/hooks/pre-push` (локальная операция, в коммит не идёт).

**5. НЕ делал (осознанно)**: разгрузку `decisions.md` — она уже сделана в этой сессии (102 → 85 КБ, ADR-037…039 в архив).

**КАК ПРОВЕРЕНО**: `webviewTitleUnread.vitest.js` 15/15; `sharedWiring.test.cjs` 33/33; `rendererConsoleGuard` 240 файлов, 0 нарушений; полный `vitest run` — **200 файлов / 2648 тестов**; вся цепочка pre-push — **36 тестов, 0 падений**; линт (теперь включая `shared/`) 0; лимиты 638/638, бюджет интерфейса 34513/34640; `npm run build` ✓.

**ЧТО ПРОВЕРИТЬ ВРУЧНУЮ**: (1) счётчики непрочитанного на значках веб-мессенджеров (WhatsApp, ВК) по-прежнему растут и обнуляются при чтении — это вынесенный блок; (2) у МАКСа нет лишнего писка без карточки; (3) значок Ozon не сбрасывается в 0 при открытой вкладке; (4) ИИ-инструменты (переход к сообщению, история чата, поиск) работают — это переезд папки.

**Откат**: `git revert` этого коммита; переезд папки — единый набор переименований, откатывается целиком.

**ОСТАТОЧНЫЕ РИСКИ**: (а) 🟡 переезд общей папки проверен тестами и сборкой, но ИИ-инструменты живьём не запускались; (б) 🟡 вынесенный разбор заголовка покрыт тестами на подставных данных — живой путь (реальная вкладка) требует визуальной проверки; (в) 🟢 `ipc-message` в `webviewSetup.js` не вынесен.

### v1.2.447 — ТРИ СОВЕТА ИЗ РЕВЮ РЕАЛИЗОВАНЫ: страж «модуль подключён?», разгрузка `webviewSetup.js`, полоса «нет связи» для «Общего чата»

**КОНТЕКСТ**: пользователь выбрал 4 совета из отчёта v1.2.446. Три из них — код (ниже), четвёртый (сохранить работу в хранилище) — коммит в конце.

**1. Страж проводки для ВСЕХ вынесенных модулей** ([sharedWiring.test.cjs](../src/__tests__/sharedWiring.test.cjs), новый, 128/400)
Причина появления — реальный случай v1.2.446: файл `shared/browserBannerHider.js` лежал на диске, его 16 проверок были зелёными, а приложение его больше НЕ ЗВАЛО (вызов исчез вместе с откаченными правками) — и опасное поведение ожило незаметно.
Страж обходит ВСЕ `shared/*.js` (17 файлов) и требует у каждого живой вызов из `src/`/`main/`/`shared/`; **тесты и комментарии за вызов НЕ считаются** — иначе «подключённым» считался бы модуль, который зовёт только тест. Плюс 3 точечных проверки опасного места (плашки прячет только сторож с предохранителями, старое условие `children.length < 20` не вернулось, сбои впрыска пишутся в журнал). **20/20.** Внесён в обязательную цепочку `pre-push` и в `npm test`.

**2. Разгрузка `src/utils/webviewSetup.js`: 597 → 569/605** (запас 8 → **36 строк**)
Вынесен один обособленный блок из `dom-ready` — «спрятать плашки „браузер устарел“ + запасной впрыск перехватчика уведомлений» → новый [shared/webviewPageFixups.js](../shared/webviewPageFixups.js) (89 строк, в `shared/` = вне бюджета интерфейса; приём как у `shared/vkExecFallback.js` — нужное передаётся снаружи, потому что `detectMessengerType` живёт в `src/`).
**Полное разделение файла НЕ делалось осознанно**: оставшиеся крупные блоки (`ipc-message` ~144 строки, `console-message`) держатся на общем закрытии функции-фабрики — это переделка устройства файла, и без запуска приложения она непроверяема. Так и записано в исключении к лимиту с v0.88.x. Вынесенный блок был единственным, который зависит только от элемента страницы и её имени.
Заодно по правилу «за собой убирай»: единственная `console.log` из этого блока переписана на запись в журнал приложения (`app:log`), и устаревшая запись `src/utils/webviewSetup.js` убрана из baseline стража `rendererConsoleGuard` (он предупреждал «файл пропал из baseline»).

**3. Полоса «нет связи» для «Общего чата»** — вторая половина работы v1.2.445 (в её плане это было отложено, раздел 8)
| Файл | Что |
|---|---|
| [shared/connectionStripState.js](../shared/connectionStripState.js) (новый, 108) | Чистая логика: какое состояние считать бедой, понятные имена, что показывать, «5 с / 1 мин 20 с», тексты журнала |
| [NativeConnectionStrip.jsx](../src/native/components/NativeConnectionStrip.jsx) (новый, 88/600) | Полоса поверх списка чатов: причина, сколько ждём, кнопка «Проверить сейчас» (зовёт НАСТОЯЩУЮ проверку `tg:health-check`) |
| [NativeMainContent.jsx](../src/native/components/NativeMainContent.jsx) 70→76 | Подключение (только когда есть аккаунты и не открыт экран входа) |
| [styles-base.css](../src/native/styles-base.css) | `.native-main { position: relative }` — точка отсчёта для полосы |

**ЧТО БЫЛО НЕ ТАК**: канал состояния связи `tg:account-connection` в проекте **уже был** ([tdlibClient.js:466](../main/native/backends/tdlibClient.js) → [tdlibIpcBridge.js:128](../main/native/tdlibIpcBridge.js)), но его **НИКТО не слушал** (ноль обращений по поиску) — то есть приложение знало о потере связи и молчало.

**🔴 ЧЕСТНОЕ ОГРАНИЧЕНИЕ (и почему код сделан именно так)**: папки с документацией TDLib в `DOCS/` **НЕТ**, а в коде проекта имена состояний связи нигде не встречались. Угадывать строки в этом проекте уже приводило к ошибкам, поэтому: полоса поднимается ТОЛЬКО на состояниях, узнанных по части слова (`waitingfornetwork`, `connecting`), и ВСЕГДА — когда сам движок говорит «сети нет» (это документированный признак: `navigator.onLine` + события `online`/`offline`, `DOCS/Electron docs/tutorial/online-offline-events.md`). **Незнакомое состояние бедой НЕ считаем** — тогда ошибка безобидна (полоса не появится), а не наоборот (вечная ложная полоса поверх чатов). Само состояние пишется в журнал как есть (`[tg-conn] аккаунт …: состояние=…`), поэтому точные имена станут известны из первого живого запуска, и список можно будет расширить ФАКТОМ, а не догадкой.

**ПОЧЕМУ НЕ СВОЯ ЛЕСТНИЦА ПОВТОРОВ, КАК У ВЕБА**: веб-страницу перезагружаем мы сами, а тут связь ведёт сам Telegram-канал — он переподключается без нас. Второй механизм повторов мешал бы встроенному и мог дать шторм запросов. Поэтому наша часть — ПОКАЗАТЬ и дать честную кнопку проверки.

**ПОЧЕМУ ПОЛОСА РИСУЕТСЯ ПОВЕРХ**: `.native-main` раскладывает детей В РЯД — обычный ребёнок уехал бы вбок. Полоса `position:absolute` сверху (тот же приём, что у закреплённых блоков над прокруткой). Плата: полоса накрывает верхние ~34 точки списка чатов, пока связи нет.

**БЮДЖЕТ RENDERER**: 35480 → 35540 (факт 35494). Разгрузка в этой же версии СДЕЛАНА (28 строк ушли в `shared/`), вся чистая логика полосы — тоже в `shared/`, поэтому поднятие законно по [[decisions]] ADR-044.

**КАК ПРОВЕРЕНО**: новый [connectionStripState.vitest.jsx](../src/__tests__/connectionStripState.vitest.jsx) **17/17** — включая 🔴-ловушку «незнакомое состояние полосу НЕ поднимает», «сеть пропала от движка поднимает полосу без состояния от Telegram», кнопка зовёт настоящую проверку и блокируется на время, связь вернулась → полоса ушла + запись «связь восстановлена», таймер секунд снимается когда полосы нет; [sharedWiring.test.cjs](../src/__tests__/sharedWiring.test.cjs) **20/20**; [browserBannerHider.vitest.js](../src/__tests__/browserBannerHider.vitest.js) 19/19. Полный `vitest run` — **198 файлов / 2627 тестов**. Вся цепочка pre-push — **36 тестов** (добавлен новый), 0 падений. Линт 0. Лимиты 636/636. `npm run build` — ✓.

**ЧТО ПРОВЕРИТЬ ВРУЧНУЮ**: (1) открыть «Общий чат» → выключить Wi-Fi → сверху должна появиться полоса с причиной и растущим временем, кнопка «Проверить сейчас» отвечает; вернуть сеть → полоса уходит сама; (2) в журнале должны появиться строки `[tg-conn] аккаунт …: состояние=…` — **по ним будут известны точные имена состояний** (прислать их, чтобы расширить список фактом); (3) веб-мессенджеры по-прежнему открываются и плашки «браузер устарел» не мешают (проверка переноса блока), веб-МАКС открывается.

**Откат**: `git checkout HEAD -- src/utils/webviewSetup.js src/native/components/NativeMainContent.jsx src/native/styles-base.css src/__tests__/fileSizeLimits.test.cjs src/__tests__/rendererConsoleGuard.test.cjs scripts/hooks/pre-push package.json && rm shared/webviewPageFixups.js shared/connectionStripState.js src/native/components/NativeConnectionStrip.jsx src/__tests__/sharedWiring.test.cjs src/__tests__/connectionStripState.vitest.jsx` — но `webviewSetup.js` откатывать ОПАСНО (см. грабли v1.2.446), лучше вернуть вызов встроенного блока руками.

**ОСТАТОЧНЫЕ РИСКИ**: (а) 🟡 перенос блока доводок страницы тестами покрыт только по строкам-признакам — что плашки реально прячутся, видно лишь в живом запуске; (б) 🟡 имена состояний связи не подтверждены документацией (см. выше) — до живого запуска полоса может не появляться на состоянии, которое мы не узнали, но ложно появиться не может; (в) 🟡 полоса накрывает верх списка чатов; (г) 🟢 полное разделение `webviewSetup.js` не сделано — задача с обязательной визуальной проверкой.

### v1.2.446 — ВОССТАНОВЛЕНА защита от чёрного экрана веб-МАКС: правки в `webviewSetup.js` были потеряны, и НИ ОДИН тест этого не заметил

**ЧТО СЛУЧИЛОСЬ (моя ошибка, честно).** Во время работы над v1.2.445 я откатил один файл командой `git checkout -- src/utils/webviewSetup.js`, чтобы снять его с потолка строк. Этот файл содержал НЕЗАКОММИЧЕННЫЕ правки прошлых сессий (v1.2.434 и v1.2.435) — они были уничтожены, потому что откат возвращает файл к последнему коммиту, а последний коммит здесь `cd90ef6` (v1.2.427-429).

**ЧЕМ ЭТО БЫЛО ОПАСНО.** Вернулось прежнее широкое условие «в тексте элемента есть слова „обновите“+„браузер“ И у него меньше 20 прямых детей → спрятать». Под него попадает КОРЕНЬ приложения (текст элемента = текст всей переписки, детей у корня 2-6) → веб-МАКС мог целиком уйти в невидимость, а выглядит это как чёрный экран. Именно эту причину нашли в v1.2.434 (см. [[decisions]] ADR-042).

**ПОЧЕМУ НЕ ЗАМЕТИЛИ СРАЗУ.** Файл с предохранителями `shared/browserBannerHider.js` остался на диске и все 16 его тестов были зелёными — но приложение его больше НЕ ЗВАЛО. Все проверки смотрели ВНУТРЬ модуля и ни одна не смотрела на проводку. Полная цепочка pre-push (35 тестов) и полный прогон (2606 тестов) прошли без падений.

**ЧТО ИСПРАВЛЕНО**
| Файл | Что |
|---|---|
| [webviewSetup.js](../src/utils/webviewSetup.js) 604 → 597/605 | Опасный встроенный скрипт снова заменён на `buildBannerHiderScript()` из [shared/browserBannerHider.js](../shared/browserBannerHider.js); возвращена запись в журнал `[banner-hider] впрыск НЕ прошёл для <id>: <причина>` вместо пустого «проглатывания» ошибки (v1.2.435) |
| [browserBannerHider.vitest.js](../src/__tests__/browserBannerHider.vitest.js) 173 → 198/400 | **Новый страж проводки, 3 проверки**: приложение действительно зовёт `buildBannerHiderScript()`; старое условие `children.length < 20` НЕ вернулось; сбой впрыска пишется в журнал. 16 → **19/19** |

**🔴 ГРАБЛЯ (записана в [[webview-navigation-ui]])**: `git checkout -- <файл>` уничтожает НЕЗАКОММИЧЕННУЮ работу; в этом проекте больше сотни файлов месяцами живут без коммита, поэтому такой откат недопустим — надо править файл вперёд. И вторая половина граблей: тест, проверяющий только «внутренности» вынесенного модуля, не докажет, что модуль вообще подключён — нужна проверка ПРОВОДКИ.

**Проверено**: `browserBannerHider.vitest.js` 19/19; линт 0; лимиты 631/631 (`webviewSetup.js` 597/605); вся цепочка pre-push 35 тестов без падений.

**Проверено дополнительно**: пройдены все файлы `shared/*.js` — у каждого есть живой вызов из кода приложения (`browserBannerHider.js` был единственным «осиротевшим», больше таких нет).

**Откат**: `git checkout HEAD -- src/utils/webviewSetup.js` — НЕ делать по причине выше; правильный откат этой записи — вернуть встроенный скрипт руками (не нужно).

### v1.2.445 — ПЕРЕПОДКЛЮЧЕНИЕ после обрыва связи: мессенджеры поднимаются сами + экран «Нет связи» с отсчётом

**ЖАЛОБА**: «после потери интернета веб-подключения не восстанавливаются автоматически; при переключении на WhatsApp отображаются данные Telegram API вместо страницы мессенджера». Полный план, макет и разбор — [reconnect-plan.md](reconnect-plan.md).

**ТРИ ПРИЧИНЫ (все доказаны чтением кода, не догадки)**
1. **Никто не переподключал.** Обработчик `did-fail-load` в [webviewSetup.js](../src/utils/webviewSetup.js) только ставил метку «ошибка» — ни `reload()`, ни `loadURL()`.
2. **Слои прозрачные** — и это ответ на «вижу Telegram вместо WhatsApp». В [App.jsx](../src/App.jsx) мессенджеры лежат слоями друг на друге (`zIndex: activeId === m.id ? 2 : 0`), у слоя НЕТ своего фона → пустой слой просвечивает, и видно нижний слой «Общий чат» с чатами Telegram. Это НЕ подмена данных, а просвет.
3. **Никто не знал, что связь вернулась** — в проекте не было ни одного слушателя `online`/`offline` (поиск по `src/` и `main/` дал ноль).

**ЧТО СДЕЛАНО**
| Файл | Что |
|---|---|
| [shared/reconnectPlan.js](../shared/reconnectPlan.js) (новый, 193/300) | ЧИСТАЯ логика: лестница пауз, список сетевых кодов, планирование попытки, тексты записей журнала. В `shared/` — вне бюджета renderer |
| [WebviewOfflineOverlay.jsx](../src/components/WebviewOfflineOverlay.jsx) (новый, 136/700) | Экран по согласованному макету: причина, обратный отсчёт, полоса, «Повторить сейчас» + «Журнал». **Непрозрачный фон закрывает просвет (причина 2)**. Рисуется ПОВЕРХ — сам webview прятать нельзя (Chromium усыпляет скрытые). Секунды считает сам, чтобы из-за отсчёта не перерисовывалось всё окно |
| [useWebviewReconnect.js](../src/hooks/useWebviewReconnect.js) (новый, 135/150) | Проводка: `bindReconnect` вешает слушатели на элемент страницы, ведёт запись по каждому мессенджеру, один таймер просыпается ровно к сроку (а не тикает каждую секунду), повтор через `loadURL` |
| [App.jsx](../src/App.jsx) 946→965 | Подключение хука + отрисовка экрана внутри слоя мессенджера + `bindReconnect(el, m.id)` в ссылке на элемент |

**ПРАВИЛА ПОВТОРОВ**: 5с → 10с → 20с → 40с → 60с → далее по 60с; **у МАКСа не меньше 15с** (у него уже был шторм «Too many requests», ADR v1.2.417); попытки без предела, пока страница не поднялась; счётчик сбрасывается при успехе; при возврате сети ближайшая попытка идёт СРАЗУ; кнопка «Повторить сейчас» доступна всегда.

**Повторяем ТОЛЬКО на сетевых кодах** (`-2, -7, -21, -100, -101, -102, -105, -106, -109, -118, -137, -324`). Код `-3` (ERR_ABORTED — обычная отмена перехода) НЕ считается обрывом: иначе перезагружали бы живую страницу и **стирали недописанное сообщение**. Это закреплено двумя тестами-ловушками.

**ФАКТЫ (🥇 из `DOCS/Electron docs`)**: (1) «`net.isOnline()` … return value of `true` is inconclusive» (`api/net.md`) — «сеть есть» только подсказка, поэтому она лишь ускоряет попытку, а решает сама загрузка; (2) renderer использует `navigator.onLine` + события `online`/`offline` (`tutorial/online-offline-events.md`) — берём их, а не опрос по таймеру в главном процессе (у модуля `net` события в доке нет); (3) `<webview>.loadURL()` «resolve when the page has finished loading… rejects if the page fails to load» (`api/webview-tag.md:278`) — готовый признак «получилось / не вышло».

**🔴 ЗАЩИТА, КОТОРУЮ Я СНАЧАЛА ОБОШЁЛ (поймал страж, исправлено по-настоящему)**: первая версия `bindReconnect` вешала слушатели напрямую через `el.addEventListener` — мимо учёта `el._chatcenterListeners`, по которому [App.jsx:360-364](../src/App.jsx) снимает подписки при удалении вкладки. То есть подписка осталась бы навсегда (утечка). Тест `memoryLeaks.test.cjs` («Нет el.addEventListener в setWebviewRef») это поймал. Исправлено: регистрация идёт через тот же список; добавлен тест на ЗАПРЕЩЁННЫЙ случай — проверяет, что оба слушателя попали в учёт и снимаются повтором чистки из App.jsx.

**ПОЧЕМУ НЕ ТРОГАЛ `webviewSetup.js`**: файл стоит на своём потолке 605 строк. Две строки сигнала выводили его на 607 → по правилу проекта нельзя поднимать потолок, надо разделять файл (это отдельная большая задача). Поэтому слушатели вешаются прямо на элемент, а `webviewSetup.js` не изменён вовсе.

**БЮДЖЕТ RENDERER**: 35300 → 35480 (факт 35426). Это НОВАЯ функция: экран 136 строк + проводка 135. Вся чистая логика уже вынесена в `shared/` (193 строки), разгружать больше нечего — по [[decisions]] ADR-044 поднятие тут законно (оно запрещено ВМЕСТО разгрузки).

**ЖУРНАЛ**: `[net] связь пропала` · `[net] связь появилась — пробуем поднять мессенджеры: N` · `[reconnect] WhatsApp: обрыв связи, код=-106 ERR_INTERNET_DISCONNECTED, попытка 1 через 5с` · `[reconnect] WhatsApp: попытка 3 не удалась (код=-106 …), следующая через 40с` · `[reconnect] WhatsApp: повтор по кнопке пользователя` · `[reconnect] WhatsApp: связь восстановлена за 47с (попыток: 4)` · `[reconnect] Ozon: код=-20 (…) — повтор не нужен`.

**КАК ПРОВЕРЕНО**: новый [reconnectPlan.vitest.js](../src/__tests__/reconnectPlan.vitest.js) **24/24** (лестница пауз, минимум МАКСа, коды ошибок, планирование, «пора/не пора», тексты записей, мусор на входе); новый [useWebviewReconnect.vitest.jsx](../src/hooks/useWebviewReconnect.vitest.jsx) **19/19** (фейковые таймеры: обрыв → ожидание → попытка → успех; путь ошибки с ростом паузы 5→10→20; возврат сети; кнопка; страница поднялась сама; двойная привязка; элемента ещё нет; снятие таймера при размонтировании; защита от утечки; рендер экрана + блокировка кнопки во время попытки + непрозрачный фон). Полный `vitest run` — **197 файлов / 2606 тестов** (было 195/2563). **ВСЯ цепочка pre-push — 35 тестов, 0 падений.** Линт 0. Лимиты 631/631. `npm run build` — `built in 13.93s`.

**ЧТО ПРОВЕРИТЬ ВРУЧНУЮ**: открыть веб-мессенджер → выключить Wi-Fi → должен появиться экран «Нет связи» с отсчётом (чатов Telegram сквозь него быть НЕ должно) → нажать «Повторить сейчас» → включить сеть обратно → экран уходит сам, в журнале `связь восстановлена за …`.

**Откат**: `git checkout HEAD -- src/App.jsx src/__tests__/fileSizeLimits.test.cjs src/__tests__/componentScope.test.cjs shared/changelogData.js && rm shared/reconnectPlan.js src/hooks/useWebviewReconnect.js src/components/WebviewOfflineOverlay.jsx src/__tests__/reconnectPlan.vitest.js src/hooks/useWebviewReconnect.vitest.jsx`.

**ОСТАТОЧНЫЕ РИСКИ**: (а) 🟡 сам обрыв связи в живом приложении не воспроизводился — экран и повтор проверены тестами с заглушкой страницы, нужна ручная проверка; (б) 🟡 если мессенджер отвечает страницей-заглушкой «нет интернета» (а не ошибкой загрузки), `did-fail-load` не придёт и экран не появится — тогда сработает только возврат сети; (в) 🟢 «Общий чат» (Telegram API), три скрытые страницы Ozon и ИИ-панель в этот заход НЕ включены — осознанно, см. раздел 8 плана.

### v1.2.444 — ДОВОДКА ИКОНКИ по придирчивому ревью: свой файл `.ico` (иначе в панели задач серое пятно), сломанный тест-страж, защита запуска и записи в журнал

**КОНТЕКСТ**: ревью работы v1.2.443 нашло 5 дефектов, два из них серьёзные. Все закрыты.

**№1 🔴 Падал тест из ОБЯЗАТЕЛЬНОЙ цепочки — код нельзя было отправить.**
[overlayIcon.test.cjs:53](../src/__tests__/overlayIcon.test.cjs) сторожил строку `42, 171, 238` — цвет простого синего круга, который в v1.2.443 заменён настоящим знаком. Результат: `36 ✅ / 1 ❌`, а `overlayIcon` стоит в списке хука [pre-push](../scripts/hooks/pre-push) (строка 24) → отправка кода блокировалась. Проверка не удалена, а ПЕРЕНАСТРОЕНА на то, что теперь важно: знак берётся из единого источника, порядок цветов `bgra`, у рисования есть запасной путь (3 проверки вместо 1, файл 39/39).
🔴 **ГРАБЛЯ (записана в [[webview-navigation-ui]])**: удаляя код, надо искать тесты, которые ЦИТИРУЮТ удаляемое (`grep` по числам/строкам), а не только запускать «свои» тесты. Я прогнал 14 тестов из 34 и пропустил тот единственный, что относился к изменённому файлу.

**№2 🔴 Иконка в панели задач была бы серым пятном.**
Корень (🥇 по коду electron-builder 26.15.3, `app-builder-lib/out/util/iconConverter.js`): если дать ему PNG, он делает `.ico` **уменьшением** этой картинки. Замер: уменьшение 512→16 оставляет **1 белый пиксель вместо 41** (сравнил с нарисованным сразу 16 px, обе картинки смотрел увеличенными в 14 раз). Отдельно нарисованные мелкие размеры (`PNG/chatcenter-icon-16.png`) при этом не использовались НИКЕМ.
ФИКС: собираем `.ico` сами — [shared/appIconMark.js](../shared/appIconMark.js) `buildICO()` (259/300) кладёт внутрь **6 картинок 256/128/64/48/32/16, каждая НАРИСОВАНА под свой размер** (у мелких линия толще). `build.win.icon` переведён с `build/icon.png` на `build/icon.ico`.
Почему это работает (🥇 тот же файл, строки 190-203): «If source already has the target extension … return it directly» — готовый `.ico` electron-builder **не перекодирует**, только читает заголовок и требует максимальный размер ≥256. Проверено его же порядком чтения: `картинок внутри 6, максимальный размер 256` ✅.
Формат `.ico` (спецификация Microsoft): заголовок 6 байт (0,0 | тип=1 | число картинок) + по 16 байт на картинку (ширина/высота, где 0 = 256; слоёв=1; бит=32; длина и смещение данных) + сами картинки; с Vista внутрь можно кладать PNG — им и пользуемся.

**№3 🟡 В окне «Что нового» пользователю было обещано то, чего нет** — «для 16 и 32 линия толще, иначе в панели задач…». Для трея правда, для панели задач — нет (следствие №2). После №2 обещание стало правдой, текст уточнён ([changelogData.js](../shared/changelogData.js)).

**№4 🟡 Косметический значок мог не дать приложению запуститься, и молча.**
Рисование стояло прямо в списке настроек окна, а вызов `createWindow` в [main.js:311](../main/main.js) ничем не обёрнут → ошибка рисования = окна нет вообще, в журнале ни строки. ФИКС: в [windowManager.js](../main/utils/windowManager.js) значок рисуется ДО создания окна под защитой, при сбое `icon` не задаётся (окно откроется со стандартным); в [overlayIcon.js](../main/utils/overlayIcon.js) у трея запасной кадр — прозрачный буфер нужного размера (`nativeImage` его принимает).

**№5 🟡 Доработка была «немой»** — 62 мс на пути запуска не логировались, хотя в том же файле каждый шаг пишется через `wlog()`. ФИКС: `wlog('icon drawn 128px in Nms')` и `wlog('icon FAILED: …')`; у трея `[tray] значок трея готов: 32px за Nмс` и `[tray] знак нарисовать не удалось: …` (уровень WARN).

**🟢 По правилу «за собой убирай»**: `build/icon-256.png` никем не использовался — убран из скрипта и с диска (тест это закрепляет).

**КАК ПРОВЕРЕНО**: [appIconMark.vitest.js](../src/__tests__/appIconMark.vitest.js) **30/30** — включая 🔴-ловушку «внутри .ico лежат картинки, нарисованные под каждый размер» (сверка ПОБАЙТОВО: каждая картинка внутри = `markPNG(этот размер)`), чтение заголовка тем же порядком, что у electron-builder, «256 записан нулём», совпадение файла на диске с тем, что собирает код, и две ловушки на защиту запуска (рисование вынесено из настроек окна + записи в журнал). [overlayIcon.test.cjs](../src/__tests__/overlayIcon.test.cjs) **39/39**. **ВСЯ цепочка pre-push — 35 тестов, 0 падений** (в прошлый раз прогонял 14 из 34 — отсюда и пропущенный №1). Полный `vitest run` — **195 файлов / 2563 теста**. `npm run build` — `built in 12.36s`. Линт 0. Лимиты 626/626 (⚠️ `shared/appIconMark.js` 259/300 — 86%, при следующем росте разделить: рисование и упаковка в файлы это две разные темы).

**ЧТО ПРОВЕРИТЬ ВРУЧНУЮ**: (1) полный перезапуск → значок у часов + записи `[tray] значок трея готов` и `[startup-window] … icon drawn 128px` в журнале; (2) `npm run dist:win` → переустановить → **иконка в панели задач должна быть чёткой** (это и есть проверка №2), а в журнале сборки НЕТ строки «application icon is not set».

**Перерисовать иконку** после правки цвета/геометрии: `node scripts/make-app-icon.cjs` (пишет `build/icon.ico`, `build/icon.png`, три файла в `PNG/`).

**Откат**: `git checkout HEAD -- main/utils/overlayIcon.js main/utils/windowManager.js package.json shared/changelogData.js src/__tests__/overlayIcon.test.cjs shared/appIconMark.js scripts/make-app-icon.cjs && rm -f build/icon.ico`.

**ОСТАТОЧНЫЕ РИСКИ**: (а) 🟡 сам установщик с новым `.ico` ещё не собирался — проверка «панель задач чёткая» ждёт пересборки; (б) 🟢 Electron для сырых картинок ждёт цвет, уже умноженный на прозрачность, а мы отдаём обычный: полупрозрачных точек в значке трея 40 из 1024 (только скруглённые углы) — возможен еле заметный светлый ободок, без запуска не проверить; (в) 🟢 вариант знака без плитки (`tile: false`) даёт тёмную кромку по краям (смешивание идёт с цветом плитки) — нигде не используется; (г) 🟢 `PIXEL_FONT` и `drawPixelText` в `overlayIcon.js` — мёртвый код, живой только благодаря старому тесту (**был мёртв до этой работы**, не трогал).

### v1.2.443 — ЗНАК ПРИЛОЖЕНИЯ: своя иконка (трей + окно + установщик) и знак на экранах загрузки

**КОНТЕКСТ**: у приложения НЕ БЫЛО своей иконки вообще. Доказательство — журнал сборки v1.2.442: `default Electron icon is used, reason=application icon is not set` (это сообщение печатает сам electron-builder, `platformPackager.js: getOrConvertIcon` → когда `build.win.icon` не задан). В трее при этом рисовался просто синий круг цвета `rgb(42,171,238)` — телеграмный голубой, никак не связанный с продуктом. По макетам (плиты 01–03, обсуждались с пользователем) выбран вариант **№02 «Сходящиеся линии», оттенок «Лазурь»**: белая реплика с хвостиком + три лазурные полосы (#38BDF8) на тёмной плитке.

**ЕДИНЫЙ ИСТОЧНИК РИСУНКА** — новый корневой [shared/appIconMark.js](../shared/appIconMark.js) (212/300):
• `drawMark({size, order, tile, pad})` рисует знак в буфер картинки: сглаживание пробами по сетке (SS×SS на пиксель), фигуры считаются через расстояния (отрезок → полосы с круглыми концами, знаковое расстояние до скруглённого прямоугольника → контур пузыря, половинные плоскости → хвостик). `order: 'bgra'` — для Electron на Windows, `'rgba'` — для файла.
• `encodePNG` / `markPNG` — свой кодировщик PNG (подпись + IHDR + IDAT + IEND, таблица CRC32, сжатие встроенным `node:zlib`). **Новых зависимостей НЕ добавлено** — в проекте уже был приём «рисуем картинку в буфер руками» ([overlayIcon.js](../main/utils/overlayIcon.js) так делает бейдж).
• Геометрия задана ОДИН раз (пузырь `22,10,38×34,r11`; полосы `x 3→17` на `y 18/32/46`; хвостик `31,44 → 31,57 → 43,44`) — те же числа стоят в разметке обеих заставок, и это закреплено тестом «геометрия одинаковая в трёх местах».

**ГДЕ ТЕПЕРЬ ЗНАК**
| Место | Как сделано |
|---|---|
| Трей (значок у часов) | [overlayIcon.js](../main/utils/overlayIcon.js) `createTrayBadgeIcon` — `drawMark({size:32, order:'bgra'})` вместо синего круга (12 мс) |
| Окно приложения | [windowManager.js](../main/utils/windowManager.js) — `icon: nativeImage.createFromBuffer(drawMark({size:128…}))`. Рисуем в память, а не читаем файл: пути в разработке и в установленной версии разные (62 мс на старте) |
| Установщик и ярлык | [scripts/make-app-icon.cjs](../scripts/make-app-icon.cjs) → `build/icon.png` 512×512 + `build.win.icon` в `package.json`. electron-builder сам делает `.ico` |
| Папка иконок мессенджеров | `PNG/chatcenter-icon.png` 256×256 + `-32.png` + `-16.png` (рядом с `max-icon.png`, `VK-icon.png`, `whatsapp-icon.png`) |
| Стартовая заставка | [index.html](../index.html) — знак 76 px, полосы «влетают» слева по очереди (`@keyframes ccBarIn`, с уважением к «без анимаций») |
| Экран загрузки чатов | [ChatListLoadingSplash.jsx](../src/native/components/ChatListLoadingSplash.jsx) + [styles-chatlist-loading.css](../src/native/styles-chatlist-loading.css) — знак 72 px над надписью «ЦентрЧатов» |

**ДВЕ ЛОВУШКИ, ПОЙМАННЫЕ ПО ХОДУ** (обе записаны в [[webview-navigation-ui]]):
1. 🟡 **Мелкий размер нельзя просто уменьшить.** Первая версия рисовала знак с отступом 14% и линией 7 — на 16 px он превратился в пятно (проверено «лупой»: каждый пиксель увеличен в 14 раз). Исправлено: при размере ≤24 px линия 9.5, отступ плитки 5% (вместо 14%). Тест считает пиксели по цветам и падает, если на 16 px не видно И белого пузыря, И лазурных полос.
2. 🟡 **Градиент текста делает прозрачным вложенный рисунок.** Шапка заставки имела `background-clip: text; color: transparent` на ОБЩЕМ блоке — вставленный внутрь знак стал бы невидимым. Градиент перенесён на отдельный `.native-chatload-brandtext`; тест-ловушка проверяет, что на общем блоке `color: transparent` больше нет.

**Время рисования** (замерено): 32 px — 12 мс, 128 px — 62 мс, 512 px — 246 мс. Проб на пиксель сделано зависимым от размера (мелкие 4×4, крупные 2×2): при 16 пробах значок окна отнимал 278 мс на старте, что заметно.

**КАК ПРОВЕРЕНО**: новый [appIconMark.vitest.js](../src/__tests__/appIconMark.vitest.js) **21/21** — цвета выбранного варианта, размер буфера, разный порядок цветов rgba/bgra, видимость обеих частей знака на 16/32/256, скруглённые углы плитки, прозрачный вид без плитки, корректность файла PNG (подпись/размер/IEND/сжатие), наличие `build/icon.png` **не меньше 256×256** (требование electron-builder: `iconConverter.js` — «Icon must be at least 256x256 pixels»), иконка в папке `PNG/`, прописка `build.win.icon` с существующим путём, трей больше не рисует синий круг, у окна задан значок, знак на обеих заставках, единая геометрия. Полный `vitest run` — **195 файлов / 2554 теста** (было 194/2533). Линт 0. Лимиты **626/626**. Цепочка cjs-стражей: `mainRuntime` 109/109 (главный процесс реально грузит новый модуль), `appStructure` 60/60, `integrationChains` 18/18, `smokeTest` 39/39, `memoryLeaks` 31/31, `ipcChannels` 45/45, `projectHealth` 33/33, `rendererConsoleGuard` 0 нарушений.

**ЧТО ПРОВЕРИТЬ ВРУЧНУЮ**: (1) значок у часов (трей) — знак, а не круг; (2) обе заставки — знак с «влетающими» полосами; (3) ПОСЛЕ пересборки и переустановки — иконка в панели задач и на ярлыке, а в журнале сборки НЕТ строки «application icon is not set».

**Перерисовать иконку** после правки цвета/геометрии — одна команда: `node scripts/make-app-icon.cjs`.

**Откат**: `git checkout HEAD -- main/utils/overlayIcon.js main/utils/windowManager.js index.html src/native/components/ChatListLoadingSplash.jsx src/native/styles-chatlist-loading.css package.json && rm shared/appIconMark.js scripts/make-app-icon.cjs src/__tests__/appIconMark.vitest.js build/icon.png build/icon-256.png PNG/chatcenter-icon*.png` (вернёт синий круг и отсутствие иконки).

**ОСТАТОЧНЫЕ РИСКИ**: (а) 🟡 лазурь близка к синему Telegram — пользователь предупреждён и выбрал сознательно; при желании цвет меняется в `MARK` одной строкой + перегенерация; (б) 🟢 `.ico` делает electron-builder, свой формат мы не собираем — если он однажды перестанет конвертировать PNG, потребуется готовый `.ico`; (в) 🟢 внешний вид заставок и трея автотестами не проверяется (только наличие строк) — нужна визуальная проверка.

### v1.2.441-v1.2.442 — Аватарки: доводка по ревью (кнопка «Обновить фото» видна на значке, неудача не теряется, ключ записи журнала без цифры) + РАЗГРУЗКА бюджета интерфейса (данные «Что нового» и код проверок вынесены, App.jsx 1074→946, планка опущена 36090→35300) (архив → [archive/features-v1.2.441-442.md](archive/features-v1.2.441-442.md))

### v1.2.439-v1.2.440 — АВАТАРКИ веб-аккаунтов: вечные метки в памяти страницы (чужое фото у Telegram, нет фото у МАКС) + доводка (снимок для любого мессенджера, причина отказа в журнал, пункт меню «Обновить фото») (архив → [archive/features-v1.2.439-440.md](archive/features-v1.2.439-440.md))

### v1.2.438 — ОЗОН, третий заход: память «уже показывали» перенесена ИЗ чужой страницы В наше приложение (запись в localStorage Ozon не доходила) (архив → [archive/features-v1.2.438.md](archive/features-v1.2.438.md))

### v1.2.437 — ФИКС «Ozon: один и тот же вопрос уведомляет каждую минуту»: отпечаток считался от позиционно взятого товара + память ЗАМЕНЯЛАСЬ вместо дополнения; плюс запись о смене вкладки (архив → [archive/features-v1.2.437.md](archive/features-v1.2.437.md))

### v1.2.436 — ФИКС «выкидывает в ЦентрЧатов»: загрузка списка мессенджеров отбирала активную вкладку на КАЖДОМ монтировании (в разработке — на каждую горячую перезагрузку от правки файла) (архив → [archive/features-v1.2.436.md](archive/features-v1.2.436.md))

### v1.2.435 — Доводка фикса чёрного экрана веб-МАКС по самопроверке: журнал отказов по КАТЕГОРИЯМ (без цифр в ключе), сбой впрыска больше не молчит, сторож реже, страж ссылок перестал быть слепым (архив → [archive/features-v1.2.435.md](archive/features-v1.2.435.md))

### v1.2.434 — Чёрный экран веб-МАКС: НАСТОЯЩАЯ причина — НАШ ЖЕ впрыск прятал приложение (`display:none` на корне) → предохранители по тексту/высоте/площади/детям + отчёт в журнал (архив → [archive/features-v1.2.434.md](archive/features-v1.2.434.md))

### v1.2.433-v1.2.424 — МАКС: фантомы/дубль уведомлений, тормоз наблюдателя, анти-завал (лавина из одного чата), фикс MaxListeners + сага сторожа «пустого экрана» (3 итерации, теория перерисовки ОПРОВЕРГНУТА в v1.2.434) (архив → [archive/features-v1.2.424-433.md](archive/features-v1.2.424-433.md))
### v1.2.423-v1.2.416 — Сага «чёрный экран веб-МАКС»: 5 попыток (1px-пинок → SW/429-шторм → реальный UA → диагностика сокета → sandbox=no) + доводки (архив → [archive/features-v1.2.416-423.md](archive/features-v1.2.416-423.md)). НАСТОЯЩАЯ причина найдена только в v1.2.434 — см. запись выше.
### v1.2.415-v1.2.406 — Единый экран загрузки чатов + ВК-значок рейла (console __CC_UNREAD__) + Ozon-виджет/«Отзывы» + ФИКС/диагностики чёрного экрана МАКС (архив → archive/features-v1.2.406-415.md)
### v1.2.405-v1.2.401 — Заставка загрузки чатов: один экран, видимая анимация (счётчик/аватарки), на всё окно, бесшовный старт (архив → archive/features-v1.2.401-405.md)
### v1.2.400-v1.2.396 — Старт/заставка загрузки: живые кружки, старт на «ЦентрЧатов», мин-время видимости, холодный старт; Ozon «Вопросы» reload 3мин→1мин (архив → archive/features-v1.2.396-400.md)
### v1.2.395-v1.2.393 — Заставка первой загрузки чатов: старт саги (7 улучшений) + мин-время видимости + холодный старт + разбор ВК (архив → archive/features-v1.2.393-395.md)

### v1.2.392-v1.2.389 — Ozon «Отзывы»: rv-счётчик (Вариант C), откат ведения счётчика, убрана авто-перезагрузка, фильтр статуса «Получено…» (архив → archive/features-v1.2.389-392.md)


### v1.2.388-v1.2.381 — Ozon счётчики разделов (msg/qa/rv корни+откаты, парсер rv→msg, фильтр статуса «Отзывов», Шаг B/C/D) + ВК аватарки/анти-дубль/диагностика «Игры» (архив → archive/features-v1.2.381-388.md)

### v1.2.380-v1.2.373 — Ozon (значок рейла/авто-освежение «Вопросов»/логотип по типу/«Печатает…»/разведка «Отзывов» Шаг A) + ВК аватарки+анти-дубль + фикс авто-ответа (архив → archive/features-v1.2.373-380.md)

### v1.2.372-v1.2.367 — Ozon фон (пульс/память показанного/впрыск после перезагрузки/оба раздела) + окно «на весь экран» при клике по уведомлению/задаче (архив → archive/features-v1.2.367-372.md)

### v1.2.366-v1.2.361 — Ozon Шаг 3А: диагностика фоновой «Вопросы» + подтверждение работы (впрыск сторожа через executeJavaScript) + ERR_ABORTED→WARN (архив → archive/features-v1.2.361-366.md)

### v1.2.360-v1.2.349 — Счётчики виджета Ozon (msg/qa) + сторож «Вопросов» Шаг 2 (Ответы=0=новый) + фон Шаг 3a/3b + фиксы по ревью + окно ready-to-show (архив → archive/features-v1.2.349-360.md)

### v1.2.348-v1.2.329 — Виджет Ozon (ранний: док/перетаскивание/место) + окно «на весь экран» + ночная стена уведомлений + сторож «Покупатели» + логотип Ozon + заставка (архив → archive/features-v1.2.329-348.md)

### v1.2.328-v1.2.313 — Ozon (строгий режим сессии/антибот, каталог, разведчик структуры) + логотипы ВК/МАКС/WhatsApp + фиксы логов/дедупа уведомлений (архив → archive/features-v1.2.313-328.md)

### v1.2.312 — старт: не заваливать всплывашками offline-бэклог (архив → archive/features-v1.2.303-312.md)

### v1.2.311-v1.2.305 — Сага «чёткий аватар веб-Telegram»: диагностики → фикс → ПОДТВЕРЖДЕНО (архив → archive/features-v1.2.305-311.md)

### v1.2.303-304 — веб-значки рейла: чистые бейджи + свечение + поднятие при перетаскивании (архив → archive/features-v1.2.303-312.md)

### v1.2.302-v1.2.293 — верхняя панель (линейные значки, единая строка), 3-зонный рейл, фикс фото из уведомления, стекло-кнопка, чистка dist (архив → archive/features-v1.2.293-302.md)

### v1.2.288-v1.2.268 — Веб-аватарки (диагностика + чёткое фото из Настроек Telegram), скрытие верхних вкладок, карточки аккаунта/контакта, чистка журнала (архив → archive/features-v1.2.268-288.md)

### v1.2.267-v1.2.256 — Окно «Добавить» (протокол→мессенджер) + портал полосы «← Общий чат» (архив)

Перенесено в [archive/features-v1.2.256-267.md](archive/features-v1.2.256-267.md) (2026-08-19): единая полоса (веб встроен в нативный рейл, SourceRail удалён, ADR-032); одна кнопка «＋ Добавить» → окно «протокол→мессенджер» + доводки; портал полосы на уровне App (v1.2.260 откат→v1.2.262 рабочий) + кнопка «← Общий чат» над вебом.

### v1.2.254-v1.2.244 — боковой рейл SourceRail (Этапы 1–3, ЗАКРЫТ разворотом v1.2.256, архив)
Подход отдельной панели SourceRail закрыт (Модель 🅰️, ADR-032). История: [archive/features-v1.2.244-255.md](archive/features-v1.2.244-255.md).

### v1.2.243-v1.2.237 — Боковой рейл (флаг/тумблер) + карточка контакта (читаемость/контур/заметка) + МАКС мьют-фикс/диагностика (архив)

Перенесено в [archive/features-v1.2.237-243.md](archive/features-v1.2.237-243.md) (2026-09-01, разгрузка под лимит 100 КБ).

### v1.2.236-v1.2.222 — галочки списка + карточка контакта + МАКС-диагностика + строка отправки + пузырь «↓N» (архив)
Перенесено в [archive/features-v1.2.222-236.md](archive/features-v1.2.222-236.md).

### v1.2.221-v1.2.214 — Порядок фото/«текст отдельно» + ВК-навигация vk.ru + фикс счётчика ВК + окно отправки (архив)

Перенесено в [archive/features-v1.2.214-221.md](archive/features-v1.2.214-221.md) (2026-08-13, разгрузка под лимит 100 КБ): ждём серверное подтверждение фото/альбома перед текстом — порядок у получателя (v1.2.214) + таймаут-WARN (v1.2.215); «перейти в чат» по клику ВК-уведомления под vk.ru + имя мессенджера в статусе (v1.2.216) + тест-репродюсер (v1.2.217) + доводка сравнения имени (v1.2.218); фантомная «1 непрочитано» ВК — счётчик перестал считать бейджи «Друзья/Игры» (v1.2.219); окно отправки фото/файла — одна кнопка «Отправить» + инфо-строка (v1.2.220); лента уведомлений — не дёргать список при чтении + видимая прокрутка (v1.2.221).

### v1.2.213-v1.2.205 — Окно отправки фото: подпись/лимит 1024/«текст отдельно»/порядок/диагностика (архив)

Перенесено в [archive/features-v1.2.205-213.md](archive/features-v1.2.205-213.md) (2026-08-11, разгрузка под лимит 100 КБ): фикс двойной вставки фото + разбит windowManager (v1.2.205) + подтверждающий лог (v1.2.206); окно фото — 7 функций просмотра (v1.2.207); счётчик подписи + «фото, затем текст отдельно», лимит Telegram 1024 (v1.2.209); лимит подписи и «текст отдельно» для видео/документов (v1.2.210); длинный текст режется ≤4096 и шлётся ПОСЛЕ загрузки фото — порядок (v1.2.211); доводки «ждём именно это фото» + число сообщений (v1.2.212); диагностика отправки — лог провала/номера/результат (v1.2.213).

### v1.2.204-v1.2.197 — Окно отправки фото: инструменты просмотра (зум/поворот/сдвиг) + много фото + память окна (архив)

Перенесено в [archive/features-v1.2.197-204.md](archive/features-v1.2.197-204.md) (2026-08-11, разгрузка под лимит 100 КБ): окно отправки фото с зумом/поворотом/сдвигом (v1.2.197-198), логи потока + прозрачность PNG + пропуск в альбоме (v1.2.199), запоминание «на весь экран» (v1.2.200), приподнятая карточка + растущее поле подписи (v1.2.201), чистая логика памяти окна + тесты (v1.2.202), несколько фото в одном сообщении (v1.2.203), кнопки в шапке + вставка Ctrl+V + свободнее зум (v1.2.204).

### v1.2.196–v1.2.185 — диагностика ВК/МАКС, установщик/Win32, прокрутка чата (архив)

Перенесено в [archive/features-v1.2.185-196.md](archive/features-v1.2.185-196.md).

### v1.2.184-v1.2.170 — Логотип Telegram/живой статус собеседника/переключатель режимов/фикс поиска/читаемость шапки (архив)

Перенесено в [archive/features-v1.2.170-184.md](archive/features-v1.2.170-184.md).

### v1.2.169-v1.2.161 — Фильтр аккаунтов (мультивыбор+соло) + самолечение + закрепы/ресайз рейла (архив)

Перенесено в [archive/features-v1.2.161-169.md](./archive/features-v1.2.161-169.md) (2026-07-31, разгрузка под лимит 100 КБ): откат «приклеенного» списка закреплённых (161/162); фильтр аккаунтов на левой панели, клик=вкл/выкл, двойной=соло, кнопка «Все» + доводки (163/164); ресайз левого рейла перетаскиванием (165); полоса «Закреплённое» — читаемый фон + переход к сообщению (166/167); фиксы «после удаления 2-го аккаунта оставшийся не грузился» + самолечение фильтра при старте (168/169).

### v1.2.160-v1.2.156 — Сага «перетаскивание закреплённых» (ОТКАЧЕНА в v1.2.161, архив)

Перенесено в [archive/features-v1.2.156-160.md](./archive/features-v1.2.156-160.md) (2026-07-30): серия попыток сделать ручное перетаскивание порядка закреплённых чатов (HTML5 drag → свой образ за курсором → живая перестановка → вынос в отдельный не-виртуальный `PinnedChatList` с Pointer Events). **Итог — откачено в v1.2.161**: закреплённые должны листаться в ОБЩЕМ списке, а не быть отдельным блоком; перетаскивание же требует отдельного списка. Урок: drag-порядок и виртуальный список react-window (строки по индексу-слоту) несовместимы без замены движка. Из серии сохранились только НЕ-drag доводки (цвет-грань уведомления по `accountId`, цвет-полоса в узком режиме).

### v1.2.155-v1.2.147 — Цвет-метки аккаунтов (различимые цвета/полоса/обводка) + фиксы входа/удаления 2-го аккаунта (архив)

Перенесено в [archive/features-v1.2.147-155.md](./archive/features-v1.2.147-155.md) (2026-07-31, разгрузка под лимит 100 КБ): цвет аккаунта в закреплённых/уведомлении (v1.2.155); устойчивые цвет-метки — первый свободный + сохранение вместо хеша (v1.2.154); полоса/обводка/выбор цвета 🎨 (v1.2.153); спиннер ввода кода Telegram (v1.2.152); карточка выхода не заезжает под панель задач + спиннер «думания» (v1.2.150-151); сброс «залипшего success» гейта входа + разгрузка NativeApp (v1.2.148-149); фикс «чёрный экран» после добавления 2-го аккаунта (v1.2.147).

### v1.2.146-v1.2.140 — Добавление/удаление аккаунта (диагностика + фиксы следов) + закреп 📌 + маскировка кода в логах (архив)

Перенесено в [archive/features-v1.2.140-146.md](./archive/features-v1.2.140-146.md) (2026-07-31, разгрузка под лимит 100 КБ): фикс папки сессии при удалении аккаунта (v1.2.145) + чистка кэш-файла/проброс переименования (v1.2.146); диагностика чёрного экрана и операций с аккаунтом + ErrorBoundary вокруг InboxMode (v1.2.142-143); маскировка кода входа в логах (v1.2.144); значок 📌 в углу аватарки (v1.2.141); откат авто-чистки осиротевших закреплений — теряла данные (v1.2.140).

### v1.2.139-v1.2.138 — Локальное закрепление чатов (📌): базовая фича + доработки (архив)

Перенесено в [archive/features-v1.2.138-139.md](./archive/features-v1.2.138-139.md) (2026-07-30, разгрузка под лимит 100 КБ): локальное закрепление чатов полоской+значком 📌, правый клик, хранение в localStorage, НЕ синхронизируется с Telegram (v1.2.138); доработки — 📌 в узком режиме, устойчивый порядок по массиву pinnedIds, вынос `filterSortChats`, чистка осиротевших (позже откачена v1.2.140) (v1.2.139). Перетаскивание порядка сделано позже (v1.2.156→160).

### v1.2.137-v1.2.134 — Уведомление снимается при прочтении на другом устройстве + CI-фикс TZ снапшота + ВК vk.ru (архив)

Перенесено в [archive/features-v1.2.134-137.md](./archive/features-v1.2.134-137.md) (2026-07-30, разгрузка под лимит 100 КБ): прочитал чат в Telegram на телефоне → карточка-уведомление снимается, частичное чтение + чистая `shouldDismissForRead` (v1.2.137); snapshot ChatListItem сделан TZ-независимым — падал на CI под UTC (v1.2.135); ВК основной адрес → vk.ru + убрана лишняя ошибка `GUEST_VIEW_MANAGER_CALL` запасного впрыска (v1.2.134).

### v1.2.133-v1.2.130 — Строка списка чатов: время + имя отправителя (группы) + «Вы:» + значок форума + имя автора в уведомлении (архив)

Перенесено в [archive/features-v1.2.130-133.md](./archive/features-v1.2.130-133.md) (2026-07-30, разгрузка под лимит 100 КБ): время последнего сообщения + имя автора в превью групп/форумов + значок 🗂️ форума (v1.2.130); «Вы:» для своих + единая `lastSenderLabel` (v1.2.131); имя автора в заголовке уведомления + чистка мёртвого `pickNotifTitle` (v1.2.132); фикс залипания имени в превью + вынос `nativeStoreLastMsgIpc.js` (v1.2.133).

### v1.2.129-v1.2.126 — Уведомления: «невидимая стена»/heartbeat-сторож + ВК переносы строк (архив)

Перенесено в [archive/features-v1.2.126-129.md](./archive/features-v1.2.126-129.md) (2026-07-30, разгрузка под лимит 100 КБ): гасим «невидимую стену» сразу по видимой высоте (v1.2.128), heartbeat-сторож без спама (v1.2.127) и его первая версия (v1.2.126); ВК — переносы строк/абзацы в тексте уведомления через `_vkNodeText` (v1.2.129).

### v1.2.125-v1.2.122 — Уведомление «поверх всех» + ВК-хук (разгрузка/эмодзи/mute-детект/переносы строк) (архив)

Перенесено в [archive/features-v1.2.122-125.md](./archive/features-v1.2.122-125.md) (2026-07-29, разгрузка под лимит 100 КБ): окно уведомления надёжно поверх всех — `screen-saver`+reassert (v1.2.125); ВК-хук — удалён мёртвый toast-наблюдатель + эмодзи из `<img alt>` (v1.2.124), сужен mute-селектор + тесты чистых функций (v1.2.123), сохранены переносы строк + снят предел текста 1000 (v1.2.122).

### v1.2.121-v1.2.116 — Telegram: не показываем сторис; ВК/vk.ru: диагностики значка 🔕 + текст целиком + фикс «не уведомлять о заглушённых» (архив)

Перенесено в [archive/features-v1.2.116-121.md](./archive/features-v1.2.116-121.md) (2026-07-29, разгрузка под лимит 100 КБ): Telegram Web — гасим системную фразу «опубликовал(а) историю» (v1.2.121); ВК/vk.ru — итоговый фикс «не уведомлять о заглушённых чатах 🔕» по видимости значка (v1.2.120), текст уведомления целиком без своей мерки (v1.2.119), серия временных диагностик значка «беззвучно» (v1.2.116-118, диагностики давно удалены).

### v1.2.115-v1.2.108 — ВК переезд на vk.ru (детект/текст/диагностика) + время в источнике (архив)

Перенесено в [archive/features-v1.2.108-115.md](./archive/features-v1.2.108-115.md) (2026-07-27, разгрузка под лимит 100 КБ): переезд ВК на vk.ru + определение домена (v1.2.109); настоящий детект новых сообщений по списку чатов (v1.2.112) + подтверждение логом (v1.2.113); чистый текст уведомления по структуре строки (v1.2.114); 3 фикса по ревью — чистка времени/шторм на старте/дубль (v1.2.115); серия временных диагностик структуры vk.ru (v1.2.110/111); время сообщения в строке источника, единый формат (v1.2.108).

### v1.2.107-v1.2.101 — Уведомления: сторож стены + видео/фото в карточке (архив)

Перенесено в [archive/features-v1.2.101-107.md](./archive/features-v1.2.101-107.md) (2026-07-24, разгрузка под лимит 100 КБ): сторож «невидимой стены» + чистая decideNotifResize + быстрее закрытие (v1.2.107); фикс осиротевшего положительного отчёта — «невидимое зависшее окно», ловящее клики (v1.2.106); «Прочитано» для native-Telegram (v1.2.105); видео в уведомлении — постер/плеер как в чате, видео-как-документ, крутилка по факту (v1.2.101-104).

### v1.2.100-v1.2.90 — Уведомления/видео/фото + диагностика Telegram (архив)

Перенесено в [archive/features-v1.2.90-100.md](./archive/features-v1.2.90-100.md) (2026-07-23, разгрузка под лимит 100 КБ): страховка крутилки одиночного фото (v1.2.100), «размытый фон» одиночного фото (v1.2.99), крупный аватар + источник в шапке (v1.2.98), раскладка «Стопка» для всех карточек (v1.2.96-97), одиночное фото/видео в native-уведомлении (v1.2.95), диагностика фото/ссылок Telegram native+web (v1.2.90/92/93/94), расчёт вертикали дока в чистую функцию + тест (v1.2.91).

### v1.2.89-v1.2.74 — Док/подсказка/закреп + имя аккаунта + Telegram-аватар + альбом (архив)

Перенесено в [archive/features-v1.2.74-89.md](./archive/features-v1.2.74-89.md): фиксы дока/подсказки/закрепа (стабильный якорь верха, панель задач Windows, снятие snap, rAF/throttling; v1.2.76-89), доведение имени аккаунта до закрепа/подсказки (v1.2.83-88), Telegram аватар+источник в уведомлении (v1.2.75), листание альбома + чёткие плитки (v1.2.74).

### v1.2.73-v1.2.59 — Док/подсказка/закреп: серия фиксов (архив)

Перенесено в [.memory-bank/archive/features-v1.2.59-73.md](archive/features-v1.2.59-73.md) при разбиении (21 июля 2026). Архитектура — decisions.md ADR-017/ADR-018.

### v1.2.58-v1.2.46 — VK: тост/sidebar/active-chat уведомления, MAX dedup, ribbon (архив)

Перенесено в [.memory-bank/archive/features-v1.2.46-58.md](archive/features-v1.2.46-58.md) при разбиении (17 июля 2026).

### v1.2.45-v1.2.37 — VK DOM observer / диагностика уведомлений (архив)

Перенесено в [.memory-bank/archive/features-v1.2.37-45.md](archive/features-v1.2.37-45.md) при разбиении (17 июля 2026).

### v1.2.36-v1.2.30 — диагностика MAX/системы (архив)

Полные записи перенесены в [archive/features-v1.2.30-36.md](./archive/features-v1.2.30-36.md), чтобы active features.md оставался меньше лимита memory-bank. Суть: диагностика стала ручной, фоновой, с последним отчётом, полной MAX/VK-цепочкой и понятными кнопками; pipeline уведомлений этим блоком не менялся, кроме описанных в соответствующих версиях точечных MAX/VK исправлений.
### v1.2.29 — диагностика запускается только вручную и не мешает работе (архив)

Полная запись перенесена в [archive/features-v1.2.29.md](./archive/features-v1.2.29.md), чтобы активный features.md оставался меньше лимита memory-bank. Суть: большая модалка диагностики больше не стартует запись сама, управление записью стало явным, общий лог не очищается.
### v1.2.28 — MAX: sidebar watcher не создаёт фантомы во время поиска (архив)

Полная запись перенесена в [archive/features-v1.2.28.md](./archive/features-v1.2.28.md), чтобы активный features.md оставался меньше лимита memory-bank. Суть: `max-sidebar` при активном поиске MAX обновляет baseline, но не создаёт ribbon из старого preview; реальные `max-notification-api` и `max-sw-showNotification` не отключались.

**Структура файла**: этот features.md содержит только **последние активные версии**. Старое — в архиве:

| Архив | Содержимое | Размер |
|---|---|---|
| [`archive/features-v0.95.39.md`](./archive/features-v0.95.39.md) | v0.95.39 (убран twoPhase + RAF×2 + 350мс easeOutCubic; стабилизировано v0.95.40+) | ~2 КБ |
| [`archive/features-v0.95.40.md`](./archive/features-v0.95.40.md) | v0.95.40 (большие emoji + a11y reduced-motion + sticky bottom on media; стабилизировано v0.95.41+) | ~3 КБ |
| [`archive/features-v0.95.41.md`](./archive/features-v0.95.41.md) | v0.95.41 (Custom emoji premium WebM/WebP + reduced-motion интеграционные тесты; стабилизировано v0.95.42+) | ~3 КБ |
| [`archive/features-v0.95.42.md`](./archive/features-v0.95.42.md) | v0.95.42 (сохранение поиска + история + ✕ + подсветка совпадений; стабилизировано v0.95.43+) | ~2 КБ |
| [`archive/features-v0.95.38.md`](./archive/features-v0.95.38.md) | v0.95.38 (фикс дубля сообщений через updateMessageSendSucceeded, ⏳ индикатор, mistakes static guard) | ~2 КБ |
| [`archive/features-v0.95.35-37.md`](./archive/features-v0.95.35-37.md) | v0.95.35-37 (fade-in changelog, auto-scroll outgoing-other-device, sending_state polish, mistakes/outgoing-two-cases.md; стабилизировано v0.95.38+) | ~3 КБ |
| [`archive/features-v0.95.34.md`](./archive/features-v0.95.34.md) | v0.95.34 (темовые vars в :root, вспышка bubble, WhatsNewModal UX; стабилизировано v0.95.40+) | ~2 КБ |
| [`archive/features-v0.95.33.md`](./archive/features-v0.95.33.md) | v0.95.33 (фикс «цвет не применяется» через querySelectorAll, регресс-тест blur, деловой стиль; финал в v0.95.34) | ~2 КБ |
| [`archive/features-v0.95.32.md`](./archive/features-v0.95.32.md) | v0.95.32 (производительность WhatsNewModal: убран backdrop-filter + contain + деловой стиль changelog) | ~2 КБ |
| [`archive/features-v0.95.31.md`](./archive/features-v0.95.31.md) | v0.95.31 (аккаунты вниз + drag-n-drop + multi-user typing + throttle реакций; стабилизировано v0.95.34+) | ~3 КБ |
| [`archive/features-v0.95.30.md`](./archive/features-v0.95.30.md) | v0.95.30 (плавная auto-scroll + 5 цветовых тем + dropdown + opacity 0.95; стабилизировано v0.95.33-34) | ~3 КБ |
| [`archive/features-v0.95.29.md`](./archive/features-v0.95.29.md) | v0.95.29 (реакции 👍❤️🔥 + Telegram-style header + General 📢 + render-counter; стабилизировано v0.95.31-34) | ~5 КБ |
| [`archive/features-v0.95.28.md`](./archive/features-v0.95.28.md) | v0.95.28 (Telegram-style auto-scroll к новому + ↓N без «слепой зоны» Schmitt; стабилизировано v0.95.31) | ~4 КБ |
| [`archive/features-v0.95.27.md`](./archive/features-v0.95.27.md) | v0.95.27 (расширенная диагностика send pipeline; стабилизировано v0.95.29) | ~3 КБ |
| [`archive/features-v0.95.23-26.md`](./archive/features-v0.95.23-26.md) | v0.95.23 – v0.95.26 (курсор в input, initial backfill, voice/spellcheck/action-bar/WhatsNew, фикс 47-дневного бага unreadCount; стабилизировано) | ~15 КБ |
| [`archive/features-v0.95.19-22.md`](./archive/features-v0.95.19-22.md) | v0.95.19 – v0.95.22 (диагностика tg-new-message, финал jump-to-end, бейдж форум-группы, форум-overlay; стабилизировано) | ~6 КБ |
| [`archive/features-v0.95.15-18.md`](./archive/features-v0.95.15-18.md) | v0.95.15 – v0.95.18 (итеративный fetch + форум-топики + двухфазный scroll + ForumTopicEmptyState; стабилизировано v0.95.20-21) | ~8 КБ |
| [`archive/features-v0.95.12-14.md`](./archive/features-v0.95.12-14.md) | v0.95.12 – v0.95.14 (3 итерации jump-to-end до итеративного fetch v0.95.15, полная сага в jump-to-end-saga.md) | ~32 КБ |
| [`archive/features-v0.95.8-9.md`](./archive/features-v0.95.8-9.md) | v0.95.8 – v0.95.9 (счётчик ↓ обнуляется + анимация + live compact + порог 128) | ~14 КБ |
| [`archive/features-v0.95.5-7.md`](./archive/features-v0.95.5-7.md) | v0.95.5 – v0.95.7 (sticky pinned overlay, кнопка ↓ Telegram-style, drag-to-resize) | ~24 КБ |
| [`archive/features-v0.95.0-3.md`](./archive/features-v0.95.0-3.md) | v0.95.0 – v0.95.3 (контигуити-фикс, afterId load-newer, мигание ↓ Schmitt trigger, диагностика «дёрг») | ~13 КБ |
| [`archive/features-v0.94.1-7.md`](./archive/features-v0.94.1-7.md) | v0.94.1 – v0.94.7 (TDLib listener leak, scroll caskade, спокойная загрузка, пилюля прогресса — стабилизированы v0.95.0-2) | ~24 КБ |
| [`archive/features-v0.93.0.md`](./archive/features-v0.93.0.md) | v0.93.0 (pixel-perfect scroll restore через LocationOptions.offset, superseded v0.94.0) | ~7 КБ |
| [`archive/features-v0.92.0-6.md`](./archive/features-v0.92.0-6.md) | v0.92.0 – v0.92.6 (сага Virtuoso scroll restore, superseded v0.94.0) | ~37 КБ |
| [`archive/features-v0.91.11-24.md`](./archive/features-v0.91.11-24.md) | v0.91.11 – v0.91.24 (сага scroll restore: 13 версий → миграция на virtuoso) | ~47 КБ |
| [`archive/features-v0.91.1-10.md`](./archive/features-v0.91.1-10.md) | v0.91.1 – v0.91.10 (initial-load, scroll-jump, newBelow, forum topics, updateChatLastMessage) | ~12 КБ |
| [`archive/features-v0.87.106-114.md`](./archive/features-v0.87.106-114.md) | v0.87.106 – v0.87.114 (multi-account UI финал, кнопки режимов, мьют чата, аватарки отправителей в группах) | ~22 КБ |
| [`archive/features-v0.87.93-105.md`](./archive/features-v0.87.93-105.md) | v0.87.93 – v0.87.105 (multi-account native, login flow, разбиения, cc-media://) | ~30 КБ |
| [`archive/features-v0.87.80-92.md`](./archive/features-v0.87.80-92.md) | v0.87.80 – v0.87.92 (pre-push hook, разбиения 4-7, AccountContextMenu) | ~16 КБ |
| [`archive/features-v0.87.65-79.md`](./archive/features-v0.87.65-79.md) | v0.87.65 – v0.87.79 (план разбиения 1-3, pre-push hook, bubble UI) | ~54 КБ |
| [`archive/features-v0.87.51-64.md`](./archive/features-v0.87.51-64.md) | v0.87.51 – v0.87.64 (groupedUnread удалён, pre-commit hook) | ~54 КБ |
| [`archive/features-v0.87.40-50.md`](./archive/features-v0.87.40-50.md) | v0.87.40 – v0.87.50 (итерации native scroll + unread) | ~40 КБ |
| [`archive/features-v0.87-early.md`](./archive/features-v0.87-early.md) | v0.87.0 – v0.87.39 (запуск native + ранние фиксы) | ~140 КБ |
| [`archive/features-pre-v0.87.md`](./archive/features-pre-v0.87.md) | v0.1.0 – v0.86.10 (до native-режима, 3 марта – 14 апреля 2026) | ~210 КБ |

**Архив не читается по умолчанию.** Запрос к нему — только при явной просьбе («что было в v0.85», «покажи старый changelog»).

**До рефакторинга v0.87.57** файл был 445 КБ (3371 строк, 323 версии). После — ~100 КБ в корне.

---

### Архив v1.2.27 и старше

Подробная история v1.2.27 и более ранних версий перенесена в [archive/features-v1.2.27-and-older.md](./archive/features-v1.2.27-and-older.md). Активный файл держим компактным, чтобы memory-bank быстро читался и проходил лимиты.
