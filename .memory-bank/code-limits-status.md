# Статус лимитов файлов кода — текущий снапшот

**Версия**: v0.87.68 (24 апреля 2026)
**Обновляется**: при каждом заметном росте файлов. Автообновление возможно через `node src/__tests__/fileSizeLimits.test.cjs`.

**Зачем отдельный файл**: конкретные числа (размеры, проценты) стареют за дни. CLAUDE.md должен держать только **правила**, а снапшот состояния — здесь.

---

## 🔴 Исключения (файлы с повышенным потолком)

Эти файлы зафиксированы в массиве `KNOWN_EXCEPTIONS` в `src/__tests__/fileSizeLimits.test.cjs`. Тест не падает пока они не превысят потолок.

| Файл | Сейчас | Потолок | Категория / причина | План |
|---|---|---|---|---|
| `main/native/telegramHandler.js` | 1260 | 1300 | Крупная интеграция GramJS | Разбить на `telegramAuth / Messages / Chats / Media` (4-6 ч) |
| `src/native/modes/InboxMode.jsx` | 777 | 800 | Экран native-чата | Разбить на `InboxMode + InboxMessageList + InboxHeader` (2-3 ч) |
| `src/utils/webviewSetup.js` | 590 | 600 | Настройка WebView | Low priority (исторически большая утилита) |
| `src/utils/messengerConfigs.js` | 332 | 400 | Конфиги всех мессенджеров | Держим вместе специально |
| `src/utils/consoleMessageHandler.js` | 403 | 450 | Парсер console-message | Логически цельный |
| `main/handlers/dockPinHandlers.js` | 572 | 600 | Крупный handler | Low priority |

**Детали по разбиению** `telegramHandler.js` и `InboxMode.jsx`: см. [`handoff-code-limits.md`](./handoff-code-limits.md).

---

## 🟡 Близко к лимиту (80%+ от базового, но в пределах)

Эти файлы ещё не в исключениях, но **скоро** туда попадут.

| Файл | Сейчас | Лимит | % |
|---|---|---|---|
| `src/App.jsx` | 600 | 600 | **100%** 🔥 |
| `main/main.js` | ~598 | 600 | **99%** 🔥 |
| `src/hooks/useIPCListeners.js` | 147 | 150 | **98%** |
| `src/hooks/useTabContextMenu.js` | 127 | 150 | 85% |

**⚠️ `src/App.jsx` — 100% лимита**. Следующая строка → красный тест. Нужно **либо разбить**, **либо** поднять потолок в `KNOWN_EXCEPTIONS`.

---

## 🟢 Норма

140+ файлов проверены, все укладываются в лимиты. Общий размер renderer кода (`src/` без тестов): ~10600 строк (лимит 20000).

---

## 🔁 Как обновлять этот файл

После существенного роста файла или добавления новых:

1. Запусти `node src/__tests__/fileSizeLimits.test.cjs` — увидишь все размеры
2. Если тест упал на новом файле → добавь в `KNOWN_EXCEPTIONS` (если разбиение не планируется) или разбивай
3. Обнови таблицы выше с новыми числами
4. Версию файла подними до текущей версии проекта

**Не пиши конкретные числа в CLAUDE.md** — они стареют. Только здесь.
