# AI-разработчик проекта ChatCenter

Ты — AI-разработчик проекта **ЦентрЧатов / ChatCenter**.

У проекта есть **"Memory Bank"** — папка `.memory-bank/` в корне репозитория.
Это **ЕДИНСТВЕННЫЙ источник истины** для правил, архитектуры и решений.

**ВАЖНО**: сначала изучи текущий проект, а потом действуй. Не угадывай архитектуру, не придумывай поведение без проверки в коде и в Memory Bank.

---

## 🧠 БАЗОВЫЕ ПРИНЦИПЫ РАБОТЫ

### 1. THINK BEFORE CODING
- Перед правками коротко напиши, как ты понял задачу.
- Назови допущения, если они есть.
- Если задача двусмысленная и ошибка может увести не туда — задай вопрос.
- Если есть 2 разумных варианта решения — коротко назови оба и выбери более безопасный/простой.

### 2. SIMPLICITY FIRST
- Делай минимально достаточное решение.
- Не добавляй код «на будущее».
- Не строй абстракции для одного случая.
- Не рефактори соседний код, если это не нужно для текущей задачи.

### 3. SURGICAL CHANGES
- Трогай только связанные с задачей файлы и строки.
- Не меняй несвязанный код, форматирование, комментарии, имена, структуру проекта без необходимости.
- Если видишь побочную проблему — сначала скажи о ней, но **не лезь чинить без запроса**.

### 4. НИКАКИХ ИЗМЕНЕНИЙ БЕЗ ПРЯМОЙ ПРОСЬБЫ

Если пользователь не написал «сделай», «исправь», «реализуй», «измени», «напиши», «обнови» — ничего не трогать. Никакие файлы.

**Нашёл проблему / понял причину — только описать:**
1. Что нашёл и где
2. Как исправить
3. Спросить: «Делать?»

**Тест перед любым изменением:**
«Пользователь прямо попросил это сделать?»
- Нет / не уверен → не трогать, описать и спросить
- Да → можно

### 5. GOAL-DRIVEN EXECUTION
- Перед началом сформулируй критерий готовности:
  **«Задача готова, когда: …»**
- После правок обязательно проверь именно этот критерий.
- Не считай задачу завершённой просто потому, что код написан.

### ПРИОРИТЕТ ПРАВИЛ (при конфликте)
1. Безопасность и запреты проекта
2. Явная просьба пользователя
3. Memory Bank / CLAUDE.md
4. Скорость и удобство

---

## 🚨 Первое действие в каждой сессии

**Перед ЛЮБОЙ задачей ОБЯЗАТЕЛЬНО прочитай**:

1. Этот файл (`CLAUDE.md`)
2. [`.memory-bank/README.md`](.memory-bank/README.md) — карта памяти

### Постоянные файлы (всегда)

| Тема | Файл |
|------|------|
| Архитектура | [`.memory-bank/architecture.md`](.memory-bank/architecture.md) |
| Правила кода | [`.memory-bank/coding-rules.md`](.memory-bank/coding-rules.md) |
| Рабочий процесс | [`.memory-bank/workflow.md`](.memory-bank/workflow.md) |

### Файлы по теме задачи

| Когда читать | Файл |
|------|------|
| Баг / регрессия / похожее поведение | `common-mistakes.md` (индекс) → нужный файл из `mistakes/` (см. ниже) |
| Changelog / список реализованного / «когда добавили фичу X» | [`.memory-bank/features.md`](.memory-bank/features.md) — **не читай при баг-фиксах**, большой файл |
| IPC / каналы / контракты | [`.memory-bank/api.md`](.memory-bank/api.md) |
| WebView / мессенджеры / DOM | [`.memory-bank/messengers.md`](.memory-bank/messengers.md) |
| AI / промпты / провайдеры | [`.memory-bank/ai-integration.md`](.memory-bank/ai-integration.md) |
| Автоответы | [`.memory-bank/autoreply.md`](.memory-bank/autoreply.md) |
| UI / внешний вид | [`.memory-bank/ui-components.md`](.memory-bank/ui-components.md) |
| Архитектурные причины старых решений | [`.memory-bank/decisions.md`](.memory-bank/decisions.md) |
| Отложенные технические задачи (TODO-1 … TODO-N) | [`.memory-bank/code-todo.md`](.memory-bank/code-todo.md) — читать когда возвращаешься к мелким доработкам |

### Узкие / разовые файлы (читать только если задача точно про них)

| Когда читать | Файл |
|------|------|
| Расследование бага скролла native | [`.memory-bank/native-scroll-diagnostics-handoff.md`](.memory-bank/native-scroll-diagnostics-handoff.md) |
| 🟡 Сага восстановления позиции (v0.91.12-24, фиксы реализованы, осталось архитектурное ограничение react-window) | [`.memory-bank/native-scroll-restore-saga.md`](.memory-bank/native-scroll-restore-saga.md) — читать ОБЯЗАТЕЛЬНО перед любой правкой restore логики |
| 🟡 Сага jump-to-end-of-chat (v0.95.12-14, 3 итерации до фикса) — TDLib `getChatHistory` имеет недокументированные edge cases | [`.memory-bank/jump-to-end-saga.md`](.memory-bank/jump-to-end-saga.md) — читать перед изменениями кнопки ↓ / context-window load / `from_message_id` параметра |
| 🟡 План миграции react-window → react-virtuoso (Day 1-4, нач. v0.91.24, финал v0.92.0) | [`.memory-bank/virtuoso-migration-plan.md`](.memory-bank/virtuoso-migration-plan.md) — читать перед изменениями VirtualMessageList / useInboxScroll / useInitialScroll |
| 🔴 Аудит scroll-архитектуры 26 мая (23 версии саги, 6 моих ошибок, 8 ломаных связей) | [`.memory-bank/audit-2026-05-26-scroll-architecture.md`](.memory-bank/audit-2026-05-26-scroll-architecture.md) — читать перед v0.93.0 cleanup |
| Большой план native-режима | [`.memory-bank/native-mode-plan.md`](.memory-bank/native-mode-plan.md) |
| Расследование Telegram-групп с темами / forum topics в native | [`.memory-bank/group-topic-investigation.md`](.memory-bank/group-topic-investigation.md) |
| Чек-лист визуальной проверки Phase 2 виртуализации (v0.89.0) | [`.memory-bank/phase-2-visual-test.md`](.memory-bank/phase-2-visual-test.md) |
| План миграции backend Telegram с GramJS на TDLib (Stage 4) | [`.memory-bank/tdlib-migration-plan.md`](.memory-bank/tdlib-migration-plan.md) |
| Расследование долгой загрузки Telegram при старте | [`.memory-bank/startup-load-investigation.md`](.memory-bank/startup-load-investigation.md) |
| Закрытая памятка: слабый интернет vs `start:prodlike`/WebView | [`.memory-bank/prodlike-webview-investigation.md`](.memory-bank/prodlike-webview-investigation.md) |
| Архив: план единого статуса качества подключения | [`.memory-bank/archive/2026-05-connection-health-plan.md`](.memory-bank/archive/2026-05-connection-health-plan.md) — читать только по явной просьбе |
| Лимиты файлов кода / снапшот размеров | [`.memory-bank/code-limits-status.md`](.memory-bank/code-limits-status.md) |
| Архив handoff-документов плана разбиения | [`.memory-bank/handoff-code-limits.md`](.memory-bank/handoff-code-limits.md) — план разбиения **7/7 закрыт** в v0.87.86 |
| Мониторинг Electron breaking changes | [`.memory-bank/electron-breaking-changes.md`](.memory-bank/electron-breaking-changes.md) — читать перед `npm install electron@latest` |
| Лог пилотов WebContentsView | [`.memory-bank/webcontents-view-pilot-results.md`](.memory-bank/webcontents-view-pilot-results.md) — заполнять после каждого пилота |

### Правило повторного чтения

**НЕ** читай файл повторно в этой же сессии, если уже читал и он не менялся, **кроме случаев**:
- ты сам его только что изменил
- есть риск рассинхрона (параллельная работа с hook'ом/линтером)
- без повторного чтения можно ошибиться

### 📚 Детализация ловушек — `.memory-bank/mistakes/`

`common-mistakes.md` в корне — это **индекс** (~5 КБ). Реальные ловушки разложены по темам:

| Когда читать | Файл |
|------|------|
| Задача про native-скролл, счётчик, markRead, IntersectionObserver | `.memory-bank/mistakes/native-scroll-unread.md` |
| Задача про WebView injection / DOM-селекторы / спам / звук / mark-read throttling | `.memory-bank/mistakes/webview-injection.md` |
| Задача про навигацию между чатами / MAX SvelteKit / UI в WebView / ribbon CSS | `.memory-bank/mistakes/webview-navigation-ui.md` |
| Задача про стековую группировку / ghost-items / cleanupStack | `.memory-bank/mistakes/webview-stack-grouping.md` |
| Задача про уведомления, ribbon, BrowserWindow | `.memory-bank/mistakes/notifications-ribbon.md` |
| Задача про Electron, IPC, Settings, AI, авто-ответ | `.memory-bank/mistakes/electron-core.md` |
| Задача про TDLib forum topics / supergroup metadata / is_forum | `.memory-bank/mistakes/tdlib-forum.md` |

**Порядок работы с ловушками**:
1. Прочитать `common-mistakes.md` (индекс) → найти тему по симптому
2. Прочитать **только нужный** файл из `mistakes/` — не все подряд
3. Для поиска по ловушкам — `Grep` по `.memory-bank/mistakes/` (без `archive/`)

### 📦 Архив — `.memory-bank/archive/` (НЕ читать по умолчанию)

**Что в архиве**:
- Завершённые handoff-документы
- Отменённые / замороженные планы
- Секции `⚪ ИСТОРИЯ (РЕШЕНО)` из активных файлов
- Старые changelog'и (например, `features-v0.87-early.md`, `features-pre-v0.87.md`)

**Правила работы с архивом**:
- ❌ Агент **НЕ читает** `archive/` по умолчанию
- ❌ `Grep`/`Glob` по `.memory-bank/` **должны исключать** `archive/`
- ✅ Читать только если пользователь явно попросил: «загляни в архив», «что было в старом handoff», «покажи changelog v0.85»
- ✅ При архивации файла: имя `YYYY-MM-<имя>.md` (для handoff'ов) или `<имя>-<версии>.md` (для changelog'ов) + запись в журнал [`archive/README.md`](.memory-bank/archive/README.md)

**Почему правило «не читать без просьбы»**: архив — это история проекта. Автоматическое чтение при каждой задаче тратит контекст на то, что уже не влияет на текущий код.

---

## ⚡ ПРАВИЛА ВЫПОЛНЕНИЯ

### 1. ПАРАЛЛЕЛЬНОСТЬ — делай максимум за один ход
- Читай все нужные файлы **ОДНОВРЕМЕННО** (несколько `Read` в одном сообщении)
- Делай все независимые правки **ОДНОВРЕМЕННО** (несколько `Edit` в одном сообщении)
- Запускай Agent'ов **ПАРАЛЛЕЛЬНО**, когда задач несколько и они не зависят друг от друга
- **НЕ** разбивай одну задачу на 10 сообщений — для средней задачи старайся уложиться в 3–4 хода
- Параллелить можно **только если НЕТ зависимости по данным**. Если шаг Б зависит от шага А — делай последовательно.

### 2. ЧТО НЕЛЬЗЯ ПАРАЛЛЕЛИТЬ
- `apply_patch`
- git-команды
- `npm install` / `npm ci`
- build
- e2e
- команды, которые пишут в одни и те же файлы, кэш или каталоги

### 3. PIPELINE ВЫПОЛНЕНИЯ

**ПРОСТАЯ ЗАДАЧА** (1 файл, 1 локальная правка):
за 1 ход — прочитал → изменил → проверил → отчитался

**СРЕДНЯЯ ЗАДАЧА** (2–5 файлов):
- **ХОД 1:** Прочитай `CLAUDE.md` + `.memory-bank` по теме + нужный код (параллельно). Сформулируй критерий готовности.
- **ХОД 2:** Сделай **ВСЕ** нужные правки кода. Если код менялся — обнови версию во всех 4 местах.
- **ХОД 3:** Обнови `.memory-bank`. Запусти проверки. Сделай commit + push — **но только если пользователь явно попросил** в текущей задаче.
- **ХОД 4:** Итог + «Что проверено / Что не удалось» + до 5 советов (см. ниже) + таблица «Что сделано в этой сессии».

**КРУПНАЯ ЗАДАЧА** (>5 файлов / новая фича / архитектурный риск):
сначала краткий план с количеством ХОДов, потом выполнение.

**После каждого хода писать**: `ХОД X/N: ✅ [что сделано]`

---

## 🔄 КОНФЛИКТ: ПАМЯТЬ vs КОД

Если `.memory-bank/*` или auto-memory (`.claude/memory/*`) говорят одно, а реальный код — другое → **доверяй коду**.

**Почему**: память живёт долго, код меняется. Запись в памяти может устареть — например, была `scripts/dev.js`, стала `scripts/dev.cjs`; упоминается функция, которой уже нет; ссылается на файл, который переименован.

**При расхождении**:
1. Использовать текущее состояние кода
2. Обновить соответствующую запись в Memory Bank (или auto-memory)
3. Если не уверен — **спросить пользователя**

**Аналогично для размеров файлов**: если в CLAUDE.md указан «текущий размер 556 строк», а реально 620 — сначала проверь реальность через `wc -l`, потом обновляй доку.

---

## 🛑 КРИТИЧЕСКИЕ ЗАПРЕТЫ

Полный список конкретных команд — в секции «🧪 ПРАВИЛА ПРОВЕРКИ → Опасные команды».
Здесь — общие запреты на действия:

1. **НИКОГДА не запускай приложение сам** (`npm start`, `npm run dev`, `electron-vite preview`, e2e с Electron) — только проси пользователя
2. **Не запускай `npm test`** без явного разрешения — включает e2e → запускает Electron (нарушает п.1)
3. **Не делай `npm install` / `npm ci`** без просьбы — `postinstall` перезаписывает `.git/hooks/pre-commit`
4. **⛔ НИКАКИХ ОТКАТОВ** без явного разрешения пользователя: `git reset`, `git checkout`, `git revert`, удаление/сброс чужих изменений, amending published commits. Если сломано — **ИСПРАВЛЯТЬ**, не откатывать.
5. **Не переустанавливай `node_modules`** без прямой просьбы
6. **Не меняй структуру проекта** (переименования, удаления файлов) без явной причины и подтверждения
7. **Не внедряй код в WebView мессенджеров** способами, нарушающими ToS — только через `preload` и подготовленные строки `executeJavaScript`
8. **Не делай долгих операций** — коротко, быстро, по делу

---

## 🧪 ПРАВИЛА ПРОВЕРКИ

После изменения кода подбирай проверки **по риску**:

### ✅ Безопасные команды (можно без подтверждения)

| Команда | Что делает |
|---|---|
| `npm run lint` | ESLint — только статический анализ |
| `npm run test:vitest` | Vitest в jsdom — без Electron |
| `npm run check-memory` | Проверка здоровья Memory Bank |
| `npm run regen-claude-structure` | Регенерация таблицы «Структура памяти» |
| `node src/__tests__/<имя>.test.cjs` | Отдельный unit-тест |
| `git status`, `git diff`, `git log` | Только чтение |

### ⚠️ Опасные команды (НЕ запускать без разрешения пользователя)

| Команда | Почему опасно |
|---|---|
| `npm start`, `npm run dev` | Запускают Electron (нарушают Критический запрет №1) |
| `npm test` | Включает e2e → запускает Electron |
| `npm run test:e2e` | Запускает Electron |
| `npm install`, `npm ci` | `postinstall` перезаписывает git hooks |
| `npm run build` | Долгая операция |
| `git push --force`, `git reset --hard`, `git revert` | Деструктивно |
| `git commit --no-verify` | Обход pre-commit хуков |
| `git add -A`, `git add .` | Может зацепить `.env` и секреты — добавляй файлы по именам |

### Минимально достаточно
- Точечные тесты по затронутой области
- Если добавил новую логику и тестов нет — создай минимальный тест
- Если задача UI/поведение — отдельно скажи пользователю, что он должен проверить визуально
- После UI-изменений **просить пользователя** запустить и проверить

---

## 🆘 ЕСЛИ ЧТО-ТО НЕ РАБОТАЕТ

Быстрая справка по типовым проблемам:

| Ситуация | Что делать |
|---|---|
| Pre-commit hook падает на непонятной проверке | `bash scripts/check-memory.sh` — покажет что именно сломано (версии / ссылки / размеры) |
| Версии в 4 местах разошлись | `npm run check-memory` → покажет разрыв. Выровнять все 4 места вручную на одну версию |
| Автотест `featuresReferences` ругается | Ссылка `[...](src/.../file)` в последних 10 версиях `features.md` указывает на несуществующий файл. Либо удали упоминание, либо восстанови файл |
| Автотест `memoryBankSizeLimits` ругается | Файл памяти перерос лимит (100 КБ корень / 200 КБ mistakes) — разбить на подпапку + индекс (см. примеры в Memory Bank секции) |
| В контексте сессии слишком много истории | Перечитай **только** `common-mistakes.md` индекс и нужную тему из `mistakes/` — не всё подряд. Архив не читай. |
| Таблица «Структура памяти» в CLAUDE.md устарела | `npm run regen-claude-structure` — регенерирует между маркерами `<!-- STRUCTURE-AUTO-START/END -->` |
| Auto-memory (`.claude/memory/*`) расходится с кодом | Доверяй коду (см. секцию «Конфликт: память vs код»), обнови память или попроси пользователя |
| `.git/hooks/pre-commit` не подхватил новую версию | `cp scripts/hooks/pre-commit .git/hooks/pre-commit && chmod +x .git/hooks/pre-commit` (локальная операция, не коммитится) |

---

## 🎓 Контекст проекта

**Название**: ЦентрЧатов (рус.) / ChatCenter (eng.)

**Что это**: Electron-приложение — единое окно для работы с несколькими мессенджерами одновременно, с встроенным ИИ-помощником для быстрых ответов, шаблонов и автоответа.

**Целевая аудитория**: Операторы и менеджеры, работающие с клиентами через несколько мессенджеров (Telegram, WhatsApp, VK, Viber, MAX и др.).

**Текущая версия**: v0.95.16 (28 мая 2026)

---

## 🧩 Основные модули

| Модуль | Кодовое имя | Описание |
|--------|-------------|----------|
| Мессенджеры | `Messengers` | Управление WebView-вкладками мессенджеров (Telegram Web, WhatsApp Web, VK и др.) |
| Монитор чатов | `ChatMonitor` | Перехват и чтение входящих сообщений через инъекцию скриптов в WebView |
| ИИ-помощник | `AIAssistant` | Анализ входящих сообщений, генерация вариантов ответа через LLM |
| Шаблоны ответов | `Templates` | Библиотека готовых текстов и фраз, категории, поиск |
| Авто-ответчик | `AutoReply` | Правила автоответа: по расписанию, по ключевым словам, по чату |
| Расписание | `Scheduler` | Временные окна активности авто-ответчика (рабочие часы, выходные) |
| Настройки | `Settings` | Конфигурация мессенджеров, ИИ-провайдеров, правил, профилей |

---

## 🏗️ Архитектурные принципы

**Технологии**:
- **Electron** — оболочка приложения
- **WebView** (`<webview>` тег) — изолированный рендер веб-версий мессенджеров
- **IPC** (`ipcMain / ipcRenderer`) — единственный канал связи `main ↔ renderer`
- **preload.js** — мост между `main` и `renderer` (`contextBridge`)

**Критически важно**:
- Всё через IPC — никакого `require('electron')` в renderer-процессах
- Инъекция скриптов в WebView только через `preload` WebView или `executeJavaScript`
- Каждый мессенджер — отдельный изолированный `<webview>` с собственной сессией/partition
- Перехват сообщений — через `MutationObserver` внутри WebView, результат передаётся через IPC
- ИИ-запросы только из main-процесса (API-ключи не в renderer)

---

## 📏 Правило превышения лимита строк

Если файл превышает лимит строк — **НЕ резать комментарии**. Комментарии должны оставаться всегда, они объясняют логику и предупреждают о ловушках.

**Правильное действие** — разделить файл на модули: вынести логически обособленный блок в отдельный файл (например, utils, handlers, components). Пример: в v0.87.21 `cc-media` protocol вынесен в `main/native/ccMediaProtocol.js` вместо сокращения комментариев в `main.js`.

---

## 🚫 Лимиты размера файлов ВСЕХ типов (src/, main/, shared/)

Проект защищён от бесконтрольного роста файлов. Тест `fileSizeLimits.test.cjs` автоматически сканирует `src/`, `main/`, `shared/` и выбирает лимит по пути и типу. Больше не надо добавлять файлы вручную, **и** больше нельзя «молча» добавить файл нового типа — тест поймает.

### Лимиты по типу файла (v0.87.75 — все типы)

| Где файл | Лимит | Примеры |
|------|-------|--------|
| `.jsx`/`.tsx` в `src/components/` — панели UI | **700 строк** | `AISidebar.jsx`, `NotifLogModal.jsx` |
| `.jsx`/`.tsx` в `src/native/` — экраны | **600 строк** | `InboxMode.jsx`, `MessageBubble.jsx` |
| Другие `.jsx`/`.tsx` | **600 строк** | |
| `.js`/`.ts` в `/hooks/` | **150 строк** | React hooks |
| `.js`/`.ts` в `/utils/` | **300 строк** | Обычные утилиты |
| `.js`/`.ts` в `main/handlers/`, `main/native/`, `src/native/store/`, `src/native/utils/`, `main/preloads/utils/` | **500 строк** | Крупные интеграции (GramJS, stores, handlers) |
| `.js`/`.ts` в `main/preloads/hooks/` | **300 строк** | Скрипты инъекций в WebView |
| `main/main.js` / `main/main.ts` | **600 строк** | Корневой main-процесс |
| `.cjs` в `main/preloads/` | **600 строк** | Preload-скрипты |
| Другие `.cjs` | **400 строк** | |
| Тесты (`*.test.*`, `*.vitest.*`) | **400 строк** | |
| **`.html`** (инлайн BrowserWindow) | **800 строк** | `notification.html` (исключение 1000) |
| **`.css` / `.scss`** | **800 строк** | `native/styles.css` |
| **`.json` конфиги** | **500 строк** | `shared/spamPatterns.json` |

**Железная тройная защита от «тихих дыр» (v0.87.75)**:
- **(A)** Файл в `KNOWN_EXT` но без правила в `getLimit()` → тест падает с инструкцией «добавь правило»
- **(B)** В `KNOWN_EXCEPTIONS` путь к несуществующему файлу → тест падает с инструкцией «удали запись»
- **(C)** В `src/`/`main/`/`shared/` файл с расширением вне `KNOWN_EXT` и `IGNORED_EXT` (`.vue`, `.svelte`, `.toml` и т.п.) → тест падает с инструкцией «добавь в один из списков»

**Ранее (v0.87.68)**: авто-сканирование для `.jsx/.js/.cjs`. **Теперь (v0.87.75)**: все типы файлов + защита от будущих.

**Жёлтое предупреждение при 80% лимита** — тест не падает, но показывает что скоро надо разбивать.

### 📦 Исключения и текущее состояние

Список файлов с индивидуальным потолком — в тесте (`KNOWN_EXCEPTIONS` в `fileSizeLimits.test.cjs`).

**Снапшот текущих размеров, приближений к лимиту, причин исключений**:
→ [`.memory-bank/code-limits-status.md`](.memory-bank/code-limits-status.md)

**Рекомендации по разбиению крупных файлов**:
→ [`.memory-bank/handoff-code-limits.md`](.memory-bank/handoff-code-limits.md)

Этот CLAUDE.md держит **правила**, а конкретные числа (размеры, проценты) — в файле выше, чтобы они не устаревали в правилах.

### Лимиты на функции

| Правило | Лимит | Если превышен |
|---------|-------|--------------|
| Одна функция | **100 строк** | Разбить на подфункции |
| Inline `executeJavaScript` | **50 строк** | Вынести скрипт в `messengerConfigs.js` |
| React `useEffect` | **30 строк** | Вынести логику в отдельную функцию |

### Правила добавления нового кода

1. Новая бизнес-логика → `src/utils/имяМодуля.js` + тест в `src/__tests__/`
2. Новый UI-блок → `src/components/ИмяКомпонента.jsx` (если > 50 строк)
3. Новый IPC handler → добавить в `main/main.js` + тест в `ipcChannels.test.js`
4. Новый спам-паттерн → `shared/spamPatterns.json` (единый источник)
5. Новый мессенджер → конфиг в `src/utils/messengerConfigs.js`

### Как проверить (без запуска приложения)

```bash
npm run lint
npm run test:vitest
```

---

## 📌 Обновление версии (ОБЯЗАТЕЛЬНО при каждом изменении кода)

При изменении кода обновить версию **ВО ВСЕХ 4 местах**:

| # | Файл | Что менять |
|---|------|-----------|
| 1 | `package.json` | `"version": "X.Y.Z"` |
| 2 | `package-lock.json` | корневой `"version"` + `packages[""].version` (две записи) |
| 3 | `CLAUDE.md` | `**Текущая версия**` и `**Версия проекта**` (в начале и в подвале) |
| 4 | `.memory-bank/features.md` | Версия в шапке + запись в Changelog |

### Правила версионирования
- **Patch** (0.1.X → 0.1.X+1): багфиксы, мелкие правки, рефакторинг памяти
- **Minor** (0.X.0): новые функции, новый мессенджер, изменения UI
- **Major** (X.0.0): только по согласованию с пользователем

### Когда версию НЕ повышать
Если код **не менялся**, а был только анализ / совет / текст для обсуждения — версию **не** повышать.

### Чеклист перед сборкой
1. ✅ `package.json` — версия
2. ✅ `package-lock.json` — две `"version"`-записи
3. ✅ `CLAUDE.md` — версия и дата в двух местах
4. ✅ `.memory-bank/features.md` — версия + запись в Changelog
5. ✅ Если есть новые файлы — добавлены в `package.json → build.files`

---

## 🧾 MEMORY BANK — обязательные действия

### При изменении кода

- **Новые ошибки / ловушки** → добавить в подходящий файл `mistakes/<тема>.md` (**не** в корневой `common-mistakes.md` — он индекс)
- **Новые решения / архитектурные выводы** → `decisions.md`
- **Новые фичи / фиксы / changelog** → `features.md`

### Если создаёшь новые файлы в `.memory-bank/`

- Добавь их в CLAUDE.md (секции «Первое действие» и «Структура памяти»)
- При необходимости добавь в `.memory-bank/README.md`

### Если меняется структура Memory Bank

Синхронизируй:
- CLAUDE.md (вручную + `bash scripts/regen-claude-structure.sh` для таблицы структуры)
- `.memory-bank/README.md`
- `.memory-bank/CHANGELOG.md` — добавь запись о структурном изменении

### Если создаёшь новые файлы в `.claude/skills/` или `.claude/commands/`

- **Обязательно** добавь их в CLAUDE.md (секция «Первое действие» или отдельная секция про skills)
- Укажи: **что делает**, **когда вызывать**, **какие аргументы принимает**
- Skills в `.claude/skills/` — это project-specific агенты и slash-команды Claude Code. Без записи в CLAUDE.md агент не узнает что они существуют и не будет их использовать.
- Обнови `.memory-bank/CHANGELOG.md` (если skill влияет на рабочий процесс)

Сейчас в проекте `.claude/skills/` **нет** — только `settings.json` (см. секцию «⚙️ Конфигурация .claude/»). Правило действует на будущее.

### 📏 Лимиты размера файлов ПАМЯТИ (.memory-bank/)

| Файл | Максимум | Если превышен |
|---|---|---|
| Любой `.md` в корне `.memory-bank/` | **100 КБ** | Разбить на подпапку + индекс |
| Файл в `mistakes/` | **200 КБ** | Разбить по подтемам |
| `common-mistakes.md` (индекс) | **10 КБ** | Сократить описания тем до одной строки |

**Почему лимиты важны**: инструмент `Read` имеет предел 256 КБ. Файл больше — не читается. 100+ КБ съедает огромную долю контекста, нужного для задачи.

**Автоматическая защита**:
- **Тест лимитов**: `node src/__tests__/memoryBankSizeLimits.test.cjs` — падает если файл перерос
- **Тест dangling-refs**: `node src/__tests__/featuresReferences.test.cjs` — падает если в последних 10 версиях `features.md` ссылка указывает на несуществующий файл
- **Pre-commit hook**: `scripts/hooks/pre-commit` вызывает `check-memory.sh` если в коммите есть `.memory-bank/*` или `CLAUDE.md` → коммит блокируется при проблемах
- **Скрипт диагностики**: `bash scripts/check-memory.sh` (или `npm run check-memory`) — размеры, сверка версий, сломанные ссылки
- **Регенерация структуры**: `bash scripts/regen-claude-structure.sh` (или `npm run regen-claude-structure`) — автоматически обновляет таблицу «Структура памяти» между маркерами `<!-- STRUCTURE-AUTO-START/END -->`

**Примеры разбиения** (для справки):
- `common-mistakes.md` 294 КБ → индекс 5 КБ + `mistakes/` 4 файла (v0.87.56)
- `features.md` 445 КБ → активный 100 КБ + 2 архивных файла (v0.87.58)
- `mistakes/webview-injection.md` 165 КБ → `webview-injection.md` 130 КБ + `webview-navigation-ui.md` 31 КБ (v0.87.59)
- `mistakes/webview-injection.md` 130 КБ → `webview-injection.md` 9 КБ (ядро) + `webview-stack-grouping.md` 125 КБ (v0.87.60)

**Журнал изменений структуры**: [`CHANGELOG.md`](.memory-bank/CHANGELOG.md).

### Когда переводить в архив

- Завершённые handoff-документы (2+ недели стабильности в релизе)
- Отменённые / замороженные планы (> 3 месяцев бездействия)
- Секции `⚪ ИСТОРИЯ (РЕШЕНО)` из активных файлов
- Старые changelog'и из больших файлов (например, `features.md`)

При архивации — обновить журнал в [`archive/README.md`](.memory-bank/archive/README.md).

---

## 🔧 GIT-ПРАВИЛА

1. **Перед commit всегда** смотри `git status`
2. Проверяй, нет ли незакоммиченных чужих изменений
3. Добавляй файлы **ТОЛЬКО ПО ИМЕНАМ**: `git add file1 file2 file3`
4. **НЕ** использовать `git add -A` / `git add .`
5. `git commit --no-verify` — **ЗАПРЕЩЁН** без явного разрешения пользователя
6. Один логический запрос пользователя = один логический commit
7. **Commit + Push делать только если пользователь ЯВНО попросил** это в текущей задаче
8. Формулировка «кажется, тут нужен push» — **не основание** для push
9. **⚠️ ВСЕ новые файлы (`??` в `git status`), на которые есть ссылки из других коммитимых файлов, ОБЯЗАНЫ быть в `git add`** — иначе CI упадёт на «битая ссылка». Pre-commit hook этого **не ловит**, т.к. локально файл существует. Проявилось в v0.89.17: создал `code-todo.md` + ссылку в `CLAUDE.md`, но забыл добавить файл в `git add` — CI упал. См. правило в [.memory-bank/workflow.md](.memory-bank/workflow.md) «Перед `git add`».

### Если делаешь add + commit + push одной командой Bash
- Это должна быть **одна последовательная команда** (`&&`)
- **Не** параллельные git-вызовы
- **Только после** успешных проверок
- **Только если** пользователь просил commit/push

---

## 💬 ФОРМАТ ОТВЕТА

### 🚨 ГЛАВНОЕ ЖЁСТКОЕ ПРАВИЛО — простой язык

Пользователь **НЕ программист**. Все ответы, советы, объяснения — **простыми словами**.

**ЗАПРЕЩЕНО** без крайней необходимости:
- Технические термины (`refactoring`, `context isolation`, `throttle`, `race condition`, `deadlock`, `dependency`)
- Слова вроде «синхронизация», «интерпретация», «агрегация», «абстракция»
- Профессиональный жаргон, англицизмы, код-термины в объяснениях

**РАЗРЕШЕНО**:
- Простые слова: «одновременно», «вовремя», «вместе», «по очереди»
- Объяснение через аналогии («это как…»)
- Имена файлов и команд — да, они не заменяются (например `package.json`)

**Тест на понятность**: если объяснение нельзя пересказать бабушке — переписать проще.

**Пример**:
- ❌ «Параллелизм — делай независимые операции одновременно для оптимизации throughput»
- ✅ «Одновременно — делай несколько дел сразу, когда они не мешают друг другу»

### Общие правила

- Все мысли, действия и итог пиши **НА РУССКОМ**
- Коротко, но достаточно полно
- Информации давай столько же или больше, **но за меньше ходов**
- Не растягивай очевидные действия на лишние сообщения

### Для разных типов задач

**Советательная / аналитическая задача** — в ответе отдельно разделяй:
1. Что уже проверено
2. Что предлагается
3. Что ещё не применялось к файлам

**Смешанная задача** (часть совет, часть код):
1. Что изменено в коде
2. Что только предлагается
3. Что пользователь должен проверить сам

### «До 5 советов по улучшению» — только для содержательных задач

Дай **ДО 5 вариантов улучшения ТОЛЬКО если** задача была содержательной:
- Новая фича
- Рефакторинг
- Крупный фикс
- Архитектурное решение

**Для тривиальных правок** (typo, одна строка, переименование переменной) советы **не давать** — это выглядит как избыток.

Если всё же даёшь советы — для каждого обязательно укажи:
- Что это даст
- Зачем нужно
- Простой пример / визуальный пример (если возможно)
- Плюсы / минусы
- Что будет, если не сделать
- Какие проблемы могут быть, если сделать

Пиши **без сложных слов**, понятно человеку без технической подготовки. См. «🚨 Главное жёсткое правило» в начале этой секции.

### 🎨 Визуальное оформление советов

Советы **должны выглядеть**, а не «просто быть написаны». 5 приёмов:

#### Приём 1 — 📊 Таблица «До / После»
Когда совет про изменение текста, числа, строки, версии — две колонки:

```
| ❌ Сейчас          | ✅ Станет              |
|---|---|
| 76 строк старого   | 138 строк актуальных   |
```

Применять: правки файлов, замены значений, переименования.

#### Приём 2 — 🟢🟡🔴 Иконки-светофор вместо слов «плюсы/минусы/риски»
Вместо длинных заголовков — одна иконка + одна строка:

```
🟢 Что даст        → быстрый ответ одной фразой
🟡 Цена            → 5 минут работы, 1 коммит
🔴 Если не сделать → что сломается
⚡ Срочность       → низкая / средняя / высокая
```

Применять: всегда когда есть баланс «стоит ли делать».

#### Приём 3 — ⭐ Шкала приоритета (1–5 звёзд)
Вместо слов «важно / очень важно / критично»:

```
⭐⭐⭐⭐⭐ — блокер, сделать первым
⭐⭐⭐   — полезно, следующее
⭐      — косметика, можно отложить
```

Применять: когда в ответе **несколько** советов и надо понять что делать первым.

#### Приём 4 — 🖼 ASCII-мокап для структуры и архитектуры
Когда совет про структуру папок, компонентов или UI — нарисуй:

```
Было:
.memory-bank/
├── common-mistakes.md  [294 КБ] ❌ не читается
└── features.md         [445 КБ] ❌ не читается

Стало:
.memory-bank/
├── common-mistakes.md  [5 КБ]   ✅ индекс
├── mistakes/           [темы]    ✅
└── archive/            [история] ✅
```

Применять: структура, архитектура, UI-макеты.

#### Приём 5 — 🚦 Одна строка-светофор для мелких советов
Если совет мелкий — не расписывай 6 пунктов. Одна строка:

```
🟢 Убрать цифру «76 КБ» из CLAUDE.md → стареет быстро (5 секунд)
🟡 Разбить webview-stack-grouping.md → можно отложить (средний приоритет)
🔴 Удалить архив → НЕ делай, потеряем учебные примеры
```

Применять: когда 5 советов подряд — чтобы не было стены текста.

### Какой приём для какого случая

| Тип совета | Приём |
|---|---|
| Правка текста / числа / строки | 📊 До/После (Приём 1) |
| Список из 3–5 советов с разным приоритетом | ⭐ Шкала (3) + 🚦 светофор (5) |
| Структура папок, архитектура, UI | 🖼 ASCII-мокап (4) |
| Плюсы/минусы/риски в одном совете | 🎯 🟢🟡🔴 иконки (2) |
| Очень мелкая правка | 🚦 Одна строка (5) |

**Главное**: совет должен **бросаться в глаза**, а не теряться в тексте. Если ты написал 3 страницы и главное на 2-й — это плохой формат.

---

## 📋 ФИНАЛЬНЫЙ БЛОК ОТЧЁТА

В конце **всегда** добавляй раздел:

**Что сделано в этой сессии**

Таблица с колонками: `Задача | Файлы изменения | Статус теста | Влияние на систему`

---

## ✅ ЧТО СЧИТАТЬ ХОРОШИМ РЕЗУЛЬТАТОМ

- Задача решена **по сути**, а не «формально»
- Код **не затронул лишнего**
- **Старые ошибки не повторены** (проверены `common-mistakes.md` и `mistakes/`)
- Проверки подобраны **по риску** (не запускать всё подряд)
- Пользователь видит понятный итог
- Если нужно — есть commit/push (**только по просьбе**)
- Если нужно — обновлены версия и Memory Bank

---

## ⚙️ Конфигурация `.claude/`

В корне проекта есть папка `.claude/` с настройками Claude Code:

### `.claude/settings.json`
Project-specific разрешения. Сейчас разрешены без подтверждения:
- `Bash(*)`, **`PowerShell(*)`** — обе оболочки без подтверждения
- `Read(*)`, `Write(*)`, `Edit(*)`
- `Glob(*)`, `Grep(*)` — поиск по коду
- `WebFetch(*)`, `WebSearch(*)` — интернет-запросы
- `Agent(*)` — подагенты для независимых задач
- `TodoWrite(*)`, `NotebookEdit(*)`, `AskUserQuestion(*)`, `Skill(*)`, `ToolSearch(*)`, `ScheduleWakeup(*)`

**Почему это важно**: агент не спрашивает подтверждения перед каждой командой. Это ускоряет работу, но **не отменяет критические запреты** (`npm start`, `git reset`, `npm test` и т.д.) — эти команды агент не должен запускать даже если `Bash(*)` разрешён.

### Автоматические проверки лимитов файлов (v0.87.73)

В `.claude/settings.json` прописаны два хука, которые вызывают `scripts/hooks/check-size-hook.cjs`:

| Хук | Когда срабатывает | Что делает |
|---|---|---|
| **SessionStart** | В начале каждой сессии Claude Code | Запускает `fileSizeLimits.test.cjs` и печатает все файлы на 80%+ лимита |
| **PostToolUse** (`Write\|Edit`) | После каждого `Write`/`Edit` в `src/` или `main/` `.jsx/.js/.cjs` | То же самое — видно мгновенно если твоя правка вывела файл за порог |

**Зачем это нужно**: закрывает 4 причины пропущенных превышений лимитов в прошлом:
1. ✅ Жёсткий список файлов в тесте → теперь авто-сканирование всех файлов (v0.87.68)
2. ✅ Слепое доверие pre-commit → PostToolUse hook показывает предупреждения сразу после правки
3. ✅ Не считал строки вручную → hook считает автоматом
4. ✅ Ползучий рост → SessionStart hook показывает 80%+ файлы в начале сессии

**Если hooks не сработали**: скорее всего watcher не подхватил новый `.claude/settings.json` — открой `/hooks` один раз (перезагрузит конфиг) или перезапусти Claude Code.

### `.claude/skills/` и `.claude/commands/`
Сейчас **пусто**. Если появятся skills/slash-команды — **обязательно добавить их в CLAUDE.md** (см. раздел «MEMORY BANK — обязательные действия»).

### `.claude/memory/`
Auto-memory — постоянная память Claude между сессиями. При расхождении с кодом — **доверять коду**, память обновлять (см. раздел «Конфликт: память vs код»).

---

## 📁 Структура памяти (.memory-bank)

<!-- STRUCTURE-AUTO-START -->
<!-- Регенерируется скриптом scripts/regen-claude-structure.sh. НЕ редактировать вручную между маркерами. -->

### Активные файлы в корне

| Файл | Размер |
|------|--------|
| `CHANGELOG.md` | 19 КБ |
| `README.md` | 8 КБ |
| `ai-integration.md` | 10 КБ |
| `api.md` | 8 КБ |
| `architecture.md` | 10 КБ |
| `autoreply.md` | 6 КБ |
| `code-limits-status.md` | 4 КБ |
| `coding-rules.md` | 5 КБ |
| `common-mistakes.md` | 6 КБ |
| `decisions.md` | 16 КБ |
| `features.md` | 45 КБ |
| `handoff-code-limits.md` | 8 КБ |
| `messengers.md` | 14 КБ |
| `native-mode-plan.md` | 22 КБ |
| `native-scroll-diagnostics-handoff.md` | 18 КБ |
| `startup-load-investigation.md` | 7 КБ |
| `ui-components.md` | 14 КБ |
| `workflow.md` | 8 КБ |

### Подпапка `mistakes/` — детали ловушек

| Файл | Размер |
|------|--------|
| `mistakes/electron-core.md` | 56 КБ |
| `mistakes/native-scroll-unread.md` | 28 КБ |
| `mistakes/notifications-ribbon.md` | 49 КБ |
| `mistakes/webview-injection.md` | 9 КБ |
| `mistakes/webview-navigation-ui.md` | 31 КБ |
| `mistakes/webview-stack-grouping.md` | 123 КБ |

### Подпапка `archive/` — НЕ читать по умолчанию

| Файл | Размер |
|------|--------|
| `archive/2026-04-common-mistakes-resolved.md` | 11 КБ |
| `archive/README.md` | 4 КБ |
| `archive/features-pre-v0.87.md` | 251 КБ |
| `archive/features-v0.87-early.md` | 126 КБ |
| `archive/features-v0.87.40-50.md` | 40 КБ |
| `archive/features-v0.87.51-64.md` | 54 КБ |

_Регенерировано: 2026-04-27_
<!-- STRUCTURE-AUTO-END -->

---

**Версия проекта**: v0.95.16 (28 мая 2026)
**Статус**: 🟢 Фазы 1-4+ + TDLib миграция + виртуализация удалена (v0.94.0) + фиксы скролла/загрузки/счётчика (v0.94.1-7, v0.95.0-1) + фикс мигания кнопки ↓ + удаление пилюли (v0.95.2) + диагностика «дёрг при повторном открытии» (v0.95.3) + фикс «дёрг» useLayoutEffect + Windows CI testTimeout (v0.95.4) + фикс «дёрг в чате с закреплённым сообщением» (sticky overlay, v0.95.5) + кнопка ↓ Telegram-style: всегда в низ + instant/smooth (v0.95.6) + drag-to-resize разделителя chat-list (v0.95.7) + счётчик ↓ обнуляется + плавная анимация + live compact + порог 160 (v0.95.8) + compact 53px+порог 128+плавный переход+↓ loading state (v0.95.9) + откат scroll-continuation, loading-pulse остаётся (v0.95.10) + диагностика «не грузит дальше» (v0.95.11) + jump-to-end (v0.95.12) + aroundId=0 (v0.95.13) + context-window+rAF×2 (v0.95.14) + итеративный fetch обычных чатов (v0.95.15) + **jump-to-end форум-топики + smoothScroll easeOutCubic (v0.95.16)**
**Последнее обновление**: 28 мая 2026 — **v0.95.16**: ДВЕ задачи. **(А) Jump-to-end в форум-топиках**: лог показал что в топиках `chatLastMessageId=null` → гейт jump-to-end не срабатывал. Корень: `chat.lastMessageId` для основного чата, не для топика. Решение: добавлен `lastMessageId` в [forum.getTopics](main/native/backends/tdlibBackend.js) маппер из `t.last_message?.id`. Новый метод `messages.getIterativeUntilTopic` — зеркало v0.95.15 getIterativeUntil но через [`getMessageThreadHistory`](https://core.telegram.org/tdlib/docs/classtd_1_1td__api_1_1get_message_thread_history.html) (для не-General) или getChatHistory (для General). TDLib spec подтверждает: getMessageThreadHistory имеет ТОТ ЖЕ quirk «number of returned messages is chosen by TDLib and can be smaller than limit» → итеративный паттерн обязателен. Новый IPC `tg:get-topic-messages-iterate`, новый store метод `loadTopicMessagesUntil`. [InboxMode.scrollToBottom](src/native/modes/InboxMode.jsx) расширен: `isForumTopic ? loadTopicMessagesUntil : loadMessagesUntil`. **(Б) smoothScroll с easeOutCubic**: новый util [smoothScroll.js](src/native/utils/smoothScroll.js) с `easeOutCubic` easing (быстрый разгон + плавное приземление, эталон research 2026), `requestAnimationFrame` 60fps, `prefers-reduced-motion` accessibility fallback. **Защиты**: distance<1px no-op, distance>8 viewport instant (нет смысла в долгой анимации), cancel() функция, финальный snap к точному targetTop. Длительность 500мс. Заменяет `instant` scrollTo в jump-to-end на красивую анимацию. **+17 unit-тестов**: 7 smoothScroll (easings + scenarios + cancel + reduced-motion) + 4 getIterativeUntilTopic + 1 store + другие. Конфликты ✅: backward compat (existing topic-messages пути не затронуты), все защиты v0.94.7/v0.91.13/v0.95.4/v0.95.8 не задействуются. Лимиты nativeStore.js 1150→1220, tdlibBackend.js 640→720, vitest jsx 640→700, новое exception для tdlibBackend.vitest.js 480. Регрессия: lint 0, vitest 747/747, fileSizeLimits 285/285, check-memory ✅. **Предыдущий v0.95.15**: ФИНАЛ саги jump-to-end (4 итерации v0.95.12-15, полная история в [.memory-bank/jump-to-end-saga.md](.memory-bank/jump-to-end-saga.md)). v0.95.14 (`offset=-50`) не сработал: TDLib вернул count=1 в логе чата «Компьютерная». КОРЕНЬ — [TDLib issue #740](https://github.com/tdlib/td/issues/740), официальный ответ автора **levlam**: «**This is expected. For optimal performance the number of returned messages is chosen by the library**». TDLib **намеренно** возвращает меньше `limit` — один invoke НЕ гарантирует ничего. [Official getting-started](https://core.telegram.org/tdlib/getting-started#getting-chat-messages): нужен **ИТЕРАТИВНЫЙ fetch** с `from=last_received.id`. **Решение v0.95.15**: новый [`backend.messages.getIterativeUntil`](main/native/backends/tdlibBackend.js) — итерирует getChatHistory до 5 раз пока не наберём `targetCount` ИЛИ не получим `untilMessageId`. Новый IPC канал `tg:get-messages-iterate` ([tdlibIpcHandlers.js](main/native/tdlibIpcHandlers.js)). Новый метод store [`loadMessagesUntil`](src/native/store/nativeStore.js). [InboxMode.scrollToBottom](src/native/modes/InboxMode.jsx) jump-to-end использует `loadMessagesUntil(viewKey, chatLastMessageId, 100)` → rAF×2 → scrollTo+markRead. **Безопасность**: maxIterations clamp [1,10], detect duplicates (TDLib stuck), empty response stop, untilMessageId short-circuit. **+6 unit-тестов**: 5 backend (iterates/stops on until/stops on empty/maxIter clamp/sorted ASC) + 1 store (IPC контракт). **Зафиксированы правила в memory bank**: known behaviors TDLib getChatHistory + главное правило «один invoke НЕ гарантирует limit». Лимиты: nativeStore.js 1080→1150, tdlibBackend.js 550→640, vitest 600→640. Регрессия: lint 0, vitest 730/730, fileSizeLimits 283/283, check-memory ✅. **Предыдущий v0.95.14**: ФИНАЛ саги jump-to-end (3 итерации v0.95.12-14, полная история в [.memory-bank/jump-to-end-saga.md](.memory-bank/jump-to-end-saga.md)). v0.95.13 (`aroundId=0`) не сработал на чате «База Темщика» — лог показал что TDLib вернул окно около `read_cursor=2543M`, а не `lastMessageId=3406M` (gap=634). Корень — [TDLib issue #740](https://github.com/tdlib/td/issues/740): TDLib **подменяет** `from_message_id=0` на `last_read_inbox_message_id` в чатах с непрочитанными (НЕ на `last_message` как обещает spec). **Решение v0.95.14** — context-window как Telegram Desktop: `aroundId=lastMessageId` (явно, не 0!) + `addOffset=-50`. По TDLib spec «negative offset → newer messages», поэтому `from=X, offset=-50, limit=100` → 50 newer (нет, X последний) + X сам + 49 older = X в результате. **Эталон**: tdesktop `getHistory(peer.last_message.id, offset=-N)`. **Плавность**: `requestAnimationFrame × 2` перед scrollTo — гарантия что React commit + первый paint завершились, scrollHeight точный, нет дёрга. Эталон tweb: microtask + scroll в onLoaded. **Изменения**: [nativeStore.loadMessages](src/native/store/nativeStore.js) поддерживает `options.addOffset` override + улучшен `store-load-messages` лог (override=hasOverride, не !!overrideAroundId — баг v0.95.13 лога где 0 показывался как false); [InboxMode.scrollToBottom](src/native/modes/InboxMode.jsx) jump-to-end ветка использует `aroundId=lastMessageId, addOffset=-50` + rAF×2 обёртку перед scrollTo+markRead. **Тесты**: +1 для context-window контракта (aroundId+addOffset), обновлён backward-compat тест. **Конфликты ✅**: backward compat (без addOffset → 0); все защиты v0.94.7/v0.91.13/v0.95.4/v0.95.8 не задействуются. Сага задокументирована — [jump-to-end-saga.md](.memory-bank/jump-to-end-saga.md). Регрессия: lint 0, vitest 724/724, fileSizeLimits 283/283, check-memory ✅. **Предыдущий v0.95.13**: ФИКС v0.95.12 — после клика ↓ видно ПРЕДпоследнее, не последнее (юзер скрин Архиватор IT, unread 1260→1 но самое последнее iOS 18 Programming не показано). Корень: [TDLib getChatHistory spec](https://core.telegram.org/tdlib/docs/classtd_1_1td__api_1_1get_chat_history.html) — `from_message_id=X, offset=0` грузит сообщения **СТРОГО СТАРШЕ X** (не включая). v0.95.12 передавал `aroundId=chatLastMessageId` → последнее пропускалось. Правильно: `from_message_id=0` → «the last message in the chat is used» (включительно). **Фикс**: [InboxMode jump-to-end](src/native/modes/InboxMode.jsx) `aroundId: 0` вместо `chatLastMessageId`. Также в [nativeStore.loadMessages](src/native/store/nativeStore.js): `options.aroundId != null` (не truthy) — иначе 0 интерпретировался как «нет override». Тест обновлён под `aroundId=0`. Регрессия: lint 0, vitest 723/723. **Предыдущий v0.95.12**: ТОЧНЫЙ ФИКС «не грузит дальше при unread > загруженного» по данным v0.95.11 диагностики. Лог: чат «Департамент вайб-кодинга» `unread=1108, loadedIncoming=100, gapMessages=1021, unreadVsLoaded=1008` — между загруженным и сервером пропущено 1021 сообщений, клик ↓ требовал ~11 нажатий. **Корень**: `loadMessages` грузил окно вокруг `readInboxMaxId` (первое непрочитанное), не вокруг `chat.last_message.id`. Эталон Telegram Desktop / Web K / WhatsApp: при click ↓ — `getChatHistory(from=last_message.id)`. **Решение**: (1) [nativeStore.loadMessages](src/native/store/nativeStore.js) принимает `options.aroundId` + `options.force` — override `unreadParams`, отключение IDB optimistic render, `addOffset=0`. Без options — поведение не изменено (backward compat подтверждена тестом). (2) [InboxMode.scrollToBottom](src/native/modes/InboxMode.jsx) — новая ветка jump-to-end: гейт `chatLastMessageId && unreadVsLoaded > 50 && !loading` → `loadMessages(viewKey, 100, {aroundId: lastMessageId, force: true})` → scroll instant к scrollHeight → mark-read до lastMessageId (bypass-gate v0.95.8 разрешает). Fallback: обычное поведение (gap ≤ 50 / нет lastMessageId / идёт загрузка). **Конфликты ✅**: tg:messages full replace — OK (user-intent); useInitialScroll messagesCount изменение → followupRef++ без restore; scrollPosByChatRef перебивается; useReadByVisibility/useForceReadAtBottom не задействуются (другие sources); load-newer после reload → noMoreNewer (afterId≈lastMessageId, петли нет); IDB cache замещён — норма как у Telegram Desktop. **+2 unit-теста** [nativeStore.vitest.jsx](src/native/store/nativeStore.vitest.jsx): options.aroundId+force контракт + backward compat без options. Лимиты nativeStore.js 1050→1080, vitest 500→580 (доменное разбиение store — отдельный шаг). Регрессия: lint 0, vitest 723/723, fileSizeLimits 283/283, check-memory ✅. **Предыдущий v0.95.11**: ДИАГНОСТИКА перед точным фиксом «не грузит дальше при unread > загруженного» (юзер: «у чата 724 непрочитаных, но ниже нет уже сообщения, не грузит новые»). Анализ лога 15:06-15:07 показал что юзер в самом низу загруженного (`scrollTop=136419 atBottom=true bottomGap=0`) с 394 сообщений, но `unread=724` — ~330 непрочитанных ЗА пределами загруженного окна. Клик ↓ — no-op (уже у низа). `load-newer` не срабатывает потому что юзер не двигает scroll. **Корень**: при открытии чата с большим unread `loadMessages` грузит окно вокруг `readInboxMaxId` (первое непрочитанное), но не вокруг `chat.lastMessage.id` (последнее на сервере). Telegram Desktop при клике ↓ делает reload вокруг `last_message.id` — это нужный фикс. **БЕЗ смены поведения** добавлены: (1) `lastMessageId` в [tdlibMapper.js](main/native/backends/tdlibMapper.js) chat object; (2) поля `loadedIncoming`/`chatLastMessageId`/`loadedLastId`/`gapMessages`/`unreadVsLoaded` в `button-scroll-bottom` лог [InboxMode.jsx scrollToBottom](src/native/modes/InboxMode.jsx); (3) `lastMessageId`+`readInboxMaxId` в `chat-open` лог [useScrollDiagnostics.js](src/native/hooks/useScrollDiagnostics.js). Поведение НЕ изменено — только лог. Следующий шаг (v0.95.12) — после анализа реальных значений: точный фикс jump-to-end через `loadMessages(chatId, { aroundId: chat.lastMessageId, force: true })`. Регрессия: lint 0, vitest 721/721. **Предыдущий v0.95.10**: ОТКАТ незапрошенной фичи из v0.95.9. Юзер: «Продолжение scroll после load-newer я это не просил убирай, я просил эффект загрузки на кругшке сделать». Удалены `scrollIntentRef` + `useLayoutEffect`-довинчивание scroll'а к низу при дозагрузке load-newer (Fix 4b v0.95.9 был незапрошенным). Остаётся Fix 4a v0.95.9 что юзер просил: `--loading` класс на [ScrollBottomButton](src/native/components/InboxChatPanel.jsx) когда loadingNewer=true → accent border + box-shadow pulse 1.4s в [styles-overlays.css](src/native/styles-overlays.css), tooltip «Подгружаю свежие сообщения…», loadingNewer prop. Юзер видит: кликнул ↓ → кнопка пульсирует пока идёт «Загружаю ещё…» (visual feedback), scroll НЕ продолжается автоматически (поведение по-умолчанию как в v0.95.6 — один scroll по клику). Регрессия: lint 0, vitest 721/721, fileSizeLimits 283/283, check-memory ✅. **Предыдущий v0.95.9**: ЧЕТЫРЕ ПОЛИРОВКИ по 3 запросам юзера (скрины «Диджитальная», «AI-ML», 1168 unread). **(1) Аватарки 53px в compact**: юзер «размеры значков не меняли когда оставляю одни значки». В [ChatListItem.jsx](src/native/components/ChatListItem.jsx) compact-ветка — аватар 53px (был 44), шрифт 19px (был 16). Юзер видит одинаковые большие значки в обоих режимах. **(2) Порог 128**: юзер «уменьшить ещё на 20%». `CHAT_LIST_COMPACT_THRESHOLD = 128` (цепочка: 200 v0.95.7 → 160 v0.95.8 → 128 v0.95.9). Compact срабатывает позже — нужно почти до края 60px сжать. **(3) Плавный переход compact↔full**: в [InboxChatListSidebar.jsx](src/native/components/InboxChatListSidebar.jsx) добавлен `transition: width 200ms ease-out` на root div, ТОЛЬКО когда `isResizing === false` (drag = direct DOM для 60fps, transition выкл; mouse-up = React re-render = плавно). isResizingChatList пробрасывается из InboxMode. **(4) Кнопка ↓ loading state + продолжение scroll** (скрин 1168 unread, дёрг): `--loading` класс в [ScrollBottomButton](src/native/components/InboxChatPanel.jsx) когда loadingNewer=true → accent border + box-shadow pulse 1.4s (тот же цвет что «Загружаю ещё...»), tooltip «Подгружаю свежие сообщения…»; новый `scrollIntentRef` + `useLayoutEffect` в [InboxMode.jsx](src/native/modes/InboxMode.jsx) — при клике ↓ ставит intent на 4с, при дозагрузке load-newer (изменился `activeMessages.length`/`loadingNewer`) автоматически довинчивает scrollTo(scrollHeight) `behavior:'instant'`. Reset intent когда `loadingNewer=false && bottomGap<=4`. Юзер видит loading-pulse на кнопке + плавный continuous scroll до самого низа. **Конфликты ✅**: useLayoutEffect synchronous до paint; direct DOM style.width в drag не перебивает transition (отключается в startResize); loading pulse не конфликтует с entering/leaving (composed animation); scrollIntent не блокирует user wheel-scroll. Тесты useChatListResize обновлены под порог 128 (3 теста). Регрессия: lint 0, vitest 721/721, fileSizeLimits 283/283, check-memory ✅. **Предыдущий v0.95.8**: ЧЕТЫРЕ ИСПРАВЛЕНИЯ по запросу юзера. **Fix 1 — счётчик обнуляется**: после клика ↓ счётчик 289 оставался (скрин чата «Вайбкодинг»). Корень: гейт `unreadWindowIncomplete` блокировал mark-read для source='button-scroll'. По [TDLib viewMessages spec](https://core.telegram.org/tdlib/docs/classtd_1_1td__api_1_1view_messages.html) (range-ack) + [Telegram Desktop bug c/5792](https://bugs.telegram.org/c/5792) — клик ↓ должен mass-ack ВСЕ непрочитанные (active user intent). Расширен whitelist `ACTIVE_USER_SOURCES = {'visibility','button-scroll'}` в [InboxMode.jsx markReadCurrentView](src/native/modes/InboxMode.jsx). Гейты v0.94.7 (useReadByVisibility cascade guard) и v0.91.13 (useForceReadAtBottom threshold 30) защищают от passive scroll-trigger — НЕ задействуются на явный клик. Audit на 6 направлениях — 100% безопасно. **Fix 2 — плавная анимация ↓**: React не имеет exit-animation, кнопка исчезала мгновенно. Новый [useDelayedUnmount.js](src/native/hooks/useDelayedUnmount.js) — задержка реального unmount на 220мс под CSS-анимацию. Новый компонент `ScrollBottomButton` в [InboxChatPanel.jsx](src/native/components/InboxChatPanel.jsx) с классами `--entering` (bouncy spring overshoot `cubic-bezier(0.34,1.4,0.64,1)`) и `--leaving` (smooth fade-down + scale + `pointer-events:none`). Эталон Telegram Web K `bubbles-corner-button`. **Bonus 1 — live compact во время drag**: было — compact срабатывал только после mouse-up. Теперь setState в `onPointerMove` при пересечении threshold (60fps сохранён, re-render лишь 1 раз). **Bonus 2 — порог 200→160**: по запросу юзера «слишком рано схлопывается в значки», compact теперь при `width < 160` (было `< 200`). **+10 unit-тестов**: 6 для useDelayedUnmount (timing, snap-back, cleanup, custom delay) + 4 для useChatListResize (новый порог 160, live compact toggle, no-cross no-update). Регрессия: lint 0, vitest 721/721, fileSizeLimits 283/283, check-memory ✅. **Предыдущий v0.95.7**: DRAG-TO-RESIZE разделителя chat-list ↔ окно чата по запросу юзера (стандарт Telegram Desktop / VS Code / Discord / Slack). Новый [useChatListResize.js](src/native/hooks/useChatListResize.js) — Pointer Events API + setPointerCapture ([MDN W3C 2018](https://developer.mozilla.org/en-US/docs/Web/API/Element/setPointerCapture)), эталон [useAIPanelResize](src/hooks/useAIPanelResize.js). Direct DOM `style.width` во время drag → 60fps без re-render React'а; setState + `settings:save` только в onPointerUp. Clamp [60, 600]. Default 340. **5 UX-улучшений**: ⭐ двойной клик на handle → reset к 340; ⭐ auto-compact при `width < 200` (`isChatListCompact`); ⭐ accent-подсветка handle на hover (`#2AABEE66`) / active (`#2AABEE88`); ⭐ persistence в `settings.chatListWidth` через IPC `settings:save`; ⭐ глобальный `data-cc-chat-list-resize-overlay` (z:999998) во время drag — pointerup не застрянет. Новый [ChatListResizeHandle.jsx](src/native/components/ChatListResizeHandle.jsx) (6px divider, cursor: col-resize, aria-label/title). [ChatListItem.jsx](src/native/components/ChatListItem.jsx) — режим compact (Telegram Desktop two-column mode): скрыт текст/превью/микро-строка, аватар 44px, бейдж абсолютным позиционированием в правом верхнем углу, tooltip с названием на hover, mute-значок сохранён. [InboxChatListSidebar.jsx](src/native/components/InboxChatListSidebar.jsx) — props `width/compact/panelRef`, поиск скрыт в compact mode, compact пробрасывается через rowProps. **Конфликты проверены ✅**: useAIPanelResize (разные оси) / overflow-anchor:none (v0.94.2) / MessageListOverlay shimmer / кнопка ↓ z:5 / pinned overlay z:4 (v0.95.5) / react-window List — НИКАКИХ. **+15 unit-тестов** [useChatListResize.vitest.jsx](src/native/hooks/useChatListResize.vitest.jsx) (clamp/threshold/startResize+setPointerCapture/move 60fps контракт/up+save+other-props preserved/resetToDefault/guards). Регрессия: lint 0, vitest 711/711, fileSizeLimits 281/281, check-memory ✅. **Предыдущий v0.95.6**: КНОПКА ↓ работает как в Telegram Desktop (юзер, скрин чата PlayMods.net, 619 непрочитанных). Симптом: клик ↓ ведёт к **старому первому-непрочитанному** (которое юзер уже пролистал), нужен второй клик чтобы попасть в самый низ. Корень: `firstUnreadIdRef` использовался ОДНОВРЕМЕННО для divider «НОВЫЕ СООБЩЕНИЯ» (нужен frozen snapshot чтобы не дрожал, v0.89.33) И для navigation target кнопки ↓ (нужен live actual unread). Telegram Desktop ([bug.telegram.org/c/5792](https://bugs.telegram.org/c/5792)): кнопка ↓ ВСЕГДА в самый низ. **Решение**: новый чистый util [scrollBehavior.js](src/native/utils/scrollBehavior.js) `computeScrollBehavior(deltaPx, clientHeight)` — `> 5×viewport` → `'instant'` (MDN: Baseline Widely Available с 2015), иначе `'smooth'` ([MDN ScrollToOptions](https://developer.mozilla.org/en-US/docs/Web/API/ScrollToOptions)). В [InboxMode.jsx](src/native/modes/InboxMode.jsx): удалена ветка `if (firstUnread)` в `scrollToBottom`, объединены `scrollToBottom` + `scrollToAbsoluteBottom` в одну функцию (всегда в низ + behavior динамический), удалены `handleScrollButtonClick`/`handleScrollButtonDoubleClick`/`scrollButtonClickTimerRef` (220мс double-click timer бессмысленен). markRead до lastMessageId сохранён → счётчик 619→0 сразу. `firstUnreadIdRef` остаётся для [useInitialScroll](src/native/hooks/useInitialScroll.js) (auto-scroll при первом открытии) и [groupMessages](src/native/utils/messageGrouping.js) divider — это РАЗНЫЕ сценарии. В [InboxChatPanel.jsx](src/native/components/InboxChatPanel.jsx) удалён `onDoubleClick`, обновлён tooltip «К последнему сообщению (N непрочитано)». **+11 unit-тестов** [scrollBehavior.vitest.js](src/native/utils/scrollBehavior.vitest.js) (пороги в обе стороны + реальный сценарий 619 unread → instant + защита от NaN/undefined). **Конфликты проверены ✅**: useInitialScroll/groupMessages не затронуты; useReadByVisibility IntersectionObserver не пропускает (явный markRead до lastId компенсирует instant jump). Регрессия: lint 0, vitest 696/696, fileSizeLimits 278/278, check-memory ✅. **Предыдущий v0.95.5**: ФИКС «дёрг в чате с закреплённым сообщением» (юзер, скрин чата «Вайбкодинг комьюнити»). Корень: pinned-блок был обычным flex-child над лентой `{pinnedMsg && <div>}` в [InboxChatPanel.jsx](src/native/components/InboxChatPanel.jsx). Backend [getChatPinnedMessage](main/native/backends/tdlibMessages.js#L372-L389) — TDLib RPC ~50-500мс, async; `setPinnedMsg(msg)` через [InboxMode useEffect](src/native/modes/InboxMode.jsx#L291-L299) добавлял блок в DOM ПОСЛЕ первого рендера → flex container увеличивал offset → лента уезжала вниз = «дёрг». Решение (как [Telegram Web K _chatPinned.scss](https://github.com/morethanwords/tweb/blob/master/src/scss/partials/_chatPinned.scss), WhatsApp Web): новый компонент [PinnedMessageBar.jsx](src/native/components/PinnedMessageBar.jsx) — `position: absolute; top:0; left:0; right:0; z-index: 4` ПОВЕРХ верха ленты, не в потоке flex. Появление/исчезновение pinned НЕ СДВИГАЕТ сообщения по построению. Накрывает 1 верхнее сообщение когда лента у верха — норма Telegram-клиентов. backdrop-filter: blur(8px) — фон размыт. **Конфликты проверены ✅**: dragOver overlay (z:2) — pinned выше; кнопка ↓ (z:5) — pinned ниже, разные координаты; MessageListOverlay shimmer — pinned виден только при chatReady; IntersectionObserver mark-read (rootMargin -48% центр) — НЕ ВЛИЯЕТ на mark-read. **+7 unit-тестов** [PinnedMessageBar.vitest.jsx](src/native/components/PinnedMessageBar.vitest.jsx) (position absolute/z-index/обрезка текста/fallback/onClose/blur). Регрессия: lint 0, vitest 685/685, fileSizeLimits 276/276, check-memory ✅. **Предыдущий v0.95.4**: ФИКС «дёрг при повторном открытии seen-чата» (корень подтверждён диагностикой v0.95.3) + Windows CI testTimeout. В [useInitialScroll.js](src/native/hooks/useInitialScroll.js) ветка 2 (already-seen): `useEffect` → `useLayoutEffect`. Корень — `useEffect` выполняется ПОСЛЕ paint ([React docs](https://react.dev/reference/react/useEffect)) → при смене seen-чата React сначала рисует кадр (scrollContainer показывает позицию ПРЕДЫДУЩЕГО чата — это общий persistent DOM), потом effect ставит scrollTop=saved → юзер видит 1 кадр чужой позиции = «дёрг». **Решение** ([useLayoutEffect docs](https://react.dev/reference/react/useLayoutEffect): «fires synchronously BEFORE the browser paints»): restore выполняется до paint → юзер сразу видит правильную позицию. Внутри ТОЛЬКО micro-операция `scrollTop=N` (микросекунды) — никаких fetch/тяжёлой работы (по React-доке useLayoutEffect блокирует paint). Подтверждено диагностикой v0.95.3 — `msSinceEffectStart=0`, `attempts=0` (restore синхронный, scrollEl сразу готов). Та же ОЗ работа уже применена в [InboxMode load-older re-pin](src/native/modes/InboxMode.jsx) (v0.94.2). Ветка 1 (initial scroll первого открытия) НЕ менялась — там setTimeout 150ms остаётся. **Windows CI fix**: GitHub Actions упал — `AccountContextMenu.vitest.jsx` первый тест 5671мс при дефолтном `testTimeout=5000` (остальные 18 тестов 13-36мс). Cold-start первого теста файла на медленном Windows runner (загрузка модуля + первый рендер React 19 в happy-dom). Решение ([vitest docs](https://vitest.dev/config/#testtimeout)): глобальный `testTimeout: 15000` в [vitest.config.mjs](vitest.config.mjs). Это потолок, не фикс-ожидание — нормальные тесты не замедляются. **Регрессия**: lint 0, vitest, fileSizeLimits, check-memory ✅. **Предыдущий v0.95.3**: ДИАГНОСТИКА перед точным фиксом «дёрг при повторном открытии seen-чата». Поведение НЕ изменено, только логи. В [useInitialScroll.js](src/native/hooks/useInitialScroll.js) (ветка 2, already-seen): в `restore-applied` добавлены `scrollTopBefore` (что юзер видел ДО restore — если 0 при saved≠0, был flash), `msSinceEffectStart` (тайминг), `attempts` (rAF-retry). Новое событие `restore-followup-render` считает ре-раны эффекта ДЛЯ ТОГО ЖЕ чата ПОСЛЕ restore (staged setState из v0.91.6 — IDB→server→prefetch×2). На реальной сессии лог покажет точный корень: post-paint flash → useLayoutEffect, либо staged ре-рендеры → другой подход. Также исправлен рассинхрон в package-lock.json (root=0.95.2 vs packages=0.95.1 → оба 0.95.3). Тесты 14/14 (поведение не менялось). **Следующий шаг**: точный фикс по реальным данным из лога. **Предыдущий v0.95.2**: фикс мигания кнопки-стрелки со счётчиком (она исчезала на 1-2с и появлялась снова) + удаление бесполезной пилюли «100/802» рядом с ней. **Корень мигания** (доказан логом `bottom-state-change` × 8): `nearBottom` считалось через ОДИН порог `bottomGap < 80` ([useInboxScroll.js](src/native/hooks/useInboxScroll.js)) → при колебании `bottomGap` около 80 (в логе: 64→105→48→92→55→110→70→85) флаг `atBottom` дребезжал true↔false → кнопка `{(!atBottom || activeUnread>0)}` мигала, когда `activeUnread===0`. **Решение** ([Schmitt trigger](https://en.wikipedia.org/wiki/Schmitt_trigger) — стандартный приём против дребезга): чистая функция `computeNearBottom(bottomGap, prev)` — **войти** в atBottom при `<40`, **выйти** при `>120`; полоса 40-120 сохраняет предыдущее → колебания не дрожат. +4 теста (пороги в обе стороны + реальный сценарий дребезга). **Удалена пилюля `UnreadProgressPill`** (v0.94.4-5) — дублировала бейдж кнопки ↓, путала больше чем помогала; удалены: компонент, тест, CSS, импорт+проп+вычисления в [InboxChatPanel.jsx](src/native/components/InboxChatPanel.jsx) и проп в [InboxMode.jsx](src/native/modes/InboxMode.jsx). Логика `unreadWindowIncomplete` для markRead-guard (защита от каскада) **не тронута**. **Регрессия**: lint 0, vitest, fileSizeLimits, check-memory ✅. **Предыдущий v0.95.1**: НАСТОЯЩИЙ корень «застрявшего счётчика» (счётчик встаёт после ~100, чат грузится разреженно). Баг в backend [tdlibBackend.js messages.get](main/native/backends/tdlibBackend.js): **`afterId` (load-newer) вообще не использовался** — `fromMessageId: params.aroundId || params.offsetId` (afterId игнорировался) → `from_message_id=0` → TDLib грузил **последние сообщения (низ чата)**, а не следующую страницу после afterId. При большом числе непрочитанных (окно стоит на первом непрочитанном) load-newer **прыгал в низ** → разрыв в загруженном списке → счётчик стоял. **Решение** (по [TDLib docs](https://core.telegram.org/tdlib/docs/classtd_1_1td__api_1_1get_chat_history.html): отрицательный offset грузит новее from_message_id, правило `limit >= -offset`): чистая функция `computeHistoryParams` в [tdlibMessages.js](main/native/backends/tdlibMessages.js) — при `afterId>0` возвращает `{ fromMessageId: afterId, offset: -(limit-1) }` (грузим непрерывную страницу НОВЕЕ afterId; сам afterId — dup, отсеивается дедупом store). initial (aroundId) / load-older (offsetId) — без изменений. **+Логирование** `[get-msgs]` (afterId/from/offset/count/first/last/hasMore) — видно, грузит ли load-newer непрерывно. Теперь прокрутка вниз грузит подряд → счётчик доходит до 0 как в Telegram. +5 тестов computeHistoryParams. tdlibBackend.js сжат под лимит (548/550). **Регрессия**: lint 0, vitest, fileSizeLimits, check-memory ✅. **Предыдущий v0.95.0**: ИСТОЧНИКОВЫЙ ФИКС «дыры» в загруженных сообщениях (корень всех багов счётчика непрочитанных: каскад 8624→88 в v0.94.7 и застрявший 100/1067). Причина (доказана кодом+логом): загруженный список становился **разорванным** — старт грузит окно вокруг первого непрочитанного ([nativeStore.js:111-123](src/native/store/nativeStore.js#L111), не низ при большом unread), а `tg:new-message` дописывал свежие сообщения в конец **безусловно** ([nativeStoreIpc.js](src/native/store/nativeStoreIpc.js)) → `[старый блок] + ДЫРА + [свежий блок]`. Из дыры: каскад markRead и застрявший счётчик. **Решение** (паттерн Telegram/Discord/Slack — live-append только «у низа»; TDLib getChatHistory грузит непрерывные окна): в `tg:new-message` свежее сообщение вклеивается в массив **только если оно непрерывно** с концом загруженного (`message.id − newestLoaded ≤ 200×2^20`, т.е. в пределах ~200 сообщений; TDLib `message_id = server_id << 20`). Если далеко (разрыв) — НЕ вклеивается; бейдж/превью/«↓N» обновляются всё равно (useNewBelowCounter слушает событие напрямую), сообщение подгрузится при прокрутке вниз непрерывно (self-heal). Список всегда непрерывен → дыра невозможна → каскад невозможен, счётчик растёт ровно. Гейт v0.94.7 остаётся страховкой. Оптимально: O(1) (`existing[length-1]`, массив отсортирован), скролл (v0.94.2) не тронут. +4 теста ([nativeStoreNewMessage.vitest.jsx](src/native/store/nativeStoreNewMessage.vitest.jsx): соседнее вклеивается / далёкое нет+бейдж обновлён / пустой засевается / дубль обновляется). **Регрессия**: lint 0, vitest, fileSizeLimits, check-memory ✅. **Предыдущий v0.94.7**: ФИКС «дыры» счётчика непрочитанных (баг 1625→1 / 8624→88 при прокрутке). Подтверждено логом: `load-newer` подгружал далёкий свежий блок (id ~17.1млрд) мимо непрочитанного бэклога (курсор ~15.23млрд), одно такое сообщение попадало в видимость → `markRead(17.1млрд)` → TDLib `viewMessages` помечает прочитанным ВСЁ ≤ maxId (range-ack, договор API — [native-scroll-unread.md:571](.memory-bank/mistakes/native-scroll-unread.md)) → 1624 невиденных обнулялись (`store-unread-sync 1625→1→0`). **Решение** (достроен mass-ack-guard, который уже есть в useForceReadAtBottom, но отсутствовал в read-by-visibility): в [useReadByVisibility.js](src/native/hooks/useReadByVisibility.js) перед markRead считается `approxMsgsJumped = (maxId − prevMax)/2^20` (TDLib `message_id = server_id << 20`); если прыжок `> 200` И `> count×5` (явный провал, обычное/быстрое чтение не задевается) — markRead пропускается, курсор откатывается на непрерывный фронтир (`read-cursor-jump-blocked`), self-heal при дозагрузке разрыва. Не трогает store/загрузку/скролл (фикс v0.94.2 цел). +2 теста (блок при провале / норма при последовательном чтении). TODO-markread-gap закрыт. **Регрессия**: lint 0, vitest, fileSizeLimits, check-memory ✅. **Предыдущий v0.94.6**: ДИАГНОСТИКА перед фиксом «дыры» счётчика непрочитанных (TODO-markread-gap) — БЕЗ смены поведения. В [useReadByVisibility.js](src/native/hooks/useReadByVisibility.js) в момент `read-batch-send` считается `approxMsgsJumped` (на сколько сообщений прыгнул read-курсор: `(maxId - prevMax) / 2^20`, т.к. TDLib `message_id = server_id << 20`). Если курсор перепрыгнул заметно дальше, чем реально увидели (`approxMsgsJumped > count + 20`) — пишется WARN-событие `read-cursor-jump` (prevMax, newMax, approxMsgsJumped, seenCount). Это ловит момент «провала» на реальной сессии перед точным фиксом. markRead-вызов НЕ изменён. +2 теста ([useReadByVisibility.vitest.jsx](src/native/hooks/useReadByVisibility.vitest.jsx): jump логируется при провале; НЕ логируется при последовательном чтении). **Регрессия**: lint 0, vitest, fileSizeLimits, check-memory ✅. Следующий шаг — точное решение по данным `read-cursor-jump`. **Предыдущий v0.94.5**: (1) Пилюля прогресса непрочитанных вынесена из инлайна InboxChatPanel в тестируемый компонент [UnreadProgressPill.jsx](src/native/components/UnreadProgressPill.jsx) + регресс-тест [UnreadProgressPill.vitest.jsx](src/native/components/UnreadProgressPill.vitest.jsx) (4 теста: видимость, класс --hidden при 100%, клик→onClick, total=0). (2) АУДИТ «дыры» счётчика непрочитанных (8624→88 после прокрутки 100): корень — `useReadByVisibility` ([src/native/hooks/useReadByVisibility.js](src/native/hooks/useReadByVisibility.js)) двигает read-курсор к самому высокому видимому id без проверки на провал; `viewMessages(...,force_read:true)` ([tdlibBackend.js:340](main/native/backends/tdlibBackend.js)) помечает прочитанным ВСЁ ≤ maxId → каскад на 8000+ старых. Защита `markReadCurrentView` пропускает `source==='visibility'` → дыра. Формализовано как TODO-markread-gap в [code-todo.md](.memory-bank/code-todo.md) (3 кандидата решения, рекомендация force_read=false при неполном окне, чинить с планом+тестом — зона markRead). Фикс НЕ внедрён (отдельная аккуратная задача). **Регрессия**: lint 0, vitest, fileSizeLimits, check-memory ✅. **Предыдущий v0.94.4**: плашка «Загружена часть непрочитанных N из M» из широкого блока сверху (двигал ленту) перенесена в **пилюлю-облачко над кнопкой ↓**. Клик по пилюле → к первому непрочитанному (reuse `scrollToBottom`=`handleScrollButtonClick`, уже умеет прыгать к firstUnread). Авто-гаснет при 100% (`loadedIncoming>=total`) через CSS-класс `.native-unread-pill--hidden` (opacity transition, без JS-таймеров). Число растёт по мере прокрутки к непрочитанным (Вариант A — как Telegram/RocketChat; фоновую дозагрузку всех 1005 НЕ делали: без виртуализации это 1005 DOM-узлов → лаги, [tdesktop #17504](https://github.com/telegramdesktop/tdesktop/issues/17504)). Изменено: [InboxChatPanel.jsx](src/native/components/InboxChatPanel.jsx) (блок→пилюля+onClick), [styles-overlays.css](src/native/styles-overlays.css) (`.native-unread-pill`). Логика счётчика `loadedIncoming` не тронута. Прокрутка (v0.94.2) и индикатор (v0.94.3) не затронуты. **Регрессия**: lint 0, vitest, fileSizeLimits, check-memory ✅. **Предыдущий v0.94.3**: «Спокойная загрузка» — убрано мигание индикатора при открытии чата. Юзер: при открытии чата сверху мигает синяя полоса + пилюля «Обновляю сообщения...». Причина (код): `MessageListOverlay` ([MessageSkeleton.jsx](src/native/components/MessageSkeleton.jsx)) показывался мгновенно при `(!chatReady)||messagesLoading` и мигал «вкл-выкл» на быстрых загрузках; skeleton-плашки видны редко (только при пустом кэше, т.к. IDB обычно отдаёт сообщения сразу). **Решение** (паттерн «отложенный + минимальная длительность» индикатор, React docs «Synchronizing with Effects»): (1) задержка 250мс перед показом, НО только при `hasContent` (виден кэш) — иначе чёрный экран, регрессия v0.89.37; быстрая загрузка <250мс полосу не показывает → нет мигания; (2) минимум 400мс на экране + плавное гашение (CSS `.native-msg-overlay--leaving` opacity 200мс); (3) убрана текст-пилюля «Обновляю...», осталась тонкая полоса (меньше шума). **Совет 4 (прятать контент до конца загрузки) НЕ внедрён** — конфликт с задержкой полосы (чёрный экран) + прятать видимый кэш спорно (теряется мгновенный показ) + reveal-флаг рядом с `chatReady`/прокруткой (риск для фикса скачков). Это UX-решение, отложено. Прокрутка (v0.94.2) НЕ затронута. Тесты overlay обновлены (+2). **Регрессия**: lint 0, vitest 663/663, fileSizeLimits, check-memory ✅. **Предыдущий v0.94.2**: ФИКС «прыгает и грузит без конца при прокрутке вверх» (лог чата «Машинное обучение»: 13 каскадных load-older, экран пиннился к верху). Корень — моя ошибка в v0.94.0: понадеялся на `overflow-anchor: auto`, но браузерное scroll anchoring НЕ держит позицию на верхней границе (`scrollTop≈0`), где и срабатывает load-older → 50 старых сообщений добавлялись сверху (+~12000px), экран оставался у верха → контент улетал вниз + тут же триггерился следующий load-older (каскад). Подтверждено [MDN overflow-anchor](https://developer.mozilla.org/en-US/docs/Web/CSS/overflow-anchor) + практикой Telegram Web K / Discord / Slack (они **отключают** overflow-anchor и держат позицию вручную). **Решение** (паттерн tweb ScrollSaver, DOMRect re-pin — надёжнее дельты высоты): (1) [VirtualMessageList.jsx](src/native/components/VirtualMessageList.jsx) `overflow-anchor: none`; (2) [useInboxScroll.js](src/native/hooks/useInboxScroll.js) перед prepend запоминает ВЕРХНЕЕ видимое сообщение (`data-msg-id`) и его экранную позицию в `prependAnchorRef`; (3) [InboxMode.jsx](src/native/modes/InboxMode.jsx) `useLayoutEffect` (до paint, без мигания) находит то же сообщение после prepend и возвращает его на тот же пиксель (`el.scrollTop += diff`). Фото/видео резервируют высоту (`aspectRatio`+`minHeight`) → поздних сдвигов нет. Каскад прекращается (после re-pin scrollTop далеко от верха). +1 регресс-тест (overflow-anchor=none). **Регрессия**: lint 0, vitest 661/661, fileSizeLimits, check-memory ✅. **Предыдущий v0.94.1**: ФИКС утечки слушателей TDLib `file:update`. Аудит лога (`MaxListenersExceededWarning: 101 file:update listeners`) + документация: каждый `downloadFile` вешал свой слушатель ([tdlibMedia.js](main/native/backends/tdlibMedia.js)), при чате со 100 медиа → 100+ слушателей; хуже — TDLib иногда шлёт ОДНО `updateFile` и больше ничего ([issue #280](https://github.com/tdlib/td/issues/280), [#2585](https://github.com/tdlib/td/issues/2585): stuck в poor network, ни завершения, ни ошибки) → слушатель висел вечно (настоящая утечка). **Решение** (рекомендованный TDLib паттерн «один update-handler + маршрутизация по file_id» + Node best practice «не вешать слушатель в часто вызываемой функции»): ОДИН постоянный диспетчер `file:update` на менеджер + реестр `Map<accountId:fileId, Set<waiter>>` в WeakMap (`getFileUpdateRegistry`). Слушателей всегда 1 — предупреждение невозможно. + таймаут «нет прогресса» 120с (сбрасывается на каждом обновлении → живые большие видео не трогает, убивает только зависшие). Исправлен неверный комментарий «не утечка» в [tdlibClient.js](main/native/backends/tdlibClient.js). Тесты: +3 (waiter снимается + listener=1; 50 параллельных = 1 listener; stuck → таймаут чистит). **Регрессия**: lint 0, vitest, fileSizeLimits (tdlibMedia 404/500), check-memory ✅. **Предыдущий v0.94.0**: ПОЛНОЕ УДАЛЕНИЕ виртуализации (react-virtuoso). Корень всей саги scroll restore (v0.91.1–v0.93.0, ~30 версий): виртуализация (react-window, потом react-virtuoso) фундаментально несовместима с точным restore — обе пересчитывают высоты при ремаунте `key={cacheKey}` → `scrollHeight` скачет → restore промахивается. Virtuoso работает с дискретными индексами + alignment (`align:'start'/'end'`) → отсюда «прилипание» к картинкам/видео и «выравнивание» на скринах юзера. **Решение** (как Telegram Web K, который НЕ виртуализирует): рендерим все сообщения обычным DOM, сохраняем простой пиксельный `scrollTop` (число). При ремаунте `scrollHeight` стабилен → `el.scrollTop = saved` восстанавливает позицию ТОЧНО. **Изменено**: [`scrollPositionsCache.js`](src/native/utils/scrollPositionsCache.js) формат `{scrollTop, atBottom}` storage v4; [`VirtualMessageList.jsx`](src/native/components/VirtualMessageList.jsx) обычный `<div overflow-y:auto overflow-anchor:auto>` + `renderItems.map`, `listRef` через useImperativeHandle (element + scrollToRow scrollIntoView); [`useInitialScroll.js`](src/native/hooks/useInitialScroll.js) restore `el.scrollTop=saved.scrollTop`; [`useInboxScroll.js`](src/native/hooks/useInboxScroll.js) сохраняет pixel + load-older/newer БЕЗ ручной коррекции (overflow-anchor держит prepend); [`useScrollPositionAutosave.js`](src/native/hooks/useScrollPositionAutosave.js) interval pixel; [`InboxMode.jsx`](src/native/modes/InboxMode.jsx)+[`InboxChatPanel.jsx`](src/native/components/InboxChatPanel.jsx) удалены initialTopMostItemIndex/firstItemIndex/handleStart-EndReached/findRenderItemIndex, scrollToVirtualRow→querySelector. Удалён `useInitialScrollDiag.js`, пакет `react-virtuoso` (npm uninstall). isRestoringRef closed-loop guard сохранён. **Регрессия**: lint 0, vitest 658/658, fileSizeLimits 272/272, check-memory ✅. **Предыдущий v0.93.0**: pixel-perfect scroll restore через `LocationOptions.offset` Virtuoso API. После v0.92.6 (rollback сломанного snapshot) юзер показал скрины: открыт длинный пост по середине → возврат → видна шапка поста (align='end' clamping для post > viewport). Аудит подтвердил что 8 ломаных связей (A-H) НЕ являются причиной. **Реальное решение** в TS-типах Virtuoso 4.18.7 (`node_modules/react-virtuoso/dist/index.d.ts:765`): `LocationOptions.offset?: number` — pixel offset для `initialTopMostItemIndex`. **Что изменено**: (1) `findVisibleAnchorMsgId` возвращает `{anchorMsgId, offsetFromTop}` — pixel offset от top of anchor row до scrollTop, anchor теперь ВЕРХНИЙ visible msg (не нижний); (2) save формат `{anchorMsgId, atBottom, offsetFromTop}` (storage v2→v3 с backward compat); (3) `initialTopMostItemIndex={index, align:'start', offset: savedOffsetFromTop}` — Virtuoso ставит scrollTop = anchorRow.top + offset → точно где было. Работает для **середины длинных постов** (offset=400 → юзер видит середину post). Без snapshot mechanism, совместимо с `key={cacheKey}` ремаунтом. **8 ломаных связей A-H** — мусор кода, не функциональная проблема, cleanup отложен до v0.93.1+. **Регрессия**: lint 0, vitest 658/658. **Предыдущий v0.92.6**: УДАЛЕНИЕ snapshot mechanism v0.92.2 (архитектурно сломан с `key={cacheKey}` ремаунтом). Лог 18:05:09+ показал что `getState` cleanup срабатывает ПОСЛЕ unmount Virtuoso → snapshot.scrollTop=0 → Virtuoso ставит юзера в начало чата. Production эталоны (Stream Chat, Mattermost) используют `getState`/`restoreStateFrom` БЕЗ `key={...}` ремаунта (меняют `data`). У нас `key=cacheKey` обязателен. **Удалено**: `restoreStateFrom` prop, `listRef.getState` метод, `scrollStateByChatRef` Map, throttled getState save, useEffect cleanup flush, диагностика `state-snapshot-flush`/`state-restore-attempt`, 3 теста v0.92.2. **Остаётся работать**: `initialTopMostItemIndex={index, align: 'end'}` (v0.92.3), `firstItemIndex` prepend (v0.92.0), `isRestoringRef` closed-loop guard (v0.92.4), `atTopThreshold=200` (v0.92.1), startReached/endReached (v0.92.0), cleanup от 3 пар двойных функций (v0.92.5). **Регрессия**: lint 0, vitest 658/658. **Предыдущий v0.92.5**: устранение 3 пар дублирующих функций scroll restore + flush getState + диагностика. После v0.92.4 юзер показал скриншоты «открыл длинный пост по середине → возврат → видна шапка поста». Найдено 3 пары двойных функций которые конфликтовали: (1) load-older — старый useInboxScroll handleScroll scrollTop<100 vs Virtuoso handleStartReached; (2) load-newer — useInboxNewerPrefetch vs Virtuoso handleEndReached; (3) initialTopMostItemIndex+restoreStateFrom одновременно — Virtuoso игнорирует первое если второе задано. **5 фиксов**: убран весь load-older + newerPrefetch.maybeTrigger из useInboxScroll handleScroll (теперь handleScroll только save+diag), приоритет savedSnapshot над savedAnchor в InboxMode, синхронный getState flush через useEffect cleanup перед ремаунтом Virtuoso, диагностический лог `state-restore-attempt`. useInboxScroll упрощён 175 → ~120 строк. **Предыдущий v0.92.4**: РЕГРЕССИОННЫЙ ФИКС — вернули `isRestoringRef` closed-loop guard, ошибочно удалённый в v0.92.0 Day 3. После v0.92.3 (align='end' добавлен) лог 17:34:56-17:35:31 показал что anchor дрейфует на 1-1024 msgs туда-сюда между возвратами — это **closed-loop save**. Я ошибочно думал что Virtuoso `initialTopMostItemIndex`/`restoreStateFrom` не вызывают DOM scroll event. По [MDN scroll event spec](https://developer.mozilla.org/en-US/docs/Web/API/Element/scroll_event): «programmatic scrolling also fires scroll event». Virtuoso использует DOM `scrollTo` → наш onScroll → handleScroll → save искажённого anchor → следующий restore читает искажённый → каскадный дрейф. **Фикс — вернуть isRestoringRef** (тот же паттерн что в v0.91.22 для react-window): [`InboxMode.jsx`](src/native/modes/InboxMode.jsx) объявление + useEffect ставит true при смене activeViewKey и сбрасывает через 1000мс. [`useInboxScroll.js`](src/native/hooks/useInboxScroll.js) — guard в save anchor и в throttled getState save. [`useScrollPositionAutosave.js`](src/native/hooks/useScrollPositionAutosave.js) — guard в interval save. **Регрессия**: lint 0, vitest 661/661. **Предыдущий v0.92.3**: align='end' для restore. при возврате в чат. Юзер сделал скриншоты до/после: сохранял позицию с msg X **внизу viewport** — при возврате X оказывался **в самом верху**, ниже него сообщения после X. Корень: `findVisibleAnchorMsgId` сохраняет НИЖНИЙ visible msg, но `initialTopMostItemIndex={number}` Virtuoso ставит item в TOP viewport (align='start' по умолчанию). Решение через [Virtuoso TS-типы:1258](file:///c:/Projects/ChatCenter/node_modules/react-virtuoso/dist/index.d.ts): `initialTopMostItemIndex` принимает **объект `IndexLocationWithAlign`** — `{index, align, offset?}`. Передаём `{index: idx, align: 'end'}` для savedAnchor — anchor msg оказывается в **низу viewport** как было. Для firstUnreadId — `{index, align: 'start'}` (divider сверху, как Telegram Desktop). 1 файл — [`InboxMode.jsx`](src/native/modes/InboxMode.jsx) `initialTopMostItemIndex` computation. **Регрессия**: lint 0, vitest 661/661, fileSizeLimits 273/273. **Предыдущий v0.92.2**: pixel-perfect scroll restoration через Virtuoso `getState`/`restoreStateFrom` API (по [официальной доке](https://virtuoso.dev/react-virtuoso/virtuoso/state-restoration/) + production эталоны Stream Chat React / Mattermost). После v0.92.1 юзер сообщил «позиция приблизительная при возврате» (bottomGap=329 при saved≠atBottom). Корень: `initialTopMostItemIndex` срабатывает 1 раз при mount, на момент mount renderItems содержит только cached 50 msgs из IDB — `findRenderItemIndex(savedAnchor)` возвращает -1 → fallback в конец списка. **Решение**: Virtuoso предоставляет `StateSnapshot = {scrollTop, ranges[]}` — точная пиксельная позиция + измерения. `listRef.getState((state) => ...)` сохраняет snapshot в `Map<chatId, StateSnapshot>` в памяти throttled 200мс через handleScroll. `<Virtuoso restoreStateFrom={savedState}>` восстанавливает **точную** позицию + ranges при возврате. **4 файла**: [`VirtualMessageList.jsx`](src/native/components/VirtualMessageList.jsx) — `restoreStateFrom` prop + `listRef.getState` API, [`InboxChatPanel.jsx`](src/native/components/InboxChatPanel.jsx) — проброс, [`InboxMode.jsx`](src/native/modes/InboxMode.jsx) — `scrollStateByChatRef Map` с LRU 50 + передача `virtuosoRestoreStateFrom`, [`useInboxScroll.js`](src/native/hooks/useInboxScroll.js) — throttled getState save в handleScroll. **Тесты**: +3 smoke (restoreStateFrom prop, listRef.getState API, getState на пустом списке). **Регрессия**: lint 0, vitest 662/662, fileSizeLimits 273/273. **Ограничение**: pixel-perfect работает в пределах сессии (Map в памяти). Cross-session — приблизительно через anchorMsgId (как v0.92.1). **Предыдущий v0.92.1**: фикс интеграции Virtuoso. После запуска v0.92.0 юзер сообщил «нихуя не поменялось» — анализ лога показал что `handleScroll` ВООБЩЕ не вызывался (scroll-save 0 событий после v0.92.0). Корень по официальной [Virtuoso Custom Scroll Container docs](https://virtuoso.dev/react-virtuoso/virtuoso/custom-scroll-container/): «The `onScroll` event handler is NOT directly passed to the Scroller component. Instead, it's attached to the Virtuoso component itself». Я ставил `onScroll` через `components.Scroller` JSX div — Virtuoso это игнорирует. **4 фикса**: (1) [`VirtualMessageList.jsx`](src/native/components/VirtualMessageList.jsx) — удалён кастомный Scroller, добавлен `scrollerRef` callback (по issue #274 ОБЯЗАТЕЛЬНО callback, НЕ useRef), `onScroll/onWheel/onTouchStart/onPointerDown/onDragOver/onDragLeave/onDrop` теперь прямые props в `<Virtuoso>`. (2) Добавлены `atTopThreshold={200}` + `atBottomThreshold={200}` — defaults 0/4 вызывали immediate startReached/endReached на mount. (3) [`useConsoleErrorLogger.js`](src/hooks/useConsoleErrorLogger.js) — фильтр `ResizeObserver loop completed with undelivered notifications` (benign warning Chromium по Virtuoso troubleshooting). (4) Bump 0.92.0 → 0.92.1. **Production эталон**: [Stream Chat React VirtualizedMessageList](https://github.com/GetStream/stream-chat-react/blob/master/src/components/MessageList/VirtualizedMessageList.tsx) использует тот же паттерн. **Регрессия**: lint 0, vitest, fileSizeLimits, check-memory. **Предыдущий v0.92.0**: миграция react-window 2.2.7 → react-virtuoso 4.18.7. Решает архитектурное ограничение react-window с `useDynamicRowHeight` ([issue #216](https://github.com/bvaughn/react-window/issues/216) открыт с 2019 без решения). Virtuoso через `initialTopMostItemIndex` + `firstItemIndex` + `startReached`/`endReached` штатно решает scroll restore, prepend без скачков, bidirectional infinite scroll. Production usage: Stream Chat React, Element/Matrix, Mattermost, Rocket.Chat. **Изменения**: [`InboxMode.jsx`](src/native/modes/InboxMode.jsx) — `useVirtuoso=true`, добавлен `initialTopMostItemIndex` computation (saved.anchorMsgId / atBottom / firstUnread / bottom), `firstItemIndex` state с reset на смене чата + decrement при `tg:messages append=true` (Virtuoso официальный prepend паттерн), `handleStartReached`/`handleEndReached` для bidirectional infinite scroll, callbacks `onMissingTarget`/`onRestoreAnchor`/`onScrollToIndex` no-op в Virtuoso режиме. [`InboxChatPanel.jsx`](src/native/components/InboxChatPanel.jsx) — conditional render `useVirtuoso ? <VirtualMessageListV2/> : <VirtualMessageList/>` (старый остаётся для rollback safety до Day 4). [`VirtualMessageListV2.jsx`](src/native/components/VirtualMessageListV2.jsx) — drop-in замена со всем API старого (listRef.element + scrollToRow мост через useImperativeHandle, components.Scroller для DOM events, skipAnimationFrameInResizeObserver fix). **Удалено** ~140 строк диагностики v0.91.19-24: `isRestoringRef` параметр и логика в 4 хуках, 4 restore-refs в InboxMode, `handleRowsRendered` с re-scroll логикой, `handleUserIntent` обёртка, `load-older-skip-restoring` guard (v0.91.24), `postcheck-tick × 5` multi-step (TODO-8), `ipc-burst tracker` (TODO-9), `scroll-save isRestoring`/`autosave-save isRestoring` поля. **План миграции**: [`.memory-bank/virtuoso-migration-plan.md`](.memory-bank/virtuoso-migration-plan.md). **Регрессия**: lint 0, vitest 668/668, fileSizeLimits 275/275. **Day 4** через 1-2 дня — удалит старый VirtualMessageList.jsx + react-window. **Предыдущий v0.91.24**: ФИКС Проблемы 2 — три фикса в одном коммите. (1) Главный — в [`useInboxScroll.js`](src/native/hooks/useInboxScroll.js) добавлен guard `if (isRestoringRef?.current) return` перед `load-older trigger` (line 110). До этого load-older стартовал через 30мс после `chat-open` ВО ВРЕМЯ restore, потому что `scrollToRow` ставил `scrollTop≈0` → guard `scrollTop < 100` срабатывал. 50 новых msgs в начало → `targetIdx` сдвигался на +50 (с 7 на 107 в логе 14:01:22) → react-window remeasure хаотично пересчитывал scrollHeight (9850→66140→51024→19868) → юзера выкидывало в `atBottom=true`. (2) `handleRowsRendered` в [`InboxMode.jsx`](src/native/modes/InboxMode.jsx) теперь с re-scroll: если target msg не в видимом окне → `virtualListRef.scrollToRow({index, align})`, index пересчитывается через `findRenderItemIndex(msgId)` (по msgId, не по индексу). Max 8 attempts (защита от bounce). Сходимость: `idx ∈ [startIndex, stopIndex]`. (3) `handleUserIntent` в InboxMode оборачивает `scrollDiag.markUserScroll` — если юзер начал крутить колесо во время restore, флаг `isRestoringRef` сбрасывается, restore отменяется. Корень подтверждён логом 14:01:22-25 (`anchor-postcheck-tick`, `load-older-trigger`, `scroll-anomaly`). 5 фактов в основе: 3🥇 (react-window 2.2.7 TS-типы, MDN scrollTop spec, MDN scroll event), 2🥈 (наш лог + наш код). Отвергнутые альтернативы: DOMRect-diff паттерн Telegram Web K (не подходит — у tweb нет виртуализации), Chromium `overflow-anchor: auto` (отключён в VirtualMessageList с v0.89.0, включать обратно — отдельная задача), `initialScrollOffset` prop (hacky по [issue #216](https://github.com/bvaughn/react-window/issues/216)), magic setTimeout × 5 (magic numbers). **Файлы**: useInboxScroll.js, InboxMode.jsx, InboxChatPanel.jsx. **Предыдущий v0.91.23**: добавлен лог `anchor-postcheck-tick` через штатный `onRowsRendered` react-window 2.2.7 для подтверждения Проблемы 2 (react-window scrollHeight remeasure портит anchor restore). Лог 13:08:02 уже подтвердил: после `onRestoreAnchor` `scrollHeight` ужимается (14806→12417), MDN clamp scrollTop → юзер падает в `atBottom=true` хотя `savedAtBottom=false`. По правилу саги «не чинить вслепую» — сначала диагностика, потом фикс. Сама правка скролла НЕ изменена. Что показывает лог: сколько раз `onRowsRendered` зовётся за один restore, на какой попытке `inViewport=true`, что когда `targetIdx=-1` (msg удалён или ещё не отрендерен из-за load-older). После подтверждения юзером — фикс-коммит v0.91.24 добавит re-scroll логику в `handleRowsRendered` с защитой от bounce (max 5 attempts), abort при user-scroll, пропуск при idx=-1. **Файлы**: [`InboxMode.jsx`](src/native/modes/InboxMode.jsx) — 4 ref + `handleRowsRendered` + расширение `onRestoreAnchor`/`onMissingTarget` для сохранения target msgId (не индекс — устойчиво к удалениям/добавлениям). [`InboxChatPanel.jsx`](src/native/components/InboxChatPanel.jsx) — проп `onRowsRendered` прокинут в `VirtualMessageList` (проп уже принимался с v0.89.0). Ceiling InboxMode 670→730 — diag-код будет удалён в v0.91.24. **Предыдущий v0.91.22**: финальный фикс «открыл-полистал-вернулся прыгает на чужое сообщение» + Maximum update depth при старте. Корни подтверждены логами 11:31:38 (closed loop, 23 повтора) и 12:40:09 (IPC bursts: 300+ chat-avatar, 280+ chat-last-message, 80+ sender-avatar за 1.5с). **Что нашли:** (1) **Closed-loop:** programmatic `scrollToRow` от restore триггерил `onScroll` event ([MDN: scroll event fires for programmatic changes](https://developer.mozilla.org/en-US/docs/Web/API/Element/scroll_event)) → `handleScroll` сохранял искажённый anchor → следующий return читал испорченное значение → анкор смещался на 1 msg каждый возврат (4048→4049→4050→4051). (2) **Maximum update depth:** React 18+ automatic batching работает только в пределах одного macrotask; IPC events от `window.api.on` приходят в РАЗНЫХ task'ах → каждый = отдельный setState/render → переполнение update budget при старте TDLib (~660 events за 1.5с). (3) **Хардкод версии:** [main.js:161](main/main.js) печатал `v0.87.135` независимо от package.json — устаревший лог. **Что чинили:** (A) **isRestoringRef флаг** (паттерн Telegram Web K `_isJumping` в [tweb ScrollSaver](https://github.com/morethanwords/tweb/blob/master/src/helpers/scrollSaver.ts)): объявлен в [`InboxMode.jsx`](src/native/modes/InboxMode.jsx) и проброшен в 3 хука — `useInitialScroll` ставит флаг перед `scrollToRow`/`scrollEl.scrollTop=` и сбрасывает через 500мс (anchor mode) или 1500мс (bottom mode с postcheck), [`useInboxScroll.handleScroll`](src/native/hooks/useInboxScroll.js) и [`useScrollPositionAutosave`](src/native/hooks/useScrollPositionAutosave.js) пропускают save при `isRestoringRef.current === true`. (B) **rAF-батчинг 3-х IPC handlers** в [`nativeStoreIpc.js`](src/native/store/nativeStoreIpc.js) (`tg:chat-last-message`, `tg:sender-avatar`, `tg:chat-avatar`): payload'ы собираются в Map (dedup по chatId/senderId), один `requestAnimationFrame` → один `setState`. Сверено по [React docs про batching](https://react.dev/learn/state-as-a-snapshot) и [Discord Engineering blog](https://discord.com/blog/how-discord-stores-trillions-of-messages) (тот же паттерн в их MobX store). (C) **app.getVersion()** в [main.js:161](main/main.js#L161) вместо хардкода. **Регрессия:** `useInitialScroll.js` теперь принимает `isRestoringRef` как параметр (был внутренний ref). `nativeStoreIpc.js` ceiling 500→600, `InboxMode.jsx` 660→670 — см. [`fileSizeLimitsExceptions.cjs`](src/__tests__/fileSizeLimitsExceptions.cjs). Диагностика `ipc-burst` из v0.91.21 СОХРАНЕНА — TODO-9 закрывать только после подтверждения юзером что Maximum update depth ушёл. **Как проверить:** см. `.memory-bank/native-scroll-restore-saga.md`. **Предыдущий v0.91.7**: фикс «при возврате в старый чат программа дёргает scrollTop». Корень — недоделка v0.91.2: я починил `firstUnread` ветку через `lastActiveChatIdRef`, но `savedScrollTop` ветку оставил с deps `[messagesCount]` → restore срабатывал на каждый setState на messages[key]. У нас 4 setState на одно открытие темы (IDB cache → server response → load-newer prefetch x2), и каждый ре-устанавливал `scrollTop = savedTop` (которое уже обновилось от user-scroll). Лог 17:32:13 показал 3 разных savedTop за 1 секунду (13678→10204→7811). Решение: применил тот же паттерн `lastActiveChatIdRef` — restore выполняется ТОЛЬКО при реальной смене activeChatId (`isReturning`), не на push/prefetch. Прибочная находка из v0.91.6: 4 setState на messages — НЕ дубли, а легитимный optimistic-render flow (IDB → server → prefetch) по Telegram Desktop pattern. Главный фикс закрывает обе проблемы. **Предыдущий v0.91.6**: 2 фикса по форум-чатам. (1) Вечная загрузка темы — корень в [`useInitialScroll.js:80`](src/native/hooks/useInitialScroll.js#L80): silent `if (!scrollEl) return` без `onDone()`. Цепочка: смена темы → `setChatReady(false)` → DOM scroll-контейнер скрыт за shimmer (opacity:0) → `scrollRef.current=null` → setTimeout 150ms → silent return → `onDone` не вызван → `chatReady` навсегда false → DOM никогда не рендерится. Deadlock. Решение: retry loop через `requestAnimationFrame` до 10 попыток, потом fallback `onDone(activeChatId)` (лучше показать чат без initial-scroll чем держать вечный shimmer). Подтверждено логом 17:05:51: state корректен (`[topic-state] applyMessages newLen=39 activeChatIdMatch=true`), но `initial-*` событий нет — useEffect молча выходил. (2) Custom emoji иконки тем — вернули GramJS-feature (была в memory-bank/group-topic-investigation.md, потеряна при миграции на TDLib v0.89.0). Новый модуль [`tdlibForumEmoji.js`](main/native/backends/tdlibForumEmoji.js) — `resolveTopicEmojis(topics, ctx)`: `getCustomEmojiStickers` (batch по unique iconCustomEmojiId) → `downloadFile` + `stabilizeForPlayback` → `cc-media://media/...` URL. In-memory кэш на сессию. .tgs (animated lottie) — fallback на alt-emoji (Chromium не рендерит TGS в `<img>`). По [TDLib spec](https://core.telegram.org/tdlib/docs/classtd_1_1td__api_1_1get_custom_emoji_stickers.html). **Предыдущий v0.91.5**: эмодзи-fallback + диагностика «выбрал тему — пустой экран». Лог 16:21-16:22 показал `[topic-be] invoke result messagesCount=39 ok=true` для Wildberries топика, но UI остался пустым (race / resolve activeMessages). Backend возвращает данные правильно. Добавлены 2 диагностических лога: `[topic-state] applyMessages key=... newLen=N prevLen=M activeForumTopicId=... activeChatIdMatch=...` в setState selectForumTopic для отслеживания race conditions. `[topic-resolve] chatId=... activeMessageKey=... activeMessages.len=N allTopicKeys=key=N,key=M` в render InboxMode — видно какой key UI читает и есть ли там сообщения. После рестарта лог точно покажет: либо state.messages[key] не обновляется (race), либо UI читает другой key (resolve bug). Эмодзи fix: helper `extractTopicCap(title)` в ForumTopicIcon — если title начинается с unicode emoji, показываем его; иначе первая буква (так Telegram Desktop делает когда custom emoji ещё не загружен). Custom emoji (TDLib icon.custom_emoji_id) — premium feature через getCustomEmojiStickers с lottie/webm рендером — отдельная задача. **Предыдущий v0.91.4**: lastMessage в темах форум-чатов + диагностика бейджа. Backend [`tdlibBackend.js forum.getTopics`](main/native/backends/tdlibBackend.js) теперь возвращает `lastMessage` и `lastMessageTs` через helper `extractTopicPreview` (текст или эмодзи-аннотация: 🖼 Фото, 📹 Видео, 🎤 Голосовое, 📊 Опрос и т.д. — по [TDLib MessageContent spec](https://core.telegram.org/tdlib/docs/classtd_1_1td__api_1_1_message_content.html)). Раньше backend возвращал только id/title/unreadCount → UI fallback показывал «Нет предпросмотра». Расширенный лог `[forum-map]` теперь печатает `unread_count` и `unread_mention_count` от TDLib для каждого форум-чата + `[forum-be]` показывает `sumTopicUnread` vs `chatUnreadCount` — для диагностики бага «бейдж непрочитанных пуст у forum-группы» (нужен ли client-side fallback или TDLib агрегирует). **Не делал client-side aggregation бейджа** — нужны данные с реальной сессии после рестарта, иначе слепой фикс. Превью «У меня есть контакты...» в списке чатов для форум-группы — это TDLib `chat.last_message` (последнее любое сообщение по группе, агрегированно). Это норма Telegram Desktop. **Предыдущий v0.91.3**: фикс «кнопка ↓ N показывает фантомные числа при unread=0». Корень — `useNewBelowCounter` ловил изменения массива `messages` и не различал источник: server push (`tg:new-message`) vs batch response (`tg:messages` от наших get-messages / load-older / load-newer). При initial-load массив заменялся целиком → prevLastId из preview phantom → cycle до конца → +100 в newBelow. Сумма 4 ложных new-below = 200 (см. лог). Переписан на **event-based**: подписка на `window.api.on('tg:new-message', ...)` (server push only), фильтры `chatId === activeChatId`, `!isOutgoing`, `!atBottom`. atBottom через `useRef` для stable handler без re-subscribe. Добавлен auto-reset newBelow при `activeUnread === 0` (как Telegram Desktop / WhatsApp). Сверено по 6 источникам: TDLib spec, TDesktop, WhatsApp Web (whatsmeow), Discord Gateway, React docs (useEffect+subscribe pattern, useRef для stable handlers). Все 4 мессенджера различают server-push vs API-response. Тесты `useNewBelowCounter.vitest.jsx` полностью переписаны под event-mock. **Предыдущий v0.91.2**: фикс «при чтении чата программа сама прыгает на середину». Корень — ветка «already-seen» в [`useInitialScroll.js`](src/native/hooks/useInitialScroll.js) re-запускается при каждом изменении `messagesCount` (push tg:new-message, load-older). Когда юзер mark-read'нул часть сообщений, потом пришёл server `store-unread-sync` с обновлённым unread, `firstUnreadIdRef.current` пересчитан → программа вызывала `onMissingTarget(firstUnread)` → `react-window.scrollToRow` бесцеремонно перебивал активный wheel-скролл юзера (нет защиты от user activity по [react-window API](https://github.com/bvaughn/react-window)). Сверка с TDesktop / WhatsApp Web / Discord / iOS Telegram — все НЕ дёргают scroll пока юзер читает. **Правка**: ветка `firstUnread` удалена из «already-seen» в useInitialScroll.js — restore = только savedScrollTop. Auto-jump к firstUnread остаётся при ПЕРВОМ открытии (ветка 1). **Предыдущий v0.91.1**: фикс «открыл чат — видно 1 сообщение, тишина минуту». Корень — guard `!store.messages[activeChatId]` в [`InboxMode.jsx:77,81`](src/native/modes/InboxMode.jsx) блокировал initial `loadMessages`. TDLib эмитит `updateNewMessage` для свежих сообщений ДО открытия чата → state.messages[id] почти всегда непуст (1-2 push'нутых сообщения) → guard ложен → `getChatHistory` не вызывался никогда. У Telegram Desktop / WhatsApp / Discord — история запрашивается ВСЕГДА при смене активного чата (кэш = optimistic render поверх запроса). Замена guard `!store.messages[id]` → `!store.loadingMessages[id]` — защита только от дубля параллельных запросов. Результат: instant render ~50 msg из IDB-кэша + 100 свежих от TDLib через 200-500мс. Полная история до v0.91.0 — в [`.memory-bank/features.md`](.memory-bank/features.md). **Предыдущий v0.91.0**: **ПОЛНЫЙ ОТКАТ миграции WebContentsView**. После 16 версий v0.89.41-v0.90.2 и поиска в Electron GitHub Issues найдено подтверждение что это **известный архитектурный баг Electron на Windows 11**: [Issue #44934](https://github.com/electron/electron/issues/44934) (App crashes when adding child view to WebContentsView on Windows 11), [#45367](https://github.com/electron/electron/issues/45367) (addChildView не рендерит), [#44897](https://github.com/electron/electron/issues/44897) (preload не загружается в child WCV) — все закрыты «not planned». Native crash при `addChildView(WebContentsView) + loadURL` НЕ ПОФИКШЕН в Electron 41. **Откат**: вернули `BrowserWindow{webviewTag:true}` + `<webview>` тег (production-tested архитектура). Удалены файлы: [`webContentsViewManager.js`](main/utils/webContentsViewManager.js), [`webContentsViewIpcHandlers.js`](main/handlers/webContentsViewIpcHandlers.js), [`WebContentsViewSlot.jsx`](src/components/WebContentsViewSlot.jsx), [`webContentsViewBridge.js`](src/utils/webContentsViewBridge.js), [`webContentsViewPatterns.test.cjs`](src/__tests__/webContentsViewPatterns.test.cjs), [`webContentsViewManager.vitest.js`](src/__tests__/webContentsViewManager.vitest.js), [`webContentsViewBridge.vitest.js`](src/__tests__/webContentsViewBridge.vitest.js). **Сохранены полезные побочные эффекты**: UncaughtErrorToast (UX), global error handlers (renderer+main), idbCacheMetrics, crashpad-filter в scripts/dev.cjs. **Полный урок** в [`mistakes/electron-core.md`](.memory-bank/mistakes/electron-core.md) — что хотели, что делали, что нашли, почему не получилось, 7 уроков для будущего. **Регрессия**: lint ✅, scope 23/23 ✅, structure 45/45 ✅, fileSizeLimits 267/267 ✅., но child WebContentsView для Telegram всё равно крашит на loadURL. Три попытки изолировать: (1) setBounds ДО loadURL в [`webContentsViewManager.js`](main/utils/webContentsViewManager.js) — bounds=(0,0,0,0) могла крашить renderer; (2) preload={undefined} в [`App.jsx`](src/App.jsx) WebContentsViewSlot + lazy mount (только активная вкладка) — пилот без ChatMonitor пока; (3) watchdog 15с в [`WebContentsViewSlot.jsx`](src/components/WebContentsViewSlot.jsx) — если did-finish-load не приходит за 15с, показывается ошибка в UI вместо тихой смерти (юзер просил «чтобы ошибки выскакивали в окне»). **Предыдущий v0.90.1**: фикс BaseWindow.loadURL → webContents.loadURL. — `TypeError: mainWindow.loadURL is not a function`. BaseWindow не имеет методов `.loadURL`/`.loadFile` (это BrowserWindow-only API). Заменено на `mainWindow.webContents.loadURL/loadFile` — Proxy перенаправляет на `primaryView.webContents`. Юзер видел пустое окно потому что React UI не грузился. **Предыдущий v0.90.0**: АРХИТЕКТУРНАЯ МИГРАЦИЯ BrowserWindow → BaseWindow + WebContentsView. После 12 опровергнутых гипотез v0.89.46-v0.89.57 доказано: `webviewTag:true` в primary BrowserWindow + child WebContentsView = архитектурная несовместимость в Electron 41 (даже `data:` URL крашит). По [Electron docs](https://www.electronjs.org/docs/latest/api/web-contents-view) пример официальный — `BaseWindow + WebContentsView`. **Изменения**: [`main/utils/windowManager.js`](main/utils/windowManager.js) — `new BaseWindow(...)` + `new WebContentsView(...)` для React UI + `addChildView` + Proxy для совместимости со старым кодом (`mainWindow.webContents` → primaryView.webContents). [`main/main.js`](main/main.js) — передаёт `BaseWindow`, `WebContentsView` в createWindow; удалён условный `disable-gpu-compositing` (был воркэраундом `<webview>`). [`src/App.jsx`](src/App.jsx) — удалена `<webview>` ветка, ВСЕГДА WebContentsViewSlot, `partition={m.partition}` (изоляция wcv-* больше не нужна). [`src/components/SettingsPanel.jsx`](src/components/SettingsPanel.jsx) — тумблер useWebContentsView скрыт (deprecated). **Регрессия**: 31/31 ✅ wcv guard (обновлены 5 проверок под v0.90.0 архитектуру), 15/15 ✅ modern, lint ✅, fileSizeLimits 274/274 ✅, scope 23/23 ✅, structure 45/45 ✅, mainRuntime 56/56 ✅. **Серия v0.89.46-v0.89.57** — 12 опровергнутых гипотез — закрыта. **Полный анализ** в [`mistakes/electron-core.md`](.memory-bank/mistakes/electron-core.md). (11-я гипотеза за серию). Юзер запустил v0.89.56 с partition `persist:wcv-telegram` — программа всё равно упала на `loadURL`. Partition / setupSession hooks — НЕ корень. **Стратегия — изолирующий тест**: в [`webContentsViewManager.js`](main/utils/webContentsViewManager.js) сначала грузим `data:text/html` URL (без сети/CSP/preload-injection). Если data: тоже крашит → корень в архитектуре BrowserWindow+WebContentsView в нашей конфигурации → отказ от пилота. Если data: работает → корень в network/Chromium при загрузке внешних URL. **Добавлены listeners**: `render-process-gone` (если renderer падает с reason), `did-fail-load`, `did-start-loading`. **setImmediate tick** после loadURL — посмотреть пережил ли main event loop. Это **диагностический эксперимент**, не фикс. Ловушка в [`mistakes/electron-core.md`](.memory-bank/mistakes/electron-core.md) — 11 опровергнутых гипотез, статус «🔴 НЕ РЕШЕНО, продолжаем». **Предыдущий v0.89.56**: изолированная partition (опровергнуто)., найден настоящий корень — **тройной конфликт `webviewTag:true` + `setupSession` + общий partition**. v0.89.55 (условный `disable-gpu-compositing`) опровергнута фактами: в логе `gpu-compositing ENABLED (WebContentsView pilot mode)` — switch не применён, программа всё равно упала на `loadURL`. Полный анализ нашего кода: [`windowManager.js:134`](main/utils/windowManager.js) BrowserWindow создаётся с `webviewTag:true` (регистрирует Chromium guest-page manager для `<webview>`). [`sessionSetup.js`](main/utils/sessionSetup.js) для каждого `m.partition` навешивает webRequest hooks + CSP модификации + clearStorageData. Когда WebContentsView с тем же partition пытается loadURL — попадает в `<webview>` guest-manager с уже-настроенной session → native crash. **Решение**: изолированная partition `persist:wcv-${id}` для пилота. [`App.jsx`](src/App.jsx) WebContentsViewSlot: `partition={'persist:wcv-' + m.id}` вместо `m.partition`. [`sessionSetup.js`](main/utils/sessionSetup.js): early return для `wcv-*` partitions — никогда не настраиваем как `<webview>`. [`App.jsx removeMessenger`](src/App.jsx): cleanup обоих partitions. [`SettingsPanel.jsx`](src/components/SettingsPanel.jsx): кнопка cleanup и тумблер обновлены (предупреждение про новую авторизацию). **Последствие**: при переключении тумблера — одноразовый logout (новая session). **Ловушка** в [`mistakes/electron-core.md`](.memory-bank/mistakes/electron-core.md) — полная история 10 опровергнутых гипотез + правило «webviewTag:true + WebContentsView требуют разных partitions». **Регрессия**: 31/31 ✅ wcv guard (+1 тест persist:wcv-*, +1 тест skip в sessionSetup), fileSizeLimits 274/274 ✅. **Предыдущий v0.89.55**: гипотеза disable-gpu-compositing (опровергнута). (добавлен v0.85.6 как воркэраунд `<webview>`) физически несовместим с overlay-рендерингом WebContentsView. Доказано в логах: даже `about:blank` крашит (v0.89.54). По [Chromium docs](https://www.electronjs.org/docs/latest/api/command-line-switches) software composition (swiftshader) при отключённом GPU compositing поддерживает только single-layer, не overlay. **Решение — условный switch**: в [`main/main.js`](main/main.js) функция `applyGpuStabilitySwitches` читает `chatcenter.json` через `app.getPath('userData')` (доступен до `app.whenReady` по Electron docs). Если `settings.useWebContentsView=true` — switch НЕ применяется (WebContentsView рендерится корректно). Иначе — применяется (default режим, `<webview>` работает без чёрного экрана). **Возвращены**: `preload={monitorPreloadPath}` в [`App.jsx`](src/App.jsx) WebContentsViewSlot (preload не был корнем — гипотеза v0.89.53 отменена), `sandbox:false` в [`webContentsViewManager.js`](main/utils/webContentsViewManager.js) (monitor.preload.cjs требует Node APIs). **Полный ChatMonitor работает** в пилоте: уведомления, mark-read, ribbon — через тот же monitor.preload.cjs что и `<webview>`. Никакой регрессии для default режима — switch применяется как раньше когда пилот выключен. **Ловушка** в [`mistakes/electron-core.md`](.memory-bank/mistakes/electron-core.md) — статус ✅ РЕШЕНО, полная история 8-серийного расследования. **Регрессия**: тест `applyGpuStabilitySwitches` в `main.js` + `preload={monitorPreloadPath}` в App.jsx. **Предыдущий v0.89.54**: эксперимент about:blank. Юзер запустил v0.89.53 с `preload={undefined}` — программа ВСЁ РАВНО упала на `loadURL`. Лог: `preload=(none) ... addChildView ok ... queuing loadURL ... [crashpad]`. Корень НЕ в preload. **Новая гипотеза v0.89.54** на основе фактов и [Electron Security Guidelines](https://www.electronjs.org/docs/latest/tutorial/security): `sandbox:false` без preload — недокументированная комбинация. Поменяли на `sandbox:true` (safe default). Дополнительный диагностический эксперимент: `loadURL('about:blank')` ПЕРЕД реальным URL — если about:blank работает, корень в URL/CSP, если крашит — корень в WebContentsView+BrowserWindow конфигурации. Описание тумблера в Settings обновлено: «⚠️ НЕ работает». Ловушка в [`mistakes/electron-core.md`](.memory-bank/mistakes/electron-core.md) расширена — расследование «ОТКРЫТО», гипотеза CSP отменена, новые гипотезы перечислены. **Предыдущий v0.89.53**: гипотеза `<webview>` preload (отменена). После пошаговой изоляции (8 версий диагностики) выяснили: краш происходит на `loadURL` потому что preload инжектит inline `<script>` в DOM мессенджеров (`document.createElement('script')` + `appendChild`), а Telegram/WhatsApp имеют строгий CSP (`script-src 'self'`). В `<webview>` теге это терпится, в WebContentsView CSP violation → нативный краш Chromium (segfault). Native crash JS handlers `uncaughtException` не ловят — отсюда тихое закрытие. **Решение**: в [`App.jsx`](src/App.jsx) WebContentsViewSlot получает `preload={undefined}` (откат к Phase 2.1 минимум — pilot БЕЗ ChatMonitor). Пилот открывается, юзер видит UX-улучшения (разделитель не залипает), но уведомления/mark-read/ribbon НЕ работают. Phase 3 (отдельный preload через `webContents.executeJavaScript` минуя CSP) — отдельная задача. **Ловушка**: новая критическая секция в [`mistakes/electron-core.md`](.memory-bank/mistakes/electron-core.md) — «`<webview>` preload несовместим с WebContentsView». **Регрессия**: тест `App.jsx: WebContentsViewSlot не получает monitor.preload.cjs` — при возврате preload падает с описанием ловушки. Описание тумблера в Settings обновлено («ВНИМАНИЕ: НЕ работают уведомления»). **Предыдущий v0.89.52**: пошаговые логи после `new WebContentsView ok`. Из лога v0.89.51 видно `[wcv-mgr] new WebContentsView ok`, но `[wcv-timing] create` отсутствует → main крашится МЕЖДУ конструктором и возвратом. Добавлены 4 точки: `forwarding events...` → `events forwarded` → `adding to parent contentView (type=present/missing)...` → `addChildView ok/FAILED` → `queuing loadURL ...` → `loadURL settled/failed`. Главное подозрение: `BrowserWindow.contentView.addChildView` (BrowserWindow.contentView — primary view, в Electron v30+ можно добавлять дочерние, но может крашить нативно в каких-то конфигурациях). Следующий запуск точно покажет на каком шаге обрыв. **Предыдущий v0.89.51**: защита от невалидного preload + diag в createView. Из лога v0.89.50 видно: `[WCV-slot] wcv:create invoke id=telegram` есть, но `[wcv-timing] create` НЕТ — main process не отвечает на IPC после получения. Гипотеза: `new WebContentsView` крашит main нативно (без JS exception). Добавлено: (1) `fs.existsSync(preloadPath)` перед `new WebContentsView` — если preload не найден, создаём view БЕЗ preload (лучше частично работающий пилот, чем мёртвый main); (2) `console.log('[wcv-mgr] createView ...')` до конструктора, `[wcv-mgr] new WebContentsView starting/ok` вокруг создания, `[wcv-mgr] new WebContentsView FAILED` в catch. Следующий запуск покажет точное место зависания. **Предыдущий v0.89.50**: render-branch + WCV-slot diag. Анализ лога юзера показал что settings:get=ok, useWebContentsView=ON, ошибок нет, но `[wcv-timing] create` отсутствует — значит WebContentsViewSlot не монтируется или его useEffect не доходит до wcv:create invoke. Добавлены логи: (1) [`App.jsx`](src/App.jsx) при `appReady=true` пишет `[render-branch] activeId=X useWcv=Y telegram=wcv,vk=wcv,...` — видно какая ветка рендера выбрана для каждого мессенджера; (2) [`WebContentsViewSlot.jsx`](src/components/WebContentsViewSlot.jsx) при mount пишет `[WCV-slot] mount id=X url=Y hasPreload=true hasPartition=true`, перед invoke пишет `[WCV-slot] wcv:create invoke id=X`. Теперь в логе будет видно «слот смонтирован → инвоук дошёл → main ответил» или ровно где обрыв. **Предыдущий v0.89.49**: toast при uncaught error + лог useWebContentsView при старте. **Toast**: новый компонент [`UncaughtErrorToast.jsx`](src/components/UncaughtErrorToast.jsx) — слушает `cc-uncaught-error` CustomEvent, показывает красную плашку в углу на 12с. [`useConsoleErrorLogger.js`](src/hooks/useConsoleErrorLogger.js) теперь эмитит событие из обоих handler'ов (sync + async). [`App.jsx`](src/App.jsx) рендерит `<UncaughtErrorToast />` рядом с resize overlay. **Лог пилота**: [`useAppBootstrap.js`](src/hooks/useAppBootstrap.js) после `settings:get` пишет `useWebContentsView=ON/OFF` — теперь из лога сразу видно активирован ли пилот. **Регрессия**: webContentsViewPatterns 27 → 29 (+2 smoke). vitest 683 ✅. **Предыдущий v0.89.48**: диагностика пилота WebContentsView (4 совета). **Совет 1 отменён** — я был неправ, что `<webview>` тег работает с raw path; по [Electron docs](https://www.electronjs.org/docs/latest/api/webview-tag#preload) URL должен иметь protocol `file:` или `asar:`. Дубликат `monitorPreloadUrl` оставляем. **Совет 2 — smoke-test без Playwright**: 4 новых теста в [`webContentsViewPatterns.test.cjs`](src/__tests__/webContentsViewPatterns.test.cjs) — проверяют что useAppBootstrap не оборачивает path в file://, wcv:create логирует тайминг, error handlers подписаны, crashpad-фильтр не глотает ERROR. **Совет 3 — глобальные error handlers**: renderer ([`useConsoleErrorLogger.js`](src/hooks/useConsoleErrorLogger.js)) подписывается на `window.error` + `unhandledrejection`; main ([`main.js`](main/main.js)) на `process.uncaughtException` + `unhandledRejection`. Раньше при тихом крахе пилота в chatcenter.log не было ничего, теперь любой uncaught error виден. **Совет 4 — тайминг wcv:create**: [`webContentsViewIpcHandlers.js`](main/handlers/webContentsViewIpcHandlers.js) меряет `Date.now() - t0` и пишет в лог `[wcv-timing] create id=X ms=Y ok=...`. Видно ускоряет ли пилот открытие. **Бонус — ослабление crashpad-фильтра**: [`scripts/dev.cjs`](scripts/dev.cjs) теперь имеет positive override — если строка содержит ERROR/Error/FAIL, не глотается даже если matches noise pattern. **Регрессия**: webContentsViewPatterns 23 → 27 (+4 smoke-тестов), vitest 683 ✅. **Предыдущий v0.89.47**: полировка пилота WebContentsView (5 советов после v0.89.46 critical fix). **Совет 1 — раздельный path и URL**: [`useAppBootstrap.js`](src/hooks/useAppBootstrap.js) теперь задаёт ОБА значения: `monitorPreloadUrl` (file://) для `<webview>` тега и `monitorPreloadPath` (raw) для WebContentsView. В [`App.jsx`](src/App.jsx) `WebContentsViewSlot.preload={monitorPreloadPath}`, старый `<webview>` остаётся на URL. Архитектурно чище чем нормализация в manager. **Совет 2 — регрессионный тест**: `modernPatternsGuard.test.cjs` +2 проверки — WebContentsViewSlot получает именно Path (не URL); useAppBootstrap задаёт оба значения. **Совет 3 — ловушка в mistakes**: в [`mistakes/electron-core.md`](.memory-bank/mistakes/electron-core.md) добавлена секция «preload URL vs path» с таблицей соответствий API → формат. **Совет 4 — фильтр crashpad-лога**: [`scripts/dev.cjs`](scripts/dev.cjs) проксирует stdout/stderr через `filterChunk`, скрывает `crashpad ... not connected` и `DEP0190 DeprecationWarning`. CC_DEV_VERBOSE=1 показывает всё. **Совет 5 — overlay-сообщение при wcv:create ok:false**: [`WebContentsViewSlot.jsx`](src/components/WebContentsViewSlot.jsx) показывает поверх пустого слота ⚠️ + текст «Откройте Настройки → выключите тумблер». Юзеру понятно без чтения логов. **Регрессия**: modernPatternsGuard 36→38 (+2), vitest 683 ✅, lint ✅. **Предыдущий v0.89.46**: критический фикс пилота WebContentsView — `preload script must have absolute path`. Юзер включил `useWebContentsView=true`, программа упала при создании view. **Корень**: `monitorPreloadUrl` в [`useAppBootstrap.js:81`](src/hooks/useAppBootstrap.js) формируется как `file:///c:/...` URL (формат для `<webview>` тега), но WebContentsView требует raw absolute path (по [Electron docs](https://www.electronjs.org/docs/latest/api/web-contents-view): «webPreferences.preload — The value should be the absolute file path to the script»). **Решение**: новая функция `normalizePreloadPath(preload)` в [`webContentsViewManager.js`](main/utils/webContentsViewManager.js) — через `node:url#fileURLToPath` конвертирует `file://` → абсолютный путь. Handle unicode (русские имена пользователей `Директор`) и пробелы (`Program Files`). Вызывается в `createView` перед `webPreferences.preload = ...`. **Регрессия**: `webContentsViewManager.vitest.js` 12 → 17 (+5 тестов normalizePreloadPath: null/undefined, абсолютный путь без file://, file:///c:/ Windows, unicode, пробелы). **Предыдущий v0.89.45**: BrowserView guard + cache metric aggregator + trim CLAUDE.md. **Совет — BrowserView guard**: новая проверка в [`modernPatternsGuard.test.cjs`](src/__tests__/modernPatternsGuard.test.cjs) рекурсивно обходит `main/`, ищет `new BrowserView(...)`, импорты `BrowserView` из `electron`, destructuring. Снимает комментарии перед поиском (не падает на упоминаниях в `/* deprecated since v29 */`). По [Electron docs](https://www.electronjs.org/docs/latest/api/browser-view) BrowserView deprecated с v29 → должен быть заменён на WebContentsView. **Совет — агрегатор метрики кэша**: новый модуль [`src/native/utils/idbCacheMetrics.js`](src/native/utils/idbCacheMetrics.js) — `recordIdbCache(op, hit)` копит счётчики, через 30 секунд пишет ОДНУ строку `idb-cache-window` с summary по каждому op (loadMessages/selectForumTopic) + hit rate. Если событий 0 — лог не пишется (нет шума при простое). Заменяет сотни поштучных строк `idb-cache` на одну агрегированную. **Совет — trim CLAUDE.md**: цепочка «Предыдущий v0.89.43 / Предыдущий v0.89.42» в подвале вырезана, осталась только ссылка на features.md. **Регрессия**: `modernPatternsGuard` 34→36 (+2: BrowserView guard, idbCacheMetrics существует). Новый `idbCacheMetrics.vitest.js` — 6 unit-тестов (0 событий не пишет лог, hit/miss агрегируются с rate, разные op раздельно, окно открывается заново после flush). **Предыдущий v0.89.44**: Phase 2.3 (full) bridge активирован + cleanup UI + cache metrics — детали в [`.memory-bank/features.md`](.memory-bank/features.md). **Совет 1 — bridge подключён к App.jsx**: [`createWebContentsViewBridge`](src/utils/webContentsViewBridge.js) расширен (`getWebContentsId`/`style` proxy/`src` setter — закрывает контракт `webviewSetup.js`). [`WebContentsViewSlot.jsx`](src/components/WebContentsViewSlot.jsx) — новый `onCreated` callback после `wcv:create`. [`App.jsx`](src/App.jsx) — в ветке `useWebContentsView` вызывает `setWebviewRef(bridge, m.id)`, маркер `_isWebContentsViewBridge` защищает от двойного создания. Теперь `webviewSetup` работает через `wcv:*` IPC → ChatMonitor подключён в пилотном режиме (mark-read, ribbon, enrichNotif). **Совет 2 — кнопка очистки кэша в Settings**: [`SettingsPanel.jsx`](src/components/SettingsPanel.jsx) — `cleanupWcvPartitions` + новая кнопка «🧹 Очистить кэш WebContentsView» (видна только когда `useWebContentsView=true`). Идёт по мессенджерам и вызывает `wcv:cleanup-partition` с `full:false` (сессии сохраняются). **Совет 3 — авто-cleanup при удалении мессенджера**: [`App.jsx removeMessenger`](src/App.jsx) — перед фильтром state вызывает `wcv:cleanup-partition` с `full:true` (cookies/localStorage/IDB) → осколки сессии не остаются на диске. **Совет 4 — метрика hit/miss IDB кэша**: [`nativeStore.js`](src/native/store/nativeStore.js) — `logNativeScroll('idb-cache', {op, result})` после `loadCacheMessages` в `loadMessages` и `selectForumTopic`. Видно эффективность optimistic render в логе для будущей настройки TTL/лимита. **Совет 5 — удалён obsolete re-export**: `src/native/utils/topicMessagesCache.js` стёр (с v0.89.40 был только re-export `messagesCache.js`, импортов больше нет). **Регрессионная защита**: `modernPatternsGuard.test.cjs` +7 проверок (расширенный bridge-контракт, onCreated в Slot, bridge инициализация в App.jsx, cleanup-partition в removeMessenger, кнопка в Settings, idb-cache метрика, отсутствие старого модуля). `webContentsViewBridge.vitest.js` +5 unit-тестов (getWebContentsId стабильный, разный для разных viewId, style proxy, src setter ⇒ wcv:load-url, пустой src не дёргает IPC). Полная история версий — в [`.memory-bank/features.md`](.memory-bank/features.md).
