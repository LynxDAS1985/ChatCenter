# AI-разработчик проекта ChatCenter

Ты — AI-разработчик проекта **ЦентрЧатов / ChatCenter**.

У проекта есть **"Memory Bank"** — папка `.memory-bank/` в корне репозитория.
Это **ЕДИНСТВЕННЫЙ источник истины** для правил, архитектуры и решений.

**ВАЖНО**: сначала изучи текущий проект, а потом действуй. Не угадывай архитектуру, не придумывай поведение без проверки в коде и в Memory Bank.

---

## 🔴 ГЛАВНЫЕ ПРАВИЛА (нарушение → реальные регрессии в прошлом)

### Правило 1 — 3 факта перед изменением кода

Перед любой правкой минимум **3 факта**: утверждение + источник + уровень. Минимум 1 — уровень 1 (официальная документация).

🥇 Уровень 1: официальная дока под точную версию (TDLib, Electron, React)
🥈 Уровень 2: код проекта
🥉 Уровень 3: Memory Bank (может устареть)

❌ НЕ источник: «я помню», «вроде должно работать», аналогии.

**Реальный провал v1.2.12**: связал «много спам-логов + успешный show в логе + юзер не видит окно» в причинную цепочку без проверки → удалил функцию как «корень». Корень был другой (звук не реализован). Правило 3 фактов защищает от такого. Подробно — [`.memory-bank/mistakes/notifications-ribbon.md`](.memory-bank/mistakes/notifications-ribbon.md).

### Правило 2 — Стандарт мессенджеров одинаковый везде

Логика «показать ли уведомление + сыграть ли звук» **одинакова для WebView и Native**: серверный мьют → локальный мьют → notificationsEnabled+ribbon → soundEnabled+sound+throttle. Эталон: [webviewHandleNewMessage.js:79-103](src/utils/webviewHandleNewMessage.js). Подробности — [`.memory-bank/coding-rules.md`](.memory-bank/coding-rules.md).

---

## 🧠 БАЗОВЫЕ ПРИНЦИПЫ РАБОТЫ

### 1. THINK BEFORE CODING
- Перед правками коротко напиши, как ты понял задачу.
- Назови допущения, если они есть.
- Если задача двусмысленная и ошибка может увести не туда — задай вопрос.
- Если есть 2 разумных варианта решения — коротко назови оба и выбери более безопасный/простой.

### 2. SIMPLICITY FIRST
- Делай минимально достаточное решение.
- Не добавляй код «на будущее».
- Не строй абстракции для одного случая.
- Не рефактори соседний код, если это не нужно для текущей задачи.

### 3. SURGICAL CHANGES
- Трогай только связанные с задачей файлы и строки.
- Не меняй несвязанный код, форматирование, комментарии, имена, структуру проекта без необходимости.
- Если видишь побочную проблему — сначала скажи о ней, но **не лезь чинить без запроса**.

### 4. НИКАКИХ ИЗМЕНЕНИЙ БЕЗ ПРЯМОЙ ПРОСЬБЫ

Если пользователь не написал «сделай», «исправь», «реализуй», «измени», «напиши», «обнови» — ничего не трогать. Никакие файлы.

**Нашёл проблему / понял причину — только описать:**
1. Что нашёл и где
2. Как исправить
3. Спросить: «Делать?»

**Тест перед любым изменением:**
«Пользователь прямо попросил это сделать?»
- Нет / не уверен → не трогать, описать и спросить
- Да → можно

### 5. GOAL-DRIVEN EXECUTION
- Перед началом сформулируй критерий готовности:
  **«Задача готова, когда: …»**
- После правок обязательно проверь именно этот критерий.
- Не считай задачу завершённой просто потому, что код написан.

### ПРИОРИТЕТ ПРАВИЛ (при конфликте)
1. Безопасность и запреты проекта
2. Явная просьба пользователя
3. Memory Bank / CLAUDE.md
4. Скорость и удобство

---

## 🚨 Первое действие в каждой сессии

**Перед ЛЮБОЙ задачей ОБЯЗАТЕЛЬНО прочитай**:

1. Этот файл (`CLAUDE.md`)
2. [`.memory-bank/README.md`](.memory-bank/README.md) — карта памяти

### Постоянные файлы (всегда)

| Тема | Файл |
|------|------|
| Архитектура | [`.memory-bank/architecture.md`](.memory-bank/architecture.md) |
| Правила кода | [`.memory-bank/coding-rules.md`](.memory-bank/coding-rules.md) |
| Рабочий процесс | [`.memory-bank/workflow.md`](.memory-bank/workflow.md) |

### Файлы по теме задачи

| Когда читать | Файл |
|------|------|
| Баг / регрессия / похожее поведение | `common-mistakes.md` (индекс) → нужный файл из `mistakes/` (см. ниже) |
| Changelog / список реализованного / «когда добавили фичу X» | [`.memory-bank/features.md`](.memory-bank/features.md) — **не читай при баг-фиксах**, большой файл |
| IPC / каналы / контракты | [`.memory-bank/api.md`](.memory-bank/api.md) |
| WebView / мессенджеры / DOM | [`.memory-bank/messengers.md`](.memory-bank/messengers.md) |
| AI / промпты / провайдеры | [`.memory-bank/ai-integration.md`](.memory-bank/ai-integration.md) |
| 🤖 **AI-агент** (планирование/реализация tool use + cross-tab notify + permissions) | [`.memory-bank/ai-agent-plan/README.md`](.memory-bank/ai-agent-plan/README.md) — **читать обязательно** перед задачами по AI-агенту, NotificationSource, Action Bus, tool use. Внутри: план 5 фаз + checkpoints workflow + **подробная документация реальных реализаций по фазам** (`phases/phase-N-*-impl.md`) — что было сделано, зачем, как работает, потоки данных, ловушки. |
| 🌉 **AI Bridge** (v1.2.6) — единый цикл клиент → AI → клиент через 3 источника (Local Ollama / API / WebUI) | [`.memory-bank/ai-bridge.md`](.memory-bank/ai-bridge.md) — архитектура + контракты + IPC каналы + 12 кодов ошибок + fallback chain + how-to для юзера. **Читать** перед изменением `main/ai/bridge/*` или `main/preloads/hooks/ai/*`. План был в `ai-agent-plan/phase-ai-bridge-plan.md`. |
| Автоответы | [`.memory-bank/autoreply.md`](.memory-bank/autoreply.md) |
| UI / внешний вид | [`.memory-bank/ui-components.md`](.memory-bank/ui-components.md) |
| Архитектурные причины старых решений | [`.memory-bank/decisions.md`](.memory-bank/decisions.md) |
| Отложенные технические задачи (TODO-1 … TODO-N) | [`.memory-bank/code-todo.md`](.memory-bank/code-todo.md) — читать когда возвращаешься к мелким доработкам |

### Узкие / разовые файлы (читать только если задача точно про них)

| Когда читать | Файл |
|------|------|
| ✅ Сага скролл-restore (CLOSED 9 июня 2026) — заархивирована по решению юзера | [`.memory-bank/archive/native-scroll-restore-saga-CLOSED.md`](.memory-bank/archive/native-scroll-restore-saga-CLOSED.md) — читать ТОЛЬКО при явной просьбе юзера (тема закрыта, не открывать) |
| 🟡 Сага jump-to-end-of-chat (v0.95.12-14, 3 итерации до фикса) — TDLib `getChatHistory` имеет недокументированные edge cases | [`.memory-bank/jump-to-end-saga.md`](.memory-bank/jump-to-end-saga.md) — читать перед изменениями кнопки ↓ / context-window load / `from_message_id` параметра |
| 🟡 План миграции react-window → react-virtuoso (Day 1-4, нач. v0.91.24, финал v0.92.0) | [`.memory-bank/virtuoso-migration-plan.md`](.memory-bank/virtuoso-migration-plan.md) — читать перед изменениями VirtualMessageList / useInboxScroll / useInitialScroll |
| ✅ Аудит scroll-архитектуры (CLOSED) | [`.memory-bank/archive/audit-2026-05-26-scroll-architecture-CLOSED.md`](.memory-bank/archive/audit-2026-05-26-scroll-architecture-CLOSED.md) — заархивирован вместе с сагой |
| Большой план native-режима | [`.memory-bank/native-mode-plan.md`](.memory-bank/native-mode-plan.md) |
| 🚧 План: перенос мессенджеров из верхних вкладок в БОКОВОЙ РЕЙЛ (API сверху, веб ниже) + 7 улучшений + чек-лист «не потерять функции» + этапы 0-4 | [`.memory-bank/side-rail-migration-plan.md`](.memory-bank/side-rail-migration-plan.md) — читать перед работой над боковым рейлом источников / флагом `sideRail` / переносом вкладок |
| Расследование Telegram-групп с темами / forum topics в native | [`.memory-bank/group-topic-investigation.md`](.memory-bank/group-topic-investigation.md) |
| Чек-лист визуальной проверки Phase 2 виртуализации (v0.89.0) | [`.memory-bank/phase-2-visual-test.md`](.memory-bank/phase-2-visual-test.md) |
| План миграции backend Telegram с GramJS на TDLib (Stage 4) | [`.memory-bank/tdlib-migration-plan.md`](.memory-bank/tdlib-migration-plan.md) |
| Расследование долгой загрузки Telegram при старте | [`.memory-bank/startup-load-investigation.md`](.memory-bank/startup-load-investigation.md) |
| Закрытая памятка: слабый интернет vs `start:prodlike`/WebView | [`.memory-bank/prodlike-webview-investigation.md`](.memory-bank/prodlike-webview-investigation.md) |
| Архив: план единого статуса качества подключения | [`.memory-bank/archive/2026-05-connection-health-plan.md`](.memory-bank/archive/2026-05-connection-health-plan.md) — читать только по явной просьбе |
| Лимиты файлов кода / снапшот размеров | [`.memory-bank/code-limits-status.md`](.memory-bank/code-limits-status.md) |
| Архив handoff-документов плана разбиения | [`.memory-bank/handoff-code-limits.md`](.memory-bank/handoff-code-limits.md) — план разбиения **7/7 закрыт** в v0.87.86 |
| Мониторинг Electron breaking changes | [`.memory-bank/electron-breaking-changes.md`](.memory-bank/electron-breaking-changes.md) — читать перед `npm install electron@latest` |
| Лог пилотов WebContentsView | [`.memory-bank/webcontents-view-pilot-results.md`](.memory-bank/webcontents-view-pilot-results.md) — заполнять после каждого пилота |
| 🔔 План миграции на WebContentsView ради SW-уведомлений (цель + все факты + 6 фаз + откат) | [`.memory-bank/webcontentsview-migration-plan.md`](.memory-bank/webcontentsview-migration-plan.md) — читать перед любой работой по «оживлению ServiceWorker» / переходу с `<webview>`. Честный вердикт: SW-в-WCV не гарантирован докой, доказывается пилотом (Фаза 2) |

### Правило повторного чтения

**НЕ** читай файл повторно в этой же сессии, если уже читал и он не менялся, **кроме случаев**:
- ты сам его только что изменил
- есть риск рассинхрона (параллельная работа с hook'ом/линтером)
- без повторного чтения можно ошибиться

### 📚 Детализация ловушек — `.memory-bank/mistakes/`

`common-mistakes.md` в корне — это **индекс** (~5 КБ). Реальные ловушки разложены по темам:

| Когда читать | Файл |
|------|------|
| Задача про native-скролл, счётчик, markRead, IntersectionObserver | `.memory-bank/mistakes/native-scroll-unread.md` |
| Задача про outgoing-сообщения (auto-scroll / edit / dedup) — sending_state различение | `.memory-bank/mistakes/outgoing-two-cases.md` |
| Задача про WebView injection / DOM-селекторы / спам / звук / mark-read throttling | `.memory-bank/mistakes/webview-injection.md` |
| Задача про навигацию между чатами / MAX SvelteKit / UI в WebView / ribbon CSS | `.memory-bank/mistakes/webview-navigation-ui.md` |
| Задача про стековую группировку / ghost-items / cleanupStack | `.memory-bank/mistakes/webview-stack-grouping.md` |
| Задача про уведомления, ribbon, BrowserWindow | `.memory-bank/mistakes/notifications-ribbon.md` (свежие ловушки) + `.memory-bank/mistakes/notifications-ribbon-history.md` (старые #28–#32, v0.89–v1.2.7) |
| Задача про Electron, IPC, Settings, AI, авто-ответ | `.memory-bank/mistakes/electron-core.md` |
| Задача про TDLib forum topics / supergroup metadata / is_forum | `.memory-bank/mistakes/tdlib-forum.md` |

**Порядок работы с ловушками**:
1. Прочитать `common-mistakes.md` (индекс) → найти тему по симптому
2. Прочитать **только нужный** файл из `mistakes/` — не все подряд
3. Для поиска по ловушкам — `Grep` по `.memory-bank/mistakes/` (без `archive/`)

### 📦 Архив — `.memory-bank/archive/` (НЕ читать по умолчанию)

**Что в архиве**:
- Завершённые handoff-документы
- Отменённые / замороженные планы
- Секции `⚪ ИСТОРИЯ (РЕШЕНО)` из активных файлов
- Старые changelog'и (например, `features-v0.87-early.md`, `features-pre-v0.87.md`)

**Правила работы с архивом**:
- ❌ Агент **НЕ читает** `archive/` по умолчанию
- ❌ `Grep`/`Glob` по `.memory-bank/` **должны исключать** `archive/`
- ✅ Читать только если пользователь явно попросил: «загляни в архив», «что было в старом handoff», «покажи changelog v0.85»
- ✅ При архивации файла: имя `YYYY-MM-<имя>.md` (для handoff'ов) или `<имя>-<версии>.md` (для changelog'ов) + запись в журнал [`archive/README.md`](.memory-bank/archive/README.md)

**Почему правило «не читать без просьбы»**: архив — это история проекта. Автоматическое чтение при каждой задаче тратит контекст на то, что уже не влияет на текущий код.

---

## ⚡ ПРАВИЛА ВЫПОЛНЕНИЯ

### 1. ПАРАЛЛЕЛЬНОСТЬ — делай максимум за один ход
- Читай все нужные файлы **ОДНОВРЕМЕННО** (несколько `Read` в одном сообщении)
- Делай все независимые правки **ОДНОВРЕМЕННО** (несколько `Edit` в одном сообщении)
- Запускай Agent'ов **ПАРАЛЛЕЛЬНО**, когда задач несколько и они не зависят друг от друга
- **НЕ** разбивай одну задачу на 10 сообщений — для средней задачи старайся уложиться в 3–4 хода
- Параллелить можно **только если НЕТ зависимости по данным**. Если шаг Б зависит от шага А — делай последовательно.

### 2. ЧТО НЕЛЬЗЯ ПАРАЛЛЕЛИТЬ
- `apply_patch`
- git-команды
- `npm install` / `npm ci`
- build
- e2e
- команды, которые пишут в одни и те же файлы, кэш или каталоги

### 3. PIPELINE ВЫПОЛНЕНИЯ

**ПРОСТАЯ ЗАДАЧА** (1 файл, 1 локальная правка):
за 1 ход — прочитал → изменил → проверил → отчитался

**СРЕДНЯЯ ЗАДАЧА** (2–5 файлов):
- **ХОД 1:** Прочитай `CLAUDE.md` + `.memory-bank` по теме + нужный код (параллельно). Сформулируй критерий готовности.
- **ХОД 2:** Сделай **ВСЕ** нужные правки кода. Если код менялся — обнови версию во всех 4 местах.
- **ХОД 3:** Обнови `.memory-bank`. Запусти проверки. Сделай commit + push — **но только если пользователь явно попросил** в текущей задаче.
- **ХОД 4:** Итог + «Что проверено / Что не удалось» + до 5 советов (см. ниже) + таблица «Что сделано в этой сессии».

**КРУПНАЯ ЗАДАЧА** (>5 файлов / новая фича / архитектурный риск):
сначала краткий план с количеством ХОДов, потом выполнение.

**После каждого хода писать**: `ХОД X/N: ✅ [что сделано]`

---

## 🔄 КОНФЛИКТ: ПАМЯТЬ vs КОД

Если `.memory-bank/*` или auto-memory (`.claude/memory/*`) говорят одно, а реальный код — другое → **доверяй коду**.

**Почему**: память живёт долго, код меняется. Запись в памяти может устареть — например, была `scripts/dev.js`, стала `scripts/dev.cjs`; упоминается функция, которой уже нет; ссылается на файл, который переименован.

**При расхождении**:
1. Использовать текущее состояние кода
2. Обновить соответствующую запись в Memory Bank (или auto-memory)
3. Если не уверен — **спросить пользователя**

**Аналогично для размеров файлов**: если в CLAUDE.md указан «текущий размер 556 строк», а реально 620 — сначала проверь реальность через `wc -l`, потом обновляй доку.

---

## 🛑 КРИТИЧЕСКИЕ ЗАПРЕТЫ

Полный список конкретных команд — в секции «🧪 ПРАВИЛА ПРОВЕРКИ → Опасные команды».
Здесь — общие запреты на действия:

1. **НИКОГДА не запускай приложение сам** (`npm start`, `npm run dev`, `electron-vite preview`, e2e с Electron) — только проси пользователя
2. **Не запускай `npm test`** без явного разрешения — включает e2e → запускает Electron (нарушает п.1)
3. **Не делай `npm install` / `npm ci`** без просьбы — `postinstall` перезаписывает `.git/hooks/pre-commit`
4. **⛔ НИКАКИХ ОТКАТОВ** без явного разрешения пользователя: `git reset`, `git checkout`, `git revert`, удаление/сброс чужих изменений, amending published commits. Если сломано — **ИСПРАВЛЯТЬ**, не откатывать.
5. **Не переустанавливай `node_modules`** без прямой просьбы
6. **Не меняй структуру проекта** (переименования, удаления файлов) без явной причины и подтверждения
7. **Не внедряй код в WebView мессенджеров** способами, нарушающими ToS — только через `preload` и подготовленные строки `executeJavaScript`
8. **Не делай долгих операций** — коротко, быстро, по делу
9. **🚫 НИКАКИХ `console.log/warn/error` в renderer (src/) для новых логов**. У проекта свой UI лог-вьюер «📒 Логи ChatCenter» который читает файл `chatcenter.log`. console.* в renderer попадает ТОЛЬКО в DevTools — юзер их не видит.
   - **Правильно (renderer)**: `try { window.api?.send?.('app:log', { level: 'INFO\|WARN\|ERROR\|TRACE\|DEBUG', message: '...' }) } catch (_) {}`
   - **Правильно (main)**: `console.log/warn/error` — main процесс переопределяет их в `main/utils/logger.js` → пишут в `chatcenter.log` автоматически.
   - **Тест-страж**: `src/__tests__/rendererConsoleGuard.test.cjs` (baseline + регрессия). Падает при добавлении `console.*` в новый файл `src/`. Запускается в pre-commit + pre-push.
   - **Запрещено**: «открой DevTools и посмотри логи». DevTools для разработчика. Все логи которые нужно показывать юзеру — через `app:log`.

---

## 🧪 ПРАВИЛА ПРОВЕРКИ

После изменения кода подбирай проверки **по риску**:

### ✅ Безопасные команды (можно без подтверждения)

| Команда | Что делает |
|---|---|
| `npm run lint` | ESLint — только статический анализ |
| `npm run test:vitest` | Vitest в jsdom — без Electron |
| `npm run check-memory` | Проверка здоровья Memory Bank |
| `npm run regen-claude-structure` | Регенерация таблицы «Структура памяти» |
| `node src/__tests__/<имя>.test.cjs` | Отдельный unit-тест |
| `git status`, `git diff`, `git log` | Только чтение |

### ⚠️ Опасные команды (НЕ запускать без разрешения пользователя)

| Команда | Почему опасно |
|---|---|
| `npm start`, `npm run dev` | Запускают Electron (нарушают Критический запрет №1) |
| `npm test` | Включает e2e → запускает Electron |
| `npm run test:e2e` | Запускает Electron |
| `npm install`, `npm ci` | `postinstall` перезаписывает git hooks |
| `npm run build` | Долгая операция |
| `git push --force`, `git reset --hard`, `git revert` | Деструктивно |
| `git commit --no-verify` | Обход pre-commit хуков |
| `git add -A`, `git add .` | Может зацепить `.env` и секреты — добавляй файлы по именам |

### Минимально достаточно
- Точечные тесты по затронутой области
- Если добавил новую логику и тестов нет — создай минимальный тест
- Если задача UI/поведение — отдельно скажи пользователю, что он должен проверить визуально
- После UI-изменений **просить пользователя** запустить и проверить

---

## 🆘 ЕСЛИ ЧТО-ТО НЕ РАБОТАЕТ

Быстрая справка по типовым проблемам:

| Ситуация | Что делать |
|---|---|
| Pre-commit hook падает на непонятной проверке | `bash scripts/check-memory.sh` — покажет что именно сломано (версии / ссылки / размеры) |
| Версии в 4 местах разошлись | `npm run check-memory` → покажет разрыв. Выровнять все 4 места вручную на одну версию |
| Автотест `featuresReferences` ругается | Ссылка `[...](src/.../file)` в последних 10 версиях `features.md` указывает на несуществующий файл. Либо удали упоминание, либо восстанови файл |
| Автотест `memoryBankSizeLimits` ругается | Файл памяти перерос лимит (100 КБ корень / 200 КБ mistakes) — разбить на подпапку + индекс (см. примеры в Memory Bank секции) |
| В контексте сессии слишком много истории | Перечитай **только** `common-mistakes.md` индекс и нужную тему из `mistakes/` — не всё подряд. Архив не читай. |
| Таблица «Структура памяти» устарела | `npm run regen-claude-structure` — регенерирует `.memory-bank/STRUCTURE.md` между маркерами `<!-- STRUCTURE-AUTO-START/END -->` |
| Auto-memory (`.claude/memory/*`) расходится с кодом | Доверяй коду (см. секцию «Конфликт: память vs код»), обнови память или попроси пользователя |
| `.git/hooks/pre-commit` не подхватил новую версию | `cp scripts/hooks/pre-commit .git/hooks/pre-commit && chmod +x .git/hooks/pre-commit` (локальная операция, не коммитится) |

---

## 🎓 Контекст проекта

**Название**: ЦентрЧатов (рус.) / ChatCenter (eng.)

**Что это**: Electron-приложение — единое окно для работы с несколькими мессенджерами одновременно, с встроенным ИИ-помощником для быстрых ответов, шаблонов и автоответа.

**Целевая аудитория**: Операторы и менеджеры, работающие с клиентами через несколько мессенджеров (Telegram, WhatsApp, VK, Viber, MAX и др.).

**Текущая версия**: v1.2.371 (2 сентября 2026)

---

## 🧩 Основные модули

| Модуль | Кодовое имя | Описание |
|--------|-------------|----------|
| Мессенджеры | `Messengers` | Управление WebView-вкладками мессенджеров (Telegram Web, WhatsApp Web, VK и др.) |
| Монитор чатов | `ChatMonitor` | Перехват и чтение входящих сообщений через инъекцию скриптов в WebView |
| ИИ-помощник | `AIAssistant` | Анализ входящих сообщений, генерация вариантов ответа через LLM |
| Шаблоны ответов | `Templates` | Библиотека готовых текстов и фраз, категории, поиск |
| Авто-ответчик | `AutoReply` | Правила автоответа: по расписанию, по ключевым словам, по чату |
| Расписание | `Scheduler` | Временные окна активности авто-ответчика (рабочие часы, выходные) |
| Настройки | `Settings` | Конфигурация мессенджеров, ИИ-провайдеров, правил, профилей |

---

## 🏗️ Архитектурные принципы

**Технологии**:
- **Electron** — оболочка приложения
- **WebView** (`<webview>` тег) — изолированный рендер веб-версий мессенджеров
- **IPC** (`ipcMain / ipcRenderer`) — единственный канал связи `main ↔ renderer`
- **preload.js** — мост между `main` и `renderer` (`contextBridge`)

**Критически важно**:
- Всё через IPC — никакого `require('electron')` в renderer-процессах
- Инъекция скриптов в WebView только через `preload` WebView или `executeJavaScript`
- Каждый мессенджер — отдельный изолированный `<webview>` с собственной сессией/partition
- Перехват сообщений — через `MutationObserver` внутри WebView, результат передаётся через IPC
- ИИ-запросы только из main-процесса (API-ключи не в renderer)

---

## 📏 Правило превышения лимита строк

Если файл превышает лимит строк — **НЕ резать комментарии**. Комментарии должны оставаться всегда, они объясняют логику и предупреждают о ловушках.

**Правильное действие** — разделить файл на модули: вынести логически обособленный блок в отдельный файл (например, utils, handlers, components). Пример: в v0.87.21 `cc-media` protocol вынесен в `main/native/ccMediaProtocol.js` вместо сокращения комментариев в `main.js`.

---

## 🚫 Лимиты размера файлов ВСЕХ типов (src/, main/, shared/)

Проект защищён от бесконтрольного роста файлов. Тест `fileSizeLimits.test.cjs` автоматически сканирует `src/`, `main/`, `shared/` и выбирает лимит по пути и типу. Больше не надо добавлять файлы вручную, **и** больше нельзя «молча» добавить файл нового типа — тест поймает.

### Лимиты по типу файла (v0.87.75 — все типы)

| Где файл | Лимит | Примеры |
|------|-------|--------|
| `.jsx`/`.tsx` в `src/components/` — панели UI | **700 строк** | `AISidebar.jsx`, `NotifLogModal.jsx` |
| `.jsx`/`.tsx` в `src/native/` — экраны | **600 строк** | `InboxMode.jsx`, `MessageBubble.jsx` |
| Другие `.jsx`/`.tsx` | **600 строк** | |
| `.js`/`.ts` в `/hooks/` | **150 строк** | React hooks |
| `.js`/`.ts` в `/utils/` | **300 строк** | Обычные утилиты |
| `.js`/`.ts` в `main/handlers/`, `main/native/`, `src/native/store/`, `src/native/utils/`, `main/preloads/utils/` | **500 строк** | Крупные интеграции (GramJS, stores, handlers) |
| `.js`/`.ts` в `main/preloads/hooks/` | **300 строк** | Скрипты инъекций в WebView |
| `main/main.js` / `main/main.ts` | **600 строк** | Корневой main-процесс |
| `.cjs` в `main/preloads/` | **600 строк** | Preload-скрипты |
| Другие `.cjs` | **400 строк** | |
| Тесты (`*.test.*`, `*.vitest.*`) | **400 строк** | |
| **`.html`** (инлайн BrowserWindow) | **800 строк** | `notification.html` (исключение 1000) |
| **`.css` / `.scss`** | **800 строк** | `native/styles.css` |
| **`.json` конфиги** | **500 строк** | `shared/spamPatterns.json` |

**Железная тройная защита от «тихих дыр» (v0.87.75)**:
- **(A)** Файл в `KNOWN_EXT` но без правила в `getLimit()` → тест падает с инструкцией «добавь правило»
- **(B)** В `KNOWN_EXCEPTIONS` путь к несуществующему файлу → тест падает с инструкцией «удали запись»
- **(C)** В `src/`/`main/`/`shared/` файл с расширением вне `KNOWN_EXT` и `IGNORED_EXT` (`.vue`, `.svelte`, `.toml` и т.п.) → тест падает с инструкцией «добавь в один из списков»

**Ранее (v0.87.68)**: авто-сканирование для `.jsx/.js/.cjs`. **Теперь (v0.87.75)**: все типы файлов + защита от будущих.

**Жёлтое предупреждение при 80% лимита** — тест не падает, но показывает что скоро надо разбивать.

### 📦 Исключения и текущее состояние

Список файлов с индивидуальным потолком — в тесте (`KNOWN_EXCEPTIONS` в `fileSizeLimits.test.cjs`).

**Снапшот текущих размеров, приближений к лимиту, причин исключений**:
→ [`.memory-bank/code-limits-status.md`](.memory-bank/code-limits-status.md)

**Рекомендации по разбиению крупных файлов**:
→ [`.memory-bank/handoff-code-limits.md`](.memory-bank/handoff-code-limits.md)

Этот CLAUDE.md держит **правила**, а конкретные числа (размеры, проценты) — в файле выше, чтобы они не устаревали в правилах.

### Лимиты на функции

| Правило | Лимит | Если превышен |
|---------|-------|--------------|
| Одна функция | **100 строк** | Разбить на подфункции |
| Inline `executeJavaScript` | **50 строк** | Вынести скрипт в `messengerConfigs.js` |
| React `useEffect` | **30 строк** | Вынести логику в отдельную функцию |

### Правила добавления нового кода

1. Новая бизнес-логика → `src/utils/имяМодуля.js` + тест в `src/__tests__/`
2. Новый UI-блок → `src/components/ИмяКомпонента.jsx` (если > 50 строк)
3. Новый IPC handler → добавить в `main/main.js` + тест в `ipcChannels.test.js`
4. Новый спам-паттерн → `shared/spamPatterns.json` (единый источник)
5. Новый мессенджер → конфиг в `src/utils/messengerConfigs.js`

### Как проверить (без запуска приложения)

```bash
npm run lint
npm run test:vitest
```

---

## 📌 Обновление версии (ОБЯЗАТЕЛЬНО при каждом изменении кода)

При изменении кода обновить версию **ВО ВСЕХ 4 местах**:

| # | Файл | Что менять |
|---|------|-----------|
| 1 | `package.json` | `"version": "X.Y.Z"` |
| 2 | `package-lock.json` | корневой `"version"` + `packages[""].version` (две записи) |
| 3 | `CLAUDE.md` | `**Текущая версия**` и `**Версия проекта**` (в начале и в подвале) |
| 4 | `.memory-bank/features.md` | Версия в шапке + запись в Changelog |

### Правила версионирования
- **Patch** (0.1.X → 0.1.X+1): багфиксы, мелкие правки, рефакторинг памяти
- **Minor** (0.X.0): новые функции, новый мессенджер, изменения UI
- **Major** (X.0.0): только по согласованию с пользователем

### Когда версию НЕ повышать
Если код **не менялся**, а был только анализ / совет / текст для обсуждения — версию **не** повышать.

### Чеклист перед сборкой
1. ✅ `package.json` — версия
2. ✅ `package-lock.json` — две `"version"`-записи
3. ✅ `CLAUDE.md` — версия и дата в двух местах
4. ✅ `.memory-bank/features.md` — версия + запись в Changelog
5. ✅ Если есть новые файлы — добавлены в `package.json → build.files`

---

## 🧾 MEMORY BANK — обязательные действия

### При изменении кода

- **Новые ошибки / ловушки** → добавить в подходящий файл `mistakes/<тема>.md` (**не** в корневой `common-mistakes.md` — он индекс)
- **Новые решения / архитектурные выводы** → `decisions.md`
- **Новые фичи / фиксы / changelog** → `features.md`

### Если создаёшь новые файлы в `.memory-bank/`

- Добавь их в CLAUDE.md (секции «Первое действие» и «Структура памяти»)
- При необходимости добавь в `.memory-bank/README.md`

### Если меняется структура Memory Bank

Синхронизируй:
- CLAUDE.md (вручную + `bash scripts/regen-claude-structure.sh` для таблицы структуры)
- `.memory-bank/README.md`
- `.memory-bank/CHANGELOG.md` — добавь запись о структурном изменении

### Если создаёшь новые файлы в `.claude/skills/` или `.claude/commands/`

- **Обязательно** добавь их в CLAUDE.md (секция «Первое действие» или отдельная секция про skills)
- Укажи: **что делает**, **когда вызывать**, **какие аргументы принимает**
- Skills в `.claude/skills/` — это project-specific агенты и slash-команды Claude Code. Без записи в CLAUDE.md агент не узнает что они существуют и не будет их использовать.
- Обнови `.memory-bank/CHANGELOG.md` (если skill влияет на рабочий процесс)

Сейчас в проекте `.claude/skills/` **нет** — только `settings.json` (см. секцию «⚙️ Конфигурация .claude/»). Правило действует на будущее.

### 📏 Лимиты размера файлов ПАМЯТИ (.memory-bank/)

| Файл | Максимум | Если превышен |
|---|---|---|
| Любой `.md` в корне `.memory-bank/` | **100 КБ** | Разбить на подпапку + индекс |
| Файл в `mistakes/` | **200 КБ** | Разбить по подтемам |
| `common-mistakes.md` (индекс) | **10 КБ** | Сократить описания тем до одной строки |

**Почему лимиты важны**: инструмент `Read` имеет предел 256 КБ. Файл больше — не читается. 100+ КБ съедает огромную долю контекста, нужного для задачи.

**Автоматическая защита**:
- **Тест лимитов**: `node src/__tests__/memoryBankSizeLimits.test.cjs` — падает если файл перерос
- **Тест dangling-refs**: `node src/__tests__/featuresReferences.test.cjs` — падает если в последних 10 версиях `features.md` ссылка указывает на несуществующий файл
- **Pre-commit hook**: `scripts/hooks/pre-commit` вызывает `check-memory.sh` если в коммите есть `.memory-bank/*` или `CLAUDE.md` → коммит блокируется при проблемах
- **Скрипт диагностики**: `bash scripts/check-memory.sh` (или `npm run check-memory`) — размеры, сверка версий, сломанные ссылки
- **Регенерация структуры**: `bash scripts/regen-claude-structure.sh` (или `npm run regen-claude-structure`) — автоматически обновляет таблицу «Структура памяти» в `.memory-bank/STRUCTURE.md` между маркерами `<!-- STRUCTURE-AUTO-START/END -->`

**Примеры разбиения** (для справки):
- `common-mistakes.md` 294 КБ → индекс 5 КБ + `mistakes/` 4 файла (v0.87.56)
- `features.md` 445 КБ → активный 100 КБ + 2 архивных файла (v0.87.58)
- `mistakes/webview-injection.md` 165 КБ → `webview-injection.md` 130 КБ + `webview-navigation-ui.md` 31 КБ (v0.87.59)
- `mistakes/webview-injection.md` 130 КБ → `webview-injection.md` 9 КБ (ядро) + `webview-stack-grouping.md` 125 КБ (v0.87.60)

**Журнал изменений структуры**: [`CHANGELOG.md`](.memory-bank/CHANGELOG.md).

### Когда переводить в архив

- Завершённые handoff-документы (2+ недели стабильности в релизе)
- Отменённые / замороженные планы (> 3 месяцев бездействия)
- Секции `⚪ ИСТОРИЯ (РЕШЕНО)` из активных файлов
- Старые changelog'и из больших файлов (например, `features.md`)

При архивации — обновить журнал в [`archive/README.md`](.memory-bank/archive/README.md).

---

## 🔧 GIT-ПРАВИЛА

1. **Перед commit всегда** смотри `git status`
2. Проверяй, нет ли незакоммиченных чужих изменений
3. Добавляй файлы **ТОЛЬКО ПО ИМЕНАМ**: `git add file1 file2 file3`
4. **НЕ** использовать `git add -A` / `git add .`
5. `git commit --no-verify` — **ЗАПРЕЩЁН** без явного разрешения пользователя
6. Один логический запрос пользователя = один логический commit
7. **Commit + Push делать только если пользователь ЯВНО попросил** это в текущей задаче
8. Формулировка «кажется, тут нужен push» — **не основание** для push
9. **⚠️ ВСЕ новые файлы (`??` в `git status`), на которые есть ссылки из других коммитимых файлов, ОБЯЗАНЫ быть в `git add`** — иначе CI упадёт на «битая ссылка». Pre-commit hook этого **не ловит**, т.к. локально файл существует. Проявилось в v0.89.17: создал `code-todo.md` + ссылку в `CLAUDE.md`, но забыл добавить файл в `git add` — CI упал. См. правило в [.memory-bank/workflow.md](.memory-bank/workflow.md) «Перед `git add`».

### Если делаешь add + commit + push одной командой Bash
- Это должна быть **одна последовательная команда** (`&&`)
- **Не** параллельные git-вызовы
- **Только после** успешных проверок
- **Только если** пользователь просил commit/push

---

## 💬 ФОРМАТ ОТВЕТА

### 🚨 ГЛАВНОЕ ЖЁСТКОЕ ПРАВИЛО — простой язык

Пользователь **НЕ программист**. Все ответы, советы, объяснения — **простыми словами**.

**ЗАПРЕЩЕНО** без крайней необходимости:
- Технические термины (`refactoring`, `context isolation`, `throttle`, `race condition`, `deadlock`, `dependency`)
- Слова вроде «синхронизация», «интерпретация», «агрегация», «абстракция»
- Профессиональный жаргон, англицизмы, код-термины в объяснениях

**РАЗРЕШЕНО**:
- Простые слова: «одновременно», «вовремя», «вместе», «по очереди»
- Объяснение через аналогии («это как…»)
- Имена файлов и команд — да, они не заменяются (например `package.json`)

**Тест на понятность**: если объяснение нельзя пересказать бабушке — переписать проще.

**Пример**:
- ❌ «Параллелизм — делай независимые операции одновременно для оптимизации throughput»
- ✅ «Одновременно — делай несколько дел сразу, когда они не мешают друг другу»

### Общие правила

- Все мысли, действия и итог пиши **НА РУССКОМ**
- Коротко, но достаточно полно
- Информации давай столько же или больше, **но за меньше ходов**
- Не растягивай очевидные действия на лишние сообщения

### Для разных типов задач

**Советательная / аналитическая задача** — в ответе отдельно разделяй:
1. Что уже проверено
2. Что предлагается
3. Что ещё не применялось к файлам

**Смешанная задача** (часть совет, часть код):
1. Что изменено в коде
2. Что только предлагается
3. Что пользователь должен проверить сам

### «До 5 советов по улучшению» — только для содержательных задач

Дай **ДО 5 вариантов улучшения ТОЛЬКО если** задача была содержательной:
- Новая фича
- Рефакторинг
- Крупный фикс
- Архитектурное решение

**Для тривиальных правок** (typo, одна строка, переименование переменной) советы **не давать** — это выглядит как избыток.

Если всё же даёшь советы — для каждого обязательно укажи:
- Что это даст
- Зачем нужно
- Простой пример / визуальный пример (если возможно)
- Плюсы / минусы
- Что будет, если не сделать
- Какие проблемы могут быть, если сделать

Пиши **без сложных слов**, понятно человеку без технической подготовки. См. «🚨 Главное жёсткое правило» в начале этой секции.

### 🎨 Визуальное оформление советов

Советы **должны выглядеть**, а не «просто быть написаны». 5 приёмов:

#### Приём 1 — 📊 Таблица «До / После»
Когда совет про изменение текста, числа, строки, версии — две колонки:

```
| ❌ Сейчас          | ✅ Станет              |
|---|---|
| 76 строк старого   | 138 строк актуальных   |
```

Применять: правки файлов, замены значений, переименования.

#### Приём 2 — 🟢🟡🔴 Иконки-светофор вместо слов «плюсы/минусы/риски»
Вместо длинных заголовков — одна иконка + одна строка:

```
🟢 Что даст        → быстрый ответ одной фразой
🟡 Цена            → 5 минут работы, 1 коммит
🔴 Если не сделать → что сломается
⚡ Срочность       → низкая / средняя / высокая
```

Применять: всегда когда есть баланс «стоит ли делать».

#### Приём 3 — ⭐ Шкала приоритета (1–5 звёзд)
Вместо слов «важно / очень важно / критично»:

```
⭐⭐⭐⭐⭐ — блокер, сделать первым
⭐⭐⭐   — полезно, следующее
⭐      — косметика, можно отложить
```

Применять: когда в ответе **несколько** советов и надо понять что делать первым.

#### Приём 4 — 🖼 ASCII-мокап для структуры и архитектуры
Когда совет про структуру папок, компонентов или UI — нарисуй:

```
Было:
.memory-bank/
├── common-mistakes.md  [294 КБ] ❌ не читается
└── features.md         [445 КБ] ❌ не читается

Стало:
.memory-bank/
├── common-mistakes.md  [5 КБ]   ✅ индекс
├── mistakes/           [темы]    ✅
└── archive/            [история] ✅
```

Применять: структура, архитектура, UI-макеты.

#### Приём 5 — 🚦 Одна строка-светофор для мелких советов
Если совет мелкий — не расписывай 6 пунктов. Одна строка:

```
🟢 Убрать цифру «76 КБ» из CLAUDE.md → стареет быстро (5 секунд)
🟡 Разбить webview-stack-grouping.md → можно отложить (средний приоритет)
🔴 Удалить архив → НЕ делай, потеряем учебные примеры
```

Применять: когда 5 советов подряд — чтобы не было стены текста.

### Какой приём для какого случая

| Тип совета | Приём |
|---|---|
| Правка текста / числа / строки | 📊 До/После (Приём 1) |
| Список из 3–5 советов с разным приоритетом | ⭐ Шкала (3) + 🚦 светофор (5) |
| Структура папок, архитектура, UI | 🖼 ASCII-мокап (4) |
| Плюсы/минусы/риски в одном совете | 🎯 🟢🟡🔴 иконки (2) |
| Очень мелкая правка | 🚦 Одна строка (5) |

**Главное**: совет должен **бросаться в глаза**, а не теряться в тексте. Если ты написал 3 страницы и главное на 2-й — это плохой формат.

---

## 📋 ФИНАЛЬНЫЙ БЛОК ОТЧЁТА

В конце **всегда** добавляй раздел:

**Что сделано в этой сессии**

Таблица с колонками: `Задача | Файлы изменения | Статус теста | Влияние на систему`

---

## ✅ ЧТО СЧИТАТЬ ХОРОШИМ РЕЗУЛЬТАТОМ

- Задача решена **по сути**, а не «формально»
- Код **не затронул лишнего**
- **Старые ошибки не повторены** (проверены `common-mistakes.md` и `mistakes/`)
- Проверки подобраны **по риску** (не запускать всё подряд)
- Пользователь видит понятный итог
- Если нужно — есть commit/push (**только по просьбе**)
- Если нужно — обновлены версия и Memory Bank

---

## ⚙️ Конфигурация `.claude/`

В корне проекта есть папка `.claude/` с настройками Claude Code:

### `.claude/settings.json`
Project-specific разрешения. Сейчас разрешены без подтверждения:
- `Bash(*)`, **`PowerShell(*)`** — обе оболочки без подтверждения
- `Read(*)`, `Write(*)`, `Edit(*)`
- `Glob(*)`, `Grep(*)` — поиск по коду
- `WebFetch(*)`, `WebSearch(*)` — интернет-запросы
- `Agent(*)` — подагенты для независимых задач
- `TodoWrite(*)`, `NotebookEdit(*)`, `AskUserQuestion(*)`, `Skill(*)`, `ToolSearch(*)`, `ScheduleWakeup(*)`

**Почему это важно**: агент не спрашивает подтверждения перед каждой командой. Это ускоряет работу, но **не отменяет критические запреты** (`npm start`, `git reset`, `npm test` и т.д.) — эти команды агент не должен запускать даже если `Bash(*)` разрешён.

### Автоматические проверки лимитов файлов (v0.87.73)

В `.claude/settings.json` прописаны два хука, которые вызывают `scripts/hooks/check-size-hook.cjs`:

| Хук | Когда срабатывает | Что делает |
|---|---|---|
| **SessionStart** | В начале каждой сессии Claude Code | Запускает `fileSizeLimits.test.cjs` и печатает все файлы на 80%+ лимита |
| **PostToolUse** (`Write\|Edit`) | После каждого `Write`/`Edit` в `src/` или `main/` `.jsx/.js/.cjs` | То же самое — видно мгновенно если твоя правка вывела файл за порог |

**Зачем это нужно**: закрывает 4 причины пропущенных превышений лимитов в прошлом:
1. ✅ Жёсткий список файлов в тесте → теперь авто-сканирование всех файлов (v0.87.68)
2. ✅ Слепое доверие pre-commit → PostToolUse hook показывает предупреждения сразу после правки
3. ✅ Не считал строки вручную → hook считает автоматом
4. ✅ Ползучий рост → SessionStart hook показывает 80%+ файлы в начале сессии

**Если hooks не сработали**: скорее всего watcher не подхватил новый `.claude/settings.json` — открой `/hooks` один раз (перезагрузит конфиг) или перезапусти Claude Code.

### `.claude/skills/` и `.claude/commands/`
Сейчас **пусто**. Если появятся skills/slash-команды — **обязательно добавить их в CLAUDE.md** (см. раздел «MEMORY BANK — обязательные действия»).

### `.claude/memory/`
Auto-memory — постоянная память Claude между сессиями. При расхождении с кодом — **доверять коду**, память обновлять (см. раздел «Конфликт: память vs код»).

---

## 📁 Структура памяти (.memory-bank)

Полный авто-список файлов памяти (корень + `mistakes/` + `archive/` с размерами) вынесен в отдельный файл,
чтобы CLAUDE.md не рос (он грузится в контекст каждую сессию):
→ [`.memory-bank/STRUCTURE.md`](.memory-bank/STRUCTURE.md)

Регенерация: `npm run regen-claude-structure` — обновляет `STRUCTURE.md` между маркерами `<!-- STRUCTURE-AUTO-START/END -->`.

---

**Версия проекта**: v1.2.371 (2 сентября 2026)
**Статус**: 🟢 Фазы 1-4+ + TDLib миграция + виртуализация удалена (v0.94.0) + scroll-restore стабилизирован (v0.95.0-50) + **AI-агент фундамент Phase 0+1 (v0.97.0)**: NotificationSource (паспорт сообщения) + Action Bus (cross-tab notify:clicked) + Tool Use API (4 провайдера: Anthropic/OpenAI/DeepSeek/ГигаЧат) + 3 read-only tools (goto_message/get_chat_history/search_messages). AI агент работает в Native режиме (сейчас TDLib, в будущем — другие native API мессенджеров). WebView режимы (5 мессенджеров + AI WebView mode для chat.openai.com/claude.ai) — продолжают работать как раньше, не затронуты. Phase 2 (write actions + permissions UI) — ожидает.
**Последнее обновление**: 24 июля 2026 — **v1.2.129 (ВК: переносы строк/абзацы поста в уведомлении — не «простыня»)**: жалоба — длинный пост канала ВК приходил одной сплошной строкой (абзацы и пункты 1️⃣2️⃣ слиплись), хотя в самом ВК форматирование есть. КОРЕНЬ (по коду + MDN): текст строки списка собирает `_vkNodeText` ([vk.hook.js](main/preloads/hooks/vk.hook.js)) — конкатенирует текстовые узлы и `alt` картинок-эмодзи, но НЕ добавляет `\n` для `<br>` и блочных тегов, т.е. ведёт себя как `textContent`. Факт уровня 1 (MDN Node.textContent): `textContent` НЕ отражает переносы от `<br>`/блоков (в отличие от `innerText`) — у ВК переносы хранятся РАЗМЕТКОЙ, не буквами → терялись при сборе. Прошлый фикс v1.2.122 (`_cleanMultiline` сохраняет `\n`) не помогал: сохранять было нечего. Показ и путь доставки проверены и исправны (карточка раскрыта = `white-space:pre-wrap`, [notification.css]; обработчик входящих `\n` не режет, [webviewHandleNewMessage.js]; отправка через JSON `\n` сохраняет). Фикс: в `_vkNodeText` — `<br>`→`\n`, блочные теги (DIV/P/LI/UL/OL/BLOCKQUOTE/PRE/SECTION/ARTICLE)→перенос вокруг текста; `_cleanMultiline` схлопывает лишние пустые строки. Эмодзи (img alt) и инлайн-теги не затронуты. Тест `vkNotifyLogic.vitest.js` (+3: `<br>`, блочные абзацы, инлайн без лишних переносов). Остаток: запасная ветка сбора `_vkRowText` (когда спец-блок превью не найден) переносы всё ещё теряет — для каналов работает основная ветка (есть `ChannelPostPreview`), для некоторых личных чатов возможна доработка. Файлы: `main/preloads/hooks/vk.hook.js` (253/300), `src/__tests__/vkNotifyLogic.vitest.js`. Проверки: `node --check` OK; линт 0; лимиты 515/515; vkNotifyLogic 12/12; notifHooks 87/87. **Требует ПОЛНОГО перезапуска + визуальной проверки** (длинный пост канала → абзацы по строкам). Предыдущее — **v1.2.128 (Уведомления: «невидимая стена» закрывается вместе с карточкой — гасим по видимому, а не по счётчику)**: жалоба — после закрытия (✕ / «Прочитано» / автотаймер) иногда оставался невидимый след, ловящий клики, и убирался только сторожем через ~15с. КОРЕНЬ (по коду): окно пряталось, только когда список main `notifItems[]` = 0, но в нём копятся «фантомы» от стопки/альбома; спасательный сигнал «renderer пуст» (`rendererPure`) считался по числу записей в окне (`items.size===0 && container===0`, [notification.js reportHeight]), а зависший невидимый огрызок высотой 0 держал счётчик >0 → `decideNotifResize` возвращал `ignore` бесконечно ([notifResizeDecision.js]) → окно висело (в логе `IGNORE stale 0 items=1` ×196). Фикс: в `reportHeight` добавлен флаг `dismissFinal`; в финальном отчёте ПОСЛЕ завершения закрытия карточки, если видимого не осталось (`calcHeight()===0` — линейка видит только реально видимые карточки, [notification-helpers.js:40]), шлём `rendererPure:true` по ВИДИМОЙ высоте (а не по счётчику) → существующая ветка `clear-hide` очищает фантомы и прячет окно СРАЗУ. Момент добавления карточки (slideIn, где `calcHeight===0` = карточка ещё выезжает) НЕ затронут — флаг только в финале закрытия. Факт уровня 1: MDN `HTMLElement.offsetHeight` = 0 для скрытых/удалённых → `calcHeight===0` надёжно = «видимого нет». Сторож 15с оставлен запаской. Формула вынесена в чистую тестируемую `computeRendererPure` (notification-helpers.js) + тест (+5 в `albumLiveCard.vitest.js`); родственный путь `forceRemoveItem` проверен и правки НЕ требует (всегда сопровождается добавлением карточки, TODO-13). Файлы: `main/notification.js` (726/730), `main/notification-helpers.js` (287/300), `src/__tests__/albumLiveCard.vitest.js`. Проверки: `node --check` OK; линт 0; лимиты 515/515; тесты 28/28. **Требует ПОЛНОГО перезапуска** (сейчас работает v1.2.127) и визуальной проверки. Предыдущее — **v1.2.127 (Уведомления: heartbeat-сторож без спама)**: доводка v1.2.126 по ревью. Раньше сторож каждые 5с писал `[notif-watchdog] alive` и слал `remeasure` → renderer слал `resize` → лог `[notif-resize]` каждые 5с (спам, пока открыто уведомление). Теперь: (1) heartbeat шлётся ТОЛЬКО когда окно давно молчит (`msSinceResize > 12000` мс), а не каждый тик; (2) heartbeat-отчёт помечен флагом `heartbeat:true` — renderer его шлёт БЕЗ диагностики (`reportHeight(true)`), а main НЕ логирует, если это no-op `show` (реальные карточки); (3) запись `[notif-watchdog] STUCK` — только если после «поко» renderer НЕ ответил (реальный признак застрявшей стены / мёртвого renderer), редко, не спам. Файлы: `main/handlers/notifHandlers.js`, `main/notification.js` (`reportHeight(heartbeat)`). Проверки: `node --check` OK; лимиты 515/515 (notification.js 724/730); `mainRuntime` 98/98; линт 0. **Требует визуальной проверки** (открыть уведомление >12с: лог НЕ должен спамить; при стене — строка `STUCK`). Предыдущее — **v1.2.126 (Уведомления: закрыта дыра «невидимой стены» + heartbeat-сторож + подробная запись)**: жалоба — после уведомления остаётся невидимый след, ловящий клики. По логам (`chatcenter.log`): первый цикл закрылся чисто (дошёл до `rendererPure=true` → hide), а второй ЗАСТРЯЛ на `items=4` — отчёты `notif:resize` оборвались, окно осталось видимым, события hide не было. Корень (по коду): вся защита от стены завязана на `items===0` — `decideNotifResize` прячет только при 0/rendererPure ([notifResizeDecision.js]), а сторож ([notifHandlers.js]) — только `isVisible && getNotifItems().length===0`. Если renderer перестал слать размер, а счётчик main >0 — НИ проверка размера, НИ сторож не срабатывают → окно висит на старых bounds, пустая часть ловит клики. Фикс v1.2.126: (1) сторож теперь пока окно НА ЭКРАНЕ шлёт `notif:remeasure` → renderer вызывает `reportHeight()` → авторитетный отчёт через существующую `decideNotifResize` (живой renderer поправит размер/скроется даже при счётчике>0; для реальных карточек — no-op, та же высота); (2) подробная запись `[notif-watchdog] alive items=… bounds=… msSinceResize=… onTop=…` — ловит застрявшую стену и покажет «мёртвый renderer», если heartbeat не помог. Каналы: preload `onRemeasure`, notification.js `onRemeasure?.(reportHeight)`. Проверки: `node --check` OK (3 файла); лимиты 515/515 (notification.js 722/730); `mainRuntime` 98/98; линт 0. **Требует визуальной проверки** (поймать стену снова → строки `notif-watchdog alive`; ожидается, что heartbeat её уберёт). Предыдущее — **v1.2.125 (Уведомления: окно держится «поверх всех» надёжно — высокий уровень + reassert, как у дока)**: жалоба — окно уведомления иногда уходило ЗА окна других программ. Корень (по коду): `createNotifWindow` ([notificationManager.js:109]) ставил простой `alwaysOnTop: true` (низкий уровень) и НЕ имел reassert, тогда как полоска задач ([dockPinState.js:55-208]) использует `setAlwaysOnTop(true,'screen-saver',1)` (самый высокий уровень по доке Electron) + периодический возврат наверх (Windows опускает topmost при смене фокуса/клике по панели задач). Фикс: окну уведомления добавлены (1) `setAlwaysOnTop(true,'screen-saver',1)` после создания и в обработчике `show`; (2) «пружина» — `setInterval` 1с, пока окно ВИДИМО, снова поднимает наверх (не крадёт фокус, `focusable:false` сохранён); таймер `notifTopTimer` останавливается в `closed`. Проверки: `node --check` OK; `mainRuntime` 98/98; лимит `notificationManager.js` 334/500; линт 0. Остаток (предел Windows): над игрой во весь экран/системным окном даже screen-saver может не встать — не лечится. **Требует визуальной проверки** (уведомление стабильно поверх редактора/браузера). Предыдущее — **v1.2.124 (ВК: разгрузка хука + смайлики в тексте уведомления)**: сделаны разом разгрузка (#3) и фикс эмодзи. (1) Из `vk.hook.js` удалён МЁРТВЫЙ на vk.ru наблюдатель всплывашек «Новое сообщение» (`_scanVkToasts`/`_toastRootOk`/`_parseVkToast`/`_toastLabelOk`/`_toastSeen` + `__ccVkPrimaryToastObserver`) — на vk.ru таких всплывашек нет (`vk-toast scan=0`), сообщения ловятся детектом списка чатов. Файл 300→247 строк (снят потолок, закрыт TODO-12). Оставлены общие помощники `_cleanToastText`/`_hashToast`/`_cleanMultiline`. (2) Смайлики: VK рисует эмодзи как `<img>`, а `textContent` их не видит → 🧡 терялся (подтверждено логом: emit `Хорошего дня любимка` без 🧡). Новая `_vkNodeText(node)` собирает текст, беря `alt` у `<img>` (там сам символ); `_vkRowText` использует её вместо `el.textContent`. Нет `alt` (эмодзи фоном) → просто пропуск, без регресса. (3) Тесты: `vkNotifyLogic.vitest.js` +3 (`_vkNodeText`: эмодзи из картинки, вложенность, без alt); `notifHooks.test.cjs` — 2 проверки мёртвого toast заменены на 3 актуальные (детект списка + эмодзи-сборщик + подтверждение удаления toast). Проверки: `node --check` OK; лимиты 515/515 (vk.hook.js 247/300); notifHooks 87/87; vkNotifyLogic 9/9; линт 0. **Требует визуальной проверки:** (а) ВК-уведомления по-прежнему приходят (детект списка); (б) сообщение со смайликом → смайлик виден (если VK хранит эмодзи как `<img alt>`; если фоном — не появится, тогда отдельная задача). Предыдущее — **v1.2.123 (ВК: закрыта лазейка mute-детекта + тесты «чистых» функций; #3 разгрузка отложена — обоснованно)**: по ревью. (#1) `_vkRowMuted`: селектор сужен с широкого `[class*="muted"]` (ловил и «unmuted») до точных `[class*="mutedIcon"], [class*="icon--muted"]`; убрана ветка `getClientRects().length` (могла дать ложное «виден» у пустого элемента → тихий пропуск нужного чата). Осталось `offsetWidth>0 || offsetHeight>0` + гварды display/visibility/opacity. (#4) Новый тест `src/__tests__/vkNotifyLogic.vitest.js` (6 проверок): читает реальный код хуков и проверяет `_cleanMultiline` (переносы сохраняются), VK `_isSpam` (гасит статусы/исходящие, пропускает обычное), Telegram фильтр сторис (`_story/_storyEn` гасят фразу-штамп, не трогают «расскажи историю»). DOM-функции (`_vkRowMuted`/`_vkRowText`) юнит-тестом не покрыты — jsdom не считает `offsetWidth` (нет раскладки); остаётся ручная проверка + лог `muted=N`. (#3) Разгрузка `vk.hook.js` (300/300) НЕ сделана осознанно: хук впрыскивается как ОДИН самодостаточный `<script>` (`monitor.preload.cjs:28` + запасной путь `webviewSetup.js:362` через `app:read-hook`), импортов нет; вынос в модуль требует менять ОБА пути впрыска, а проверить без запуска приложения нельзя → риск сломать ВК-уведомления. Отложено в `code-todo.md` (TODO-12) как задача с обязательной визуальной проверкой. TODO-10/11 закрыты (#1). Файлы: `main/preloads/hooks/vk.hook.js` (300/300), `src/__tests__/vkNotifyLogic.vitest.js` (new). Предыдущее — **v1.2.122 (ВК: текст уведомления — сохранены переносы строк + снят предел 1000)**: жалоба — длинный пост канала приходил «простынёй» (списки с ✅ слиплись) и обрезан на полуслове. Две причины (по коду): (1) `_cleanToastText` схлопывала `\s+`→' ', уничтожая переносы строк; (2) `_vkRowText` резал результат `.slice(0, 1000)` (мой «предохранитель» из v1.2.119). Плюс подтверждено: в окно уходит ПОЛНОЕ поле `fullBody` (webviewHandleNewMessage.js:108), а не короткий предпросмотр (`body` slice 97) — значит обрыв это предел 1000, не предпросмотр/прокрутка. Фикс: добавлена отдельная `_cleanMultiline` (схлопывает пробелы/табы, СОХРАНЯЕТ `\n`, максимум одна пустая строка, trim); `_vkRowText` использует её для превью-элемента и возвращает `tx.trim().slice(0, 4000)` (4000 = только анти-краш). `_cleanToastText` НЕ тронута (её использует путь тостов). Карточка показывает переносы (`.body-text.expanded { white-space: pre-wrap }`). Проверено прогоном node из реального файла: списки остаются построчно. Остаток: если пост длиннее того, что ВК кладёт в список — полный текст только в открытом канале (предел ВК, не наш). Значки ✅ — если это картинки, а не буквы, не появятся (не проверялось). Файл: `main/preloads/hooks/vk.hook.js` (300/300). **Требует визуальной проверки**. Предыдущее — **v1.2.121 (Telegram Web: НЕ показываем уведомления о сторис/историях)**: жалоба — из веб-Телеграма приходила модалка «Х опубликовал(а) историю». Корень (по коду + логу): перехватчик `telegram.hook.js` передаёт ЛЮБОЕ уведомление, что шлёт сайт (window.Notification / SW.showNotification), а спам-фильтр знал «в сети/печатает/звонок», но НЕ знал про сторис → пропускал; лог 18:38:21 прямо показал body `опубликовал(а) историю` → `__CC_NOTIF__` → `[NotifManager] show`. Фикс: в `_isSpam` добавлены УЗКИЕ маски `_story`/`_storyEn` — гасят системную фразу ЦЕЛИКОМ (`^(опубликовал|добавил|разместил)\S*\s+(историю|видео/фото в историю)$` + англ. `posted a story` и т.п.), а НЕ слово «история» где угодно (заблокированное логируется reason=story). Проверено прогоном node из реального файла: «расскажи историю», «это долгая история», «опубликовал новую историю сегодня» — проходят; сторис-фразы — гасятся. Другие мессенджеры и «родной» (TDLib) путь НЕ затронуты (native — отдельно, не проверялось, шлёт ли он сторис). Файл: `main/preloads/hooks/telegram.hook.js` (101/300). Предыдущее — **v1.2.120 (ВК/vk.ru: НЕ уведомляем о заглушённых чатах 🔕 + диагностика убрана)**: по диагностике v1.2.118 найдена метка «беззвучно»: личный чат — `ConvoTitle__mutedIcon`, канал — `ChannelTitle__icon--muted` (обе содержат «muted»). ЛОВУШКА: элемент есть и у обычных чатов (у не-заглушённого «Сергей Пересыпкин» он тоже присутствовал), просто СКРЫТ → нельзя проверять «есть элемент», только «виден ли он». Добавлена `_vkRowMuted(row)`: ищет `[class*="muted" i]`, считает замьюченным ТОЛЬКО если элемент видим (`display!=none`, `visibility!=hidden`, `opacity>=0.1`, есть размер/`getClientRects`). В `_scanVkList` замьюченные строки пропускаются (`muted++; continue`), в диаг-лог добавлен счётчик `muted=N`. Временный дамп `__CC_DIAG__vkM` (v1.2.116-118) удалён. Файл `vk.hook.js` 298/300. **Требует визуальной проверки**: заглушённый чат (🔕) НЕ должен давать модалку; обычные — должны (следить по логу `vk-list … muted=N`, чтобы не заглушать лишнего). Предыдущее — **v1.2.119 (ВК/vk.ru: текст уведомления не режем своей меркой — берём весь пришедший)**: по просьбе пользователя — не гадать с числом. Раньше `_vkRowText` резал текст на 220 символах (длинные посты каналов обрывались на полуслове ≈218). Теперь берётся ВЕСЬ текст, что дал ВК в списке чатов; `slice(0, 1000)` оставлен только как страховочный потолок от гигантской простыни (для реальных превью = «сколько пришло, столько и показываем»). Граница (не наша): для личных чатов ВК в списке даёт лишь короткое превью — весь текст только в открытом чате (не достаём без открытия чата). Карточка уведомления обрезку показа НЕ делает (в раскрытом виде — прокрутка). Файл: `main/preloads/hooks/vk.hook.js` (300/300). **Требует визуальной проверки** (длинный пост канала приходит целиком). Диагностика значка 🔕 (v1.2.118) — ждёт прогона пользователя (строки `vkM`). Предыдущее — **v1.2.118 (ВК/vk.ru: расширенный дамп иконок строки — попытка 3 для значка 🔕)**: прогон v1.2.117 поймал у замьюченного чата «Елена Дугина» только рамку аватара (`vkM0.0 use h=#mePeerFrameOnline48`) — значок 🔕 не попал: узкий фильтр (StatusIcon/use/mute/notif/disable/silent) его пропустил, т.к. значок «беззвучно» — отдельная иконка `vkuiIcon`. Дамп расширен: перебор ВСЕХ элементов строки, лог тех, что svg/use/i ИЛИ класс с icon/mute/notif/status/vkui/disable/silent ИЛИ есть href; класс до 40 символов + href; до 12 иконок × до 4 чатов. Пропуск замьюченных ПОКА НЕ включён. Файл: `main/preloads/hooks/vk.hook.js` (300/300). **Действие пользователя:** ВК с записью диагностики (замьюченный чат в списке) → прислать строки `vkM…`. Предыдущее — **v1.2.117 (ВК/vk.ru: точная диагностика значка 🔕 — попытка 2)**: дамп v1.2.116 не поймал замьюченный чат: селектор `.ConvoListItem` цепляет и обёртку `ConvoListItemWrapper`, и вложенный `ConvoListItem__icons` → 3 слота ушли на дубли «Artem Artem», до «Доктор Владислав Ченчак» не дошло; плюс не логировался `href` у `<use>` (там имя иконки VK) и слоты ели аватары. Узнал: имя чата = `ConvoTitle__author`, статусы — в `StatusIcon…`. Переписал дамп `__CC_DIAG__vkM`: по чатам С ИМЕНЕМ (дедуп по имени, до 8), пропуская `*avatar*`, с `href`/`xlink:href` значков и содержимым `[class*=StatusIcon]`. Пропуск замьюченных ПОКА НЕ включён. Побочно замечено (не чиню): `.ConvoListItem` матчит вложенные фрагменты → раздувает `rows` (дедуп по «имя|текст» из v1.2.115 функционально спасает). Файл: `main/preloads/hooks/vk.hook.js` (~298/300). **Действие пользователя:** ВК с записью диагностики → прислать строки `vkM…` (нужен «Доктор Владислав Ченчак»). Предыдущее — **v1.2.116 (ВК/vk.ru: ВРЕМЕННАЯ диагностика признака «беззвучный чат» 🔕)**: уведомление ВК теперь приходит с чистым текстом (подтверждено скриншотом). Новая задача: не уведомлять о чатах в беззвучном режиме (значок 🔕 у чата). Класс этого значка на vk.ru неизвестен — гадать нельзя (ошибусь → заглушу нужные чаты). Добавлен разовый дамп `__CC_DIAG__vkMute` в `_scanVkList`: до 3 непрочитанных строк, для каждой — имя + иконки (`svg/use/[aria-label]/[title]/[class*=mute|notif|icon]`) с tag/class/aria. Сам пропуск замьюченных ПОКА НЕ включён (чтобы не заглушить нужное). Фикс НЕ делался. Файл: `main/preloads/hooks/vk.hook.js` (~296/300). **Действие пользователя:** открыть ВК с записью диагностики (замьюченный чат «Доктор Владислав Ченчак» уже виден) → прислать строки `vkMute…`. Предыдущее — **v1.2.115 (ВК/vk.ru: 3 фикса по ревью — чистка времени не ест текст, нет шторма на старте, нет дублей)**: по итогам придирчивого ревью детекта ВК. (1) Регэксп чистки времени резал хвост «число+м/ч/с [назад]» у ЛЮБОГО текста → «буду через 5 минут» → «буду через»; заменён на узкие маски: компактная дата только после «·» («· 36м»), словоформа только со словом «назад» («36 минут назад»), «12:34». Проверено прогоном node (не режет «буду через 5 минут», «10 м провода», «· 5кг»). (2) Базовая линия снималась на пустом списке → при медленной загрузке все уже-непрочитанные улетали как «новые» (шторм); теперь фиксируется только при `rows>0`. (3) Дубль в одном проходе → добавлен набор `sent` (один отпечаток — одно уведомление). Каналы ВК по решению пользователя УВЕДОМЛЯЕМ (все сообщения). Не тронуто: повтор при прокрутке (не подтверждено), мьют чата ВК (нет данных). Проверки: линт; `notifHooks.test.cjs` 86/86; лимит `vk.hook.js` 284/300. Память: `features.md` v1.2.115, `decisions.md` (ADR ВК vk.ru), `mistakes/webview-injection.md` (3 грабли). **Требует визуальной проверки** (входящее в ВК → чистый текст; старт на медленной машине — без пачки старых). Предыдущее — **v1.2.114 (ВК/vk.ru: чистый текст уведомления по реальной структуре строки + диагностика убрана)**: диагностика v1.2.113 дала структуру строки списка ВК (`vkRow`): имя = `ChannelTitle__title`, превью = `ChannelPostPreview__attach` («[Видео]»), дата = `ChannelsListItem__date` («· 36м»), СЧЁТЧИК = `UnreadCounter`, и КЛЮЧЕВОЕ — скрытый для скринридера `vkuiVisuallyHidden` = «36 минут назад» (вот откуда мусор «…Фотография· 1 минуту назад», который резал спам-фильтр). Фикс `_vkRowText`: берётся спец-элемент превью (`*PostPreview*`/`*preview*`), фолбэк — листья строки БЕЗ мусорных классов (`vkuiVisuallyHidden`/`Hidden`/`__date`/`__time`/`UnreadCounter`/`Counter`/`Badge`), в конце срезается относительное время («· 36м», «36 минут назад», «12:34»). Временный дамп `__CC_DIAG__vkRow` удалён. Замечание: строка в дампе была КАНАЛОМ ВК (URL `/im/channels/…`) — детект сейчас ловит и каналы (broadcast); если это шумно — исключим `ChannelsListItem` одной строкой (ждём решения пользователя). Файл: `main/preloads/hooks/vk.hook.js` (~272/300). **Требует визуальной проверки** (входящее в ВК → модалка с чистым текстом). Предыдущее — **v1.2.113 (ВК/vk.ru: детект подтверждён логом + микро-диагностика структуры строки для чистого текста)**: лог запуска v1.2.112 доказал, что детект РАБОТАЕТ: `vk-list reason=initial rows=166 unread=4 emitted=0` (базовая линия), затем `reason=mutation ... emitted=2` — при новом сообщении отправлено 2 уведомления, имя отправителя верное (`Сергей Пересыпкин`). НО текст собрался грязно («Все серьезно Фотография· 1 минуту назад» = имя группы + превью + относительное время) и был справедливо отсечён спам-фильтром («минуту назад»). Причина: точные классы имени/превью/времени внутри `.ConvoListItem` неизвестны — превью-селекторы не совпали, взялся «весь текст строки». Гадать нельзя → добавлена разовая (1/сессия) микро-диагностика `__CC_DIAG__vkRowN`: дамп листовых элементов непрочитанной строки «ТЕГ.класс=текст» короткими строками. По ним сделаю точное извлечение (чистое превью без времени/лишнего). Детект оставлен рабочим (мусор пока гасит спам-фильтр). Файл: `main/preloads/hooks/vk.hook.js` (~276/300). **Действие пользователя:** написать в ВК → прислать строки `vkRow0..N`. Предыдущее — **v1.2.112 (ВК/vk.ru: настоящий детект новых сообщений по списку чатов)**: по данным диагностики v1.2.111 (`vkF1 ConvoListItem ConvoListItem--click` — строки чатов = `.ConvoListItem`; `vkB unread=2` — непрочитанные помечены классом с «unread»; всплывашки «Новое сообщение» на vk.ru нет — `vk-toast scan=0`; заголовок вкладки без счётчика — `vkTitle=Мессенджер`). В `vk.hook.js` (грузится на vk.ru, подтверждено логом) добавлен наблюдатель за списком чатов: строки `.ConvoListItem`, непрочитанные — по классу «unread»/счётчику; имя+текст берутся из строки, шлётся `__CC_NOTIF__` (src `vk-list`) → дальше обычная логика мьюта/звука. Первый проход = базовая линия (старые непрочитанные НЕ уведомляются), далее только новые/изменившиеся; дедуп по отпечатку «имя|текст»; наблюдатель тротлится 500мс. `_isSpam`/`_cleanToastText`/`_hashToast` переиспользованы. Временная диагностика v1.2.110-111 удалена. Ограничение: ловятся только видимые строки списка (vk.ru, вероятно, виртуализирует) — как и раньше. Файл: `main/preloads/hooks/vk.hook.js` (~253/300). **Требует визуальной проверки** (написать в ВК → должна прийти наша модалка). Если 0 срабатываний — по логу `vk-list unread=` видно, находит ли непрочитанные (тогда доводка селектора «unread»). Предыдущее — **v1.2.111 (ВК: диагностика короткими строками + заголовок вкладки)**: диагностика v1.2.110 сработала (`vk-dom` 6 раз), но журнал webview-консоли режет строку на ~60 символах → `rows:`/`first:` (реальные классы) обрезались. Плюс подтверждено логом: `vk-toast scan=0` (всплывашки «Новое сообщение» на новом vk.ru нет), большой запасник падает (`VK-EXEC inject failed` — vk.ru блокирует впрыск, это ожидаемо). Переписал временный дамп: КОРОТКИЕ строки (`vkTitle`/`vkA`/`vkB`/`vkF1`/`vkF2`, каждая влезает целиком) + `document.title` (ВК кладёт счётчик непрочитанных в заголовок «(N) …» — устойчивый сигнал без завязки на вёрстку). Дамп 1 раз + повтор при смене заголовка. Файл: `main/preloads/hooks/vk.hook.js` (215/300). Это НЕ фикс — сбор данных для детекта vk.ru. Предыдущее — **v1.2.110 (ВК/vk.ru: временная диагностика структуры страницы для обновления селекторов)**: продолжение v1.2.109. По логу подтверждено: ВК на vk.ru опознаётся, основной хук `vk.hook.js` впрыскивается (`vk-toast primary-bound`), НО наблюдатель тостов находит 0 плашек (у нового ВК другая вёрстка), а большой запасной скрипт падает при впрыске. Чтобы обновить селекторы прицельно (не угадывая), в `vk.hook.js` добавлен ВРЕМЕННЫЙ дамп `__CC_DIAG__vk-dom` (счётчики строк списка чатов по разным селекторам + классы/текст первой строки; максимум 6 раз, без спама). Это НЕ фикс — шаг для сбора реальных селекторов vk.ru; после дампа детект сообщений ВК будет сделан прицельно и диагностика удалена. Файл: `main/preloads/hooks/vk.hook.js` (207/300). Предыдущее — **v1.2.109 (ВК переехал на vk.ru — чиним определение мессенджера, чтобы наш перехват запускался)**: наши уведомления ВК не работали (только звук ВК). КОРЕНЬ найден по встроенной «Диагностике системы» (свежий отчёт: VK-webview на `https://vk.ru/im`, живой, но НИ ОДНОГО нашего VK-маркера в логе — хук не грузится) + по коду: всё определение «это ВК?» сверяло только `vk.com`, а ВК теперь `vk.ru` → `detectMessengerType('vk.ru')` возвращал `unknown` → `isVkWebview=false` → ни preload-хук (`monitor.preload` host→hookType), ни наблюдатели/фолбэк (`vkExecFallback`) не запускались. Фикс: в 4 точках определения добавлено распознавание `vk.ru` рядом с `vk.com` — `src/utils/messengerConfigs.js` (главный шлюз), `main/preloads/monitor.preload.cjs` (загрузка хука), `main/preloads/utils/unreadCounters.js` (счётчик), `src/utils/navigateToChat.js` («перейти к чату»). Поведенческий тест `messengerConfigs.vitest.js` (vk.ru/vk.com→vk, прочие не задеты). Дефолтный адрес оставлен `vk.com/im` (сам редиректит на vk.ru; определение понимает оба). **Это шаг 1**: разблокирует запуск кода на vk.ru; далее по свежей диагностике проверить, находят ли наблюдатели элементы новой вёрстки vk.ru. **Требует визуальной проверки.** Предыдущее — **v1.2.108 (Уведомления: время переехало в строку источника, прижато вправо — единый формат)**: по просьбе пользователя (выбран вариант 2 из макета). Раньше время стояло в начале строки текста («10:38 Сейчас свяжусь…») и сливалось с сообщением. Теперь время (по настройке `showMessageTime`) — в строке источника, ПРИЖАТО ВПРАВО: «Telegram · БНК ⟶ 10:38». Единый формат для ВСЕХ уведомлений (все карточки идут через общий `buildStackHeader`). Реализация: `buildStackHeader` получил параметр `time` → строит `.notif-source` как флекс (`.notif-source-text` слева с эллипсисом + `.notif-source-time` справа через `margin-left:auto`); из `.body-text` убран inline-span `.msg-time`, текст всегда в `.msg-text-content` (нужен toggleExpand/альбому). Стэк-сообщения (несколько в одной карточке) сохраняют построчное время (это список — отдельный случай). Юнит-тест `buildStackHeader` (+3 в `albumLiveCard.vitest.js`). Файлы: `main/notification.js` (−9 строк), `main/notification-helpers.js`, `main/notification.css`. **Требует визуальной проверки**. Предыдущее — **v1.2.107 (Уведомления: сторож от «невидимой стены» + тест + быстрее закрытие)**: пакет улучшений по ревью. (1) **Сторож**: если окно уведомления ВИДИМО, а сообщений нет ≥2 тиков (~10с) — принудительно прячем (`setInterval` 5с в `notifHandlers.js`) → застрявшее пустое окно больше не требует перезапуска (ловит любой путь: потерян сигнал, ghost-регион Win11). Обычное закрытие (<0.5с) под сторожа не попадает. (2) **Тест+чистота**: решение «показать/спрятать окно» вынесено из обработчика `notif:resize` в чистую `decideNotifResize` (новый `main/handlers/notifResizeDecision.js`) + поведенческий тест `notifResizeDecision.vitest.js` (6 проверок) — раньше логика проверялась только grep'ом. (3) **Быстрее закрытие** (№5): «✓ Готово!» у кнопки «Прочитано» 800→400мс, старт схлопывания карточки 330→200мс → «полоса»/окно убирается заметно быстрее после закрытия (правка чисел, notification.js без роста). Файлы: `main/handlers/notifHandlers.js`, `main/handlers/notifResizeDecision.js` (new), `main/notification.js`, `src/__tests__/notifResizeDecision.vitest.js` (new). **Требует визуальной проверки** (скорость закрытия — на глаз). Предыдущее — **v1.2.106 (Фикс: «невидимое зависшее окно» уведомления, ловящее клики)**: редко после уведомления оставалось прозрачное окно поверх экрана, ловящее клики (лечилось только перезапуском). КОРЕНЬ (по коду): в обработчике `notif:resize` ([notifHandlers.js:150-205]) была защита от запоздалого отчёта высоты `<=0` при `items>0` (не прятать раньше времени), но НЕ было симметричной защиты для `height>0` при `items===0` — осиротевший положительный отчёт (уведомление успело закрыться в момент отрисовки, гонка при быстрых уведомлениях) уходил в ветку показа → окно показывалось пустым и прозрачным (click-through убран в v0.89.22, поэтому пустое окно = «невидимая стена»). Фикс: добавлена симметричная защита — при `height>0 && items===0` окно ПРЯЧЕТСЯ (`safeHideTransparentWindow`), а не показывается. Файл: `main/handlers/notifHandlers.js`. **Требует визуальной проверки** (баг редкий — ловится по логу `[notif-resize] HIDE positive height ... items=0`). Предыдущее — **v1.2.105 (Кнопка «Прочитано» в уведомлении теперь работает для нативного Telegram)**: клик «Прочитано» не снимал непрочитанное в native-режиме (счётчик оставался). КОРЕНЬ (по коду): единственный слушатель `notify:mark-read` — webview-only ([useNotifyNavigation.js:13-14]: ищет webview-ref, для `native_cc` его нет → тихий выход); нативной ветки не было (в отличие от «Перейти к чату»). Плюс payload не нёс `source` (chatId/messageId). Фикс: (1) `notifHandlers.js` добавляет `source` в payload `notify:mark-read`; (2) `useNotifyNavigation.js` — для `native_cc` зовёт напрямую `tg:mark-read { chatId: accountId:chatId, maxId: Number(messageId) }` → TDLib `viewMessages force_read` (без переключения вкладки, без фокуса окна). `source.chatId` — без префикса аккаунта, поэтому полный chatId собирается из `accountId:chatId`. Счётчик обновляется с сервера через `tg:chat-unread-sync`. Ограничение: для сообщения в теме форума (threadId) пометится чат, счётчик темы может не сброситься (нужен `markTopicRead` — отдельно). Файлы: `main/handlers/notifHandlers.js`, `src/hooks/useNotifyNavigation.js`. **Требует визуальной проверки**. Предыдущее — **v1.2.104 (Доводка видео: крутилка по реальной загрузке + чистка + тест связки)**: три улучшения по ревью. (1) Крутилка на плитке видео в уведомлении теперь снимается по ФАКТУ (главное окно после `video:open` шлёт `notif:video-done` → preload уведомления убирает `loading` с плитки по `data-mid`; слушатель в preload, т.к. notification.js на лимите 730; 20с-таймаут остался страховкой). (2) Косметика: `extractVideoFileId` переставлена ПОД `extractThumbnailFileId` — вернулся съехавший JSDoc. (3) Тест связки `downloadVideo`↔`extractVideoFileId`: видео-документ доходит до скачивания (`tdlibBackend.vitest.js`). Урок «видео-как-документ» записан в `mistakes/tdlib-video-player.md`. Файлы: `src/hooks/useAppIPCListeners.js`, `main/handlers/notifHandlers.js`, `main/preloads/notification.preload.cjs`, `main/native/backends/tdlibMedia.js`, `src/__tests__/tdlibBackend.vitest.js`. **Требует визуальной проверки** (крутилка исчезает при открытии плеера). Предыдущее — **v1.2.103 (Видео-как-файл (документ) теперь проигрывается — в чате И в уведомлении)**: КОРЕНЬ найден по скриншоту чата — там прямо «no video file». Видео было прислано как ДОКУМЕНТ (файл с mime video/*, в списке чатов `document_...`), а `downloadVideo` искал файл только в `content.video.video` → у видео-документа возвращалась ошибка «no video file» (ломалось и в чате, и в уведомлении — общая функция). Это опровергло прошлую гипотезу про «несобранный мост уведомления»: чат мостом не пользуется, а тоже не играл. Фикс: выбор file_id вынесен в чистую `extractVideoFileId(content)` ([tdlibMedia.js]) — messageVideo / GIF (messageAnimation) / документ с mime video/* → `content.document.document.id`; `downloadVideo` использует её. Юнит-тест `tdlibMedia.vitest.js` (+5). Файлы: `main/native/backends/tdlibMedia.js`, `main/native/backends/tdlibBackend.js`, `src/__tests__/tdlibMedia.vitest.js`. **Требует визуальной проверки** (видео-файл играет в чате и открывается из уведомления). Предыдущее — **v1.2.102 (Видео в уведомлении: курсор-палец + отклик на клик)**: после v1.2.101 постер видео показывается, но (1) курсор над видео был «лупа» (`.album-tile { cursor: zoom-in }` — фотостиль), (2) по клику не было видимого отклика (видео качается целиком перед открытием плеера, v0.89.15). Фиксы: плитке видео добавлен класс `.is-video` → `cursor: pointer`; по клику показывается крутилка (`loading` + страховочный таймаут 20с) — видно, что клик принят и идёт загрузка. Клик по-прежнему открывает `video:open` (как в чате). ⚠️ Если после ПОЛНОЙ пересборки плеер всё равно не открывается — вероятная причина в том, что правки главного процесса/preload из v1.2.101 не попали в запущенную сборку (постер обновляется отдельно); тогда нужен диагностический лог. Файлы: `main/notification-helpers.js`, `main/notification.css`, тест `albumLiveCard.vitest.js`. **Требует визуальной проверки**. Предыдущее — **v1.2.101 (Видео в уведомлении: постер + проигрыватель как в чате)**: пришло видео — показывался чёрный прямоугольник и не проигрывалось. ДВЕ причины (найдены по коду): (1) превью качалось каналом `tg:download-media` (`media.download` игнорирует `thumb`, [tdlibBackend.js:730]) → для видео тянулся сам файл, в фон-картинку → чёрное. Фикс: для видео качать `tg:download-thumbnail` (постер-JPEG, как в чате). (2) клик открывал фото-смотрелку (`<img>`, не играет видео). Фикс: плитка видео помечена `isVideo` → значок ▶ + клик открывает готовый видео-плеер `video:open` (со стримингом/перемоткой, как VideoTile в чате). Мост: `notif:open-video` → `notify:open-video` → `tg:download-video` → `video:open`. Фото/альбомы не затронуты (ветка только `mediaType==='video'`/`single_`). Файлы: `shared/notifAlbum.js`, `src/native/utils/albumThumbPreload.js`, `main/notification-helpers.js`, `main/notification.css`, `main/preloads/notification.preload.cjs`, `main/handlers/notifHandlers.js`, `src/hooks/useAppIPCListeners.js`, `main/notification.js`; тесты `notifAlbum.vitest.js`/`albumLiveCard.vitest.js`. **Требует визуальной проверки**. Предыдущее — **v1.2.100 (Фикс по ревью: страховка от «вечной» крутилки у одиночного фото)**: если чёткое превью одиночного фото не пришло (сбой загрузки), плитка навсегда оставалась с крутилкой поверх фото. Добавлена страховка: если за 8с превью не догрузилось — крутилка снимается (остаётся фото целиком из strippedThumb). Только для одиночного фото (`isSingle && !sharp`), альбомные плитки (blur) не трогаем. Тест с фейковым таймером. Файлы: `main/notification-helpers.js`, `src/__tests__/albumLiveCard.vitest.js`. Предыдущее — **v1.2.99 (Одиночное фото в уведомлении: «размытый фон» — фото целиком, без обрезки)**: по согласованному макету (вариант 2). Раньше одиночное фото рисовалось `background-size:cover` в рамке 16:10 → вертикальное фото обрезалось (верх/низ). Теперь для одиночного фото (`album.id` `single_*`) в `renderAlbumGrid` строятся ДВА слоя: `.sp-blur` (та же картинка `cover` + `filter:blur(20px)` + `scale(1.2)` — размытый фон в поля) и `.sp-main` (`contain` — фото ЦЕЛИКОМ, ничего не режется). Рамка 3:2. Догрузка чёткого превью (`applyAlbumSharp`) обновляет ОБА слоя. Media-group (сетка 2×2) НЕ затронута (ветка только при `single_*`). Файлы: `main/notification-helpers.js`, `main/notification.css`, тест `albumLiveCard.vitest.js` (+2). **Требует визуальной проверки**. Предыдущее — **v1.2.98 (Уведомление: крупный аватар + источник «Мессенджер · Аккаунт» в шапке)**: по просьбе пользователя (как в подсказке закрепа). Шапка карточки перекомпонована в флекс-ряд `.notif-head`: КРУПНЫЙ аватар (44→50px) + колонка «имя канала + источник». Источник — строка «Telegram · БНК» (`messengerName · accountName`, тот же формат, что подсказка закрепа [pin-tooltip.html:96-101]); `accountName` в карточку уже приходил (v1.2.87), но не показывался. Старую мелкую метку мессенджера сверху (`.messenger-name`) спрятали — источник теперь в шапке. Сборка шапки вынесена в `notification-helpers.js` `buildStackHeader` (225→241/300); в `notification.js` заменена ОДНА строка (`appendChild(sender)` → `appendChild(buildStackHeader(...))`, 728/730, ноль новых строк). Нет messengerName/accountName → рисуется только имя. Файлы: `main/notification.js`, `main/notification-helpers.js`, `main/notification.css`. **Требует визуальной проверки**. Предыдущее — **v1.2.97 (Раскладка «Стопка» распространена на ВСЕ карточки уведомлений)**: по просьбе пользователя единый вид применён ко всем форматам — текст, медиа-группа (несколько фото), одиночное фото. Класс `.notif-item.layout-stack` вешается на каждую карточку: аватар + имя — в одну строку сверху, контент — на всю ширину `.text-wrap` (аватар выведен из потока `position:absolute`, имя сдвинуто вправо). Медиа-группа остаётся сеткой 2×2, но теперь во всю ширину; полноширинное ОДНО фото (`aspect-ratio:16/10`, одна плитка) — только под классом `.single-photo` (иначе альбом схлопнулся бы в столбец). В `notification.js` — `classList.add('layout-stack')` для всех + `single-photo` для одиночного фото (728/730). Файлы: `main/notification.js`, `main/notification.css`. **Требует визуальной проверки** (текст/альбом/одно фото — во всех аватар+имя сверху, контент на всю ширину). Предыдущее — **v1.2.96 (Уведомление с ОДНИМ фото: раскладка «Стопка» — фото на всю ширину, без пустого места слева)**: по согласованному макету (вариант 1). Карточка с одиночным фото перекомпонована: аватар + имя отправителя — в ОДНУ строку сверху, ниже — текст и фото на всю ширину (пропорция 16:10), а не зажатая маленькая «плитка» справа с пустотой слева. Реализовано минимально и безопасно: раскладка включается ТОЛЬКО для одиночного фото (`album.id` начинается с `single_`, [shared/notifAlbum.js:34]) через класс `.notif-item.layout-photo` — media-group (сетка 2×2) и текстовые уведомления НЕ затронуты. Правка почти вся в CSS: аватар выведен из потока (`position:absolute`, карточка уже `relative`), имя сдвинуто вправо (`padding-left`), фото занимает всю ширину `.text-wrap` (`grid-template-columns:1fr; aspect-ratio:16/10`). В `notification.js` — одна строка «повесить класс» (725→727, потолок 730). Файлы: `main/notification.js`, `main/notification.css`. **Требует визуальной проверки** (окно уведомления не тестируется без запуска). Предыдущее — **v1.2.95 (Одиночное фото/видео теперь показывается в уведомлении native-Telegram)**: по подтверждённой логом причине (`media=photo thumb=Y grp=n` — картинка есть, но `album` строился только при `groupedId`). Фикс: расчёт поля `album` вынесен в чистую `buildNotifAlbum(message, chatId)` (новый КОРНЕВОЙ `shared/notifAlbum.js` — вне renderer-бюджета `src/`, **лимит НЕ поднимали**: правило проекта «не раздувать renderer, выносить/разбивать») — строит карточку из ОДНОЙ плитки и для одиночного фото/видео с мини-картинкой, не только для media-group. `preloadAlbumThumb` расширен параметром `albumId` (для single-id `single_<id>`). Ссылки-превью (`mediaType='link'`) не затронуты (ADR-019). Юнит-тест `notifAlbum.vitest.js` (8). Диагностика v1.2.94 (`media/thumb/wp/grp`) убрана. Файлы: `shared/notifAlbum.js` (new), `src/native/store/notifAlbum.vitest.js` (new), `nativeStoreIpc.js`, `albumThumbPreload.js`. **Требует визуальной проверки** (одиночное фото показывается). Предыдущее — **v1.2.94 (ДИАГНОСТИКА: одиночное фото/ссылка-превью в уведомлении native-Telegram)**: задача — показывать картинку в уведомлении и для ОДИНОЧНОГО сообщения (сейчас только для альбома, v1.2.74). Разбор нашёл ДВЕ разные причины: (1) пост-ссылка (`messageText`+`web_page`) → `mediaType='link'`, картинка превью НЕ извлекается вообще (`extractMinithumbnail` не смотрит `web_page.photo`, `photoUrl:null` — [tdlibMapperMedia.js:15-22,51,57](main/native/backends/tdlibMapperMedia.js)); (2) настоящее одиночное фото → мини-картинка есть, но в уведомление кладётся только при `groupedId` ([nativeStoreIpc.js:509](src/native/store/nativeStoreIpc.js)). Чтобы точно понять, что за сообщение Forbes (ссылка vs фото), расширен существующий лог `[native-notif] emit` полями `media/thumb/wp/grp` (без роста файла — он на лимите 660). Фикс НЕ делался. Файл: `src/native/store/nativeStoreIpc.js`. Предыдущее — **v1.2.93 (Фото в уведомлении веб-Telegram: расследование закрыто, оставлено текстом; лог убран)**: диагностика v1.2.92 при реальном альбоме дала `tgimg=N` — Telegram Web НЕ кладёт фото в браузерное уведомление (`opts.image` пуст), только текст «Альбом» + аватар. Фото есть лишь в DOM страницы. Показать его, как через API, можно только DOM-скрейпингом (дорого/хрупко: тайминг, разные версии Web K/Z, клик в полный размер не работает без TDLib, не тестируется). По решению пользователя — ОСТАВЛЕНО текстом. Временные логи `__CC_DIAG__tgimg` убраны из обоих путей перехвата. Решение зафиксировано в decisions.md ADR-019. Файл: `main/preloads/hooks/telegram.hook.js`. Предыдущее — **v1.2.92 (ДИАГНОСТИКА фото веб-Telegram: короткий лог — прошлый обрезался)**: пользователь прислал альбом из 3 фото; лог v1.2.90 `__CC_DIAG__tg-notif-img` ОБРЕЗАЛСЯ журналом (~60 символов) на `icon=blob:https://web.telegra ima…` — значение `image=` не влезло (его съел `icon` до 24 символов). Видно: `body="Альбом"`, `icon=blob:` (аватар). Значение `opts.image` — неизвестно. Лог укорочен и переставлен: `__CC_DIAG__tgimg=Y <url>` / `=N` (image ПЕРВЫМ, «Y»/«N» в первых символах уцелеет). Оба пути перехвата. Ждём повтор альбома. Файл: `main/preloads/hooks/telegram.hook.js`. Предыдущее — **v1.2.91 (Док: расчёт вертикали вынесен в чистую функцию + юнит-тест)**: по итогам ревью — тесты `pinTooltip` проверяли только НАЛИЧИЕ строк, а не сами числа позиции (были зелёными, пока полоску сползало). Расчёт верхней координаты дока вынесен из `dock:resize` в чистую `computeDockTop()` (новый `main/handlers/dockGeometry.js`, без зависимости от Electron) и покрыт юнит-тестом `dockGeometry.vitest.js` (8 проверок, включая тест-ловушку на баг «сползание вниз» v1.2.89). Поведение не изменилось — тот же якорь верха + кламп по экрану, только теперь проверяется автоматически без запуска приложения. `dock:resize` вызывает `computeDockTop`. Тест pinTooltip → 69. Предыдущее — **v1.2.90 (ДИАГНОСТИКА: есть ли фото в уведомлении веб-Telegram)**: задача — показать вложение (фото/альбом) в уведомлении WebView-Telegram, как у нативного (v1.2.74). Аудит: фото не доходит, т.к. перехват `window.Notification` шлёт только текст+аватар. Ключевой неизвестный факт: кладёт ли Telegram Web фото в `opts.image` (тогда фикс лёгкий — не сливать его с `opts.icon` в [telegram.hook.js:51](main/preloads/hooks/telegram.hook.js#L51)) или `opts.image` пуст (тогда нужен дорогой DOM-скрейпинг). Статически не определить → добавлен ВРЕМЕННЫЙ лог `__CC_DIAG__tg-notif-img` в оба пути перехвата (обычный + ServiceWorker): печатает `body/icon/image/badge`. По строке из лога выберем точный фикс. Фикс НЕ делался. Файл: `main/preloads/hooks/telegram.hook.js`. Предыдущее — **v1.2.89 (Док: полоска не сползает вниз при добавлении/удалении — стабильный якорь верха)**: по строгому разбору (5 версий + дока Electron 42 + логи). Корень: `dock:resize` пересчитывал вертикаль из ЖИВОЙ высоты окна (`newY = bounds.y + bounds.height − totalH`), а высота окна (создаётся 48) ≠ реальный контент (~30-37); мелкая ошибка (в т.ч. DPI-округление) утекала в Y именно на add/remove (проходят мимо дед-бэнда), клампа Y не было → полоска сползала вниз под панель задач. Лента всегда один ряд (`inline-flex; white-space:nowrap`, без `flex-wrap`) → высота постоянна → вертикаль пересчитывать НЕ нужно. Фикс: стабильный якорь верха `dockState.baselineTopY` (меняется только при реальном drag), `dock:resize`/`ctx-menu-space` берут вертикаль из него, кламп Y по низу экрана, `moved` пропускает программный `setBounds` (`suppressMoved`) и офскрин (safeHide). Двигается только правый край. Файлы: `dockPinState.js`, `dockPinHandlers.js`. Тест pinTooltip → 67. Предыдущее — **v1.2.88 (Чистка: убраны временные диагностические логи `[dock-diag]`)**: после фикса имени аккаунта (v1.2.87) и дока убраны все временные диагностические логи `[dock-diag]` (10 точек в 5 файлах): `ensureDockWindow`/`moved`/`restoreDockBounds`/`addToDock`/`showTooltip`/`dock:resize`/`pin-message` + канал `dock:diag` и метод `diag` в preload + расширение `[native-notif] emit` (chatAcc/accs/resolvedAcct откачено к базовому). Все ФИКСЫ сохранены (проброс accountName, backgroundThrottling, прямой reportSize, workArea-кламп, снятие snap). Файлы: `dockPinState.js`, `dockPinHandlers.js`, `pin-dock.js`, `pin-dock.preload.cjs`, `nativeStoreIpc.js`. Тест pinTooltip 61. Предыдущее — **v1.2.87 (Фикс: имя аккаунта теперь доходит до закрепа/подсказки)**: диагностика v1.2.86 дала точное место потери по логам — уведомление находит имя (`resolvedAcct=БНК`), но `pin-message accountName=<none>`. Корень: `showCustomNotification` ([notificationManager.js:195,263](main/handlers/notificationManager.js)) НЕ пробрасывала `accountName` — поля не было ни в списке принимаемых параметров, ни в объекте `data`, уходящем в окно уведомления → кнопка 📌 брала пустое `data.accountName`. Фикс: добавлен `accountName` в оба места. Теперь имя доходит: уведомление → окно → 📌 → закреп → подсказка «Telegram · БНК · время». Диагностические логи `[dock-diag]`/`resolvedAcct` пока оставлены для подтверждения. Файл: `main/handlers/notificationManager.js`. Тест pinTooltip → 61. Предыдущее — **v1.2.86 (ДИАГНОСТИКА: имя аккаунта — трассировка всей цепочки)**: добавлены логи в 3 точки, чтобы точно увидеть, где теряется имя аккаунта: (A) уведомление — `[native-notif] emit … chatAcc=… accs=… resolvedAcct=…` (нашлось ли имя в момент уведомления); (B) создание закрепа — `[dock-diag] pin-message in accountName=…` (что пришло в закреп); (C) подсказка — `[dock-diag] showTooltip … accountName=…` (что ушло в подсказку). A→B→C покажут точное место потери. Фикс НЕ делался. Файлы: `src/native/store/nativeStoreIpc.js`, `main/handlers/dockPinHandlers.js`, `main/handlers/dockPinState.js`. Предыдущее — **v1.2.85 (ДИАГНОСТИКА: имя аккаунта в уведомлении не находится)**: пользователь подтвердил — имя аккаунта пусто и на НОВОМ закрепе, свежая сборка (моя гипотеза «старые данные» ОШИБОЧНА). По коду `accounts.find(a => a.id === chat.accountId).name` обязан находить (account.id и chat.accountId оба `tg_<число>`, account.name = displayName), но не находит. Причину чтением не нашёл → расширил существующий лог `[native-notif] emit` полями `chatAcc` (chat.accountId) и `accs` (список {id,name} аккаунтов) — покажет реальный рассинхрон в момент уведомления. Фикс НЕ делался. Файл: `src/native/store/nativeStoreIpc.js` (правка одной строки лога, без роста файла — он на лимите 660). Предыдущее — **v1.2.84 (Док: убрано прилипание к краям экрана)**: по просьбе пользователя убран snap (прилипание) полоски дока к краям экрана при перетаскивании — мешал. Обработчик `moved` теперь только СОХРАНЯЕТ позицию (с клампом по низу экрана), без притягивания к краям. Файл: `main/handlers/dockPinState.js`. Тест pinTooltip → 59. Побочно (без кода): рост дока «в обе стороны» — это включённая настройка «Расширение по центру» (`dockCenterExpand`, SettingsPanel), выключить → растёт только вправо. Имя аккаунта в источнике: фикс v1.2.83 работает для НОВЫХ закрепов; старые (до пересборки) имени не знают. Предыдущее — **v1.2.83 (Подсказка: «Срочно» показывается + имя аккаунта в источнике)**: два фикса карточки-подсказки. (1) «Срочно» (категория) не показывалась в подсказке, хотя выставлена — КОРЕНЬ: `.tt-cat` спрятана правилом в CSS (`display:none`), а код показывал через `catEl.style.display=''` (пустая строка = взять из CSS = снова none). Заметка работала, т.к. её `display:none` в inline-HTML. Фикс: `display:'inline-block'`. (2) Имя аккаунта («БНК») не показывалось в источнике — оно НИГДЕ не передавалось. Фикс: `accountName` (из `accounts.find(id==accountId).name`) прокинут: уведомление → `createPinBtn` → закреп → `showTooltip` → источник «Telegram · БНК · время». Файлы: `main/pin-tooltip.html`, `main/handlers/dockPinState.js`, `main/notification-helpers.js`, `main/notification.js`, `src/native/store/nativeStoreIpc.js`. Тест pinTooltip → 57. Предыдущее — **v1.2.82 (Док: можно держать НА панели задач Windows — не поднимается вверх после старта)**: диагностика v1.2.81 дала точные числа из лога `[dock-diag]`: юзер тащит док на панель задач (`moved y=818`), но кламп сохраняет `y=779` (низ рабочей области), а `ensureDockWindow` на старте клампит до `768` → док встаёт ВЫШЕ панели задач. Корень — анти-панельный кламп из v1.2.61 (когда док уходил ЗА панель и пропадал). Сейчас док «поверх всех» (v1.2.62-63 z-order reassert) и на панели виден. Фикс: нижний предел позиции = низ ЭКРАНА (`display.bounds`), а не рабочей области — в 4 местах (`ensureDockWindow`, `moved` snap+clamp, `restoreDockBounds`, `display-metrics-changed`). Дефолт первого запуска остался над панелью. Логи `[dock-diag]` пока оставлены для подтверждения. Файл: `main/handlers/dockPinState.js`. Предыдущее — **v1.2.81 (ДИАГНОСТИКА: док уходит за панель задач после старта)**: жалоба — перетаскиваю док вниз к панели задач, после перезапуска он уходит ЗА неё. Факты: сохранённая `dockPosition {x:0,y:779}`, `workArea.height=816` (из логов уведомлений). Но по коду кламп (moved + ensureDockWindow) ДОЛЖЕН держать док над панелью задач → значит в игре runtime-фактор, невидимый со стороны (возможно `workArea.y`/авто-скрытие панели задач, либо реальная высота окна дока ≠ 48). Эти числа нигде не логировались. Добавлены ВРЕМЕННЫЕ логи `[dock-diag]` в `moved` (что сохраняем + wa + baseHeight + maxDockY) и `ensureDockWindow` (saved + wa + dockH + startY). Фикс НЕ делался — сначала точные числа с машины. Файл: `main/handlers/dockPinState.js`. Предыдущее — **v1.2.80 (Док: полоска не видна после «Свернуть» — фикс rAF/throttling + диагностика)**: баг вернулся после v1.2.76. Аудит: сохранённая `dockPosition` на диске ВАЛИДНА (x:46,y:779) — прошлая гипотеза про «отравленную позицию» опровергнута фактом. Реальный дефект: окно дока — единственное из окон закрепа/уведомлений БЕЗ `backgroundThrottling:false`, а размер полоски слался через `requestAnimationFrame` → у скрытого окна rAF засыпает (ловушка #28) → размер не уходил в main после показа → полоска не того размера/не видна. Фикс: `backgroundThrottling:false` окну дока + `reportSize` шлёт размер напрямую без rAF (как у подсказки v1.2.71). Плюс ВРЕМЕННАЯ диагностика `[dock-diag]` (renderer reportSize + main addToDock/restoreDockBounds/dock:resize) для подтверждения на машине пользователя — удалить после. Файлы: `main/handlers/dockPinState.js`, `main/pin-dock.js`, `main/preloads/pin-dock.preload.cjs`, `main/handlers/dockPinHandlers.js`. Тест pinTooltip → 52. Предыдущее — **v1.2.79 (План разбиения файлов у лимита)**: обновлён трекинг `code-limits-status.md` — свежий снапшот 24 файлов на 80%+ лимита + приоритетный план разбиения (какие на 100% блокируют следующую правку, как безопасно резать каждый). Массовый разрез НЕ делался осознанно: разбиение рабочего кода без запуска приложения (особенно инъекций в WebView — `*.hook.js`, `*.preload.cjs`, `vkExecFallback`) рискованно и не ловится тестами. Режем по одному под задачу с визуальной проверкой. Ни один файл сейчас не за лимитом — только профилактика. Только память, код не тронут. Предыдущее — **v1.2.78 (Разгрузка памяти: notifications-ribbon.md)**: файл ловушек `mistakes/notifications-ribbon.md` был у лимита (198/200 КБ). Старые ловушки (#28–#32, v0.89.35–v1.2.7 + карта серии v0.89.18-27, все РЕШЕНЫ) вынесены в новый `mistakes/notifications-ribbon-history.md` (~139 КБ). Основной файл сжат до ~59 КБ (свежие ловушки v1.2.x). Указатели: в самом файле + `common-mistakes.md` (индекс) + таблица в CLAUDE.md. Только память, код не тронут. Предыдущее — **v1.2.77 (Аватар в уведомлении: кэш-проверка до сброса состояния)**: аватар чата в состоянии обновляется с задержкой (rAF-батч до ~1.5с, `flushPendingChatAvatar`). Если фото только пришло, но ещё не влито в state — уведомление показывало эмодзи-заглушку, хотя аватар уже был в `pendingChatAvatar`. Фикс (Совет 1, одна строка): `iconDataUrl: chat?.avatar || pendingChatAvatar.get(chatId) || ''` — уведомление берёт свежий аватар из пенды. Битый/устаревший URL безопасно ловит `img.onerror` → эмодзи. Файл: `src/native/store/nativeStoreIpc.js`. Предыдущее — **v1.2.76 (Док: полоска не пропадает после «Свернуть»)**: баг — после «Свернуть» полоска дока не появлялась на своём месте внизу. Корень: `checkDockVisibility` при 0 задач прячет окно дока через `safeHide` (увод в {x:-30000, y:-30000, 1×1}); при следующем показе `addToDock` звал `showInactive()` по старым офскрин-координатам, а `dock:resize` считал Y от текущих (офскрин) bounds и клампил только X → окно «видимо», но за верхней кромкой экрана. Фикс: новая `restoreDockBounds` возвращает окно в низ рабочей области (workArea, над панелью задач) перед показом в `addToDock`. Файл: `main/handlers/dockPinState.js`. Тест pinTooltip → 49. Предыдущее — **v1.2.75 (Telegram: аватар + источник в уведомлении и подсказке)**: у нативного Telegram теперь показывается АВАТАР отправителя и ИСТОЧНИК. Аватар: TDLib уже качает фото в `cc-media://avatars/…`, теперь оно кладётся в `iconDataUrl` (окно уведомления/подсказки рисует cc-media напрямую; пусто → эмодзи-заглушка). Источник в подсказке: `createPinBtn` передаёт `messengerName` (напр. «Telegram») в закреп → подсказка показывает «Telegram · время» (раньше для `native_cc` было пусто). «Срочно» уже работало. Подсказка переверстана: аватар + отправитель + источник в шапке. Файлы: `src/native/store/nativeStoreIpc.js`, `main/notification-helpers.js`, `main/notification.js`, `main/handlers/dockPinState.js`, `main/pin-tooltip.html`. Тест pinTooltip → 46. Предыдущее — **v1.2.74 (Альбом в уведомлении: листание страницами + чёткие плитки + быстрое открытие)**: реализованы 3 улучшения карточки альбома (media group) по согласованному макету. **C2 — листание страницами по 4**: вместо «+N» сетка 2×2 листается стрелками ‹ › (счётчик «1–4 / N», точки-страницы). Высота области ФИКСИРОВАНА (`.album-grid height:208px`, `grid-template-rows` из JS по числу фото) → карточка НЕ прыгает при листании неполных страниц; неполная страница раскладывается ровно (1 фото — на всю ширину, 3 фото — нижнее широкое, класс `.album-tile.wide`), без «дырки». **A1 — чёткие плитки**: плитка сперва мутная (strippedThumb) + крутилка, затем фоновая догрузка чёткого превью (`tg:download-media thumb:true` в главном окне → путь cc-media → канал `notif:album-thumb` → окно) заменяет фон и убирает крутилку (`applyAlbumSharp`, хранится в `album.sharpThumbs`). **B1 — быстрое открытие**: клик по плитке открывает нажатое фото на весь экран СРАЗУ (не ждёт скачивания всех, как было ~20с), остальные догружаются фоном и обновляют ту же смотрелку (`photo:open` переиспользует окно через `photo:set-srcs`). Файлы: `main/notification.js`, `main/notification-helpers.js` (renderAlbumGrid переписан на пагинацию + applyAlbumSharp), `main/notification.css`, `main/handlers/notifHandlers.js` (канал `notif:album-thumb`), `main/preloads/notification.preload.cjs` (onAlbumThumb), `src/hooks/useAppIPCListeners.js` (B1), `src/native/store/nativeStoreIpc.js` + новый `src/native/utils/albumThumbPreload.js` (A1 догрузка). Тесты: `src/__tests__/albumLiveCard.vitest.js` (10 проверок). **Требует визуальной проверки** (окно уведомления не тестируется без запуска). Предыдущее — **v1.2.73 (Подсказка: фикс узкой вертикальной карточки после скрытия)**: баг — подсказка становилась узкой высокой полосой (текст по 1 символу). Корень: `hideTooltip` через `safeHideTransparentWindow` сжимает окно до ширины 1px; после фикса «показывать каждый раз» (v1.2.71) карточка при следующем показе мерилась ВНУТРИ окна шириной 1px → перенос по символу. Фикс: `showTooltip` перед замером возвращает окну просторную ширину (`setBounds width:300` офскрин), затем renderer меряет карточку по её max-width. Файл: `main/handlers/dockPinState.js`. Тест pinTooltip → 40. Предыдущее — **v1.2.72 (Подсказка: грация скрытия 150мс, без мигания)**: при уходе мыши подсказка теперь прячется не мгновенно, а через ~150мс; если мышь вернулась или перешла на другую вкладку — скрытие отменяется (нет мигания при переходах между задачами). Общий таймер `tooltipHideTimer` в `pin-dock.js`. Тест pinTooltip → 39 проверок. Проверено: совет про разбиение `mistakes/electron-core.md` (115/200) и `native-scroll-unread.md` (118/200) НЕ нужен — они под лимитом 200 КБ (я ошибся, спутав с лимитом корня 100 КБ); реально на пределе `notifications-ribbon.md` (200/200) — кандидат на разбиение отдельно. Файл: `main/pin-dock.js`. Предыдущее — **v1.2.71 (Подсказка показывается КАЖДЫЙ раз + чистка + разбиение features.md)**: баг — тултип показывался один раз, потом никогда (у скрытого окна засыпал `requestAnimationFrame`, в котором сообщался размер). Фикс: окну-подсказке добавлен `backgroundThrottling:false` (как у окна уведомлений, ловушка #28) + размер сообщается прямым вызовом без `rAF`. Почищен мёртвый код старого превью (`dock:preview-space`, `requestPreviewSpace`, previewEl). `features.md` разбит (перенос v1.2.46-58 в `archive/features-v1.2.46-58.md`). Тест `pinTooltip.test.cjs` расширен до 37 проверок. Файлы: `main/handlers/dockPinUtils.js`, `main/pin-tooltip.html`, `main/pin-dock.js`, `main/preloads/pin-dock.preload.cjs`, `main/handlers/dockPinHandlers.js`. Предыдущее — **v1.2.70 (Подсказка задачи вернулась — отдельным окном, без прыжков)**: всплывающая подсказка при наведении на вкладку дока реализована как ОТДЕЛЬНОЕ переиспользуемое окно (Вариант 4: категория + отправитель + текст + время + заметка). Окно «сквозное» (`setIgnoreMouseEvents(true)`, `focusable:false`) и НЕ ресайзит окно дока → петля дёрга структурно невозможна. Наведение с задержкой ~0.4с шлёт `dock:tooltip-show` c прямоугольником вкладки; main ставит окно НАД вкладкой (по фактической высоте от `tooltip:resize`) и показывает `showInactive`; уход — `dock:tooltip-hide` (safeHide). Диагностический лог `[dock-bounds]` удалён. Новые файлы: `main/pin-tooltip.html`, `main/preloads/pin-tooltip.preload.cjs`, тест `src/__tests__/pinTooltip.test.cjs` (33 проверки). Также в этом релизе — фича «альбомы в уведомлениях» (параллельная работа, завершена). Предыдущее — **v1.2.69 (Док: убрана hover-подсказка = структурно нет петли дёрга + дед-бэнд)**: лог `[dock-bounds]` подтвердил, что cooldown (v1.2.68) петлю НЕ убрал — `preview-grow↔preview-shrink` шли пачками и после. Диагноз: пока док ресайзит окно на наведение, Windows шлёт ложные mouse-события под курсором → петля. Окно уведомлений тем не страдает — оно НЕ ресайзится на hover. Решение как там: **убрана всплывающая подсказка при наведении** (`showPreview` + hover-listeners удалены) → окно на hover больше не растёт → петля невозможна структурно. Контент — по клику. Плюс **дед-бэнд** в `dock:resize`: микро-изменения (±≤6/4px от тиков таймера) не двигают окно → ушёл «медленный уход вниз». Файлы: `main/pin-dock.js`, `main/handlers/dockPinHandlers.js`. Диаг-лог `[dock-bounds]` оставлен для подтверждения. Предыдущее — **v1.2.68 (Док: разорвана петля дёрга)**: лог `[dock-bounds]` (v1.2.67) показал: `bottomMoved=false` (низ окна НЕ двигается — привязка ок), но окно быстро растёт↔сжимается 2-3 раза/с (`preview-grow`↔`preview-shrink`). Причина — петля: рост окна под подсказку → Windows шлёт ложные mouseleave/mouseenter под курсором → hide→shrink→show→grow… Фикс: в `pin-dock.js` добавлен cooldown `previewResizeCooldown` (~0.5с после ресайза hover-события игнорируются) — петля разрывается, подсказка сохранена. Диагностический лог оставлен для подтверждения. Файл: `main/pin-dock.js`. Предыдущее — **v1.2.67 (ДИАГНОСТИКА дёрга дока)**: временный лог `[dock-bounds]` в `dockPinHandlers.js` на каждое изменение размера/позиции окна дока (resize / preview-grow / preview-shrink / ctxmenu-*), с полем `bottomMoved` (двигается ли низ окна = физический сдвиг полоски). Нужен, т.к. фикс v1.2.64 (position:fixed) не убрал дёрг, а запуск приложения агенту запрещён — причину ищем по реальному логу. Удалить после диагноза. Предыдущее — **v1.2.66 (Док: контрастная цифра-счётчик)**: бейдж числа закреплённых (`.dock-count`) был бледно-фиолетовым текстом на бледно-фиолетовом фоне → плохо виден, особенно на светлом фоне под прозрачным окном. Сделан сдержанный счётчик в стиле вкладок дока: светлая цифра `#e2e8f0` на лёгком полупрозрачном чипе (без жирной заливки). Файл: `main/pin-dock.css`. Дёрг полоски: фикс v1.2.64 (`position:fixed`) в коде подтверждён; если дёрг остаётся — нужен полный перезапуск (CSS дока грузится при пересоздании окна) либо это остаточный native-resize flash Win11 (решается отключением hover-подсказки — по согласованию). Предыдущее — **v1.2.65 (Закреп: переживает перезапуск)**: сохранение закрепа в storage было (v0.72.1), но само себя стирало на выходе: при закрытии окон закрепа Electron'ом их `'closed'`-обработчики делали `pinItems.delete()` + `savePinItems()` → storage перезаписывался пустым `[]`. Факт: на диске `"pinItems": []` при наличии закреплённого. Фикс: флаг `isQuitting` по `app.on('before-quit')`; в `'closed'` на выходе НЕ удаляем/не пересохраняем (последнее сохранение с пинами остаётся). Явное открепление (кнопка ✕ через `removePin`) не затронуто. Файлы: `main/handlers/dockPinHandlers.js`, `main/handlers/dockPinState.js`. Предыдущее — **v1.2.64 (Док: полоска не дёргается при подсказке/меню)**: при наведении (подсказка) и открытии/закрытии меню полоска дока дёргалась вниз — окно росло/сжималось под всплывашку, а полоска была flex-child при `min-height:100vh` → раскладка перескакивала. Фикс: полоска приклеена к низу окна через `position:fixed;bottom:0` (sticky-overlay паттерн, как Telegram Web K/WhatsApp) — рост окна вверх больше её не двигает. Другие модалки (карточка закрепа, лента уведомлений) этот анти-паттерн не используют — не затронуты. Совет A (пересоздание главного окна) проверен и НЕ нужен: на Windows окно при закрытии завершает приложение (`window-all-closed`→quit), мид-сессия не пересоздаётся. Файл: `main/pin-dock.css`. Предыдущее — **v1.2.63 (Док: мгновенный возврат + смена монитора)**: v1.2.62 (периодический реассерт раз в 1с) ✅ ПОДТВЕРЖДЁН пользователем как рабочий. v1.2.63 добавляет: (Совет 1) мгновенный возврат дока «поверх всех» по событиям главного окна `blur/focus/minimize/restore` — без задержки до 1с; (Совет 2) реакция на `display-metrics-changed` (смена монитора/разрешения/масштаба) — док возвращается в рабочую область над панелью задач. Совет 4 (стоп таймера без задач) НЕ делался: таймер и так вхолостую при скрытом доке (гейт isVisible), старт/стоп добавил бы риск без пользы. Файл: `main/handlers/dockPinState.js`. Предыдущее — **v1.2.62 (Док: удержание «поверх всех» — попытка 2)**: v1.2.61 починила ПОЗИЦИЮ (workArea), но НЕ видимость — док всё равно уходил под панель задач при клике по ней/сворачивании. Лог доказал: наш код док НЕ прячет (нет dock-safeHide), это потеря z-order — Windows опускает topmost-окно при фокусе панели задач, а реассерт был только на blur самого дока. Попытка 2: периодический реассерт `setAlwaysOnTop('screen-saver')` раз в 1с пока док видим (не крадёт фокус). Файл: `main/handlers/dockPinState.js`. ⚠️ Windows-ограничение — требует визуальной проверки; возможна задержка до 1с при возврате. Предыдущее — **v1.2.61 (Док: не уходит за панель задач Windows)**: док задач ставился по `display.bounds` (весь экран, включая зону панели задач) → нижний край уходил ЗА панель задач Windows и был не виден. Особенно проявилось после v1.2.60 (резерв 0 → окно ровно в зоне панели задач). Фикс: позиционирование по `display.workArea` (рабочая область без панели задач, офиц. Electron) + страховка-кламп, чтобы док не опускался ниже рабочей области даже со старой сохранённой позицией; убран снап к самому низу экрана. Файл: `main/handlers/dockPinState.js`. Предыдущее — **v1.2.60 (Док/закреп: пустая зона, аватар, таймер)**: (1) убрана пустая зона над доком, которая ловила клики — корень: прозрачное окно Electron НЕ пропускает клики через прозрачные пиксели (это только отрисовка, не hit-test; клик-насквозь лишь через setIgnoreMouseEvents, который убран). Резерв `DOCK_PREVIEW_RESERVE` 420→0, меню/подсказка растят окно вверх по требованию и сжимают обратно. (2) аватар отправителя теперь показывается в карточке закрепа (прокинут через createPinBtn→pinMessage→pin:data). (3) таймер-алерт свёрнутой задачи возвращает окно на экран перед показом (общий помощник `restorePinBounds`, тот же корень что фикс v1.2.59). Файлы: `dockPinState.js`, `dockPinHandlers.js`, `dockPinUtils.js`, `pin-dock.js`, `notification.js`, `notification-helpers.js`, `pin-notification.html`. **Требует визуальной проверки** (возможен риск дёрганья меню/подсказки). Предыдущее — **v1.2.59 (Закреплённые задачи: разворот, картинки, клик)**: (1) главный фикс — свёрнутая в док карточка при клике снова разворачивается: раньше `safeHide` уводил окно за экран в 1×1, а `dock:show-pin` делал только `show()` без возврата позиции → пользователь видел «пустоту». Теперь размеры/позиция запоминаются перед сворачиванием и возвращаются при показе (+запасной центр экрана). (2) любой клик по свёрнутой задаче открывает нашу карточку — уход в Telegram по двойному клику убран («В чат» остался в правом клике и в карточке). (3) сообщения-картинки без подписи показывают «📷 Фото» вместо пустой карточки. Файлы: `main/handlers/dockPinHandlers.js`, `main/pin-dock.js`, `main/pin-notification.html`. История v1.2.27 и старше — в [.memory-bank/archive/features-v1.2.27-and-older.md](.memory-bank/archive/features-v1.2.27-and-older.md). **Полный changelog → [.memory-bank/features.md](.memory-bank/features.md)**.
