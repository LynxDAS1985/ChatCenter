# Зависимости
node_modules/

# Артефакты сборки
out/
dist/

# Electron Cache
.electron/

# Логи
*.log
npm-debug.log*

# Системные файлы
.DS_Store
Thumbs.db
desktop.ini

# Переменные окружения (если появятся)
.env
.env.local
.env.production

# IDE
.vscode/settings.json
.idea/

# Временные файлы
*.tmp
*.swp

# Записи веб-трафика (могут содержать куки/токены) и разовые дампы — НЕ коммитить
*.har
/PNG/*.zip

# Личные настройки Claude Code этой машины (разрешения конкретного пользователя).
# Общий .claude/settings.json КОММИТИТСЯ (правила проекта), а этот — нет: он у каждого свой
# и может содержать пути/разрешения конкретной машины. Добавлено v1.2.479.
.claude/settings.local.json
